# 云端生成与任务管理需求

## 当前执行归属与非功能合同（2026-09-22）

[17-30](../plan/17-30-App账户与云端访问执行计划.md)负责App真实账户Client、会话和owner隔离，对应ACCOUNT-REQ-010/011与ACCOUNT-ACC-011；[14-01](../plan/14-01-完整穿搭图与按需三维生成执行计划.md)负责生成用途、持久任务、配额、Provider对账和私有产物。日记/社区通过同一Client基础接各自已存在API，不依赖付费生成上线。

非功能要求统一引用OOTD-NFR-001/004/006～013；任务接纳、结果质量、端侧渲染性能是不同测量对象。已有后端账户/媒体worker/Outbox证据不等于生成worker已完成。

| 事件 | 必须保持的服务事实 | 用户可见结果 / 验收 |
| --- | --- | --- |
| 接纳/重复接纳 | owner、输入版本、用途、同意、额度、幂等与Outbox原子写入；非法输入没有外部任务 | 返回同一任务或明确冲突；CLOUD-ACC-003/008/012 |
| 外部受理未知 | 持久不确定状态，依照供应商可验证幂等/查询对账；不能盲重发计费POST | 核对状态及恢复动作；不伪造失败退款；CLOUD-ACC-006/012 |
| 图就绪/模型就绪 | 收取校验后写私有固定版本，与Look原输入一致；图和模型分开发布 | 图片先用，显式模型；旧版本不切到当前草稿；CLOUD-ACC-007/012 |
| 取消/撤回/删号 | 先关可见性、保留对账/血缘/对象清理依据；供应商成本与用户额度分账 | 停止并清理、已完成分别真实显示；CLOUD-ACC-005/009/012 |
| 队列/worker/存储中断 | PG为任务事实；有限退避和重放；消息只携ID；对象与事务之间有补偿追踪 | 返回App能恢复原任务，不能丢失或自动新建；CLOUD-ACC-004/006 |

费用、并发、任务时限和删除最长保留期必须在生产配置中有确定值及故障验证；没有配置或准入不接纳新任务。生成结果与用户照片不可放入Kafka消息/Redis临时缓存作为唯一副本。本文件下方旧阶段事实和已完成账号合同保留，当前新任务以以上归属推进。

## 完整图片与按需模型任务（2026-09-22）

当前生成类型为 image（完整 Look 图片）和 model（从已确认图版本生成静态 GLB）；视频延期。设计 approved_with_gates，真实生成任务未实现。对应 Design 07/10/20 与 14-01，已有账号/媒体/工程规则继续有效。

| ID | 规则 | 验收 |
| --- | --- | --- |
| `CLOUD-REQ-007` | 图与模型独立触发、同意、配额和状态；模型失败不影响图保存；仅传授权最小输入 | CLOUD-ACC-012 |
| `CLOUD-REQ-008` | owner+Look/revision+image hash+模型参数去重；供应商未知提交先对账，不盲重发；实际计费与用户额度分开 | CLOUD-ACC-012 |
| `CLOUD-REQ-009` | 临时输出收取并验证后落私有存储；旧版结果不覆盖新版，删除/取消迟到只清理，保留清理依据 | CLOUD-ACC-012 |

生成状态 queued/running/validating/succeeded，终态 failed/canceled/expired；取消请求、外部待核对、清理进度独立保存。App 前台退避轮询和重启恢复，不新增推送或 WebSocket。内置授权虚构成人预设无需本人照片声明，个人照片来源仍受既有成年/本人条件。

## 客户端与服务端仓库及CI

2026-09-14 用户要求在 `Then/` 下建立独立 `then-app` 与 `then-server` 项目，并补齐必要 CI。拆分只改变代码和验证边界；App 本地能力、服务端 API、数据、隐私与分期不变。

| 需求 ID | 产品合同 |
| --- | --- |
| `DELIVERY-REQ-001` | iOS 工程位于 `Then/then-app`；Go、Compose 与产品级文档位于 `Then/then-server`。 |
| `DELIVERY-REQ-002` | Go transport 的 operation 与标注类型是接口源；Swagger 与 Umi 读取运行时 `/openapi.json`，仓库不保存接口 YAML/JSON 物化文件。 |
| `DELIVERY-REQ-003` | 服务端每次提交自动检查 Go 格式、依赖完整性、vet、race 测试、真实 PostgreSQL 进程路径及受限 OCI 镜像。 |
| `DELIVERY-REQ-004` | App 的网络 Client 生成必须在实际接入服务端账号 API 的独立切片重新确定输入与 CI；当前本地三维主路径不依赖服务端物化契约。 |
| `DELIVERY-REQ-005` | CI 只使用合成数据和隔离资源，不读取仓库 `.env`、生产凭据、真实照片或用户数据；CI 通过不替代真机、视觉或生产验收。 |
| `DELIVERY-REQ-006` | 公开 API 直接使用语义根路径，不在服务端路由、Cookie、Axios 或开发代理中配置路径版本；接口演进通过 OpenAPI 文档版本、评审和客户端重新生成管理。 |

## 账号认证与Web账户管理

2026-09-22 用户要求同步前端必要设计和工程文件，执行归 [17-27](../plan/17-27-Web设计体系与工程规范同步执行计划.md)：采用已确认 shadcn/ui + Radix，统一基础页面状态、目录、主题与质量检查；账户业务范围继续由 17-13 管理。

2026-09-14 用户明确要求在 App 之外新增命名为 `frontend` 的 Web 能力。账号 MVP 是邮箱密码注册、登录、退出，以及用户对本人账户的创建、读取、修改和删除；服务端边界见 [Design 15](../design/15-账号认证与账户数据设计.md) 与 [17-12](../plan/17-12-账号认证与本人账户API执行计划.md)。2026-09-16 用户确认基础工程必须使用 Next.js App Router，不使用 Vite 或 React Router；Radix UI/TypeScript/ESLint/Prettier/Umi OpenAPI/Axios 请求基础已完成迁移，账户页面范围仍由 [Design 16](../design/16-Web账户管理设计.md) 与 [17-13](../plan/17-13-Web账户管理执行计划.md) 的 `draft` 页面合同管理。

### 用户故事

- 作为新用户，我可以通过邮箱、显示名称和密码创建账户，并立即进入本人账户页。
- 作为已有用户，我可以登录并在刷新页面后恢复有效会话。
- 作为登录用户，我可以查看和修改本人的邮箱与显示名称，且不能枚举或修改他人账户。
- 作为用户，我可以退出当前会话，或确认后删除账户及服务端全部会话。
- 作为只使用 App 本地核心的用户，我不注册、不登录仍能使用离线三维、衣橱与记录。

### 稳定需求与规则

| 需求 ID | 产品合同 |
| --- | --- |
| `ACCOUNT-REQ-001` | `frontend/` 提供注册、登录、账户读取/修改/删除和退出的可恢复完整旅程。 |
| `ACCOUNT-REQ-002` | 注册即账户 Create，本人账户页完成 Read/Update/Delete；MVP 不提供用户目录或管理员代操作。 |
| `ACCOUNT-REQ-003` | 邮箱唯一且服务端规范化；密码只保存强散列，登录失败不泄露账户是否存在。 |
| `ACCOUNT-REQ-004` | 会话令牌只以 HttpOnly、SameSite=Strict Cookie 交付，PostgreSQL 只保存令牌散列和到期时间；受保护端点拒绝无效、过期或已撤销会话时必须清除失效 Cookie，普通登录失败不得退出现有有效会话。 |
| `ACCOUNT-REQ-005` | 所有本人资源由服务端会话决定 owner；公开接口不接收可替换的 user_id。 |
| `ACCOUNT-REQ-006` | 账户删除按 19-06 先关闭访问并撤销全部会话，返回真实删除回执；媒体清理完成后物理删除账户，清理依据不能提前级联丢失。不恢复旧生活业务。 |
| `ACCOUNT-REQ-007` | Web 请求文件由运行时 `/openapi.json` 生成，不复制 DTO、URL 清单或接口 YAML。App 的网络 Client 在对应接入切片遵守同一服务端接口源。 |
| `ACCOUNT-REQ-008` | App 本地核心继续零账号可用；注册登录不能成为本地三维、衣橱和记录前置。 |
| `ACCOUNT-REQ-009` | 注册和登录按服务端直接观察到的源 IP 使用 Redis 固定窗口限流：注册每小时 5 次、登录每 15 分钟 10 次；超限返回可重试 429 和恢复秒数，Redis 不可用时认证失败关闭且不触达账号业务。 |
| `ACCOUNT-REQ-010` | App 通过真实 Client 完成主动注册/登录、本人资料读取/修改、会话恢复、退出和删除；登录取消/失效不破坏本地核心，恢复登录不能自动重放付费或公开动作。 |
| `ACCOUNT-REQ-011` | App 的云草稿、任务、媒体缓存与延迟响应按账号隔离；访客本地数据不因登录自动上传，换账号不可见前账号私有内容；删除回执区分访问关闭与完整清理。 |

公开注册、真实用户流量和生产发布还需要邮件验证、密码找回、可信代理配置、TLS/Secure Cookie、审计和备份删除窗口验收。Redis 限流由 [17-17](../plan/17-17-账号认证Redis限流执行计划.md) 实施；完成后只关闭开发环境认证限流缺口，不代表互联网生产认证系统已准入。

邮件验证与密码找回的后端合同见 [17-36](../plan/17-36-账号邮件验证与找回后端执行计划.md)：网易 163 只作发信通道，服务端挑战、投递和四个新接口已完成本地合成数据验收；发件授权码只由运行环境注入，不改变本地零账号路径或公开注册门禁。真实 163 送达及客户端入口另验。

## 后端MVP与测试服务边界

2026-09-14 用户要求按标准 Go 规范规划并开始后端 MVP，同时将测试能力独立。当前后端 B0 已包含单进程运行底座、GORM/PostgreSQL 账号 schema、注册/登录/本人账户 CRUD、健康、内嵌 OpenAPI/Swagger 与独立测试矩阵。按 [17-18](../plan/17-18-本人成年声明API执行计划.md) 增加的声明 API 只建立后续云处理前置；仍不虚构同步、生成任务或资产上传接口。

测试独立要求是：普通单元/契约测试无外部依赖，真实服务/实际进程/镜像测试在 `backend/tests` 按标签分别执行；测试只使用 `THEN_TEST_*`、loopback 和合成隔离资源，不读取生产变量或用户数据。它不是面向用户的服务，不增加测试运行角色、第二个 module 或常驻测试部署。下一阶段云入口、生成任务与同步分别受 Design 14 的 B1–B3 门槛控制。

## 数据库与中间件开发接入

2026-09-22 用户明确将消息队列改为 Kafka，并要求入口直接放在 `backend/main.go`。保留 PostgreSQL Outbox/Inbox、成功确认后标记发布、业务完成后提交消费 offset、失败重投与可验证删除；不改变 API 或云能力启用条件。当前执行见 [17-26](../plan/17-26-Kafka与工程规范化执行计划.md)。下述 2026-09-14 RabbitMQ 记录是历史环境依据，不再是新实现选型。

2026-09-14 用户要求接入数据库、MinIO、Redis 和 RabbitMQ；评估后沿用 PostgreSQL，并明确日常开发连接本机已经安装的服务，Docker 只作按需隔离备用。近期交付验证事务回滚、私有对象上传/读取/删除、TTL 失效、消息确认与重投递，执行归 [17-10](../plan/17-10-数据库与中间件开发环境执行计划.md)。不要求普通 App 用户登录或上传衣服，不将基础设施验证等同于生成任务上线；原云任务、授权、恢复、删除及生产准入要求不变。

## 当前范围与优先级

认证、逐次同意、上传、任务、恢复、配额和云删除随可选照片生成阶段交付；近期无上传三维不创建云账号、GenerationJob 或逐帧请求。Go/OpenAPI 运行基线与 Swagger 已有工作继续保留，不能当作云业务已经交付。 当前顺序与产品边界归 [总纲](10-OOTD产品需求.md)。

## 接口协作需求

Swagger 文档与客户端请求生成必须使用同一运行时 OpenAPI。当前 Gin/Huma 从 operation 和类型 tag 直接声明接口；不手写契约文件、不部署独立文档服务。Web 已通过 `@umijs/openapi` 生成请求，账户页面消费按 17-13 后续切片实施。App 当前没有已接入的云请求层。

## 状态与上位需求

- 状态：`approved_with_gates`，2026-08-30；需求边界已批准，真实云端流量受门禁。
- 上位需求：[`10-OOTD产品需求.md`](10-OOTD产品需求.md) 是产品总纲；本文是静态试穿与动态预览共用的云端生成、账号和任务管理契约，不得突破总纲的共同目标、非目标和本地优先原则。静态质量与产品边界叠加 [`14-AI虚拟试穿需求.md`](14-AI虚拟试穿需求.md)，动态任务在共用契约上再叠加 [`15-动态预览需求.md`](15-动态预览需求.md)。

发现本文与产品总纲冲突时必须记录差异并重新评审，不得在实现中静默选择。

批准本文表示共用任务契约可以进入实施计划，不表示静态或动态生成已经可以处理真实用户数据。每种生成类型的生产供应商与地域、数据清单、逐次同意、质量与内容安全、配额、成本、删除、服务目标和独立功能开关门禁全部通过前，对应入口只能关闭或运行在不含真实用户数据的隔离环境。受控静态试穿获批不自动批准动态实验；衣橱、基础推荐、穿搭保存和反馈不得依赖任一云能力上线。

## 后端服务需求与阶段边界

本节归纳既有 CLOUD-REQ-001/002/004/006 的服务需求，不新增首版云能力，不改变原需求 ID 或发布门禁。

| 需求 | 用户或运营需要的结果 | 交付边界 |
| --- | --- | --- |
| 本地核心独立（001） | 无账号、离线或云服务故障时，仍能完成角色、衣橱与穿搭核心操作 | 不为本地调参、换装或基础推荐创建云任务 |
| 云处理可追踪（002） | 用户明确发起后可以查看接纳、处理、取消和失败结果，重复操作不重复产生业务效果 | 账号、媒体、任务及幂等属于后续云切片，健康接口不是这些功能 |
| 数据目的可控（004） | 上传前知情同意，撤回或删除后停止展示并可追踪清理；登录不代替同意 | 第一份云用户数据必须同时具备授权、保留和删除能力 |
| 服务可运行与恢复（006） | 运营能区分进程存活与依赖不可用；异常配置不能假报启动成功，停止服务可释放资源 | 当前运行基线只验证隔离环境，不宣称生产可用性或完整云业务就绪 |
| 交付可验证（006） | 每次服务交付有确定版本、可复现构建和实际运行证据，明确已完成与仍受限的能力 | 本地镜像通过不代表真实流量准入；完整验收及回滚仍必需 |

技术和目录解法归 [Design 02](../design/02-后端架构.md#结论)，精确依赖归 Design 01；本节不复制 API 字段、数据库结构或执行任务。17-01/17-07 只实现上述运行/构建子集，001–006 的完整产品需求不因基础设施通过而全部关闭。

## 背景与问题

静态 AI 试穿和条件启用的动态预览都不是能在前台立即完成的本地操作。上传可能中断，生成可能排队、超时、被供应商拒绝或产生不可展示的结果，用户也可能离开页面、撤回同意、取消任务或删除源照片。如果产品只显示一个持续旋转的加载页，用户无法判断任务是否已被接受、是否仍在处理、是否消耗了可用次数，也不知道失败后该重试、换图还是等待。

云端生成还会同时处理账号、人物和衣物图片、第三方服务、有限处理能力和完整删除。产品需要把这些复杂性收敛为少量稳定、诚实、可恢复的用户状态，同时保证任何云端故障都不会破坏用户已经完成的本地穿搭决定。

## 目标用户

- 已完成本地衣橱或穿搭方案，并愿意在充分知情后主动生成本人静态试穿，或在静态前置满足后参加动态实验的成年用户。
- 网络不稳定、会在生成期间切换页面或稍后返回，希望任务可以恢复而不必重复提交的用户。
- 对生成次数、等待时间、取消后果和数据删除有明确预期，不接受不透明扣减或无限等待的用户。
- 不愿登录或上传照片、只使用本地衣橱和推荐的用户；其本地路径同样属于本需求必须保护的体验边界。

## 产品目标

1. 用户在首次云端操作前完成账号建立，并清楚知道登录只服务云端能力，本地核心无需登录；账号认证不替代“本人且年满 18 周岁”的当前政策版本声明。
2. 用户能在上传前确认本次需要的素材、用途和处理条件，并在上传中断后安全继续或重新开始。
3. 每个静态或动态生成请求都有唯一、可返回查看的任务记录；退出页面、App 重启和网络切换不会让已接纳任务失去踪迹。
4. 用户始终能区分等待、处理中、检查结果、需要操作、取消处理中、成功、失败、过期和删除处理中。
5. 用户可以取消尚未完成的生成，但产品如实说明取消是请求，不虚构第三方已立即停止或数据已全部删除。
6. 失败给出与原因相符的恢复路径；自动恢复不重复创建结果、不重复占用用户可用次数。
7. 用户在提交前和任务结束后都能理解当前可用次数、是否暂时受限以及本次任务对可用次数的影响。
8. 删除请求立即停止内容继续可见，并可查询后续清理进度和最终结果。

## 非目标

- 不把登录、云上传或 AI 生成设为衣橱录入、结构化推荐、穿搭保存和反馈的前置条件。
- 不在本需求中定义服务端框架、数据库表、对象键、缓存、消息中间件、队列拓扑或进程部署方式。
- 不自动为每套推荐批量生成，也不在用户未确认时预上传人物或衣物素材。
- 不承诺任务即时开始、取消即时完成、第三方永不失败或所有外部副本可以瞬时物理擦除。
- 不提供真实尺码、量体、版型保证、面料物理、身体评价、换脸、他人照片生成或自由图片编辑。
- 不在当前范围建立订阅、付费次数包、免费容量分层或其他商业权益体系；内部成本控制不得伪装成用户付费规则。
- 不以推送通知作为任务完成或恢复的唯一方式；未授权通知的用户仍可在 App 内查看状态。
- 不默认同步完整衣橱、日历正文、精确地址、联系人或与本次生成无关的历史数据。
- 已确认 Look 图片可按 AVATAR-REQ-044 主动保存/系统分享；视频输出延期、公开链接不在本片。数据主体访问与结构化导出由 PRD 18 独立定义。

## 功能范围

### 账号与云端入口

- 用户无需登录即可完成本地核心路径；首次发起云端生成时接入已实现的邮箱账户与会话合同，App Client 在 14-01 实施。新增登录方式不是本片依赖。
- 本地无照片核心不要求年龄声明；首次选择本人照片或首次发起含本人照片的生成前，必须完成当前政策版本的“本人且年满 18 周岁”声明，政策版本变化后重新确认。使用授权虚构成人预设不要求用户谎称预设人物是本人。
- 登录前说明将开启的云端能力、仍保留在本地的能力以及账号注销入口。
- 登录失效时保留本地成果，云端入口显示需要重新验证；不得用登录失败阻塞本地启动。
- 退出登录只断开当前设备的云端会话，不等同于删除账号、任务或云端数据；退出前必须说明差异。
- 账号被限制、注销中或已注销时，产品提供与状态相符的只读、数据主体导出或删除路径，不循环要求登录；该路径不得绕过 AVATAR-REQ-044 的当前资产访问与输出权限。

### 上传准备与恢复

- 每次上传只包含用户本次主动选择、通过本地质量检查且与生成目的直接相关的素材。
- 上传前按生成类型展示将处理的人物图、衣物图或来源静态结果摘要、预估数据量和必要处理说明；用户可以返回换图、换来源或取消。
- 上传过程展示单个素材的准备、传输、校验和需要重选状态，不把“已传输”误称为“可用于生成”。
- 网络中断、App 进入后台或系统终止后，产品在安全恢复窗口内继续未完成步骤，或明确要求重新选择素材；不得悄悄重复上传和创建第二个任务。
- 无法确认完整性、素材已过期、同意已失效或用户已删除源资产时，上传关闭失败并说明下一步。

### 异步生成与任务记录

- 共用契约当前支持完整 Look 图片和静态 GLB 两类任务；图成功不自动创建模型，模型只从已确认且有效的图版本主动生成。视频为后续独立用途。
- 每种类型在提交前展示自己的视觉真实性边界、实际处理方、地域、输入、最长保留和删除方式，并记录独立目的同意；图片生成同意不能代替模型生成或后续视频生成同意。
- 任务被服务接受后返回稳定任务记录。用户离开页面后，可以从相关 Look、生成记录或账号入口重新找到它。
- 任务记录至少展示生成类型、提交时间、所用人物/衣物的安全缩略信息、当前状态、可执行操作、结果或稳定失败说明。
- 客户端断线或状态通知丢失时，重新进入任务页主动恢复权威状态；页面超时不能自动将服务端任务判为失败或取消。
- 成功只表示对应类型的结果已通过必要校验并可安全读取；供应商返回、文件上传完成或进入结果检查均不能提前显示成功。图片与模型满足 PRD 14 和 PRD 11 当前合同，后续视频另按 PRD 15 验收。
- 临时结果将过期时，应在用户仍可采取保留或重新生成操作的情况下说明期限；过期后不显示失效链接或空白图片。

### 取消、重试与失败恢复

- 等待或处理中的任务均提供取消入口。接受取消后先进入“正在取消”，直到确认不再产生可见结果或迟到结果已被隔离清理。
- 用户取消不自动删除仍被其他功能使用的源照片或衣物；产品提供独立删除入口并说明范围。
- 状态查询、结果收取等安全操作可有限自动重试；可能计费的提交只有在供应商幂等保证下才可重试，接纳不明时先核对。原任务持续可见，重试不得被统计为新的用户提交或隐瞒新增供应商费用。
- 自动重试仅在原目的、供应商、地域、同意和素材仍有效时继续；任何条件变化都暂停并要求用户重新确认。
- 图片模糊、遮挡、类别不支持等用户可修复失败，应给出换图或修改选择；权限、同意撤回、素材已删除等永久失败不得诱导反复重试。
- 失败不能改写已选 Outfit、衣橱、推荐和穿搭记录；用户始终可以返回无图方案或单品拼图。

### 配额与接纳透明

- 在用户提交前展示当前是否可生成、可用次数或限制恢复时间，以及预计等待明显变长时的提示。
- 相同用户意图的重复点击、超时重发、自动重试和重复回调只能形成一次用户可见任务效果，也只能结算一次可用次数。
- 服务自身、供应商或内容检查导致没有可用结果时，不扣减用户最终可用次数；临时占用应自动释放并在任务记录中反映。
- 用户取消后是否恢复次数必须按任务所处阶段使用统一规则，并在取消确认前说明；不能事后以模糊“可能消耗”代替规则。
- 防滥用限流与用户正常可用次数分开表达。因短时间操作过多而暂时受限时，显示恢复时间和安全替代路径。
- 配额用尽、全局暂停或成本保护触发时，不接受一个注定长期无结果的新任务；已经接纳的任务保持可查询、可取消和可恢复。

### 删除编排

- 用户可以分别删除单个生成结果、与任务关联的临时云素材、某个源资产的云端派生物，以及全部云端 OOTD 数据。
- 删除请求被接受后，目标内容立即从正常浏览和任何外部访问路径撤下；后台清理未完成时显示“正在删除”，不得提前显示“已彻底删除”。
- 删除覆盖源与派生内容、未完成任务、迟到结果、缓存、外部处理副本和后续可访问凭据；具体范围和备份窗口遵循 [`18-隐私与数据控制需求.md`](18-隐私与数据控制需求.md)。
- 删除部分失败时继续保持不可访问，显示仍在处理或需要支持；恢复任务不得让已删除内容重新可见。
- 删除完成后提供不含图片、路径或敏感正文的结果摘要；仍在固定备份到期窗口内时单独说明最晚到期时间。

## 核心用户故事

- 作为只想整理衣橱的用户，我可以不登录、不上传照片，持续使用完整本地核心。
- 作为首次尝试云端预览的用户，我可以在登录和上传前理解为什么需要账号、会上传什么以及拒绝后的替代路径。
- 作为弱网用户，我可以在上传或生成中离开页面，稍后看到原任务的真实状态，而不是重复提交。
- 作为等待中的用户，我可以知道任务是在排队、处理中还是检查结果，并看到是否需要我操作。
- 作为希望停止处理的用户，我可以请求取消，并理解“正在取消”和“已取消”的差异。
- 作为遇到失败的用户，我可以知道应该等待、重试、换图、重新同意还是返回无图方案。
- 作为受配额限制的用户，我可以在提交前看到当前可用情况，并确认失败、重试和取消如何影响次数。
- 作为删除数据的用户，我可以立即失去对目标内容的访问，并继续查看跨系统清理是否完成。
- 作为重装或重新登录的用户，我可以恢复仍在有效期内的云端任务状态，而不会覆盖本地衣橱和穿搭事实。

## 业务规则

### 任务创建与所有权

- **`CLOUD-RULE-001`** 云端生成必须由已认证用户主动发起，并绑定本次有效的类型专属处理同意、来源修订和必要素材；使用真实人物照片时还需当前政策版本本人且 18+ 声明，授权虚构成人预设按内容来源校验。账号认证不能代替适用的年龄声明，客户端提交的账号归属不作为可信事实。
- **`CLOUD-RULE-002`** 同一提交意图只产生一个任务。重复点击、网络重发和 App 恢复返回原任务，不创建第二份结果或重复占用次数。
- **`CLOUD-RULE-003`** 任务使用提交时的最小输入快照；用户之后编辑 Look 或来源图片不得静默改变已经接纳的生成，旧任务只能归属原版本。
- **`CLOUD-RULE-004`** 已删除、正在删除、未完成校验、跨账号或不符合当前用途的素材不能创建任务。
- **`CLOUD-RULE-005`** 图片失败不触发自动生成其他人物、衣物、供应商或跨境路线；图片成功或媒体保留不自动创建模型任务，图片/模型成功也不触发视频任务。

### 状态与结果

- **`CLOUD-RULE-006`** 用户可见状态必须来自可恢复的服务事实，不以页面内存、通知到达或供应商内部状态直接替代。
- **`CLOUD-RULE-007`** 状态只能向前进入合法阶段；迟到或乱序结果不能覆盖取消、删除、过期或更晚的失败状态。
- **`CLOUD-RULE-008`** 成功任务只能引用当前用户可访问、通过对应类型安全与质量检查的结果；图片/模型执行 PRD 14 与 PRD 11，模型必须绑定其来源图版本；后续视频另执行 PRD 15。
- **`CLOUD-RULE-009`** 系统可以对瞬时故障有限重试，但必须有总次数或截止时间；超过限制进入稳定失败，禁止无限加载。
- **`CLOUD-RULE-010`** 结果被产品或用户判定为严重异常时，应立即停止展示、允许举报和删除，且不将其记为可用成功。

### 取消与恢复

- **`CLOUD-RULE-011`** “取消请求已收到”只表示系统开始停止后续处理；只有外部处理、迟到结果和可访问内容均按策略收敛后才能显示“已取消”。
- **`CLOUD-RULE-012`** 取消和删除是不同动作。取消终止任务，删除处理已经存在的内容；产品不能用其中一个替代另一个。
- **`CLOUD-RULE-013`** App 退出、后台、断网、通知关闭和客户端超时都不自动取消任务。
- **`CLOUD-RULE-014`** 恢复只能继续原任务或展示原终态；不能借恢复绕过配额、重新同意、地域或删除限制。

### 配额与公平性

- **`CLOUD-RULE-015`** 配额规则按账号和生成类型统一执行，不因设备、重装或重复登录重置；图片与模型分别预检、接纳和结算，提交前分别说明额度与预计成本；后续视频另立额度。
- **`CLOUD-RULE-016`** 最终可用次数只对按规则交付的可用结果结算；服务失败、重复请求和被安全系统拒绝的异常结果不得造成重复或永久扣减。
- **`CLOUD-RULE-017`** 任何临时占用、释放、扣减和恢复都应最终收敛到用户可理解的可用状态；支持人员不能通过查看人物图片解决配额争议。
- **`CLOUD-RULE-018`** 配额透明不等于披露安全阈值。产品可以隐藏易被滥用的内部风控细节，但必须说明用户能采取的动作和恢复时间。

### 数据删除

- **`CLOUD-RULE-019`** 删除请求一经接纳，目标内容立即退出正常读取，后续任务不得再以其创建新派生物。
- **`CLOUD-RULE-020`** 已完成删除不能因迟到回调、任务恢复、缓存或备份恢复重新开放。
- **`CLOUD-RULE-021`** 账号注销包含全部云端任务与媒体的删除编排；本地数据是否同时删除由用户在注销流程中另行选择并确认。
- **`CLOUD-RULE-022`** 删除审计只证明范围、阶段、时间和结果，不保存已删除图片、可访问链接或可还原内容。
- **`CLOUD-RULE-023`** 生成、查看、保留、删除、重试和举报只属于任务或媒体操作，不自动创建 `OutfitPlan`、`WearEvent` 或推荐偏好；推荐学习只接受另行明确提交且可撤回的结构化反馈。
- **`CLOUD-RULE-024`** 图片的主动保存/系统分享按 AVATAR-REQ-044；内部缓存不等于用户输出权限，不创建公开链接或自动发帖。

### 规则到需求簇映射

| 稳定规则 ID | 对应需求簇 |
| --- | --- |
| `CLOUD-RULE-001`～`CLOUD-RULE-005` | `CLOUD-REQ-002`、`CLOUD-REQ-003`、`CLOUD-REQ-004` |
| `CLOUD-RULE-006`～`CLOUD-RULE-010` | `CLOUD-REQ-002`、`CLOUD-REQ-003` |
| `CLOUD-RULE-011`～`CLOUD-RULE-014` | `CLOUD-REQ-002`、`CLOUD-REQ-004` |
| `CLOUD-RULE-015`～`CLOUD-RULE-018` | `CLOUD-REQ-002`、`CLOUD-REQ-006` |
| `CLOUD-RULE-019`～`CLOUD-RULE-022` | `CLOUD-REQ-004`、`CLOUD-REQ-005` |
| `CLOUD-RULE-023`～`CLOUD-RULE-024` | `CLOUD-REQ-001`、`CLOUD-REQ-002`、`CLOUD-REQ-006` |

## 用户可见状态与失败恢复

| 场景 | 用户可见状态 | 可执行操作 | 恢复原则 |
| --- | --- | --- | --- |
| 未登录使用本地核心 | 本地使用中 | 继续衣橱、推荐、记录；主动开启云端 | 不弹出循环登录要求 |
| 云端会话失效 | 需要重新登录 | 重新验证、稍后处理、继续本地功能 | 不清空本地成果，不重复任务 |
| 素材准备/上传 | 正在准备、正在上传、正在检查 | 暂停页面操作、取消、按提示换图 | 恢复原上传意图；不确定完整性时重新选择 |
| 上传失败 | 网络中断、素材不符合要求或上传已过期 | 重试、换图、返回无图方案 | 不创建生成任务，不占用最终次数 |
| 任务已接纳 | 等待处理 | 离开页面、刷新、取消 | 任务记录持续存在，断线后主动查询 |
| 生成进行中 | 正在生成 | 离开页面、取消 | 不承诺即时完成或即时取消 |
| 结果检查 | 正在检查结果 | 等待、取消 | 未通过检查不显示成功或消耗最终次数 |
| 自动恢复 | 暂时遇到问题，稍后重试 | 等待、取消 | 有限重试；条件变化时暂停并重新确认 |
| 用户可修复失败 | 需要换图或调整选择 | 换图、修改、重新提交、返回方案 | 新提交是新用户意图，旧任务保留稳定终态 |
| 服务失败 | 本次未能生成 | 稍后重试、查看配额、返回方案 | 不损失本地成果，按规则释放临时占用 |
| 取消中 | 正在取消 | 查看进度、请求删除已有内容 | 迟到结果不可重新展示 |
| 已取消 | 已取消 | 删除临时云数据、重新发起 | 不把取消描述为已完成全部删除 |
| 成功 | 对应类型的预览已完成 | 查看、保留、举报、删除 | 结果仍是 AI 视觉参考，不代表真实合身、真实角度或推荐偏好 |
| 结果过期 | 预览已过期 | 重新生成或返回无图方案 | 不返回失效链接，不静默延长保留 |
| 配额或全局容量受限 | 暂时无法生成，并显示恢复时间或条件 | 稍后再试、继续本地功能 | 不接受无法在承诺窗口处理的新任务 |
| 删除处理中 | 内容已不可见，正在清理 | 查看删除进度、联系支持 | 部分失败保持不可访问并继续重试 |
| 删除完成/等待备份到期 | 已完成，或主系统已删且备份将在明确日期前到期 | 查看结果摘要 | 不提前声称所有备份已瞬时物理删除 |

## 成功指标及防误导指标

### 成功指标

- 已接受生成任务进入用户可理解终态的比例，以及排队、处理和端到端 p50/p95。
- 上传开始到校验成功的完成率；弱网、后台和重启后的原任务恢复成功率。
- 可用生成结果率、严重异常隔离率、用户举报处理时效和等待放弃率。
- 取消请求进入稳定取消或可解释终态的比例与耗时。
- 重复点击、重发和自动重试未产生重复结果或重复占用次数的比例，目标为 100%。
- 服务失败后临时配额自动释放率、配额争议率和受限状态说明到达率。
- 删除请求立即撤销访问的比例、各阶段完成时效、部分失败恢复率和删除后资产复活事件数，复活目标为零。
- 未登录、云端关闭和 Provider 故障时，本地核心流程仍可完成的比例。

### 防误导指标

- 不把“请求已接收”“上传完成”“供应商返回”或“进入检查”计为生成成功；只有可安全读取的合格结果计入成功率。
- 不把自动重试、重复消息或 App 恢复计为新增用户生成次数或活跃度。
- 不把用户查看、保留或操作 AI 预览计为实际穿着、推荐偏好、满意、合身或购买意愿。
- 不把“取消请求已收到”计为已取消，也不把取消计为删除完成。
- 不通过隐藏失败、无限延长处理中状态、自动跨境、放宽内容检查或静默换供应商提高成功率。
- 不通过模糊配额、延迟释放临时占用或把系统失败归因于用户提高成本指标。
- 不把主系统内容不可见等同于供应商和备份全部物理删除；分别度量并公开承诺。

## 依赖与风险

- 复用现有账户与会话合同，App 接入需证明登录恢复、退出和注销行为；认证中断不能损坏本地数据，新增社交登录不作为生成前提。
- 依赖已批准的处理目的、供应商、地域、保留、训练禁用和删除能力，详见 [`18-隐私与数据控制需求.md`](18-隐私与数据控制需求.md)。
- 依赖人物与衣物本地质量检查、静态试穿质量门；动态任务还依赖有效静态来源、动态质量与设备播放门。所有类型都依赖内容安全和诚实的 AI 标识。
- 依赖云端媒体、任务恢复、幂等、配额和删除链的端到端验证，但具体实现由设计文档确定。
- 供应商延迟、错误率、接口变化和成本波动可能造成排队或暂停；本地降级必须长期可用。
- 网络中断、App 被系统终止、状态通知丢失、重复回调和迟到结果可能导致状态不一致或重复业务效果。
- 用户可能误解取消、退出登录、删除结果、删除源照片和注销账号的范围，需在每个确认页明确区分。
- 配额说明过于精细可能帮助滥用，过于模糊又会伤害信任；需同时满足透明度和安全评审。

## 发布门禁

真实用户云端生成流量必须保持关闭，直到以下条件全部满足：

1. 独立数据清单、用户协议、隐私政策、App Store 隐私资料和账号注销时限已批准，并与实际网络数据流一致。
2. 生产供应商、必要子处理方、处理与存储地域、训练禁用、输入/输出/元数据保留和删除合同证据全部有效。
3. 上传前逐次同意、拒绝后的本地替代、退出登录、撤回、取消、删除和账号注销文案完成产品、隐私、法务与无障碍验收。
4. 对应生成类型的授权测试集达到已批准质量门槛：图片按 PRD 14 验证身份与衣物保真、严重异常、内容安全和公平性；静态 GLB 按 PRD 11/14 验证来源绑定、几何/纹理、连续观察与设备预算。视频延期，未来另按 PRD 15 验收。不合格结果在展示前隔离。
5. 任务创建、重复提交、App 重启、弱网、自动重试、迟到结果、取消竞态和状态恢复通过端到端验收。
6. 配额的展示、临时占用、最终结算、失败释放、取消规则和全局暂停策略已书面批准；当前未建立任何付费权益暗示。
7. 单个结果、源资产、全部云端 OOTD 数据和账号删除均完成跨自有系统、供应商、缓存和备份窗口演练；任何删除复活均为发布阻断项。
8. 云端请求接纳可用性、生成成功率、排队与端到端 p95、任务最大等待、自动重试上限、成本熔断和删除时限具有批准值、监控和停止条件。
9. 跨账号访问、伪造资源、失效凭据、敏感日志、公开资产和回调重放测试全部通过；运行与客服默认不能浏览用户图片。
10. 云能力必须具备可独立控制的功能开关，支持隔离验证、小流量灰度、立即停止接纳新任务，并妥善恢复已接纳任务；关闭云端后本地核心验收仍通过。
11. 云接入保持现有 OOTD 账户、衣物和历史记录完整，不自动合并、上传或清空本地数据；按 PRD 18 与 CLOUD-ACC-010 验收，不再要求已退役的旧生活管理迁移。

### 未决生产决策登记

| 未决决策 | 责任角色 | 最晚关闭里程碑 | 未关闭时阻断 |
| --- | --- | --- | --- |
| 各生成类型的生产供应商、处理地域、子处理方和跨境结论 | 隐私负责人、法务负责人、安全负责人 | 对应类型首个生产执行计划批准前 | 对应类型生产执行计划与真实流量 |
| 输入、上传临时物、失败中间物、临时结果、已保留结果、任务元数据和备份的保留期 | 隐私负责人、产品负责人 | 对应类型首个生产执行计划批准前 | 同意文案、删除承诺、生产执行计划与真实流量 |
| 各生成类型的免费/受控配额、临时占用、失败/取消/迟到结果结算规则 | 产品负责人、商业负责人 | 对应类型首个生产执行计划批准前 | 配额 UI、生产执行计划与真实流量 |
| 最大任务等待、自动重试次数/截止时间、接纳可用性和端到端 p95 目标 | 产品负责人、后端负责人、质量负责人 | 对应类型首个生产执行计划批准前 | 服务承诺、生产执行计划与真实流量 |
| 对应类型质量阈值与严重问题止损条件 | 产品负责人、质量负责人；动态另含 iOS 负责人 | 生产候选盲测或动态 POC 开始前 | 评测开始、生产执行计划与真实流量 |
| 成本硬上限、总预算、容量与熔断阈值 | 商业负责人、产品负责人、后端负责人 | 对应类型首个生产执行计划批准前 | 生产执行计划、配额配置与真实流量 |

这些值必须由供应商合同、法务结论、代表性评测或生产候选实测关闭；未知时保持未决。未关闭事项不阻断不含真实用户数据的隔离 POC，但阻断对应生产执行计划批准、对外承诺和真实流量。

## 追溯关系

| 本需求 | 上位需求/设计 | 后续验证 |
| --- | --- | --- |
| `CLOUD-REQ-001` 本地核心独立 | [`10-OOTD产品需求.md`](10-OOTD产品需求.md) 的本地核心、主动图片与按需模型边界 | [`../acceptance/10-OOTD产品系统验收.md`](../acceptance/10-OOTD产品系统验收.md) 的本地、离线与云失败场景 |
| `CLOUD-REQ-002` 账号、上传与共用任务 | [`14-AI虚拟试穿需求.md`](14-AI虚拟试穿需求.md)、[`15-动态预览需求.md`](15-动态预览需求.md)、[`../design/10-OOTD服务端与异步任务设计.md`](../design/10-OOTD服务端与异步任务设计.md) | 当前图/模型共用接纳、幂等、状态、取消、配额、恢复和删除，各自叠加输入/质量验证；视频未来启用时另验 |
| `CLOUD-REQ-003` 完整图片及模型的分层质量 | [`../design/07-AI虚拟试穿设计.md`](../design/07-AI虚拟试穿设计.md) | 授权测试集、质量举报、取消和失败验收 |
| `CLOUD-REQ-004` 同意、地域与删除 | [`18-隐私与数据控制需求.md`](18-隐私与数据控制需求.md) 与 [`../design/11-OOTD权限隐私与安全设计.md`](../design/11-OOTD权限隐私与安全设计.md) | 隐私、供应商准入、删除和发布包验收 |
| `CLOUD-REQ-006` 分阶段启用 | [`../plan/10-OOTD产品实施计划.md`](../plan/10-OOTD产品实施计划.md) | [`../acceptance/10-OOTD产品系统验收.md`](../acceptance/10-OOTD产品系统验收.md) 的云端、队列恢复、安全和发布场景 |

实现切片排入近期产品计划且编码前，创建 `17-SS-*执行计划.md`，在同一文件中声明覆盖的 `CLOUD-REQ-*` 与 `CLOUD-RULE-*`、生成类型、用户状态、配额、删除、验收场景、任务和证据。功能行为映射 [`../acceptance/17-云端生成与任务管理验收.md`](../acceptance/17-云端生成与任务管理验收.md)，跨功能本地降级、隐私、删除和发布层级同时映射 [`../acceptance/10-OOTD产品系统验收.md`](../acceptance/10-OOTD产品系统验收.md)。执行计划完成只证明切片工作与证据完整，不得绕过本 PRD 的真实流量发布门禁。

## 本次设计对应的需求确认

按用户指定的 design → PRD → plan 顺序，确认 17-07 继续落实 CLOUD-REQ-006：隔离验证无论成功或启动失败都应清理本次创建的资源；清理无法确认时不得报完整通过，不删除其他服务资源。此项细化服务验证的失败边界，不新增消费者功能、云上传、认证或生产部署承诺。设计见 Design 02 的当前任务调整，任务仍维护在原 17-07。
