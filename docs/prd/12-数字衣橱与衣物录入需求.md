# 数字衣橱与衣物录入需求

## 生产三维服装需求

`WARDROBE-REQ-028`：生产服装采用来源、许可、版本和原始交付包可追溯的标准 GLB，交付即适配所选人物的实体/骨架与实际支持参数。不要求 Blender 母版、固定骨数或未经确认的 shape keys；项目人员不承担手工建模、绑骨或修衣物。需要遮蔽的区域必须有真实行为，出现穿模、漂移或形变破损的组合不发布，不能用暗改体型或整体放大掩盖问题。

首批两上装、两下装、两双鞋；基础遮蔽始终有效。外套槽位保留设计，未有合格资产前不宣称已供给。服装生产准入、运行时与代表包合同见 [Design 20](../design/20-OOTD完整产品能力与阶段架构设计.md) 和 [Three.js 研究](../design/threejs-avatar-research.md)。工程 GLB 不抵扣正式服装验收。

## 2026-09-14核心换装需求固定

当前默认使用内置服装；后文 2026-09-13 的照片/真实衣物优先排期为历史，不再阻止零衣物用户使用三维。

`WARDROBE-REQ-027`：托盘与已应用造型分开。内置首个支持集包含上装、可选外套、下装、鞋；同槽位选择替换旧选择，鞋按一双计一项。缺少必需槽位或组合未经支持时，指出问题并保持上一有效造型，不能擅自更换单品或改变体型；Dress up 对完整有效草稿执行真实本地换装，取消恢复已应用组合。只有已通过资源与组合验收的服装可以显示为可用。

内置 GarmentAsset 与真实 WardrobeItem 的身份分离；模板 Look 保存不代表拥有或实际穿着，不能直接写入以真实衣物为前提的计划/实际记录。真实衣物无 3D 模板时仍保持原录入/推荐/记录权利。此处是既有 REQ-022～026 的状态细化，不增加商城或任意资产上传。

追踪：Design 05 核心合同 → WARDROBE-REQ-027 → 11-04 / 正式资产准入 → WARDROBE-ACC-016。完整服装内容量以实际美术清单及兼容矩阵为准，首批正式供给固定为两上装、两下装和两双鞋。

## 2026-09-14内置服装目录

`WARDROBE-REQ-025`：新安装随包提供可直接换装的内置服装目录，首批覆盖 TOPS、BOTTOMS、SHOES；OUTERWEAR 仅在合格资产发布后提供；内置服装使用稳定 ID、版本、类别、颜色、兼容 rig 与自有/授权资产来源。目录是只读产品资产，不伪装成用户拥有的真实衣物，不写入用户衣橱表。

`WARDROBE-REQ-026`：用户无需上传或录入衣物即可从内置目录选择一套兼容造型。选择托盘显示数量与逐件移除，Dress up 立即提交本地三维草稿；不完整或冲突组合给出明确槽位反馈，不启动伪造 AI 加载。

## 2026-09-14近期12-05批准范围

为落实 WARDROBE-REQ-002/003/004/006 的渐进属性与历史边界，近期切片拟先提供正式度、保暖感受、雨天适用和步行适用四项手工确认信息；每项可留未知或清除为未知，不因未填阻止保存。值仅代表用户明确判断，不推断认证等级或保证实际效果。已保存计划保留旧快照，彻底删除衣物仍覆盖所有新字段；换图或只改名称/状态不得丢失属性。

具体取值、来源语义、数据变更和测试合同见 [Design 05](../design/05-数字衣橱与衣物录入设计.md#2026-09-14首批手工确认属性提案) 与 [12-05](../plan/12-05-手工衣物属性与确认来源执行计划.md)，已于 2026-09-16 获用户确认。本切片不缩减完整属性、筛选、推荐和后续模型建议范围，也不将手工属性编辑计为推荐实现或真实穿着。

2026-09-13 当前 Woo 流程由 12-03 前置单件图片导入与媒体生命周期：保存/换图中断不得丢失旧图或衣物；移除照片立即隐藏、清理可重试，重启不得复活。此为 WARDROBE-REQ-009/010/012/015 的实现细化，沿用单件衣物的输入规则，不把存储 POC 当作人物或商品图准入。

## 2026-09-13近期范围

12-01 落实已批准的无图最小录入、离线身份/保存、手工编辑和删除：名称、七类类别、四种可用状态由用户确认；搜索/筛选、重复提交与失败恢复不依赖图片、账号或网络。当前仅保存实际使用字段，未知属性不补默认值；归档/历史快照、照片与三维仍由后续原范围交付，不将第一片完成记为整项需求通过。设计合同见 Design 05 当前首片，执行与验收归 12-01。当前用户无需旧数据保留的决定适用旧开发实现，未来保存的衣物仍须保护。

## 2026-09-12实现准备与动画适配

保存组合与角色删除按 AVATAR-REQ-020/021 的共同规则处理：有效结构化组合的保存不依赖三维绘制成功，失效旧图不得成为新组合封面；角色被删除后，历史组合移除私人角色参数/引用，保留真实单品、独立服装示意和实际穿着事实。该修复细化 WARDROBE-REQ-024，不启用新云能力。

WARDROBE-REQ-022～024 的资产输入与交付流程按 [Design 05 SOP](../design/05-数字衣橱与衣物录入设计.md) 执行。只有资产清单声明且验收覆盖的体型、姿态和叠穿组合才能标为支持；角色后续增加动作时必须重新确认衣物适配，不能把站姿兼容解释成任意动作兼容。

当前 Three.js 工程舞台已具备观察/换装与受控微动，眨眼和轻微视线是首版待实现需求；正式资产尚未交付。款式与参数支持由发布清单验证，云视频不是本地三维前置。

## 状态与上位需求

| 项目 | 说明 |
| --- | --- |
| 文档状态 | `approved`；12-05 具体合同于 2026-09-16 获确认并开始执行。 |
| 上位需求 | [`10-OOTD产品需求.md`](10-OOTD产品需求.md)。 |
| 对应设计 | [`../design/05-数字衣橱与衣物录入设计.md`](../design/05-数字衣橱与衣物录入设计.md)。 |
| 需求边界 | 本文定义内置目录/换装与真实衣橱两个来源，以及本人 OOTD、单件衣物、商品图的分期录入、确认、状态和图片生命周期；推荐排序、虚拟试穿与穿后反馈分别由 13、14、16 号主题负责。 |

本文是 10 号总 PRD 的功能拆分，不替代总 PRD。个人衣橱是用户真实拥有物品的事实集合；内置目录是独立的只读产品资产，供零录入换装，不写成拥有事实，也不是购物目录。

## 背景与问题

传统数字衣橱往往要求用户在首次使用时完整拍摄和标注大量衣物，录入成本高，价值出现得晚。另一方面，只依赖图片自动识别会把不确定属性写成事实，导致后续推荐使用错误信息。

产品先用内置服装提供零录入三维体验；用户自愿管理真实穿搭时，再从今天正在穿的一套衣服开始，逐件、渐进地建立衣橱；照片和自动建议用于降低成本，但不能成为保存门槛。现实中的待洗、借出、已打包和归档状态必须可靠影响新推荐，同时不能改写历史穿搭。

## 目标用户

- 拥有一定数量衣物，但不愿一次整理完整衣柜的用户。
- 希望从一套 OOTD 或少量常穿单品开始获得价值的用户。
- 愿意拍摄衣物、选择已有商品图，或更偏好纯手工无图录入的用户。
- 经常遇到衣物待洗、借出、打包或换季归档，需要推荐反映现实状态的用户。
- 需要离线浏览和维护个人衣橱的用户。

## 产品目标

1. 用户不建库即可从内置四类目录选衣，在真实三维角色上换装并保存 Look；真实衣橱可从一件衣物逐步建立，不设建库前置。
2. 仅对自愿进入真实衣橱建档的用户，从首次建档动作起，通过手工或获准的一张本人 OOTD 照，在两分钟内确认至少两件衣物并保存可穿搭组合草稿；该草稿不自动成为穿搭计划或实际穿着。
3. 每件衣物具有稳定身份、用户确认的最小属性和明确的当前可用状态。
4. 图片与属性建议显著降低录入成本，但删除图片、识别失败或设备离线不影响结构化衣物存在。
5. 待洗、借出、已打包和归档等现实状态及时影响新推荐，历史搭配仍保留当时事实。
6. 衣物照片与人物照片、生成结果分开处理，用户可只移除照片而保留衣物和历史。

## 非目标

- 不强制首次录满指定数量衣物，不以“衣橱完整度”阻止手工搭配或穿搭记录。
- 不从完整照片库、社交媒体、购物平台、账单、通知或订单中静默抓取衣物。
- 不把模型猜测的尺码、材料、保暖、洗护、价格或品牌当作事实。
- 不建立共享或家庭衣橱、租赁、二手交易、库存估值、购物清单或商品导购。
- 不提供品牌目录、商品搜索、社区内容流、联盟链接或未拥有商品推荐。
- 不要求每件衣物必须有照片，不因图片质量不足阻断今日穿搭决定。
- 不在录入流程中实现云端试穿、动态预览或远程推荐排序。
- 首发不提供衣物图片、组合草稿或生成媒体的下载、导出、社交分享和公开链接。

## 功能范围

### 首发必备能力

- 默认浏览随包内置目录，按 TOPS、OUTERWEAR、BOTTOMS、SHOES 同页分组；不将“四类”误做固定四列。至少两件上装轮廓几何不同。
- 内置目录与“我的衣物”有明确来源；未录入真实衣物时内置目录仍完整可用，不注入示例衣物到个人衣橱。
- 选择托盘、计数、逐件移除、同槽替换、完整性校验和 Dress up 按 REQ-025～027；未支持或未验证组合不能显示可穿预览。
- 保存模板 Look 使用 PRD 16 的独立语义；切换目录不自动建立拥有关系，真实衣物关联模板必须由用户主动确认。

以下为自愿建真实衣橱的本地能力，不是内置三维前置：

- 从“今日”和“衣橱”入口新增衣物，两处写入同一个人衣橱并遵循相同确认规则。
- 首版支持无图手工新增和单件衣物图片；下表完整保留三类输入的安全契约，本人 OOTD 批量拆分和专项商品图导入为后续条件能力。
- 允许先填写名称、类别和当前状态，其他属性未知时仍可保存并稍后补全。
- 完整目标覆盖类别、颜色、图案、季节、正式度、保暖和材质；当前已按 12-05 提供正式度、保暖感受、雨天和步行适用的手工确认，模型建议须其能力获准后启用。未知可保存。
- 提供默认“可穿”视图，以及按类别、状态和已确认属性浏览或筛选。
- 支持可穿、待洗、借出、已打包与归档；状态变化影响新推荐，不静默改写已保存方案。
- 同名衣物允许是不同真实单品，不自动合并。智能疑似重复和批量逐件复核按后续条件能力交付。
- 用户可以移除衣物照片但保留名称、属性、状态和历史引用。
- 所有基础录入、编辑、浏览、状态维护和草稿处理在无网络时可用。

### 三种图片输入模式

| 模式 | 允许输入 | 处理与结果 |
| --- | --- | --- |
| 本人 OOTD | 用户主动选择、确认本人且年满 18 周岁、画面只有一个主要人物的当日穿搭照片。 | 继承 PRD 11 的选图前说明、本人/18+ 声明、多人阻断、安全解码、元数据净化和临时生命周期；只产生待确认衣物草稿与组合草稿。 |
| 单件衣物 | 平铺、悬挂或用户拍摄的一件主要衣物；一双鞋可作为一个单品。 | 明显人物或无法分离的多件重叠进入换图、手工选主体或无图路径；不自动转入人物照片流程。 |
| 商品图或购物截图 | 用户主动选择、确认有权使用且衣物确实属于本人衣橱的图片。 | 在本机先裁掉人物、价格、订单、地址和页面无关内容；不能安全去除可识别人物或敏感正文时，不保存原截图，改用换图或无图录入。 |

单件衣物 MVP 的自动检查只负责阻断当前检测器能够识别的明显人物或面部；零检出不表示系统确认图片无人，也不能自动保存。每张零检出候选仍须用户对当前图片逐项确认“没有人物或身体部位”“只有一件主要衣物或一双鞋”“本人拥有”“主要单品完整且没有多件重叠”。任一项未确认、分析失败或检测到明显人物/面部时不得生成可保存值。局部手、臂、腿、脚的自动识别属于后续本地辅助能力，不能作为首版单件衣物图片入口的完成前提或安全宣传。

同一张本人 OOTD 若还要用于数字形象，必须分别展示“衣物拆分”和“人物参考”两个目的并让用户逐项选择。只选择衣物拆分时，完成草稿确认、取消、失败或批准的临时期限到期后，应清理人物源图和人体派生内容，不得创建可复用人物形象。

### 后续或条件能力

- 本人 OOTD 批量拆分、专项商品图导入、智能疑似重复和批量逐件确认；保留本文输入与中断契约，须对应切片及质量验收后启用。
- 更丰富的批量拍摄、补拍背面、细节或洗护标。
- 获批的本地辅助识别、相似度建议和更精细的前景处理。
- 多设备结构化衣橱同步，以及经过独立同意的衣物图片同步。
- 由用户逐件确认的购买记录关联。
- 衣物图片作为静态 AI 试穿输入；必须满足单独的质量、同意和云端门禁。

条件能力关闭时，手工、无图与离线路径必须保持完整，不能展示无法完成的入口。

## 核心用户故事

- 作为新用户，我不添加任何衣物也可用内置目录换装；想管理自己穿什么时，再逐件添加真实衣物。
- 作为不愿拍照的用户，我可以用名称、类别和状态创建无图衣物并参加基础推荐。
- 作为用户，我可以从一张 OOTD 照确认多件草稿，并明确知道遮挡或未知的属性。
- 作为用户，我知道这次导入只创建衣物草稿和组合草稿；我主动保存后才形成计划，主动确认日期和“实际穿了”后才形成实际穿着。
- 作为用户，我可以连续拍摄或选择多件衣物，逐件确认后随时退出并继续。
- 作为用户，我可以修正系统建议，后续重新识别不会覆盖我的确认值。
- 作为拥有多件相同基础款的用户，我可以将疑似重复项保留为不同衣物。
- 作为用户，我可以把衣物标记待洗、借出、已打包或归档，让新推荐符合现实。
- 作为用户，我可以只删除照片，不丢失衣物事实、穿着历史和反馈。
- 作为离线用户，我可以浏览、筛选、新增和修改状态，稍后联网不会覆盖本地确认。
- 作为使用辅助技术的用户，我可以通过文字而非仅靠图片、颜色或透明度识别衣物和状态。

## 业务规则

| 需求 ID | 规则 |
| --- | --- |
| `WARDROBE-REQ-001` | 衣物必须有稳定业务身份，不以文件名、图片摘要、照片库标识或本地顺序作为跨端身份。 |
| `WARDROBE-REQ-002` | 名称、类别和当前状态是最小保存信息；图片、品牌、价格、材质和尺码不是必填项。 |
| `WARDROBE-REQ-003` | 自动识别结果默认为建议；用户确认或修正的值优先，模型更新不得静默覆盖。 |
| `WARDROBE-REQ-004` | 不确定属性保持未知；不得以默认值补齐后作为硬过滤事实。 |
| `WARDROBE-REQ-005` | 归档衣物不进入新推荐；待洗和借出默认不可用；已打包只在用户明确匹配的上下文中可用。 |
| `WARDROBE-REQ-006` | 衣物当前状态与历史搭配快照分离；改名、归档或移除照片不重写历史。 |
| `WARDROBE-REQ-007` | OOTD 中被遮挡的衣物必须标记信息不完整，不能生成隐藏细节并将其当作真实衣物事实。 |
| `WARDROBE-REQ-008` | 疑似重复只提示；在用户明确确认前，不自动合并、覆盖或删除任何一件。 |
| `WARDROBE-REQ-009` | 衣物图片可以缺失；图片丢失、删除或处理失败不得删除衣物及其历史引用。 |
| `WARDROBE-REQ-010` | 移除照片必须覆盖由该照片直接产生的展示与视觉派生内容，并处理仍在引用它的条件任务。 |
| `WARDROBE-REQ-011` | 保存的未来方案遇到衣物状态变化时显示冲突，不静默替换其中的衣物。 |
| `WARDROBE-REQ-012` | 基础衣橱不因推荐、浏览或状态维护自动上传图片、属性或用户标签。 |
| `WARDROBE-REQ-013` | 商品图导入必须由用户主动选择并确认拥有该衣物；价格、订单、地址和页面文案不得自动成为衣物事实。 |
| `WARDROBE-REQ-014` | 每个批量项目独立确认；失败或退出不回滚已经确认的衣物，也不能重复创建它们。 |
| `WARDROBE-REQ-015` | 本人 OOTD、单件衣物、商品图使用三套明确输入规则；一种模式的允许内容不能静默扩展到另一种。 |
| `WARDROBE-REQ-016` | 本人 OOTD 继承 `AVATAR-REQ-002`、`AVATAR-REQ-005`、`AVATAR-REQ-006` 与 `AVATAR-REQ-013`：本地无照片核心不要求年龄声明，选图前确认本人且 18+，多人阻断并先完成净化。 |
| `WARDROBE-REQ-017` | OOTD 导入默认只产生待确认衣物草稿和组合草稿；用户主动保存才创建 `OutfitPlan`，主动确认日期和“实际穿了”才创建 `WearEvent`。 |
| `WARDROBE-REQ-018` | 同一 OOTD 的衣物拆分与人物参考是独立目的；未选择人物参考时不得保留可复用人物源图、人体派生物或数字形象。 |
| `WARDROBE-REQ-019` | 商品图或购物截图在本机移除可识别人物、价格、订单、地址和无关页面内容后才能成为衣物图片；无法安全净化时不保存原图。 |
| `WARDROBE-REQ-020` | 自愿建立真实衣橱的建档指标要求两分钟内至少两件衣物确认成功且组合草稿原子保存成功；它不是新用户内置换装的进入或保存条件；未确认草稿、半保存和自动识别完成均不计入。 |
| `WARDROBE-REQ-021` | 首发不提供衣物、组合或生成媒体下载、导出、社交分享和公开链接。 |
| `WARDROBE-REQ-029` | 用户主动使用账号云能力时，可按本人会话创建、读取、分页列出、修改和删除无图结构化衣物；服务端不接收 owner ID，不把内置目录写成用户拥有，也不因本地浏览、推荐或状态维护自动上传。 |
| `WARDROBE-REQ-030` | 云端衣物沿用客户端生成的稳定 UUID；同 owner、同 ID、同内容创建幂等，不同内容冲突。更新和删除必须带当前 `expected_revision`，过期操作不得覆盖较新事实。 |
| `WARDROBE-REQ-031` | 云端衣橱首片只保存名称、类别、可用状态、来源、revision 与服务端时间；不上传衣物图片、路径、摘要、属性建议、历史、内置或三维资产引用。跨账号读取统一不可见，账号删除级联清理结构化衣物。 |

## 用户可见状态与失败恢复

| 状态或失败 | 用户看到 | 恢复路径 |
| --- | --- | --- |
| 零真实衣物 | 内置目录与三维穿搭可直接使用；我的衣物显示空态 | 继续内置换装，或自愿无图新增/单件选图；条件图片入口仅在获准后显示。 |
| 选择不完整或冲突 | 缺少上装/下装/鞋，或具体不支持组合；托盘状态保留 | 补选、替换、移除外套或取消；不自动改体型、单品或上传。 |
| 单项资源不合格 | 此内置款当前不可用，上一有效造型保持 | 选另一已验证款式或重试；不把静态缩略图当换装成功。 |
| 待确认草稿 | 建议、未知字段、来源和剩余数量 | 修正后保存、跳过、继续或全部丢弃未确认草稿。 |
| 图片模糊或裁切 | 一个主要问题和对推荐/视觉的影响 | 重拍、换图或保存为无图衣物。 |
| 单件衣物模式包含人物或多件重叠 | 当前无法安全形成单件素材 | 重新拍摄、手工选择主体或无图新增；不自动保存人物图，也不静默切换模式。 |
| 本人 OOTD 包含多人或声明未完成 | 当前 OOTD 不进入拆分，临时输入不保留 | 选择只有本人的照片、完成本人且 18+ 声明，或改用无图/单件衣物路径。 |
| 商品图仍含可识别人物、订单或地址 | 当前图片不能安全保存为衣物素材 | 在本机重新裁剪、换图或无图新增。 |
| 属性建议不可用 | 名称、类别和状态仍可编辑 | 手工确认最小信息并保存。 |
| 疑似重复 | 两件的已知属性、图片和状态差异 | 合并、分别保留或取消本次新增。 |
| 批量处理中断 | 已保存数量、待确认数量和本机草稿说明 | 继续、逐件处理或丢弃剩余草稿。 |
| 存储不足 | 当前项目尚未确认，已保存项目不受影响 | 清理空间后重试，或无图保存。 |
| 图片损坏或丢失 | 无图占位与结构化信息 | 重拍、换图或继续无图使用。 |
| 离线 | 明确显示本地状态，不等待网络 | 完成基础新增、编辑和状态维护；联网后仍以用户确认值为准。 |
| 状态与未来方案冲突 | 受影响方案和单品状态 | 替换、恢复状态或保留为仅计划。 |
| 照片删除中/失败 | 图片立即隐藏并说明仍在清理 | 重试清理；衣物和历史始终可用。 |

## 成功指标及防误导指标

### 成功指标

- 自愿建档用户从进入手工新增或获准的本人 OOTD 导入起，两分钟内确认至少两件衣物并原子保存组合草稿的比例；未确认草稿和自动识别完成不计入。
- 单件衣物从进入新增到确认完成的中位时间。
- 无图新增、单件图片、OOTD 拆分和批量整理各自的完成率。
- 每件衣物平均需要用户修正的字段数与后续纠错率。
- 零真实衣物用户在首次会话完成内置选衣、真实换装和 Look 保存的比例；不得计为真实衣橱完整度或实际可穿推荐。
- 待洗、借出、打包和归档状态被正确维护并影响推荐的比例。
- 批量中断后成功继续的比例，以及重复创建率。
- 无照片和离线用户完成新增、编辑与状态维护的比例。
- 图片移除后衣物与历史完整保留的成功率。

### 防误导与护栏指标

- 不把系统自动生成但未经用户确认的草稿计为已录入衣物。
- 不通过预填未知属性、默认接受建议或隐藏跳过入口缩短录入时长。
- 不把图片处理成功率等同于衣物事实准确率；需单独跟踪用户修正和未知比例。
- 不以衣橱项目数量作为核心成功指标，避免推动重复项或低质量批量导入。
- 无图衣物的保存、状态维护和基础推荐资格不得被人为降低。
- 疑似重复提示的误合并率和撤销/恢复请求必须作为质量护栏。
- 图片删除只有在承诺的直接派生内容完成清理后才计为成功。

## 依赖与风险

- 内置资产不足、穿模或来源混淆会破坏零录入价值；真实衣橱新增仍需恢复原场景与已锁定单品。
- OOTD 拆分可能受遮挡、叠穿和拍摄角度影响，易把推测细节误当事实。
- 相似基础款可能被误判重复，自动合并会污染状态和穿着次数。
- 类别、颜色、保暖和正式度建议存在偏差，需要未知态和低成本修正。
- 批量流程、低存储或异常退出可能产生未确认草稿或图片残留。
- 衣物状态维护过重会降低使用率，需要支持在详情、今日方案与穿后反馈中快速修改。
- 衣物图片仍可能包含人物、住址、订单等无关内容，需要最小化和明确恢复路径。
- 后续同步和云端试穿会扩大图片生命周期，必须独立评审，不能复用基础录入同意。

## 发布门禁

- 零账号、零自有衣物、断网新安装可浏览四类内置目录并完成有效组合的真实几何换装、取消/撤销、Look 保存和重启恢复。
- 全部发布组合与参数域通过基础遮蔽、身体/服装及服装间穿模、材质/轮廓检查；不支持资产明确不可用，54 图技术样本不能代替正式内容库验收。
- 内置 Look 不写入 WardrobeItem，不计入拥有、推荐资格或实际穿着；无模板的真实衣物仍保留原能力。

以下本地衣橱门禁按相应切片覆盖；OOTD、批量和智能建议的专属检查只在该条件能力启用前要求，不阻断内置三维：

- 无照片、无网络且不授予相机或完整图库权限时，能够新增、编辑、浏览、筛选和修改状态。
- 已开放的单件新增使用一致的衣物语义，保存后返回原上下文；OOTD/批量开放时须复验同一规则。
- 用户确认值、模型建议和未知属性在 UI 与业务行为上可区分，重新处理不会覆盖用户修正。
- 待洗、借出、打包和归档状态有可执行的推荐过滤与历史保留证据。
- 疑似重复不会自动合并；批量中断、低存储和重启不会重复创建或留下半完成衣物。
- 图片移除、衣物删除与历史保留的影响说明清楚，异常清理可重试且不误删其他衣物。
- 人物、GPS、订单和原文件名等无关信息不会因基础衣橱录入进入网络、日志或默认导出。
- 最大辅助字号、VoiceOver、深色模式和仅文字/无图状态通过核心流程验收。
- 条件云能力未通过供应商、同意、隐私和删除门槛时，基础衣橱无远程依赖且不存在不可用入口。

## 追溯关系

| 层级 | 追溯要求 |
| --- | --- |
| 总 PRD | [`10-OOTD产品需求.md`](10-OOTD产品需求.md) 定义整体冷启动、离线和真实衣物边界。 |
| 功能 PRD | 本文以 `WARDROBE-REQ-*` 作为稳定需求编号。 |
| Design | [`../design/05-数字衣橱与衣物录入设计.md`](../design/05-数字衣橱与衣物录入设计.md) 说明流程、状态、领域关系与失败降级。 |
| 执行计划 | 某个切片排入近期产品计划且编码前创建 `12-SS-*执行计划.md`，在同一文件中声明覆盖的 `WARDROBE-REQ-*`、数据迁移、媒体生命周期、兼容策略、任务与证据，并区分首发与条件能力。 |
| acceptance | 功能行为映射 [`../acceptance/12-数字衣橱与衣物录入验收.md`](../acceptance/12-数字衣橱与衣物录入验收.md)，跨功能首次价值、照片目的和发布层级同时映射 [`../acceptance/10-OOTD产品系统验收.md`](../acceptance/10-OOTD产品系统验收.md)，并保存可复核证据。 |

2026-09-13 12-03 页面保存细化：当同一次保存包含衣物字段与一张已复核照片时，两者一起生效；图片失败不得留下半完成的新增衣物或更新后的字段。此为现有保存/换图失败语义的实现约束，不新增录入模式、云端能力或历史兼容；对应 Design 05「页面保存的衣物与照片原子提交」。

### 2026-09-13衣橱浏览验收细化

分类网格展示用户真实衣物，沿用现有名称/类别/可用状态筛选；照片保持完整比例，未添加或不可读取时仍能进入编辑。名称和状态可读，辅助功能字号采用单列。保存换图、移除照片及重新进入页面后，网格不得展示旧缓存。参考限于 Woo 演示已观察到的分类浏览，不据此声称完成生成试穿或真实 3D。
