# 每日记录与穿搭社区验收

标准状态：`approved`；功能验收 `in_progress`。19-02 已完成账号状态与 handle/bio 公开资料的本机代码、OpenAPI 和 PostgreSQL 验证；日记、帖子、互动、治理、头像/通用媒体、邮件挑战和可靠删号仍未实现。关联 [PRD19](../prd/19-每日记录与穿搭社区需求.md)、[Design21](../design/21-后端产品边界与领域设计.md)、[Design22](../design/22-后端数据库设计.md)、[Design23](../design/23-后端接口设计.md)、[Plan 19-02](../plan/19-02-账号状态与公开资料执行计划.md)。

| ID | 场景与必须断言 | 必需证据 | 状态 |
| --- | --- | --- | --- |
| COMMUNITY-ACC-001 | 无衣物用户同日写两条日记、编辑、重启读取；跨账号关联/读写拒绝；删除实际事件只解除关联；未来日记日期拒绝 | service+真实 PG+HTTP | pending |
| COMMUNITY-ACC-002 | 同日计划/日记/实际事件各自聚合，补记和跨时区不漂移；删除/纠正实际事件与反馈后重算统计 | 固定日期 fixture、事务与日历响应 | pending |
| COMMUNITY-ACC-003 | 日记导入草稿只含选定公开字段；source ID/精确上下文/采集原图不出现在公开 DTO、URL、Feed 或日志；收藏者身份不公开 | DTO allowlist、两账号+匿名差分、媒体访问 | pending |
| COMMUNITY-ACC-004 | 新帖和评论审核后可见；修改已发布帖产生新版本；旧版保持；撤回、下架与审批并发，迟到任务无法复活；重复审核幂等 | PG 行锁并发、worker 重放、HTTP | pending |
| COMMUNITY-ACC-005 | 时间相同多帖稳定分页；发现/关注/作者/搜索/标签/图片执行一致可见性；下架不从旧游标漏出；发现流无私人衣橱 | Feed fixture、SQL查询计划、匿名接口 | pending |
| COMMUNITY-ACC-006 | 重复赞藏关只一条关系；取消幂等；回复不能跨帖或超过一级；评论删除隐藏正文，父删除保留允许的回复；通知不暴露收藏者 | 唯一/FK真实约束、并发/HTTP | pending |
| COMMUNITY-ACC-007 | 屏蔽与关注/评论并发仍禁止互动，解除不自动恢复；举报重复/解决/申诉可追踪，封禁隐藏公开内容 | 两账号竞态、治理状态机 | pending |
| COMMUNITY-ACC-008 | 普通用户不能提权/审核/封禁或读取举报；挑战过期/重放拒绝；验证/找回不泄露邮箱是否存在；封禁立即失效会话 | transport权限矩阵、PG、邮件测试替身 | in_progress：本人 DTO 拒绝 role/status；suspended 账号登录/旧会话/公开资料拒绝；挑战、运营 API 和封禁动作待实现 |
| COMMUNITY-ACC-009 | 私有/社区媒体多用途隔离；解码预算/EXIF清除/固定版本；删日记保留或撤回独立帖；撤回即拒绝图片；删账号不中途丢失 MinIO 清理依据 | 真实 MinIO/PG/RabbitMQ，故障恢复/全版本删除 | pending |
| COMMUNITY-ACC-010 | PG提交后队列断开最终补投；重投不重复衍生图/通知；Redis缓存丢失不丢事实；通知失效目标不回显正文 | 独立 tests/services/integration 断连与重放 | pending |
| COMMUNITY-ACC-011 | 空库生成 schema、预期 FK/CHECK/索引生效；OpenAPI 唯一且包含权限/错误/分页；生成客户端可消费；日志无私有正文 | GORM/PG、OpenAPI契约、Umi smoke、CI | in_progress：19-02 users/user_profiles 约束、级联、41-operation OpenAPI、Umi 生成和完整 integration 已通过；远程 CI 及后续表待验 |

## 19-02 当前证据

| 范围 | 命令 / 断言 | 结果 |
| --- | --- | --- |
| Go 静态与单测 | `go mod verify`、`go vet ./...`、`GIN_MODE=release go test ./... -count=1` | passed |
| 竞态 | `GIN_MODE=release go test -race ./... -count=1` | passed |
| 真实 PostgreSQL | `GIN_MODE=release go test -race -tags=services ./tests/... -count=1` | passed；隔离 schema 验证注册默认值、公开资料规范化/唯一/版本、账号状态过滤、CHECK 与删除级联 |
| HTTP/OpenAPI | profile 三条成功响应通过 kin-openapi 校验；未知字段、私有字段泄漏、401/404/409 映射有测试 | passed；运行时总数 41 |
| Umi | 临时空库启动实际 API；`THEN_OPENAPI_SCHEMA=http://127.0.0.1:18080/openapi.json npm run openapi`、`npm run typecheck`、`npm run test` | passed；生成 41 个函数和 profile client，临时库已删除 |
| 完整进程回归 | `GIN_MODE=release go test -race -tags=integration ./tests/... -count=1` | passed |
| 远程 CI | 尚未提交本工作树 | pending |

测试只能使用合成图文和测试账号。真实依赖测试保留在 `backend/tests`，不另建 test 服务。发布验收另需真实媒体、审核运营演练、保留/备份删除、邮件、HTTPS、限流预算及前端/App完整旅程；后台 API 通过不能代替运营可执行性。

19-02 的通过只认领账号状态与 handle/bio 公开资料；COMMUNITY-ACC-008/011 仍包含邮件、运营权限、全部 schema、生成客户端和 CI，因此保持 `in_progress`。其余场景仍为 pending，不能据此宣称日记或社区已经交付。
