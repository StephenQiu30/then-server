# 每日记录与穿搭社区验收

标准状态：`approved`；功能验收 `in_progress`。19-02～19-06 已通过本地与远端门禁；头像、邮件挑战和反馈统计仍未实现。关联 [PRD19](../prd/19-每日记录与穿搭社区需求.md)、[Design21](../design/21-后端产品边界与领域设计.md)、[Design22](../design/22-后端数据库设计.md)、[Design23](../design/23-后端接口设计.md)、[Plan 19-06](../plan/19-06-可靠账号删除执行计划.md)。

| ID | 场景与必须断言 | 必需证据 | 状态 |
| --- | --- | --- | --- |
| COMMUNITY-ACC-001 | 无衣物用户同日写两条日记、编辑、重启读取；跨账号关联/读写拒绝；删除实际事件只解除关联；未来日记日期拒绝 | service+真实 PG+HTTP | passed（19-03 范围） |
| COMMUNITY-ACC-002 | 同日计划/日记/实际事件各自聚合，补记和跨时区不漂移；删除/纠正实际事件与反馈后重算统计 | 固定日期 fixture、事务与日历响应 | in_progress：三类日历独立聚合、原本地日期与删除解关联已通过；feedback/statistics 尚未实现 |
| COMMUNITY-ACC-003 | 日记导入草稿只含选定公开字段；source ID/精确上下文/采集原图不出现在公开 DTO、URL、Feed 或日志；收藏者身份不公开 | DTO allowlist、两账号+匿名差分、媒体访问 | passed：公开详情/Feed/搜索/作者列表只返回 allowlist，收藏列表仅本人可读且不向作者通知 |
| COMMUNITY-ACC-004 | 新帖和评论审核后可见；修改已发布帖产生新版本；旧版保持；撤回、下架与审批并发，迟到任务无法复活；重复审核幂等 | PG 行锁并发、worker 重放、HTTP | passed：帖子与评论状态机、旧批准版本、撤回/下架/删除、原帖可见性和迟到决定均已覆盖 |
| COMMUNITY-ACC-005 | 时间相同多帖稳定分页；发现/关注/作者/搜索/标签/图片执行一致可见性；下架不从旧游标漏出；发现流无私人衣橱 | Feed fixture、SQL查询计划、匿名接口 | passed：discover/following/作者/搜索共用批准版本与可见性过滤，游标绑定过滤后的查询 |
| COMMUNITY-ACC-006 | 重复赞藏关只一条关系；取消幂等；回复不能跨帖或超过一级；评论删除隐藏正文，父删除保留允许的回复；通知不暴露收藏者 | 唯一/FK真实约束、并发/HTTP | passed：关系唯一与幂等、一级回复事务校验、评论墓碑和通知种类边界已通过真实 PG/HTTP |
| COMMUNITY-ACC-007 | 屏蔽与关注/评论并发仍禁止互动，解除不自动恢复；举报重复/解决/申诉可追踪，封禁隐藏公开内容 | 两账号竞态、治理状态机 | passed：有序锁屏蔽、双向关系清理、评论举报、申诉与新增审计动作均通过；解除不恢复关注 |
| COMMUNITY-ACC-008 | 普通用户不能提权/审核/封禁或读取举报；挑战过期/重放拒绝；验证/找回不泄露邮箱是否存在；封禁立即失效会话 | transport权限矩阵、PG、邮件测试替身 | in_progress：普通用户不能进入运营 API，admin 封禁同事务撤销会话且 restore 不重新发布；邮件挑战/找回待 B1 |
| COMMUNITY-ACC-009 | 私有/社区媒体多用途隔离；解码预算/EXIF清除/固定版本；删日记保留或撤回独立帖；撤回即拒绝图片；删账号不中途丢失 MinIO 清理依据 | 真实 MinIO/PG/RabbitMQ，故障恢复/全版本删除 | passed（后端开发）：删号先关闭访问并保留回执，RabbitMQ/Outbox 删除全部 MinIO 版本，最后一项完成后才物理删除账号；生产备份过期仍属发布门禁 |
| COMMUNITY-ACC-010 | PG提交后队列断开最终补投；重投不重复衍生图/通知；Redis缓存丢失不丢事实；通知失效目标不回显正文 | 独立 tests/services/integration 断连与重放 | passed：通知 Outbox→RabbitMQ→Inbox 重放去重，读取时复核目标与屏蔽；Redis 不承载通知事实 |
| COMMUNITY-ACC-011 | 空库生成 schema、预期 FK/CHECK/索引生效；OpenAPI 唯一且包含权限/错误/分页；生成客户端可消费；日志无私有正文 | GORM/PG、OpenAPI契约、Umi smoke、CI | in_progress：35 表、API 0.17.0 / 98-operation、生成客户端和 19-06 远端 CI 已通过；邮件/头像后续 schema 待完成 |

## 19-02 当前证据

| 范围 | 命令 / 断言 | 结果 |
| --- | --- | --- |
| Go 静态与单测 | `go mod verify`、`go vet ./...`、`GIN_MODE=release go test ./... -count=1` | passed |
| 竞态 | `GIN_MODE=release go test -race ./... -count=1` | passed |
| 真实 PostgreSQL | `GIN_MODE=release go test -race -tags=services ./tests/... -count=1` | passed；隔离 schema 验证注册默认值、公开资料规范化/唯一/版本、账号状态过滤、CHECK 与删除级联 |
| HTTP/OpenAPI | profile 三条成功响应通过 kin-openapi 校验；未知字段、私有字段泄漏、401/404/409 映射有测试 | passed；运行时总数 41 |
| Umi | 临时空库启动实际 API；`THEN_OPENAPI_SCHEMA=http://127.0.0.1:18080/openapi.json npm run openapi`、`npm run typecheck`、`npm run test` | passed；生成 41 个函数和 profile client，临时库已删除 |
| 完整进程回归 | `GIN_MODE=release go test -race -tags=integration ./tests/... -count=1` | passed |
| 远程 CI | 实现 `335dcde` 与目录重构后文档 `02cd99f` | passed；CI `35054119350`、`35054449852` 三项全绿 |

## 19-03 当前证据

| 范围 | 命令 / 断言 | 结果 |
| --- | --- | --- |
| Go 静态、构建与竞态 | `gofmt -l .`、`go mod verify`、`go vet ./...`、`go build -trimpath ./cmd/then-server`、`GIN_MODE=release go test -race ./... -count=1` | passed |
| 真实本机服务 | `GIN_MODE=release go test -race -tags=services ./tests/... -count=1 -v` | passed，59.901s；真实 PG/Redis/MinIO/RabbitMQ 验证两账号、日记 CRUD/幂等/版本/墓碑、外键、月日历和媒体用途 |
| 完整实际进程 | `GIN_MODE=release go test -race -tags=integration ./tests/... -count=1 -v` | passed，21.437s；实际 `./cmd/then-server` binary 完成普通图片上传净化→日记→日历→删除并验证断连恢复 |
| OpenAPI 与 Umi | 空库启动实际 API，读取 `/openapi.json` 后运行 `npm run openapi/typecheck/test/lint/format:check/build` | passed；API 0.14.0、48/48 唯一 operation，生成 diary client，临时数据库已删除 |
| 无缓存镜像 | `docker build --no-cache -t then-server:diary-check .`；container suite | passed；`sha256:228bc37bf2a26d80d3e53cac75b8af61063b3a82f5d53154e68f7206dc22f87a`，UID/GID 65532、只读 rootfs、API/worker/all、SIGTERM |
| 远程 CI | 实现提交 `75d7d37`，GitHub Actions `35060309711` | passed；Go quality、PostgreSQL integration、OCI container 全绿 |

## 19-04 当前证据

| 范围 | 命令 / 断言 | 结果 |
| --- | --- | --- |
| Go 静态、构建与竞态 | `gofmt -l .`、`go mod verify`、`go vet ./...`、`go build -trimpath ./cmd/then-server`、`go test -race ./... -count=1` | passed |
| 真实本机服务 | `go test -race -tags=services ./tests/... -count=1` | passed，最终回归 64.910s；真实 PG/Redis/MinIO/RabbitMQ 覆盖来源日记、固定图片版本、审核状态机、举报、下架和封禁/恢复，并拒绝撤回/下架私人草稿 |
| 完整实际进程 | `go test -race -tags=integration ./tests/... -count=1` | passed，最终回归 22.208s；实际 binary 与真实容器完成社区图片→草稿→审核→匿名正文/图片→举报→下架 404 |
| OpenAPI 与 Umi | 实际 API `/openapi.json` 执行 `npm run openapi`，再运行 typecheck/test/lint/生产 build | passed；API 0.15.0、70 个唯一 operation，生成五个社区/治理 client；全仓 format 只被本片开始前已有的未跟踪头像素材 README 阻断 |
| 无缓存镜像 | `docker build --no-cache -t then-server:community-check .`；container suite | passed；最终 manifest list `sha256:01cac98aedc502599a118f103fdf3c7771ed687145966447ea30d97fde82004c`，受限 suite 9.183s，UID/GID 65532、只读 rootfs、API/worker/all、SIGTERM |
| 远程 CI | 实现提交 `7e1611e`，GitHub Actions `35076317946` | passed；Go quality 4m14s、PostgreSQL integration 2m23s、OCI container 1m55s 全绿 |

## 19-05 当前证据

| 范围 | 命令 / 断言 | 结果 |
| --- | --- | --- |
| Go 静态、构建与竞态 | `gofmt -l .`、`go mod verify`、`go vet ./...`、`go build -trimpath ./cmd/then-server`、`go test -race ./... -count=1` | passed；HTTP adapter 竞态测试 63.287s |
| 真实本机服务 | `go test -race -tags=services ./tests/... -count=1` | passed，67.000s；真实 PG/Redis/MinIO/RabbitMQ 覆盖 34 表、关系唯一、评论审核、通知重放、屏蔽和申诉 |
| 完整实际进程 | `go test -race -tags=integration ./tests/... -count=1` | passed，25.222s；实际 binary 完成发现→赞藏关注→评论审核→通知→屏蔽→举报下架旅程 |
| OpenAPI 与 Umi | 实际 API `/openapi.json` 执行 `npm run openapi`，再运行 lint/typecheck/test/生产 build 与 scoped format | passed；API 0.16.0、98 个唯一 operation，B4 新增 28 个；全仓 format 只被任务开始前已有的未跟踪头像素材 README 阻断 |
| 无缓存镜像 | `docker build --no-cache -t then-server:community-b4-final .`；container suite | passed；manifest list `sha256:9445e5ddcd33252b68fe16c8d962c14aecd3ecc9c588b3b6c5ed29c7e2f623fd`，受限 suite 10.199s |
| 远程 CI | 实现提交 `13cd457ce6fdceaeac7a7fe7eba78c8f63091795`，GitHub Actions `35082280316` | passed；Go quality 3m07s、PostgreSQL integration 2m25s、OCI container 2m01s 全绿 |

## 19-06 当前证据

| 范围 | 命令 / 断言 | 结果 |
| --- | --- | --- |
| 静态、构建与竞态 | `gofmt -l .`、`go mod verify`、`go vet ./...`、`go build -trimpath ./cmd/then-server`、`go test -race ./... -count=1` | passed；HTTP/OpenAPI race 63.894s |
| 真实本机服务 | `go test -race -tags=services ./tests/... -count=1 -v` | passed，69.735s；活动媒体删号立即撤销会话，最终删除对象/媒体/账号并保留 complete 回执；失效通知目标重复投递只写一条 Inbox |
| 完整实际进程 | `go test -race -tags=integration ./tests/... -count=1 -v` | passed，24.457s；无媒体删号返回 202/complete，旧 Cookie 和再次登录拒绝 |
| OpenAPI 与 Umi | 临时空库实际 API → `npm run openapi/typecheck/test/lint/build` 与生成目录 format | passed；API 0.17.0 / 98 operations；临时数据库和进程已清理 |
| 远程 CI | 实现提交 `280fc2baa93818761f9818f60071417afd9960b1`，GitHub Actions `35092187350` | passed；Go quality 4m54s、PostgreSQL integration 2m30s、OCI container 1m57s 全绿 |

测试只能使用合成图文和测试账号。真实依赖测试保留在 `backend/tests`，不另建 test 服务。发布验收另需真实媒体、审核运营演练、保留/备份删除、邮件、HTTPS、限流预算及前端/App完整旅程；后台 API 通过不能代替运营可执行性。

19-02 的通过只认领账号状态与 handle/bio 公开资料；19-03 认领私人图文日记、日历和普通日记图片；19-04 认领帖子审核治理；19-05 认领发现、关系、评论、屏蔽、站内通知和内容治理申诉；19-06 只认领后端可靠删号。整体仍因反馈统计、头像、邮件和上线门禁保持 `in_progress`；本地通过不等于完整产品或生产发布已交付。
