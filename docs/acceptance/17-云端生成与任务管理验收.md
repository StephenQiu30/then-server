# 云端生成与任务管理验收

## 2026-09-14后端MVP与测试边界验收

证据 `THEN-BACKEND-MVP-TEST-BOUNDARY-20260914-01`，测试边界代码提交 `0331c66`，执行归 [17-11](../plan/17-11-后端MVP与测试边界执行计划.md)。本轮不改变 OpenAPI、生产进程、数据库 schema 或 App；交付 Design → PRD → Plan → Implementation → Acceptance 的测试边界切片。

- Go 工具链为 `go1.26.5 darwin/arm64`；`gofmt -l .` 无输出，`go mod verify`、`go test ./...`、`go vet ./...`、`go test -race ./...` 通过。
- 新增 `service_environment_test.go`，只在 `services` tag 编译；配置使用 `THEN_TEST_*`，默认指向本机 loopback，Redis 使用 DB 15。4 组远程地址在连接前拒绝，错误只包含配置名。
- `go test -race -tags=services ./tests -count=1` 共 6 项通过，耗时 1.969 秒：2 项配置隔离与 4 项 PG/MinIO/Redis/RabbitMQ 真实协议测试。
- `go test -race -tags=integration ./tests -count=1` 通过，耗时 6.565 秒：独占 PostgreSQL 容器验证实际 binary 的启动失败、SIGTERM、ready `200→503→200` 恢复与 live 持续 200；测试容器完成清理。
- `go list -deps .` 不包含 MinIO、RabbitMQ、Redis、Testcontainers；使用 `-test -tags=services` 或 `-test -tags=integration` 时才出现对应测试 SDK。生产 binary 未增加测试角色或依赖。
- 测试仍使用 PostgreSQL 临时表、MinIO 随机私有桶、Redis 随机 key、RabbitMQ 随机 quorum queue，并在完成时清理；本轮未访问用户数据或生产配置。

该证据只完成后端 B0 测试边界，不表示 B1 身份/同意/删除、B2 上传/生成任务或 B3 同步已实现，也不替代远程 CI 与生产验收。

## 2026-09-14 本机服务优先更正验收

证据 `THEN-LOCAL-SERVICES-20260914-02`。用户明确日常开发使用本机已安装的基础服务，减少容器与配置层；下方 Compose 记录保留为此前隔离环境验证证据，不再代表当前默认启动方式。

- Homebrew services 显示 PostgreSQL 18、MinIO、Redis、RabbitMQ 均为 started；PostgreSQL 返回 `18.4 (Homebrew)`，Redis PING 为 PONG，RabbitMQ diagnostics ping 成功，MinIO live health 成功。
- 标准 loopback 端口 5432、9000、6379、5672 均监听。服务测试默认使用这些地址；自定义连接只通过进程环境覆盖，不读取仓库 `.env`，SDK 错误仍不输出凭据。
- `go test ./...`、`go vet ./...`、`go test -race ./...` 通过；`go test -race -tags=services ./tests -count=1` 直接连接本机四项服务，4 项真实协议测试通过，耗时 2.168 秒。
- `docker-compose.yml` 与 `docker-compose-env.yml` 保留标准命名，四项容器均加入显式 `isolated-env` profile；`docker compose --profile isolated-env config --quiet` 通过。它们只作隔离备用，不是日常开发或 Swagger 前置。

本机验证使用合成临时资源并清理；不证明生产 TLS、HA、备份、MinIO 维护支持、Outbox/Inbox 或真实用户媒体链路完成。

## 2026-09-14 Docker结构与开发入口验收

证据 `THEN-DOCKER-STRUCTURE-20260914-01`。参考 `video-server` 的标准命名、环境样例、固定服务入口和健康检查模式，使用根 `docker-compose.yml` 作为默认入口，`docker-compose-env.yml` 作为四项开发依赖的唯一编排；按用户更正不保留脚本封装。Go 镜像仍只由 `backend/Dockerfile` 构建，未复制参考项目的 Python/Next、多 worker、代理网络或生产编排。

- `docker compose config --quiet` 通过；根 `.env.example` 仅描述格式，实际 `.env` 为 0600 且被 Git 忽略。
- `docker compose up --detach --wait`：PostgreSQL、MinIO、Redis、RabbitMQ 四个固定 digest 容器全部 healthy，端口仍仅发布到 127.0.0.1；复用原 `then-services` 命名卷与凭据，没有清理数据。
- `go test -race -tags=services ./tests -count=1`：4 项真实协议测试通过，race 开启，耗时 2.200 秒。
- `docker build --tag then-backend:local backend` 与 `go test -race -tags=container ./tests -count=1` 通过；镜像仍为单一非 root scratch runtime，容器测试验证只读根、受限资源、健康接口、SIGTERM 和未实现角色拒绝。

本次是开发结构修复，不是生产部署验收。未新增生产 Compose：worker/all 尚未实现，首份业务 schema、生产 TLS、MinIO 维护方案、Outbox/Inbox 与备份恢复均未完成。Swagger 继续内嵌于 Go API，Swift Client 继续直接消费 `backend/openapi.yaml`，不增加 Swagger 容器。

## 数据库与中间件开发环境验收

证据 `THEN-SERVICES-20260914-01`，执行归 [17-10](../plan/17-10-数据库与中间件开发环境执行计划.md)。范围：独立本机合成数据开发环境；不是生产云业务验收。

- 工具链检查通过：Xcode 26.6 / Swift 6.3.3 / Go 1.26.5；Docker daemon 29.7.2，desktop-linux。本轮未修改 iOS、API 契约或业务 schema，没有运行 App 构建/真机验收。
- `docker compose up --detach --wait`：四个固定 digest 的容器 healthy，所有发布端口均 `127.0.0.1`。PG 18432，MinIO 18900/18901，Redis 18379，RabbitMQ 18672/18673。执行 down/up 保留原命名卷与秘密后再次启动成功；未清理共享项目。
- 首轮真实协议测试全部失败：最初 internal 网络在当前 Docker 环境下未发布宿主端口。改用独立 bridge 网络后，宿主连接恢复。容器 healthy 不等于应用可连接；保留该失败作为根因记录，不将 bridge 当成出站防火墙。
- 根 `.env` 使用 0600 权限并由 Git 忽略；服务集成测试只按严格 `KEY=value` 读取所需秘密，不执行文件、不打印凭据，宽权限、符号链接、缺失键和非法值直接失败。
- `go test -race -tags=services ./tests -count=1`：4 项真实协议测试通过，race 开启，无跳过。PG major 18、JSONB 临时表的事务写入/回滚/零残留；MinIO 私有桶上传后字节一致、匿名读取 403、删除后 NoSuchKey；Redis 写读后 TTL 自动失效、匿名 PING 拒绝；Rabbit quorum queue 的持久消息 confirm、手工 nack/requeue、相同 ID 的 redelivery、ack 后队列清空。临时桶/对象/缓存/队列按所有权清理。
- Go API 曾使用同一 PG 在 127.0.0.1:18080 启动；live/ready/docs/openapi 均 200，返回契约与 backend/openapi.yaml 字节相同。当前四个开发服务保留运行；服务用 `docker compose down` 停止且保留卷。
- 直接执行 gofmt、module verify、vet、单元/race 和既有真实 PG 集成测试通过；额外 `go vet -tags=services ./tests` 通过。SDK 引起的共享间接依赖更新已在 Design 01 说明，没有升级既有 Gin/GORM 或工具链。
- 9 份修订文档的 278 个本地文件链接通过，最终另查本验收/计划链接；git diff --check 通过。既有 FF-SS 状态不由本切片改写。

验收边界：单节点 RabbitMQ 不验证仲裁故障容错；此测试主动 requeue，不代替崩溃恢复、业务幂等、Outbox/Inbox、租约/fencing、取消/迟到结果或删除竞态。MinIO 官方服务端已归档，历史镜像仅用于当前合成数据验证，生产支持/安全方案未关闭。没有接通用户上传、生成供应商、业务 worker 或生产认证；API readiness 仍只表示 PG 连接可用。


## 2026-09-14技术栈与运行依赖复核

证据 `THEN-STACK-REVIEW-20260914-01`：本轮只读核对 go.mod、main.go、router.go、Swift Package.resolved 和三维 renderer manifest/source。实际 Go API 使用 Gin/GORM/PostgreSQL/kin-openapi/slog，业务入口仅两个健康接口；RabbitMQ/Redis/对象 SDK/FFmpeg/业务 worker 未接入。App 已有 Three.js 几何样例不证明正式 GLB、骨架/蒙皮或美术验收完成。

`go list -deps .` 核对运行依赖；integration/container 标签下的测试包与源码确认 Moby/Testcontainers 用于容器验证。Gorilla mux 用于契约匹配，Gin 引入的 BSON 代码不代表项目部署 MongoDB。第一次不带标签查询 tests 包因全部文件受 build constraints 排除而返回非零；补指定标签后查询成功，这些只读命令不构成测试通过。

本次仅修改六份说明/设计/计划/验收文档，保持运行与生成代码、版本锁、OpenAPI、schema 和既有 checklist 状态。官方开源能力与 Atlas 发行版区别见 Design 01；未安装 Blender/validator 或新增运行组件，未运行产品构建/模拟器/数据库故障测试。验证：scripts/verify-toolchain.sh 通过（Xcode 26.6 / Swift 6.3.3 / Go 1.26.5）；Python 标准库检查六份修订文档的 154 个本地链接、46 个章节锚点均通过，原章节及任务 checklist 保留；git diff --check 通过。执行者 Codex，文档自检不替代产品或独立发布验收。


## 2026-09-14测试结果包门禁验收

证据 ID：`THEN-IOS-GATE-20260914-01`。范围为 [17-01 GATE-01](../plan/17-01-后端服务启动与健康契约执行计划.md#2026-09-14测试空跑门禁修复)，修复测试空跑误报；未改变 Swift 产品代码、数据、API 或依赖。

- Red：在原脚本执行 `python3 -m unittest scripts.tests.validate_ios_test.IOSValidationGateTests.test_empty_run_is_not_a_pass`，1 个测试中的 unknown/Passed 两个零计数组均失败，旧脚本退出 0，复现误通过。
- Green：`python3 -m unittest discover -s scripts/tests -p '*test.py'`，28 项通过（9.426 秒），其中 13 项门禁测试覆盖两次运行独立结果目录、真实路径传递、构建前置/构建/测试失败的原退出码、缺失结果包、摘要工具失败、非法 JSON/缺失字段/错误计数类型、零运行、失败、跳过、预期失败及参数化计数。命令替身只验证脚本控制流，不是新产品测试。
- 当前固定工具链和 `scripts/validate-ios-architecture.sh` 通过，`sh -n scripts/validate-ios.sh` 通过。本轮没有 Swift/target 变更，未重新构建或运行 App；此前构建与 UI 证据仍按其原范围记录。

对以下既有真实包逐个执行 `python3 scripts/validate-ios-test-result.py DerivedData/<名称>.xcresult`，使用真实 xcresulttool 读取，未重新运行测试：

| 结果包 | 顶层注册测试 | 新门禁退出码 | 判断 |
| --- | --- | --- | --- |
| WardrobeHandsRed20260914 | 0，result=unknown | 1 | 原 xcodebuild 退出 0，空跑现在拒绝 |
| OutfitDarkTray20260914 | 3，通过 3 | 0 | 接受原有三条 UI 旅程证据 |
| WardrobeHandsDevices20260914 | 7，通过 5、失败 2 | 1 | 身体部位检测失败继续保留；不累计设备下的参数展开数 |

验收规则：test 入口先构建，再创建独立的忽略目录与明确结果包，Xcode 非零退出直接保留原码且不重跑。Xcode 成功时必须取得有效顶层摘要，total > 0、passed = total、result = Passed，failed/skipped/expectedFailures 均为 0。手动筛选运行也须检查自己的结果包；通过只代表所选测试，不能抵扣未运行旅程、模拟器不支持能力、真机性能或完整云发布。当前完整产品测试仍有身体部位分析失败及物理检查跳过，本次修复没有将其变成通过。

## 2026-09-13生成Client正式工程验收

证据 ID：`THEN-TRANSPORT-20260913-01`。用户已确认 Design 01 的唯一 ThenTransport 技术模块例外；实现和任务归原 [17-01](../plan/17-01-后端服务启动与健康契约执行计划.md)。本节替代下方历史记录中的“App 编译仍失败”作为当前结论，不改写历史结果。

- 环境：Xcode 26.6、Swift 6.3.3、Go 1.26.5；generic iOS Simulator Debug/Release（arm64、x86_64），运行测试为全新 iPhone 17 / iOS 26.5（23F77）模拟器。没有访问既有用户 App/数据库；测试专用设备已删除。
- 正式工程：ThenTransport 静态 framework 只含两份原 YAML 输入，由原锁定插件生成 public Types/Client，默认 nonisolated、Swift 6 Complete/Approachable Concurrency；ThenApp 保持 MainActor。没有手改生成代码、手写生产 DTO、额外运行时装载、业务请求或 schema 变化。
- `scripts/validate-ios.sh build` exit 0，实际 Debug/Release 均 `BUILD SUCCEEDED`；原 39 条生成 Client 编译错误消失。随后 `xcodebuild ... build-for-testing` exit 0，App、ThenAppTests、ThenAppUITests 实际编译/链接成功。
- 静态框架测试消费端首次链接暴露 Runtime/HTTPTypes 未显式链接问题；补齐测试 target 对既有 OpenAPIRuntime 与锁定 HTTPTypes 1.6.0 的链接后通过。Package.resolved、依赖版本和业务代码未改。一次并行构建尝试因 Xcode build.db 锁退出 65，已在前一构建结束后串行重跑；不将该次失败当作成功证据。
- `python3 -m unittest discover -s scripts/tests -p '*test.py'`：18 项通过。其中新增 8 项检查当前工程和反例：App/transport 隔离、插件返回 App、重复契约编译、手写 transport 源码、复制 YAML、非 public API、缺失静态运行库链接。`scripts/validate-ios-architecture.sh`、PBX plist 和 diff 空白检查通过。
- 实际 App 宿主下 6 组 Swift Testing 共 48 项通过：ThenTransport 的 3 项覆盖非 MainActor 请求与成功解码、503 错误/重试头、非法枚举拒绝；其余为原 5 组照片领域、临时会话、净化、文件导入及系统传输测试。没有真实网络调用，`example.invalid` 仅为测试基址。
- 现有 XCUITest 的无效测试存储标识闭合失败、五 Tab 导航及取消删除确认共 2 项通过。这是历史页面回归，不是 OOTD 或真实用户数据删除验收。
- xcresult 汇总：50 项测试、0 失败、0 跳过；参数化展开共 104 次通过。证据在仓库忽略目录 `DerivedData/ThenTransport20260913.xcresult`，重跑命令见下文。本地结果未发布为远程 CI。
- `scripts/generate-ios-api.sh` exit 0；显式生成与 ThenTransport 插件文件逐字节相同。Types SHA-256：`c0617f7db86d55060ca60c62bfa309967d525378860d09a84e7ccbf541bd843e`；Client SHA-256：`5b67208605ab6c9197c51ccac2f8ddd9d1f7a1497a04c07bb70130d6be48bb0c`。Debug/Release App 包均不包含 YAML 或嵌入式 ThenTransport.framework，构建 framework 为静态 archive。

重跑时先创建全新测试模拟器，用实际 UDID 替换 `<UDID>`，不要指向有用户数据的安装；结果路径需不存在。先执行 `scripts/validate-ios.sh build`，再顺序执行：

```sh
xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug \
  -destination 'generic/platform=iOS Simulator' \
  -skipPackagePluginValidation -disableAutomaticPackageResolution build-for-testing
xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug \
  -destination 'platform=iOS Simulator,id=<UDID>' -parallel-testing-enabled NO \
  -skipPackagePluginValidation -disableAutomaticPackageResolution \
  -resultBundlePath DerivedData/ThenTransport20260913.xcresult \
  -only-testing:ThenAppTests/ThenTransportTests \
  -only-testing:ThenAppTests/AvatarPhotoQualityPolicyTests \
  -only-testing:ThenAppTests/AvatarPhotoTemporarySessionStoreTests \
  -only-testing:ThenAppTests/ImageIOAvatarPhotoSanitizerTests \
  -only-testing:ThenAppTests/AvatarPhotoFileImporterTests \
  -only-testing:ThenAppTests/SystemAvatarPhotoPickerTests \
  -only-testing:ThenAppUITests/ThenAppUITests/testInvalidUITestingStorageIDFailsClosed \
  -only-testing:ThenAppUITests/ThenAppUITests/testRootNavigationAndDestructiveConfirmationsCanBeCancelled \
  test-without-building
```

警告与边界：Xcode 对仅有插件输入的 Swift framework 报无 umbrella header、YAML 无内建编译规则；实际 Swift 模块由插件生成并已编译/链接验证，没有通过加入空 Swift/header 或隐藏警告处理。另有历史 MapKit 弃用、无效 await/nonisolated(unsafe) 提示及 AppIntents 未使用提示，未扩展修改冻结的旧功能。本次没有真机签名/Archive、完整旧 UI 套件、远程 CI、生产云网络、3D renderer 或完整照片 Host 验收。修复在工作树，未提交或推送；REL-01 保留待交付。


## 2026-09-13后端内嵌文档验收

evidence_id：`THEN-EMBEDDED-DOCS-20260913-01`；代码基线 `2bc9728` 加本次未提交修改。实施自 2026-09-12 开始，跨日完成，执行者 Codex，自检，未进行独立人工复核。当前范围归 [17-09](../plan/17-09-后端内嵌接口文档执行计划.md)，旧 17-08 为 superseded。

| 检查 | 结果与可复现证据 |
| --- | --- |
| Red → Green | `go test ./internal/platform/config -run TestDocumentationConfiguration -count=1` 首先出现 4 项失败：原代码忽略开关，未拒绝公开 IPv4/IPv6 绑定、非法/空开关；修复后全部通过 |
| 默认关闭与开启边界 | 配置测试覆盖 true/false、IPv4/IPv6 回环与公开绑定、非法值脱敏；docs_test 验证关闭时所有 7 个入口 404、开启时 200、非 GET 405 |
| 契约所有权与路由边界 | 原始契约逐字节相等；修改调用者输入后仍提供已校验版本；未列出的目录、README、哈希清单、源码映射、路径穿越与重复分隔路径均 404；健康路由仍 200 |
| 资源分发 | 官方 v5.32.15 bundle/CSS、LICENSE、bundle 第三方许可声明全部随二进制嵌入；`TestBundledSwaggerIntegrity` 核对 4 个上游文件 SHA256SUMS；HTTP 许可响应与文件字节一致 |
| Go 完整本地检查 | 最终 `scripts/validate-backend.sh integration` 通过：gofmt、module verify、vet、unit、race、真实 PostgreSQL 18.4 与实际 binary 通过；集成测试新增真实 docs/bundle/原始契约/ready 检查，并保留断连恢复和 SIGTERM 验证 |
| 最终 OCI 产物 | `docker build -t then-backend:docs-local backend`（仓库根目录）通过；更新 .dockerignore 使静态资源和许可进入编译上下文；`THEN_BACKEND_TEST_IMAGE=then-backend:docs-local go test -race -tags=container ./tests -count=1`（backend 目录）通过 |
| 真实浏览器 | 使用本机 Go binary + 隔离本机 PostgreSQL 18.4，实际访问 `/docs/` 并展开 getLiveness；看到两个健康操作、200/400/500 模型和正确 BAD_REQUEST/INTERNAL_ERROR 示例，无 Try it out。最终构建刷新后浏览器 error 日志为空；未进行完整网络抓包 |
| CSP 与运行契约 | HTTP 返回同源 CSP、no-referrer；`/openapi.yaml` 与当前契约字节一致，SHA-256 `a9f12634217d28f4118f42696e3d8ff5d35e186de489d6149a5156241fbb10b7`；禁用外部 validator、URL 配置覆盖、授权持久化 |
| 无独立文档服务的 Swift 生成 | 移除旧文档容器/网络后，`scripts/generate-ios-api.sh` 返回 0；`python3 scripts/tests/openapi_tools_test.py` 4 项通过；`git check-ignore` 确认生成源文件位于忽略的 DerivedData |
| 旧方案退役 | Compose 与文档容器管理脚本已删除，原 Docker 专属替身测试同步移除；查询项目标签无残留 Swagger 容器，不保留旧端口或参数兼容分支；其他项目容器未清理 |

本轮仅增加开发文档路由，不改变业务 OpenAPI/schema、数据库表、App 页面或媒体。原 iOS 编译失败与 ThenTransport 单模块例外继续归 17-01，未重复执行未变更的 App 构建，不将 Swift 生成通过写成 App 编译通过。生产文档、生产部署、远程 CI、3D 功能与真机验证均未在本切片宣告完成。

本机预览使用隔离且受限权限的测试数据库，无真实用户数据。临时预览路径/端口不是长期验收依赖；稳定复现方式为配置本地 DATABASE_URL 后，在 backend 运行 `API_DOCS_ENABLED=true go run .`。停止该进程与测试数据库即可清理预览；代码回滚见 17-09。

## 2026-09-12Swagger文档与请求生成验收

- evidence_id：`THEN-OPENAPI-DOCS-20260912-01`；基线 `2bc9728` 加本轮未提交修改，执行者 Codex，本机自检，尚无独立人工复核。
- `scripts/openapi-docs.sh up/down/up/status` 实际通过；容器 healthy，仅 `127.0.0.1:18108 → 8080`，源契约挂载 RW=false。down 仅清理独立文档项目，无 API/数据库变更。
- 浏览器实际打开页面并展开 getLiveness：显示“于是 OOTD API”、版本 0.3.0、OAS 3.1、getLiveness/getReadiness 和 Liveness/Readiness/Error 模型。无 Try it out 控件；initializer 实际配置只有本地 `/openapi.yaml`，validatorUrl 为空，鉴权持久化及查询覆盖关闭。未进行网络抓包，不将配置检查声称为完整网络审计。
- 本轮发现并修正 400/500 继承通用 NOT_READY 示例的问题；分别与 router.go 的 BAD_REQUEST/INTERNAL_ERROR、message、retryable=false 对照。刷新真实页面已显示修正后的示例；这是文档示例修复，不改变返回 schema 或 Handler 行为。
- 使用 HTTP 读取 `/openapi.yaml`，与源文件逐字节一致；原始版本为 OpenAPI 3.1.2。修正后 SHA-256：`a9f12634217d28f4118f42696e3d8ff5d35e186de489d6149a5156241fbb10b7`。
- `scripts/generate-ios-api.sh` 返回 0，固定工具链通过；两个输出与现有 Xcode 插件生成产物逐字节一致。Types.swift SHA-256：`d1296d5e1be48cd8e4838a393c0eeab3d5eb1bfafe6ba0d9ffecfd50faddf9be`；Client.swift SHA-256：`dccc311f72f7710b422c02cd6573eede4053b6246124e0eb47274ad6b1c21497`。错误示例变更未改变生成代码。
- `git check-ignore` 确认两个显式输出受 DerivedData 忽略规则覆盖；现有 OpenAPI 符号链接、生成配置与 target 保持不变。
- `python3 scripts/tests/openapi_tools_test.py`：5 项通过，覆盖非法参数、xcodebuild 失败、缺少生成器、生成器失败和 Docker 失败；使用隔离替身，不构成 App 测试。
- `sh -n`、`scripts/validate-ios-architecture.sh`、`scripts/validate-backend.sh unit` 通过；修正示例后 `go test -count=1 ./internal/transport` 通过。本轮没有数据库或运行 API 变更，未重复容器集成套件。原 App MainActor 编译阻断仍保留，未重跑未变更的失败构建，不将客户端生成或 Go 检查当作 App 集成通过。

复现入口及回滚见 [17-08](../plan/17-08-接口文档与客户端生成执行计划.md)。本切片开发工具验收通过，生产 Swagger、业务接口、App 完整编译和设备验证不在通过范围内。

## 2026-09-12客户端编译与门禁修复记录

- evidence_id：`THEN-IOS-GATE-20260912-01`；代码基线 `2bc9728` 加本次未提交脚本/文档变更；执行者 Codex，自检，未独立人工复核。
- `python3 scripts/tests/validate_ios_test.py`：6 项通过，覆盖 Debug/Release 顺序、工具链/架构失败停止、Debug/Release 失败停止、缺少 test destination、测试失败传播及非法参数；使用隔离替身，不冒充产品测试。
- `sh -n scripts/validate-ios.sh`：通过。
- `scripts/validate-ios.sh build`：工具链/架构检查通过，实际 Debug xcodebuild 因 `converter`/`Decodable` actor 隔离错误退出 65；Release/test 未执行，符合失败即停合同。App 编译阻断未关闭。
- evidence_id：`THEN-TRANSPORT-PROBE-20260912-01`；未经修改的实际 GeneratedSources/Types.swift、Client.swift，在固定 Swift 6.3.3、iOS Simulator arm64、Swift 6 / Complete Strict Concurrency / nonisolated 默认隔离下独立 emit-module 成功；本轮没有修改 DerivedData 源文件或工程模块。
- 隔离模块的 MainActor 消费方同时通过 typecheck：经 `@testable import` 调用实际 Client 健康操作，调用完成后更新 MainActor 状态。此验证只证明编译边界，不包含实际网络、public 访问级别生成、Xcode 插件迁移、App 链接/安装或 UI 测试；临时 probe 不作为产品资产提交。
- 可复现方法：从当前构建生成目录读取原始 Types.swift/Client.swift，以 `xcrun swiftc -swift-version 6 -strict-concurrency=complete -default-isolation nonisolated -parse-as-library -enable-testing -emit-module` 编译测试模块，指定 `arm64-apple-ios26.0-simulator`、本机 Simulator SDK 与当前 Debug-iphonesimulator 的 OpenAPIRuntime/HTTPTypes 模块路径；输出至隔离目录。消费方使用默认 MainActor、相同 SDK/依赖和 `-typecheck` 验证；不覆盖原始生成文件。
- 后续：独立 ThenTransport 的具体提案见 [Design 01](../design/01-技术选型.md#2026-09-12生成client编译兼容评估)。根 AGENTS 单模块例外待用户确认；本次不更改 17-01 的 DOC-01/REL-01 或整个切片为 completed，也不改写 Go/容器的历史结论。

## 最新恢复验证结论

证据 ID：BE-CONTAINER-20260908-02，2026-09-08，Codex 执行，基于 `17b765d` 后本轮修改，独立人工复核未完成。

按用户要求依次完成 Design 02 解法、PRD 17 需求回写、原 17-07 任务更新，再修改测试实现。资源句柄一经创建立即注册 t.Cleanup，错误返回不绕过清理；清理使用独立有界 context。测试支持文件只在 integration/container tag 编译。

- `scripts/validate-backend.sh integration`：passed，Go 格式、模块校验、vet、单元/race、真实 PostgreSQL 与进程验证通过。
- `docker build -t then-backend:local backend`：首次被官方 Go proxy 的 unexpected EOF 中断；原配置一次复验构建成功，无依赖版本变更或自动重试逻辑。
- `THEN_BACKEND_TEST_IMAGE=then-backend:local go test -race -tags=container ./tests -count=1 -v`：最终新镜像 passed，包含启动失败清理、健康、非 root/只读、资源限制、SIGTERM 及 worker/all 拒绝。
- 启动失败回归先经历 inspect 错误分类不正确导致的断言失败；最终使用独立 Docker API + errors.Is(errdefs.ErrNotFound) 确认资源删除，不把任意查询失败当成功。
- 前次 1335eed21897 容器已不存在；3e087e52b65c 为前次测试会话的已退出 PostgreSQL，已定向移除并确认不存在。未清理其他服务。
- 模块依赖版本与 go.sum 不变；errdefs 与原有 moby/api 的 direct 标记由 go mod tidy 校正。

最新本地镜像 ID：`sha256:1df65829832abf17c9f27f7f072b194e997a82732fcb2f93c589398f23c9693e`，linux/arm64。实现见 [测试资源管理](../../backend/tests/container_support_test.go)和[容器回归](../../backend/tests/container_integration_test.go)。17-07 本地范围恢复 completed；17-01 的 Swift 编译与生产发布门禁不变。下节保留上一轮失败历史，不作为当前 Docker 验证结论。

## 最新源码交付与复验结论

2026-09-08，用户明确要求提交 main 并推送。源码提交 `0d5c8a7` 已推送，包含后端运行、容器构建、app 目录迁移及按 SOP 组织的需求/设计/计划/验收文档。这是源码交付，不代表完整产品验收或生产发布。

- 工具链、iOS 架构静态检查、Go 模块校验、vet、race、真实 PostgreSQL 集成复验通过。
- Docker 镜像构建及容器健康、只读/非 root、SIGTERM 功能断言通过；但 API/PostgreSQL 测试容器清理时 Docker API 超时，本轮容器测试整体失败。不能用早前通过结果代替最新复验。
- Swift Client 构建仍失败：MainActor 隔离的 converter 无法从 Sendable closure 使用。未修改生成产物或放宽 App 并发基线。
- 639 处本地文档链接及锚点检查通过；95 个 app 移动文件无超出路径替换的产品代码变化。有限凭据模式扫描无命中，不代表完整安全审计。
- scripts 与 hooks 的额外删除尚未确认，已单独保存在 Git stash；本次源码保留校验入口并同步 app 路径。stash 不推送远端。

下文早前未提交工作树的证据作为历史保留；当前结论以本节最新复验为准，不关闭云端生产验收门禁。

## 状态与结论

- 文档状态：`draft`
- 验收结论：`pending`
- 更新日期：2026-08-30
- 发布层级：受控云端能力必选；不阻断云端开关关闭时的本地核心 GA

本文验证静态试穿和动态预览共用的认证、上传、任务、配额、恢复和删除契约。当任一上游隐私、供应商、容量或数值决策未批准时，相关场景保持 `blocked` 或 `pending`，不得用技术成功代替生产准入。

## 关联文档

- [`../prd/17-云端生成与任务管理需求.md`](../prd/17-云端生成与任务管理需求.md)
- [`../design/10-OOTD服务端与异步任务设计.md`](../design/10-OOTD服务端与异步任务设计.md)
- [`../design/11-OOTD权限隐私与安全设计.md`](../design/11-OOTD权限隐私与安全设计.md)
- [`../plan/10-OOTD产品实施计划.md`](../plan/10-OOTD产品实施计划.md)
- [`10-OOTD产品系统验收.md`](10-OOTD产品系统验收.md)

当前没有已批准的 `17-SS` 执行计划。

## 需求覆盖

| 需求 | 验收场景 |
| --- | --- |
| `CLOUD-REQ-001` | `CLOUD-ACC-001` |
| `CLOUD-REQ-002` | `CLOUD-ACC-002`～`CLOUD-ACC-006`、`CLOUD-ACC-008` |
| `CLOUD-REQ-003` | `CLOUD-ACC-003`～`CLOUD-ACC-007` |
| `CLOUD-REQ-004` | `CLOUD-ACC-002`、`CLOUD-ACC-005`、`CLOUD-ACC-009` |
| `CLOUD-REQ-005` | `CLOUD-ACC-010` |
| `CLOUD-REQ-006` | `CLOUD-ACC-001`、`CLOUD-ACC-011` |

PRD 17 已为业务规则分配 `CLOUD-RULE-001`～`CLOUD-RULE-024` 并映射到上述需求簇。本文按需求簇判定功能验收；静态和动态执行计划还必须同时声明生成类型与覆盖的规则 ID，不得以共用任务契约绕过 PRD 14/15 的增量门禁。

## 验收环境

- 隔离的 PostgreSQL 18、RabbitMQ quorum queue、Redis、私有 S3-compatible 存储和授权 Provider 沙箱。
- 同一镜像分别以 `APP_ROLE=api|worker|all` 运行；生产形态的 API/worker 独立进程必测。
- 两名隔离测试用户、失效凭据、重复幂等键、乱序消息和迟到回调夹具。
- 不使用 SQLite/内存 fake 代替真实 PostgreSQL/RabbitMQ 作为唯一证据。

## 验收场景

### CLOUD-ACC-001 本地核心独立

- 覆盖需求：`CLOUD-REQ-001`、`CLOUD-REQ-006`。
- 前置条件：用户未登录，云功能开关关闭，所有云依赖不可用。
- 操作步骤：完成无图衣橱、基础推荐、计划、实际穿着与反馈。
- 期望结果：本地成果可用并可重启恢复；云端入口被正确隐藏或解释；不会因云故障自动上传或丢失数据。
- 边界情况：在已接纳任务存在时关闭新流量，需按批准策略恢复旧任务，不影响本地核心。
- 验收证据：端到端录屏、网络抓包和开关回滚记录。
- 结果：`pending`

### CLOUD-ACC-002 认证、所有权与上传资格

- 覆盖需求：`CLOUD-REQ-002`、`CLOUD-REQ-004`。
- 前置条件：两名用户各有合成人物和衣物资产。
- 操作步骤：验证正常/失效登录、成年本人声明、逐次同意、上传准备/确认，并替换 owner 和资产 ID。
- 期望结果：owner 只来自认证上下文；认证不被当作年龄证明；未同意或不合格资产不获签名上传权限；跨用户访问统一拒绝且不泄漏存在性。
- 边界情况：过期 URL、错误 content type、超大载荷、重复 finalize 和上传后撤回同意安全失败。
- 2026-09-08 补充：上传后改变同一对象 key 的内容，再 finalize/读取；只有绑定并校验的具体版本可用，不能出现“先校验再覆盖”。URL 过期或数据库撤回不被误报为已完成所有物理清理。
- 验收证据：契约/授权测试、对象 ACL 报告和网络抓包。
- 结果：`pending`

### CLOUD-ACC-003 OpenAPI、幂等创建与输入快照

- 覆盖需求：`CLOUD-REQ-002`、`CLOUD-REQ-003`。
- 前置条件：`backend/openapi.yaml` 是唯一契约，iOS Client 由其生成。
- 操作步骤：校验契约，使用同/不同幂等键创建静态和动态任务，然后修改源衣物或穿搭。
- 期望结果：响应 schema/错误码与契约一致；同键同请求返回稳定 job，同键不同请求冲突；job 保留已接纳的版本化输入快照。
- 边界情况：空值、枚举、大小、游标和幂等过期边界可预期失败。
- 2026-09-08 补充：撤回授权或删除后用旧 key 重放，不返回旧媒体/签名 URL；额度已耗尽后重放已接纳命令，不重复占额。取消/删除/状态查询不被生成额度阻断。CI 同时验证授权先于回放及 OpenAPI `/v1` 前缀仅出现一次。
- 验收证据：OpenAPI 语法/风格/破坏性检查、iOS Client build 和 Go 契约测试。
- 结果：`pending`

### CLOUD-ACC-004 前进状态机与 App 恢复

- 覆盖需求：`CLOUD-REQ-002`、`CLOUD-REQ-003`。
- 前置条件：已接纳任务，可注入断网、杀 App 和服务器重启。
- 操作步骤：在上传、排队、处理和结果审核阶段离开/重启，从任务入口恢复。
- 期望结果：状态只合法前进；网络断开不丢任务；结果可从稳定 job ID 重新发现；展示建议轮询间隔与真实状态。
- 边界情况：旧/乱序状态不让已终态 job 回退，未找到任务时不生成虚假成功。
- 验收证据：状态机属性测试、重启录屏和跨端 trace。
- 结果：`pending`

### CLOUD-ACC-005 取消、删除与迟到结果

- 覆盖需求：`CLOUD-REQ-003`、`CLOUD-REQ-004`。
- 前置条件：任务分别处于未调用 Provider、Provider 运行中和结果已返回三个窗口。
- 操作步骤：执行取消，再执行结果/源资产删除，注入迟到回调。
- 期望结果：“已请求取消”不伪装 Provider 已停止；取消不代替删除；tombstone 阻止迟到结果可见化或重新计费。
- 边界情况：取消/完成/删除竞态不产生相互矛盾的终态。
- 验收证据：竞态测试、Provider 回调记录和删除查询。
- 结果：`pending`

### CLOUD-ACC-006 Outbox/Inbox 与故障恢复

- 覆盖需求：`CLOUD-REQ-002`、`CLOUD-REQ-003`。
- 前置条件：真实容器依赖可分别终止。
- 操作步骤：在事务提交、Outbox 发布、Provider 请求、对象写入和结果提交窗口杀进程，重复/乱序投递，清空 Redis 后恢复。
- 期望结果：已提交事实最终恢复；未提交事实不出现；Inbox、租约和 fencing token 阻止重复业务效果；Redis 丢失不改写任务真相。
- 边界情况：broker 长时间不可用时 API 执行回压，不无限接纳；DLQ 可追踪且不含敏感正文。
- 验收证据：Testcontainers 报告、数据库/队列快照、trace 和告警。
- 结果：`pending`

### CLOUD-ACC-007 结果质量审核后发布

- 覆盖需求：`CLOUD-REQ-003`。
- 前置条件：Provider 返回合格、低质量和不安全三类合成结果。
- 操作步骤：处理三类结果，再读取用户可见状态和对象存储访问。
- 期望结果：只有通过内容安全、输入一致性和质量门的结果可发布；其他结果不对用户暴露，并有可恢复安全错误。
- 边界情况：审核服务未知/超时时默认不发布，不以随机成功充数。
- 验收证据：授权样本报告、安全测试和结果 ACL 查询。
- 结果：`pending`

### CLOUD-ACC-008 配额占用、结算与释放

- 覆盖需求：`CLOUD-REQ-002`。
- 前置条件：产品、财务和容量负责人已批准可用次数、占用/结算时点、取消返还、恢复窗口和最大等待值。
- 操作步骤：分别创建成功、用户取消、系统失败、重复、超时和容量暂停任务。
- 期望结果：用户可在提交前看见影响；同一业务效果最多结算一次；失败/取消按批准规则释放；配额真相在 PostgreSQL，不依赖 Redis。
- 边界情况：上游数值未批准时本场景结果必须是 `blocked`，不得以假值通过生产验收。
- 验收证据：批准记录、配额事务/并发测试和 UI 录屏。
- 结果：`blocked`

### CLOUD-ACC-009 资产、任务、账号与备份删除

- 覆盖需求：`CLOUD-REQ-004`。
- 前置条件：账号拥有上传资产、运行任务、结果、缓存、Provider 副本和备份状态。
- 操作步骤：分别删除单个结果、源资产、全部 OOTD 云数据和账号，在中途杀 worker 并注入迟到回调。
- 期望结果：读权立即撤销；血缘删除覆盖所有已批准副本；部分失败如实展示；恢复和备份过期不复活。
- 边界情况：日志、回执和通知不含资产 URL、对象 key 或个人内容。
- 验收证据：删除 manifest、对象/数据库查询、Provider 回执和备份恢复演练。
- 结果：`pending`

### CLOUD-ACC-010 旧账号与历史数据兼容

- 覆盖需求：`CLOUD-REQ-005`。
- 前置条件：历史发布事实和数据处置策略已批准，只使用合成历史夹具。
- 操作步骤：以旧账号/数据启动新版本，进入云任务入口，执行保留、只读、导出或明确删除的获批分支。
- 期望结果：旧生活数据不自动变成 OOTD 事实；未批准的账号合并、上传或删除不发生；新云任务不改写历史记录。
- 边界情况：发布事实未知时本场景保持 `blocked` 并采用默认保留。
- 验收证据：历史夹具报告、迁移查询和产品批准记录。
- 结果：`blocked`

### CLOUD-ACC-011 运行角色、灰度、熔断与回滚

- 覆盖需求：`CLOUD-REQ-006`。
- 前置条件：具有独立 API/worker 部署和隔离流量。
- 操作步骤：扩缩 worker，分别关闭静态/动态接纳，注入 Provider 与队列容量超限，执行上一版本回滚。
- 期望结果：API 不被媒体 worker 阻塞；开关可按能力/地域停止新任务；已接纳任务按批准承诺恢复；回滚不破坏已发布 schema 和任务。
- 边界情况：缺失最大等待、停止条件、负责人或已接纳任务承诺时，禁止生产灰度。
- 验收证据：容量/故障注入报告、开关快照、回滚演练和 SLO 仪表盘。
- 结果：`blocked`

## 证据与判定

每份证据必须记录唯一 ID、commit/build、环境、依赖版本、夹具版本、步骤/命令、持久 artifact 地址、执行/复核人、时间、结果与限制。不记录令牌、敏感正文或可访问资产 URL。

只有目标云能力所依赖的所有场景均为 `passed`，且 14/15/18 号功能验收和 10 号系统组合验收独立通过时，才能开启真实用户流量。


## 17-01 后端运行基线实测记录

证据 ID：BE-RUNTIME-20260908-01。基于 main `6f0513b51134f21950fe69987bab4ea43a559812` 之后的当前未提交实现；执行者 Codex，2026-09-08，macOS / Go 1.26.5 / Docker 29.7.2 / Testcontainers 0.44.0 / PostgreSQL 18.4。使用随机合成凭据，测试结束清理所属容器。

持久复现入口：[执行计划](../plan/17-01-后端服务启动与健康契约执行计划.md)、[HTTP 契约测试](../../backend/internal/transport/router_test.go)、[数据库与进程集成测试](../../backend/tests/runtime_integration_test.go)。数据库镜像固定为 `postgres:18.4@sha256:a02db8cac496f15b094798a38254f14d6e00741f709360e5e00bb6668ea31636`。

| 验证命令（backend 目录） | 结果与证据 |
| --- | --- |
| `go test ./...` | passed；配置、HTTP 边界、错误/日志脱敏、OpenAPI 响应验证及错误响应拒绝 |
| `go vet ./...` | passed |
| `go test -race ./...` | passed；包括活动请求退出与超时强制断开 |
| `go test -race -tags=integration ./tests -v` | passed；构建实际二进制并成功 SIGTERM 退出；真实数据库 pause 后 ready 503/live 200，unpause 后 ready 200 |

首次 stop/start 故障夹具因为 Docker 动态宿主端口变化不能验证原地址恢复，已改为 pause/unpause；不将该失败误报为连接池验证通过。当前未验证数据库换地址恢复、业务 schema、Atlas、用户鉴权、worker、云服务或生产负载。真实 PostgreSQL 用例是超时/连接失效注入，不是完整数据库灾难恢复。

Swift Client 在默认 MainActor 模块中的生成代码编译失败，完整切片仍 blocked；`app/` 路径迁移不改变该问题。以上只关闭后端运行测试，不改变 CLOUD-ACC-001–011 的生产云验收结论。

## 17-01 运行异常清理与启动失败补充验证

证据 ID：BE-RUNTIME-20260908-02。2026-09-08，Codex 执行，独立人工复核尚未完成。仍基于 main `6f0513b` 后的未提交工作树；Go 1.26.5、PostgreSQL 18.4、Docker 29.7.2，依赖版本不变。

- 先运行 `go test ./internal/platform/httpserver -run TestListenerFailureClosesActiveConnections -count=1`，复现 `active request survived listener failure`。修复为监听异常先 drain 并关闭活动连接再返回。
- 修复后 `go test ./...`、`go vet ./...`、`go test -race ./...` 均通过。
- `go test -race -tags=integration ./tests -v` 通过，验证实际 binary 在缺少配置、worker/all、错误凭据、端口占用这五个场景非零且有界退出；未报 api_started，日志不包含合成密码或数据库 URL。
- 同一实际 binary 的 SIGTERM 正常退出及 PostgreSQL pause/unpause 就绪恢复仍通过；所属测试容器已清理。

相关源码快照（SHA-256）：

| 文件 | 哈希 |
| --- | --- |
| `backend/internal/platform/httpserver/server.go` | `7c3a3301bb26f0c145b61c85afafd334c7025a5f2209a205f83cf9f24f991f6b` |
| `backend/internal/platform/httpserver/server_test.go` | `770e13e09499a2e9ecca4309bcb2cb6d844fb9592e8c9d2bcfcf415cf64eb0cb` |
| `backend/tests/runtime_integration_test.go` | `02925bf2da10b8b4dd7f7299d63077ba503a41a2d1c3b97a25a11cb28f6cf233` |

限制：没有新增用户 API、业务 schema、worker 或容器发布；未验证生产环境。Swift Client 编译问题仍阻断 17-01 完整交付。本证据不改变云端生产验收状态。

## 17-07 本地容器构建与运行验证

证据 ID：BE-CONTAINER-20260908-01。2026-09-08，Codex 在 main `6f0513b` 后的未提交工作树执行；独立人工复核尚未完成。Go 1.26.5 / Docker 29.7.2 / Testcontainers 0.44.0 / PostgreSQL 18.4。

| 操作 | 结果 |
| --- | --- |
| `scripts/validate-backend.sh integration` | passed：模块 checksum、gofmt、vet、单元/race、真实 PostgreSQL 与实际进程验证 |
| `docker build -t then-backend:local backend` | passed，固定 builder digest，linux/arm64 runtime 9,631,796 bytes |
| backend 下 `THEN_BACKEND_TEST_IMAGE=then-backend:local go test -race -tags=container ./tests -count=1 -v` | passed：UID/GID 65532、只读 rootfs、256 MiB/64 PID/1 CPU 限制下 live/ready 200、SIGTERM exit 0、worker/all 非零退出 |
| 创建隔离容器并导出文件清单 | 仅应用 binary、CA、模块清单及 Docker 注入的 hosts/resolv/hostname 等文件；无源码、Go、shell、环境文件；检查容器已删除 |
| 测试资源清理 | API、未实现角色测试容器和 PostgreSQL 均由测试回收 |

本地镜像 ID：`sha256:12305b8287eacb40d572c5face3efcad14da753bf022a7f67777608dd825f0f1`，不是 registry 发布 digest。复现入口：[容器测试](../../backend/tests/container_integration_test.go)、[执行计划](../plan/17-07-后端容器构建与运行验证执行计划.md)。

限制：未发布镜像，未验证 amd64、生产 TLS/容量/高可用或完整 SBOM/许可证/漏洞；模块清单不是 SBOM。Swift Client 构建仍被既有隔离冲突阻断，未新增用户业务 API/schema，云端生产验收状态不变。
