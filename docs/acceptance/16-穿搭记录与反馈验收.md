# 穿搭记录与反馈验收

更新：2026-09-23。16-01 与 16-02 的 DOMAIN/DATA/IOS/TEST 已完成，REL-01 blocked；17-22 账号计划与 17-23 账号实际穿着均已通过后端开发验收；17-32/17-33 Web 账号计划与实际记录页完成本地合成数据验证。结构化反馈、历史复用、App 同步、人工 VoiceOver、真机数据保护和完整 PRD 16 仍未验收。

## 17-34 账号反馈与穿着统计后端子集

2026-09-23 按 [17-34](../plan/17-34-账号反馈与穿着统计API执行计划.md) 在隔离 PostgreSQL 18、Redis 和实际 Go API 中验证结构化反馈与统计：两账号隔离，反馈保存/同命令重试/更改命令冲突、旧 revision 冲突、纠正/撤回/新命令再建均通过；事件改期使原日期归零、新日期计数为 1；事件删除后反馈、回执、统计清除，账号删除亦级联清除。HTTP 合成账号旅程验证无反馈时有效实际事件仍计 1 次、反馈撤回不加减穿着次数、匿名 401；客户端从运行中 OpenAPI 0.18.0 自动生成。完整 Go race、vet、modules 与前端 lint/typecheck/format/15 tests 通过。结论仅为 `passed（账号后端开发子集）`；Web 页面、App 主动同步、云推荐学习、生产发布和真机未验，不能据此把本地 16-03 反馈与云端数据混算。

2026-09-26 以当前实现重新运行 17-34 真实 PostgreSQL `services` race 专项、`go mod verify` 和 `go vet ./...`，均通过；专项使用现有 `postgres` 库内临时 schema，测试后清理。包含此代码的 [CI 36208387693](https://github.com/StephenQiu30/then-server/actions/runs/36208387693) 全部四项 job 通过，但其工作流没有运行 `services` 专项。17-34 后端交付检查点据此完成；产品级 OUTFIT 验收状态及上述 Web、App、生产限制不变。

## 状态与结论

当前已实现真实衣物多选、今天/未来计划、实际穿着四种结果、无计划/部分补录、重复确认、纠正/删除、逐件待洗、派生次数、混合时间线和重启恢复。最大辅助字号的未来计划、取消详情与实际记录表单已通过未过滤的 contrast/textClipped 审计。17-23 已将实际事件和四态计划落实为账号 API；App 主动同步和第二设备恢复仍未实现。

本页保留最终修复证据和仍有效的验收场景；删除已经被同一问题最终结论替代的布局实验与重复失败流水。本轮已重跑实际穿着单元、UI、Debug/Release 与远端 CI，证据见文末。

## 17-32 Web 账号计划核验

2026-09-23，按 [17-32](../plan/17-32-Web穿搭计划数据核验执行计划.md) 在隔离 PostgreSQL/Redis、Go API 和 Next.js 中使用两组合成账号测试 `/plans`。A 从真实衣橱选衣；待洗衣物未逐件确认时页面阻止保存，确认后计划写入且 `/wear-events` 仍为空。外部改动使旧 revision 保存返回 409，草稿未丢，主动重载后编辑成功。未穿/恢复、取消、永久删除均更新对应状态；衣物删除选择清空快照后计划仍可读，相关 `content=null` 在页面显示占位并禁止直接编辑。B 的计划列表为空，读取 A 计划返回 404；断网后重试恢复，Cookie 清除后跳登录。21 条计划按 20+1 分页成功。

运行中 OpenAPI 客户端重生成无差异；lint、typecheck、format、11 项 Node Test、生产构建和生产依赖审计通过。页面与后端 API 根路径冲突在真实浏览器中发现，现用 `/plans` 页面、`/outfit-plans` API，生产重载确认 API 为 JSON。320px 无横向溢出，390px/1280px 布局人工检查；axe-core 4.12.1 WCAG 2 A/AA 为 0 violations / 0 incomplete，开发页 24 passes、生产页 23 passes。证据只覆盖本地合成账号；App 离线记录、实际穿着页面、人工屏幕阅读器和公开发布仍待各自验收。

## 17-33 Web 账号实际穿着核验

2026-09-23，按 [17-33](../plan/17-33-Web实际穿着数据核验执行计划.md) 在隔离 PostgreSQL/Redis、Go API、Next.js 中用两组合成账号测试 `/wear`。A 的待洗外套未逐件确认时无法保存；确认后创建部分实际记录，只有明确勾选的衬衫变待洗。同日同衣物第二次保存先返回相似候选，页面再次主动确认后才写入。来源计划保存前为 active，关联实际事件后为 completed，删除唯一关联事件后恢复 active。外部更新导致旧 revision 的纠正 409，草稿保留，主动重载取回新版本。B 列表为空、读 A 事件 404；断网可重试，Cookie 失效转登录，同一浏览器登录 B 后不显示 A 数据。23 条列表按 20+3 分页。

运行中 OpenAPI 重生成无差异；lint/typecheck/format、15 项 Node Test、生产构建和生产依赖审计通过。320/390/1280px 无横向溢出，键盘 Enter 打开删除确认、Escape 关闭；axe-core 4.12.1 WCAG2 A/AA 0 violations / 0 incomplete、25 passes。截图捕获未成功，因此不声称截图人工视觉或屏幕阅读器验收。公开发布、App 离线记录/反馈、真机和远端 CI 未由此抵扣。


## OUTFIT-ACC-011完整 Look 保存与事实隔离

- 状态：`pending`；覆盖 OUTFIT-REQ-009、AVATAR-REQ-041/042、WARDROBE-REQ-035～037。
- 前置：正式版本化内置资产、生产 Look 持久化用例；新安装、零真实衣物、断网。工程配置验证只能作技术子证据。
- 操作：选择另一套完整内置 Look→查看图片与旋转 GLB→选日期/收藏并保存→重复点击保存→杀进程重启→按日期回看→删除模型但保留图→删除 Look 并重启。14-01 接入后补验本人图片来源撤回及派生模型删除。
- 期望：只有一份 Look，人物/选衣快照、图片版本、可空模型引用、日期/原时区与收藏一致；WardrobeItem、OutfitPlan、WearEvent 和穿着次数均未增加。删模型不丢图片；删 Look 清理其私人图/模型/缓存，共享内置母版与独立衣物/穿着事实保留。本人来源删除阻止新生成并按血缘清理私人派生物，重启不复活。
- 失败：事务失败保留草稿；模型失败不冒充 Look 保存失败，不把旧图或旧模型归给新 revision；缺资产/错误版本不静默替换；A→B→C 迟到及删除竞态不能恢复旧版本。改变选衣只产生草稿，不能伪装已经完成三维换装；确认真实 WardrobeItem 后才能另建真实计划。
- 证据：本地事务/数据查询、文件生命周期、失败注入、实际 App 日期旅程和无障碍录屏；本次仅定义验收，未运行。


## 关联文档

- [`../prd/16-穿搭记录与反馈需求.md`](../prd/16-穿搭记录与反馈需求.md)
- [`../design/09-穿搭记录与反馈设计.md`](../design/09-穿搭记录与反馈设计.md)
- [`../plan/10-OOTD产品实施计划.md`](../plan/10-OOTD产品实施计划.md)
- [`10-OOTD产品系统验收.md`](10-OOTD产品系统验收.md)

当前 [16-01](../plan/16-01-穿搭计划与时间线执行计划.md) 与 [16-02](../plan/16-02-实际穿着与穿后状态执行计划.md) 均已获用户确认并完成本机开发；验收结果仍以实际证据判断。后续切片必须在同编号执行计划中映射本文场景并产出可复核证据。

## 需求覆盖

| 需求 | 验收场景 |
| --- | --- |
| `OUTFIT-REQ-001` | `OUTFIT-ACC-001` |
| `OUTFIT-REQ-002` | `OUTFIT-ACC-002` |
| `OUTFIT-REQ-003` | `OUTFIT-ACC-003` |
| `OUTFIT-REQ-004` | `OUTFIT-ACC-004` |
| `OUTFIT-REQ-005` | `OUTFIT-ACC-005` |
| `OUTFIT-REQ-006` | `OUTFIT-ACC-001`～`OUTFIT-ACC-006` |
| `OUTFIT-REQ-007` | `OUTFIT-ACC-007` |
| `OUTFIT-REQ-008` | `OUTFIT-ACC-007`～`OUTFIT-ACC-009` |
| `OUTFIT-REQ-010` | `OUTFIT-ACC-012` |
| `OUTFIT-REQ-011` | `OUTFIT-ACC-012` |
| `OUTFIT-REQ-012` | `OUTFIT-ACC-012` |
| `OUTFIT-REQ-013`～`015` | `OUTFIT-ACC-013` |

PRD 16 已为业务规则分配 `OUTFIT-RULE-001`～`OUTFIT-RULE-023` 并映射到上述需求簇。本文按需求簇判定功能验收；后续执行计划仍必须同时声明覆盖的规则 ID，规则映射变更时重新复核而不得复用已有 ID。

### OUTFIT-ACC-012 账号穿搭计划与衣物删除影响

- 覆盖需求：`OUTFIT-REQ-010`～`012`；执行：[17-22](../plan/17-22-账号穿搭计划API执行计划.md)。
- 前置：两个账号，各有可穿与不可用衣物及四项确认属性；只用合成数据。
- 操作：创建/重复创建、分页/单日读取、更新、取消、永久删除并尝试迟到重建；修改衣物事实制造冲突；分别对被计划引用的衣物执行快照清空与受影响计划删除。
- 期望：快照只来自同事务账号衣橱；日期/时区、顺序、未知与属性来源不失真；计划不产生实际穿着；owner/revision/状态冲突无部分写；影响变化要求重新确认；两种删除无原属性残留；账号删除级联。
- 证据：Service/HTTP/OpenAPI、真实 PostgreSQL transaction/constraint、实际编译二进制和远程 CI。
- 结果：`passed（后端开发范围）`。实现提交 `f0e765d4fcb27794ae5268c87fcc8e1298cde86b`；本机 unit/vet/modules/完整 race、真实 PostgreSQL services、实际二进制 integration 和受限容器均通过。PostgreSQL 证据覆盖同日多计划、服务重建、owner 隔离、不可穿确认、快照不倒灌、乐观锁、分页、取消/删除 tombstone、影响摘要竞态、两种衣物删除和账号级联；实际 HTTP 覆盖 API 0.10.0 的 6 个计划 operation、删除影响及脱敏后的 `content: null`。GitHub Actions [CI 35012071126](https://github.com/StephenQiu30/then-server/actions/runs/35012071126) 三项全绿。App Client、主动同步、实际穿着和发布未由本项抵扣。

### OUTFIT-ACC-013 账号实际穿着与四态计划

- 覆盖需求：`OUTFIT-REQ-013`～`015`；执行：[17-23](../plan/17-23-账号实际穿着API执行计划.md)。
- 前置：两个账号、可穿/不可用衣物、当日 active 计划，只使用合成结构化数据。
- 操作：标记未穿并恢复；按计划创建实际事件并重试；创建高度相似无计划事件，确认返回的候选；分页、纠正、删除；检查待洗和计划状态；执行衣物历史清空；删除账号。
- 期望：owner 隔离；服务端快照不可注入；事件/待洗/计划同事务；重复候选变化返回 409；最后一条关联事件删除后计划恢复 active；tombstone 防复活；删除影响覆盖计划与实际事件。
- 证据：Service/HTTP/OpenAPI、真实 PostgreSQL transaction/constraint、actual binary、受限 OCI 和远程 CI。
- 结果：`passed（后端开发范围）`。实现提交 `621097fcae8f84ecf3f3999ddeec8ab1b91e8aeb`；本机 unit/vet/modules/完整 race、完整 services、17-23 PostgreSQL 专项、actual binary 和最终受限 OCI 镜像均通过；同 UUID 改变待洗副作用会冲突的回归用例已覆盖。GitHub Actions [CI 35030743618](https://github.com/StephenQiu30/then-server/actions/runs/35030743618) 三项全绿。App Client、主动同步、第二设备恢复、结构化反馈和生产发布未由本项抵扣。

## 验收环境

- 最低 iOS 26 与主流尺寸模拟器；按用户决定先模拟器开发，物理保护/性能在后续真机验收，不能用模拟器抵扣。
- 具有无图、含图、归档、待洗、借出和已删除衣物的合成数据夹具。
- 覆盖离线、跨日/跨时区、低存储、强制退出和数据库写入失败。
- 最大辅助字号、VoiceOver、深色模式和 Reduce Motion。

## 验收场景

### OUTFIT-ACC-001 计划不等于实际穿着

- 覆盖需求：`OUTFIT-REQ-001`、`OUTFIT-REQ-006`；设计测试 `LOG-01`。
- 前置条件：已有一套可保存的结构化穿搭。
- 操作步骤：分别保存组合草稿、将其计划到未来日期，查看穿着次数和历史。
- 期望结果：只有计划记录，不产生 `WearEvent`、不增加穿着次数、不形成长期偏好。
- 边界情况：修改、取消或过期计划不得被转换为实际穿着。
- 验收证据：领域状态机测试、GRDB 事务查询和 UI 录屏。
- 结果：`passed（16-01/16-02 本机开发子集）`。计划保存仍保持 0 次；只有显式实际事件计数，未来计划不显示结果动作。

### OUTFIT-ACC-002 实际结果和部分事实

- 覆盖需求：`OUTFIT-REQ-002`；设计测试 `LOG-02`～`LOG-05`。
- 前置条件：存在到期计划和可用替换衣物。
- 操作步骤：分别确认“按计划穿了”、“换了几件”、“穿了别套”、“未穿”、“无计划但穿了”和仅记录部分衣物。
- 期望结果：计划快照不被改写；实际穿着精确保存已确认事实；“未穿”不惩罚衣物。
- 边界情况：未确认的类别保持未知，不自动补齐成完整套装。
- 验收证据：用例测试、快照对比和 XCUITest。
- 结果：`passed（16-02 本机开发子集）`。四种计划结果、无计划部分记录、计划快照不变和未知不补齐均有领域/数据/UI 证据。

### OUTFIT-ACC-003 结构化反馈可跳过、纠正和撤回

- 覆盖需求：`OUTFIT-REQ-003`；设计测试 `LOG-04`、`LOG-11`。
- 前置条件：已确认一次实际穿着。
- 操作步骤：跳过反馈，再填写冷热、舒适、场合和再穿意愿，然后纠正并撤回该证据。
- 期望结果：空值不等于负面；最新用户明确值生效；撤回后个性化特征可重建且不残留已撤回信号。
- 边界情况：快速连续修改、重复提交和杀进程不产生重复证据。
- 验收证据：特征重建测试、幂等测试和推荐解释审计。
- 结果：`passed（16-03 本机开发子集）`。四维反馈均可跳过，最新 correction 生效，明确删除后证据即时重建；幂等、并发冲突、事件删除、历史脱敏和两次偏冷→纠正→撤回均有领域/数据证据。iPhone 17 / iOS 26.5 模拟器完成添加、纠正和删除旅程；推荐消费、真机保护、VoiceOver 与恢复演练不在本结果内。

### OUTFIT-ACC-004 穿后衣物状态原子提交

- 覆盖需求：`OUTFIT-REQ-004`；设计测试 `LOG-08`。
- 前置条件：实际穿着包含多件衣物。
- 操作步骤：逐件选择保持可用或标为待洗，分别注入成功和写入失败。
- 期望结果：成功时实际记录和衣物状态一次提交；失败时整体回滚并允许重试。
- 边界情况：衣物已被其他操作归档时提示冲突，不静默覆盖。
- 验收证据：GRDB 事务测试、版本冲突测试和恢复录屏。
- 结果：`passed（16-02 本机开发子集）`。实际事件、快照、待洗状态、计划结果和幂等收据同事务；注入失败整体回滚。

### OUTFIT-ACC-005 历史快照与再次穿着

- 覆盖需求：`OUTFIT-REQ-005`；设计测试 `LOG-06`、`LOG-07`。
- 前置条件：历史穿搭中一件衣物已改名或不可用。
- 操作步骤：查看时间线/月历和详情，选择“再次穿这套”。
- 期望结果：历史保留当时结构化快照；图片已删除时无图降级；复用前重新检查当前衣物状态并说明冲突。
- 边界情况：已删除衣物按获批的占位/重建策略表示，不虚构当前可用性。
- 验收证据：快照迁移测试、当前/历史对比和 UI 录屏。
- 结果：`partial`。16-02 已通过历史实际快照、衣物版本变化、删除脱敏和照片引用边界；“再次穿这套”、月历和完整历史复用仍待后续切片。

### OUTFIT-ACC-006 离线、中断、幂等与时区

- 覆盖需求：`OUTFIT-REQ-006`；设计测试 `LOG-12`～`LOG-14`。
- 前置条件：设备离线，时区和日界线夹具已准备。
- 操作步骤：创建计划和实际记录，在保存时杀进程/注入低存储，重启并重复确认，再切换时区。
- 期望结果：无网可完成核心流程；事务全成功或全回滚；重复操作不增加穿着次数；展示日期与原始时区语义一致。
- 边界情况：草稿无法恢复时明确说明已丢弃的未提交内容，不谎称保存成功。
- 验收证据：故障注入报告、时区属性测试和重启录屏。
- 结果：`passed（16-02 自动化子集）`。真实 SQLite 事务、并发幂等、重复候选、原始时区、稳定混合分页和进程重启旅程通过；真机低存储/物理中断仍归 REL-01。

### OUTFIT-ACC-007 视觉生成不是穿着事实

- 覆盖需求：`OUTFIT-REQ-007`、`OUTFIT-REQ-008`；设计测试 `LOG-09`、`LOG-11`。
- 前置条件：穿搭关联静态或动态 AI 结果。
- 操作步骤：查看、保留并删除生成结果，然后检查实际穿着和推荐偏好。
- 期望结果：浏览、拖动、保留和删除媒体均不创建 `WearEvent`，不自动成为长期偏好；删除人物图后历史使用模板或结构化拼图。
- 边界情况：迟到的生成结果不能回写已删除或已撤回的穿着证据。
- 验收证据：数据查询、偏好特征审计和删除链测试。
- 结果：`pending`。16-02 未接入静态或动态生成结果；保存/查看计划不会创建实际事件，但媒体迟到与偏好隔离仍须在生成切片验收。

### OUTFIT-ACC-008 不打扰和最小生活上下文

- 覆盖需求：`OUTFIT-REQ-008`；设计测试 `LOG-15`。
- 前置条件：未授予通知、日历或位置权限。
- 操作步骤：使用应用内到期提示、跳过反馈，并检查分析/同步网络流量。
- 期望结果：核心记录不请求新权限；跳过不重复打扰；原始日程、精确位置和穿着正文不因核心功能离机。
- 边界情况：未来若开启推送、分析或同步，本场景保持阻断，直到目的分离和独立验收通过。
- 验收证据：权限录屏、网络抓包和通知计数检查。
- 结果：`passed（16-02 核心边界）`。本切片未新增通知、日历、位置、网络同步或分析权限；完整网络抓包和未来同步仍需独立验收。

### OUTFIT-ACC-009 可访问性与语义一致

- 覆盖需求：`OUTFIT-REQ-005`、`OUTFIT-REQ-008`；设计测试 `LOG-16`、`LOG-17`。
- 前置条件：最大辅助字号、VoiceOver、深色模式和 Reduce Motion 分别开启。
- 操作步骤：完成计划、实际确认、反馈、状态更新和历史复用。
- 期望结果：信息不裁切；关键控件有可理解标签和足够点击区；计划、实际和反馈的语义不只依赖颜色。
- 边界情况：无图、部分记录和错误状态与完整记录一样可操作。
- 验收证据：Accessibility Inspector 报告、真机录屏和 UI 测试。
- 结果：`partial`。最大辅助字号下未来计划和实际记录表单的 contrast/textClipped 自动审计通过；人工 VoiceOver、深色/Reduce Motion 全旅程和真机录屏仍待 REL-01。

## 2026-09-13手工计划切片验收映射

16-01 approved 合同覆盖 ACC-001 的计划不计实际、ACC-005 的最小快照/照片失效（复用/月历仍后续）、ACC-006 的计划离线/事务/时区、ACC-008/009 的最小权限与可访问性子集，以及补充 ACC-010 的彻底删除。完整功能结果仍 pending；16-01 已有生产领域/SQLite/页面实现，2026-09-14 数据层 50 项测试通过，模拟器 UI 验收进行中。需分别验证：同日多计划、IANA 时区与日界线、相同命令重试、删除后迟到写入、衣物改名不改历史、照片替换不冒充旧图、衣物彻底删除的占位/关联计划删除两条路径。完整 PRD 的实际穿着、反馈、复用/月历与发布门禁继续保留。

### OUTFIT-ACC-010衣物与计划彻底删除

覆盖 OUTFIT-RULE-021/022、Design LOG-10 与已批准删除合同。分别选择保留无属性占位、删除受影响计划/实际事件；真实 SQLite 核查两类快照的原名称、类别、版本、媒体引用和衣物关联不可恢复，影响变化要求重新确认。删除计划保留独立实际事件并解除来源，删除事件不删除计划或衣橱。结果 `passed（16-01/16-02 本机开发子集）`；账号删除与真机媒体物理删除仍按各自门禁验收。


## 2026-09-14计划数据开发验收

- 契约：16-01 approved；代码为当前未提交工作树，尚未发布。
- 环境：Xcode 26.6 / Swift 6.3.3，独立 iPhone 17 / iOS 26.5 模拟器；仅合成夹具。
- DATA-16-01：`OutfitPlanDataFinal20260913.xcresult`，命令为 `xcodebuild test` 选择 OutfitPlanTests / OutfitPlanRepositoryTests / WardrobeRepositoryTests / WardrobePhotoRepositoryTests / WardrobeViewModelTests，exit 0，50 tests / 5 suites。
- 已通过的计划子集：ACC-001 不生成实际穿着；ACC-005 改名快照不倒灌、媒体替换/移除清除旧引用；ACC-006 日期/时区、稳定分页、幂等、防复活、事务失败回滚；ACC-010 两种衣物删除策略及确认后关联竞态、计划删除保持衣橱。
- v3→v4 升级保留实际 SQLite 行、UUID、revision、ready 媒体元数据/hash 与文件字节；存储夹具不能替代图片解码与物理保护证据。
- UI、完整 PRD 实际穿着/反馈/历史复用、人工 VoiceOver、真机数据保护与发布指标未由上述测试验收。执行人 Codex，复核待定。


UI-16-01-PHOTO（2026-09-14）：`OutfitPhotoAndAudit20260914.xcresult` 的 `testOutfitPhotoMultiSelectionAndBackground` 通过，其他无障碍测试仍失败。已逐图检查真实合成衣物缩略图、照片移除后同一计划仅保留文字，以及 App 切换器中的整页隐私遮罩。附件 ID 归 16-01 证据段；该截图只证明本次模拟器显示/切后台行为，不证明物理数据保护或所有中断情况。最新数据/状态组合回归 `OutfitFinalChecks20260914.xcresult` 已输出 65 tests / 7 suites passed，UI 结果尚未收口，不能记整体验收 passed。

2026-09-14 诊断证据：独立 SwiftUI host 复现取消/滚动后一条 textClipped（无 element），产品旅程为两条；没有数据库、业务对象或真实媒体。原型、失败截图、颜色计数与未通过试验归 16-01 证据段；不据此直接声明 SDK 误报。三份选择页失败附件的字形/背景主要为不透明黑/白；首次详情“编辑计划”确认为 #0088FF / #FFFFFF，相关实际颜色问题已修复。

[CVS Health 的 XCTest 实践记录](https://github.com/cvs-health/ios-swiftui-accessibility-techniques/blob/main/iOSswiftUIa11yTechniques/Documentation/XCTestAccessibility.md) 列出了对比度和裁切误报的已观察限制；这是外部诊断线索，不证明本项目所有报告均为误报。颜色核对参考 [W3C 最低对比度说明](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)，并与真实显示、VoiceOver、动态字号分别验证；不照搬排除整个审计类别的示例。

`OUTFIT-20260914-SEMANTIC`：67 项领域/数据/VM 通过；最新标准字号完整旅程与三处审计通过（`OutfitSemanticEdit20260914.xcresult`）；最终 Debug/Release 与架构检查通过。关键源码和各证据的通过边界记录于 `OutfitSemanticEvidence20260914.json`，具体哈希归 16-01。最大字号、人工 VoiceOver 和物理发布项仍未抵扣，16-01 尚未完整验收。

## 2026-09-15最大字号未来计划与取消状态收口

证据 ID：`OUTFIT-ACCESSIBILITY-20260915-01`，关联 16-01 TEST-01 与 ACC-005/006/009/010 的计划子集。当前仓库原先只有简化穿搭 UI 测试，旧记录中的 `testOutfitDateAtLargestText` 不在可执行源码；本轮新增正式 `testOutfitPlanAtLargestAccessibilityText`，使用随机合成衣物，在最大辅助字号中选择明天、保存并核对 YYYY-MM-DD 分组，打开详情后取消和永久删除，最后清理衣物。编辑、首次详情、取消详情均执行未过滤的 contrast/textClipped 审计。

Red 首轮完整执行到清理，但取消后的详情报告原始时区与“计划不代表实际穿着”两项 contrast 失败；相同内容滚回顶部后通过。根因是用户在详情底部确认取消，而状态变化仅呈现在顶部，操作完成后缺少可见结果。生产详情加入状态变化后的顶部定位，未改颜色、字号、审计类别、计划状态机或持久数据。修复后的完整最大字号路径连续三轮通过，其中最终固定证据为 1 test、0 failures、0 skipped、64.388 秒；包含原业务旅程的回归为 2 tests、0 failures、0 skipped。OutfitPlan 领域、ViewModel、Repository 三组为 29 tests、0 failures、0 skipped。

持久证据索引为 [`16-01-outfit-accessibility-2026-09-15.json`](https://github.com/StephenQiu30/then-server/blob/dd35b002dc6e85c983a5c44a784756bdec3f1af2/docs/acceptance/evidence/16-01-outfit-accessibility-2026-09-15.json)，视觉证据为 [`未来日期最大字号`](https://github.com/StephenQiu30/then-server/blob/dd35b002dc6e85c983a5c44a784756bdec3f1af2/docs/acceptance/evidence/16-01-outfit-future-date-largest-text.png) 与 [`取消详情最大字号`](https://github.com/StephenQiu30/then-server/blob/dd35b002dc6e85c983a5c44a784756bdec3f1af2/docs/acceptance/evidence/16-01-outfit-cancelled-largest-text.png)。Debug/Release 通用 iOS Simulator 构建均为 `BUILD SUCCEEDED`。本地证据最初基于 then-app `b3d2cffe5aed16b9ead43cde664ad8c91a937c70` 加本轮修改，最终实现提交为 `2ee51c43c1a7b19bf235b53800918ec41605b1b6` 并推送 main；Actions `34877783052` 成功完成 Debug 构建和模拟器兼容测试，但工作流跳过 UI Tests。截图和测试仅使用合成名称，无用户数据。TEST-01 自动化子门禁据此完成；ACC-009 整体仍为 pending，人工 VoiceOver、真机数据保护和完整实际穿着/反馈/历史复用未由本证据抵扣，REL-01 保持 blocked。

## 2026-09-16实际穿着开发验收

16-02 于 2026-09-16 获用户确认并完成本机开发，最终 App 提交为 `ad2d8ee`、`d0b5927`、`a38a678`。真实 SQLite 证据覆盖 `ootd_wear_events_v1` 从现有属性/计划基线升级、最终外键指向、事务失败回滚、并发幂等、0.8 相似度边界、重复候选 revision、31 条混合分页、四态计划重算、两类衣物历史删除和不透明删除态。CI 等价本地单元结果为 238 项中 236 passed、2 skipped、0 failed，动态参数实际执行 379 次；两组 Vision 模拟器套件按既定不可用边界排除。

模拟器 A 完成计划→换件→待洗→保存→终止/重启→纠正→删除实际事件→计划恢复 active→待洗保持→次数归零→删除关联历史；模拟器 B 完成无计划部分记录→重复提示→明确第二次→次数 2→删除衣物关联历史。最大辅助字号用例同时验证未来计划不显示实际结果动作，并对实际记录表单执行未过滤的 contrast/textClipped 审计。三条最终结果包分别为 `ThenWearUITest-20260916-journey-6.xcresult`、`ThenWearUITest-20260916-3.xcresult` 和 `ThenWearUITest-20260916-accessibility-3.xcresult`，均为 1 passed、0 failed。

本地 Xcode 27.0 下 Debug/Release 通用 iOS Simulator 构建通过；固定 Xcode 26.6 / Swift 6.3.3 的最终远端证据为 [then-app CI 35026451568](https://github.com/StephenQiu30/then-app/actions/runs/35026451568)，Debug 构建与模拟器兼容测试均为 success。本证据完成 16-02 DOMAIN/DATA/IOS/TEST，不完成结构化反馈、再次穿搭/月历、账号同步、人工 VoiceOver、真机数据保护或恢复演练；REL-01 因这些发布证据保持 blocked。
