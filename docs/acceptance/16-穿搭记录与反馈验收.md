# 穿搭记录与反馈验收

## OUTFIT-ACC-011内置造型保存与事实隔离

- 状态：`pending`；覆盖 OUTFIT-REQ-009、AVATAR-REQ-020/021、WARDROBE-REQ-025～027。
- 前置：正式版本化内置资产、生产 Look 持久化用例；新安装、零真实衣物、断网。11-02 隔离配置验证只能作技术子证据。
- 操作：换上装并观察→选日期保存→重复点击保存→杀进程重启→按日期回看和再次应用→删除 Look；再保存一个 Look 并删除角色，重启检查。
- 期望：只有一份 Look 且版本/外观/单品/日期/原时区一致；WardrobeItem、OutfitPlan、WearEvent 和穿着次数均未增加；删 Look 清理其私有预览，共享资源保留；删角色清除历史私人参数/引用/预览，独立服装/日期可读且不复活。
- 失败：事务失败保留草稿；预览错误不伪装保存失败或把旧封面归给新 revision；缺资产/错误版本不静默替换；A→B→C 迟到及删除竞态不得恢复旧配置。只确认真实 WardrobeItem 后才能另建真实计划。
- 证据：本地事务/数据查询、文件生命周期、失败注入、实际 App 日期旅程和无障碍录屏；本次仅定义验收，未运行。


## 2026-09-14日期宽度适配与跨字号回归

证据 `OUTFIT-DATE-FIT-20260914-01`，归 16-01 TEST-01。正式日期字段改为 `ViewThatFits(in: .horizontal)`：先测量原生紧凑 DatePicker，宽度不足时使用原有完整标签与图形日历。取消固定字号阈值对整个日期区域的替换；日期绑定、Gregorian calendar、原始时区、保存/失焦/隐私与业务规则保持。新增标准字号日期专项，与原最大字号专项共享边界、可点击、contrast/textClipped、取消及放弃草稿断言；没有过滤失败或缩小字号。

| 结果包（DerivedData 下，后缀 20260914.xcresult） | 实际结果 |
| --- | --- |
| `OutfitFocusAbsent` | 1 失败，17.548 秒；仅日期对照移除 FocusState 仍裁切，撤回 |
| `OutfitStableDateLabel` | 1 最大字号专项通过，20.392 秒；AnyLayout 稳定标签试改，仅局部通过 |
| `OutfitStableDateRepeat` | 2 注册、1 通过/1 失败，固定重复两次；最大字号 15.998/13.391 秒通过，标准字号 16.109/14.822 秒对比度失败 |
| `OutfitDateOriginalCompare` | 同设备原正式页面、相同新增测试：最大字号 14.761 秒裁切，标准字号 13.937 秒通过。证明 AnyLayout 试改引入了标准字号回归，不能交付 |
| `OutfitStableDateAligned` | 2 注册、1 通过/1 失败；恢复紧凑日期靠右对齐后，最大字号 15.156 秒通过、标准字号 15.643 秒仍对比度失败，AnyLayout 试改整体撤回 |
| `OutfitFittingDate` | 最终 ViewThatFits：2 注册全通过，最大字号 14.885 秒、标准字号 13.989 秒；0 跳过/0 预期失败，结果门禁 exit 0 |
| `OutfitFittingDateJourney` | 最终源码 4 注册、3 通过/1 失败：最大字号日期 15.503 秒、标准日期 14.251 秒、标准完整未来计划旅程 70.960 秒通过；最大字号完整未来旅程 128.735 秒在取消详情的原始时区对比度失败，结果门禁 exit 1 |

已查看最终两种字号截图 `DerivedData/OutfitFittingDateProof20260914/8D902508-444C-40F6-BB94-680A63CDD5B8.png`、`FFBF7249-80B8-4F44-BB87-5703F8E25B2D.png`：标准字号仍为原紧凑日期行，最大字号为完整日期标题及图形日历。截图检查与自动审计分别成立，不推断 Apple 内部实现或笼统认定 SDK 误报。已通过最小壳不曾覆盖原字号阈值分支，因此此前孤立状态/环境对照不能替代本次跨字号测试。

最终差异仅为日期布局与标准字号专项覆盖，三份领域状态/Repository 源码哈希保持既有分页修复版本。前后源码、逐包结果和已查看截图清单：本机 `DerivedData/OutfitDateFitEvidence20260914.json`，SHA-256 `abf55004c81afc9e629f7beee2ab29d52d2ae6a36c11570d3f97ad3d033fa77e`；本机材料不是 CI 或发布归档。所有包均无跳过/预期失败，失败包保留原结论。

本次已关闭日期区域的横向边界和字号切换裁切问题。完整最大字号旅程实际完成未来日期选择、保存、编辑、取消、删除和衣物清理，但取消详情审计报告“原始时区：Asia/Shanghai”对比度失败，不能将该旅程记为通过。已查看审计前截图与失败 App/Element 附件：审计前时区在当前视口上方，失败附件显示审计将其带回可见区域；不凭黑白截图认定 SDK 误报。下一检查点是详情滚动/审计可见范围及该标签真实对比度，16-01 TEST-01 与完整目标保持进行中。

收尾：`scripts/validate-ios.sh build` 的工具链、架构及正式 Debug/Release Simulator 构建全部通过，日志 `DerivedData/OutfitFittingDateBuild20260914.log`。四份文档 48 个本地文件链接与 `git diff --check` 通过。未重复运行不受影响的 Go/数据库测试；物理设备与人工 VoiceOver 发布检查未抵扣。全部本轮进程已结束，专用 `ThenOutfitFocus20260914`（A72C0F14-984F-434C-BFDC-FF2679515799）已删除并核对不存在；原有设备及数据保持，无提交/推送。


## 2026-09-14真实编辑器外层与日期所有权对照

证据 `OUTFIT-DATE-OUTER-20260914-01`，归 16-01 TEST-01。所有试改只用于定位最大字号 `textClipped`；正式 View、RootView 和 UITest 均已恢复。未过滤问题、降低字号或取消 contrast/textClipped 审计；最小壳只省略没有实现的审计后关闭步骤，不能作为产品旅程验收。

| 结果包（DerivedData 下，后缀 20260914.xcresult） | 结果与边界 |
| --- | --- |
| `OutfitOuterControl` | 1 失败，14.913 秒；保留真实草稿/表单/load，缩减编辑器外层仍裁切 |
| `OutfitOnlyDateControl` | 1 失败，10.966 秒；缩减为仅日期仍裁切 |
| `OutfitDateBindingControl` | 1 失败，12.963 秒；改成显式读写同一草稿日期的 Binding 仍裁切 |
| `OutfitRootPrivacyControl` | 1 失败，8.097 秒；临时移除根页隐私覆盖仍裁切 |
| `OutfitSingleSheetControl` | 1 失败，8.194 秒；进一步仅保留计划 sheet 仍裁切 |
| `OutfitDateOwnership` | 3 通过；在真实根页中使用独立最小日期 Form，直接草稿绑定 8.545 秒、当前时间本地状态 8.212 秒、草稿日期本地副本 8.196 秒 |
| `OutfitCalendarScope` | 1 失败，11.964 秒；完整正式页面仅将 calendar/timeZone 从日期区域缩至 DatePicker，仍裁切；原时区值保持 |
| `OutfitDateFactors` | 3 通过；最小 Form 分别增加原 calendar/timeZone 8.145 秒、原标签 fixedSize 9.421 秒、真实草稿 load 10.288 秒 |
| `OutfitDateWidth` | 1 失败，17.029 秒；完整正式页面仅为日期标题增加整行可用宽度，仍裁切 |

九个结果包均经 `scripts/validate-ios-test-result.py` 核对：7 个失败包各 1 注册失败，2 个通过包各 3 注册通过；全部 0 跳过、0 预期失败。逐包输出、14 份原始/试改源文件哈希及恢复后五份业务源码哈希见本机 `DerivedData/OutfitOuterEvidence20260914.json`，SHA-256 `ddefd0eb21c5609c5274561f25611a5f630795d0511b6812243eec622758b8b7`；本机诊断材料不是 CI 或发布归档。

结论：独立日期 Form 在真实 Root 中能够通过，但本轮缩减、Binding、环境作用域和宽度调整均没有修复正式页面。最小 Form 的条件单独通过不能排除正式组合中的交互影响，也不证明 SDK 误报。未引入状态副本、替代架构或移除隐私保护；未改变日期、数据库、媒体、API 和业务契约。日期裁切及此前最大字号取消详情对比度仍未关闭，TEST-01 保持 in_progress。

收尾：恢复后正式 Debug Simulator 构建通过（`DerivedData/OutfitOuterFinalBuild20260914.log`），工具链、iOS 架构、两份文档 12 个本地文件链接和 `git diff --check` 通过；本轮未重跑 Release 或完整业务旅程。专用 `ThenOutfitOuter20260914`（4D82D760-AA07-4E97-BEA0-AA64147E8C99）已关闭、删除并核对不存在，原有设备与 OOTD 数据未清理。没有提交或推送。


## 2026-09-14观察状态与审计阳性对照

证据 `OUTFIT-DATE-OBSERVATION-20260914-01`，归 16-01 TEST-01。沿“同一正式 target 的最小页通过”继续比较状态驱动与展示方式；所有正常页面保留原 contrast/textClipped 审计，不屏蔽 issue。没有将 Observation 改为另一套状态架构，所有正式页面试改已撤回。

| 本机结果包（位于 DerivedData，后缀 20260914.xcresult） | 结果与证据边界 |
| --- | --- |
| `OutfitDateObservation` | 3 注册全通过：@State 值基线 9.980 秒、@State 持有 @Observable 草稿并向子页 @Bindable 注入 9.182 秒、同草稿读取状态持续 100ms 后恢复 8.701 秒；不能解释正式失败 |
| `OutfitDateLeafRed` | 当前正式代码 1 注册失败，13.517 秒，仍为日期 textClipped，先确认故障没有随前一轮工程配置清理消失 |
| `OutfitDateLeafTrial` | 1 注册失败，固定两次 12.247 / 10.508 秒；将日期 computed ViewBuilder 原样提取为 private SwiftUI View、只传原 Binding，未解决问题 |
| `OutfitDateFormTrial` | 1 注册失败，11.703 秒；保留真实草稿与编辑内容，仅去掉外层详情分支、错误/已删除/加载提示条件，仍 textClipped |
| `OutfitDateDisabledControl` | 1 注册失败，11.713 秒；完整原表单仅移除 editing Group 的 disabled 修饰器，仍 textClipped，原交互保护已恢复 |
| `OutfitDatePresentation` | 2 注册全通过，各固定两次；同一个 Identifiable 可观察草稿、同一编辑页，sheet(item:) 8.258 / 7.133 秒，sheet(isPresented:) 8.387 / 8.312 秒。未更改正式弹窗入口 |
| `OutfitDatePositive` | 2 注册、1 通过/1 失败。仅在隔离壳将标题限为 120×40pt、单行并裁切，8.307 秒在审计调用处报告 Text clipped，handler 打印目标“穿搭日期”；正常标题 8.452 秒通过。说明审计能够检出本例明显裁切，不证明正常正式页不存在其他裁切 |

阳性对照没有使用 XCTExpectFailure 或忽略问题，结果包与门禁仍保留真实失败（exit 65 / gate exit 1）。已查看失败元素截图 `OutfitDatePositiveProof20260914/46A956FD-B068-4BC5-B18C-599F6B733D44.png`，标题实际显示为“穿…”；不是用断言失败或页面未打开冒充裁切检出。其余失败包同为 gate exit 1，通过包 exit 0；全部 0 跳过/0 XCTest 预期失败。

一次无效准备尝试单独保留：`OutfitDateDisabledTrial20260914` 的源码替换断言因匹配到多处修饰器而失败，随后的 shell 却启动了旧 Form 试改的重复测试。发现后核对本轮唯一结果路径的 xcodebuild PID，发送 SIGINT 并等待退出（65）；日志已有 12.104 秒失败，但准备不正确，不能作为新 disabled 条件证据。随后将编辑限制为 editing 源码区间，并且只有准备命令 exit 0 才启动测试，形成表中独立 `OutfitDateDisabledControl` 结果；没有中断其他任务或用户设备。

隔离工程为 `DerivedData/OutfitDateObservation20260914`、`OutfitDatePresentation20260914`、`OutfitDatePositive20260914` 下的 `Probe.xcodeproj` / `Probe` scheme；正式对照使用原 `ThenAppUITests/ThenAppUITests/testOutfitDateAtLargestText`。全部单设备、独立结果包、`-parallel-testing-enabled NO`；固定重复加 `-test-iterations 2 -test-repetition-relaunch-enabled YES`。源码、结果摘要及阳性截图清单 `DerivedData/OutfitObservationEvidence20260914.json`，SHA-256 `ae6c4f7045368b52cd0446267788eaf65757d76b0c45846675bcecaac8724118`，为本机诊断材料，非 CI 或正式交付通过证据。

当前结论：Observation、这两种系统 sheet 入口，以及本轮独立组件/条件表单/禁用区域试改均未提供可验证的正式修复。五份产品源码与 `OutfitPaginationEvidence20260914.json` 的已测版本完全一致，日期规则、Woo 布局、数据、原审计及隐私/交互保护保持。后续仍需从真实编辑器中获得可稳定失败的更小复现；不得根据隔离通过、相同尺寸或当前截图完整关闭 textClipped/contrast 或宣称 SDK 误报。

收尾：工具链、iOS 架构、两份文档 12 个本地链接及 `git diff --check` 通过；恢复后正式 Debug 构建通过，日志 `OutfitObservationFinalBuild20260914.log`，未重跑 Release。所有本轮测试/构建已结束，专用 iPhone 17 / iOS 26.5 `ThenOutfitObservation20260914`（7DB30A85-0CD9-496F-BD32-128FE5955731）已删除且清单核对不存在；原有设备和 OOTD 数据未清理。无提交/推送，TEST-01 与整体目标继续进行。

## 2026-09-14正式target与最小页面对照

证据 `OUTFIT-DATE-TARGET-20260914-01`。将上一轮已通过的隔离日期页临时编译在正式 ThenApp target，使用同一工程配置、SwiftUI/依赖、Bundle ID 和最大字号启动参数；不启动正式存储或真实编辑器。专项保留日期类型、屏幕边界、可点击及 contrast/textClipped 断言，仅去掉最小壳没有实现的审计后放弃草稿步骤。`OutfitTargetControl20260914.xcresult` 为 1 注册通过，固定两次 9.829 / 7.867 秒，0 跳过，xcodebuild 与结果门禁均 exit 0。

此结果说明同一正式 target 能对该最小页面通过审计，不能把现有裁切归因于工程配置本身或笼统认定 SDK 误报。正式编辑器的状态与组合结构仍未在最小壳中等价复现；没有替换产品日期页、移除产品草稿流程或关闭原失败项。App 入口和正式 UITest 已逐字恢复，诊断前/中源码分别保存在 `DerivedData/OutfitTargetAppOriginal20260914.swift`、`OutfitTargetAppTrial20260914.swift`、`OutfitTargetTestOriginal20260914.swift`、`OutfitTargetTestTrial20260914.swift`。

同轮发现的旧 INFOPLIST_KEY 工程配置残留转由 19-02 独立修复验收，不推断它导致日期裁切。16-01 TEST-01 继续 in_progress，后续应在真实编辑器中保留状态驱动结构再缩减；无障碍、Woo/AI/真实三维及完整目标不由此诊断抵扣。

## 2026-09-14日期结构与生命周期对照

证据 `OUTFIT-DATE-STRUCTURE-20260914-01`，归 16-01 TEST-01。沿官方审计机制复核后继续缩减正式页与隔离页差异；没有过滤 contrast/textClipped，没有失败后重试直到通过。所有正式代码试改都已撤回。

| 本机结果包（均位于 DerivedData，后缀 20260914.xcresult） | 实际结果与范围 |
| --- | --- |
| `OutfitDateStructure` | 4 注册、3 通过/1 失败。基线 12.570 秒、单层隐私遮罩 8.792 秒、Task.yield 前后禁用切换 8.639 秒通过；显式 EmptyView 安全区组 14.740 秒在审计前找不到日期，不能当作裁切复现 |
| `OutfitDateConditionalInset` | 2 注册全通过，各固定重复两次。基线 11.031 / 7.566 秒；将 EmptyView 改为正式使用的条件式内容后，空安全区组 8.632 / 8.765 秒通过 |
| `OutfitDateEnvironment` | 3 注册全通过。基线 9.152 秒、给日期区域设置公历与 Asia/Shanghai 时区 8.642 秒、采用不传 handler 的默认审计调用 8.435 秒；诊断 handler 返回 false，未忽略问题 |
| `OutfitInsetFormal` | 1 注册失败；正式日期专项去掉顶部安全区，固定两次仍 textClipped，11.180 / 10.250 秒；专项边界断言通过，不能把删掉顶部已选区当作修复 |
| `OutfitHostFormal` | 1 注册失败；真实编辑器/模型/数据库保持，仅将外层 Tab 宿主临时换成最小 NavigationStack 启动按钮，两次仍 textClipped，11.477 / 10.431 秒 |
| `OutfitSectionFormal` | 1 注册失败，14.359 秒；真实表单暂时仅保留“安排”区域，仍 textClipped，选衣区域不是本次失败的必要条件 |
| `OutfitBodyFontFormal` | 1 注册失败，12.007 秒；完整正式页日期标题显式声明 .body，仍 textClipped；增加的 AX 树附件仅用于诊断，审计项未变 |
| `OutfitDateLifecycle` | 4 注册全通过。基线 8.893 秒、根页与弹窗双层隐私遮罩 8.780 秒、禁用状态分别持续 100 / 1000ms 后恢复 8.737 / 8.841 秒。延时仅存在于无数据隔离壳，不加入生产或正式验收的等待逻辑 |

八个结果包分别运行 `scripts/validate-ios-test-result.py`：上述失败包均 gate exit 1，通过包均 exit 0，全部 0 跳过/0 预期失败。固定重复只用于条件安全区、正式去安全区及最小宿主对照；注册计数不等于重复执行次数。隔离测试只说明这些条件在该壳中不足以触发问题，不能断言排除了真实 Observation 状态、组合层级或异步读取的所有影响。

新的直接对照证据：正式 `.body` 试改的 AX 树 `OutfitBodyFontProof20260914/3F9758C4-9AE3-4FBB-81B4-6CEB23441238.txt` 与通过基线的 `OutfitDateStructureProof20260914/46A3EAEE-3AE3-4664-B5DB-1F2F97D3AD25.txt` 中，日期标题均为 StaticText，frame `{{32.0,275.3},{200.3,63.3}}`；日历均为 DatePicker，frame `{{32.0,346.7},{338.0,362.0}}`。两者所在单元格也均为 `{{16.0,260.3},{370.0,463.3}}`。已查看隔离完整截图、正式完整截图与失败标题截图；可见边界及节点尺寸不能解释不同审计结果，也不足以认定 SDK 误报。

复现入口：四份隔离工程为 `DerivedData/OutfitDateStructure20260914`、`OutfitDateConditionalInset20260914`、`OutfitDateEnvironment20260914`、`OutfitDateLifecycle20260914` 下的 `Probe.xcodeproj` / `Probe` scheme；正式对照仍使用 `ThenAppUITests/ThenAppUITests/testOutfitDateAtLargestText`。全部独立结果包、单设备、`-parallel-testing-enabled NO`；固定重复加 `-test-iterations 2 -test-repetition-relaunch-enabled YES`。本机源码、试改、AX 树与逐包结果清单为 `DerivedData/OutfitStructureEvidence20260914.json`，SHA-256 `e3fd526521b2a28e84e92ec4dda3885b04bd9fbb9315123c599ff08443c0ffed`；这是诊断材料，不是 CI 归档或产品通过证据。

当前结论：恢复全部 Woo Tab、顶部已选区、完整表单、原字体、隐私保护及原正式测试。RootView 与本轮初始快照一致，另外五份源码与 `OutfitPaginationEvidence20260914.json` 的已测版本一致。没有保留降低范围或保护能力的改动。后续最小复现应保留真实状态驱动的编辑器结构再缩减，避免继续凭截图试改文字尺寸、颜色或间距；日期裁切和取消详情对比度门禁继续未通过。

收尾：工具链、iOS 架构、两份文档 10 个本地链接和 `git diff --check` 通过；恢复后的正式 Debug 构建通过，日志 `OutfitStructureFinalBuild20260914.log`，本轮未重跑 Release。全部测试及构建进程已结束，专用设备 `ThenOutfitStructure20260914`（C8DE3ADB-A2C8-4B9D-8B24-00B2BA315598）清理时已处于 Shutdown，随后删除并核实清单中不存在；未清理原有设备或 OOTD 数据，未提交/推送，整体目标保持 active。

## 2026-09-14官方审计机制与Inspector复核

证据 `OUTFIT-AX-OFFICIAL-20260914-01`，属于 16-01 TEST-01 的诊断记录，没有关闭任何失败项。Apple 的 [Performing accessibility audits for your app](https://developer.apple.com/documentation/accessibility/performing-accessibility-audits-for-your-app) 明确：审计针对当前页面；Clipped Text 检查较大 Dynamic Type 字号下可能发生的裁切，Dynamic Type 则检查文字是否支持系统字号调整，两项不同。正式专项当前检查 contrast/textClipped，不能表述为七项完整审计。当前截图文字完整、边界断言通过，不足以推翻潜在裁切报告；通过自动审计也不能替代 VoiceOver 旅程。

同一官方文档说明每次运行会产生带编号的审计结果，具体 issue 可展开解释并查看 Fix suggestions。本轮按此路径在 Accessibility Inspector 选择独立模拟器的“于是”进程，核对 Options 中七项均勾选，尝试运行；未出现编号结果、issue 或截图，因此判定本次交互未取得有效报告，既不计为零问题通过，也没有取得可用于修改布局的具体建议。未过滤 issue、修改审计选项或更改设备辅助功能设置。

[Apple Developer Forums 的 Accessibility Inspector Bugs](https://developer.apple.com/forums/thread/823968)（2026 年 4 月）中，提问者报告 UIKit 自定义动态字体的视觉表现与 Inspector 报告不一致；Apple 工程师建议提交 Feedback Assistant 复现。该案例的字体和 UI 实现不同，不能确认本项目的 SwiftUI 标准字体属于同一缺陷，也没有可据以关闭本项目门禁的修复版本。本轮未向 Apple 或其他第三方提交报告、源码或用户数据。

可重现环境：当前正式 Debug 构建、iPhone 17 / iOS 26.5，独立空数据设备 `ThenOutfitInspector20260914`（3B810E3F-A73A-44BE-A067-79E8BB63C30A）；启动参数 `-UIPreferredContentSizeCategoryName UICTContentSizeCategoryAccessibilityXXXL -AppleLanguages '(zh-Hans)' -AppleLocale zh_CN`，从今日点击“新建计划”。实际页面树包含“安排”“穿搭日期”及原生日期容器；已查看最大字号页面截图，但没有将其当作审计通过。复查五份正式源码哈希均与 `OutfitPaginationEvidence20260914.json` 一致，本轮没有生产代码或测试代码试改。

后续诊断应取得有效的编号报告与具体 issue 建议，或在可稳定失败的隔离复现中缩减正式页面的真实差异；不再重复已排除的 fixedSize、审计前滚动和简单表单分行试改。日期 textClipped、取消详情 contrast、人工 VoiceOver 与物理检查继续待完成。

收尾：`OutfitInspectorBuild20260914.log` 记录当前 Debug BUILD SUCCEEDED；工具链、iOS 架构与 `git diff --check` 通过。本轮未运行新的 XCTest 或 Release 构建。构建进程已结束，专用模拟器已关闭并删除，原有模拟器与 OOTD 数据未清理；未提交/推送。官方文章的本机诊断副本为 `DerivedData/AppleAccessibilityAudits20260914.md`，长期依据为上面的官方链接，不依赖本机副本。

## 2026-09-14日期审计起始状态与分行对照

证据 `OUTFIT-DATE-AUDIT-CONTROL-20260914-01`。核对发现原通过的隔离日历在审计前向下滚动五次，正式日期专项直接审计。本轮先固定相同无已选项页面与最大字号，对照这两种起始状态；不改正式审计项，不增加失败重试。

隔离工具纠正：首次命令误用未配置 test action 的 scheme，exit 66、未执行测试；随后按实际共享 `Probe` scheme 运行。`OutfitDateAuditStartRun20260914.xcresult` 的直接审计两次通过（9.001 / 8.715 秒），滚动组一次通过（14.194 秒）、一次在审计前失败（13.245 秒）。失败 AX 层级 `OutfitDateAuditStartProof20260914/A7C7F4F8-E3EC-4150-8E79-D7E82D90CD09.txt` 仅剩“打开计划”按钮，确认下拉关闭了隔离弹窗。该失败不能解释正式日期裁切；正式编辑页原本已禁止下拉关闭。

仅给隔离页补齐同样的 `interactiveDismissDisabled(true)` 后，`OutfitDateAuditStartGuarded20260914.xcresult` 为 2 注册全通过，两个测试各固定运行两次：直接审计 9.153 / 8.699 秒，滚动后审计 12.115 / 12.178 秒，exit 0，结果门禁通过。原 contrast/textClipped 均保留。由此不以审计前增加滚动或等待作为正式页面的修复。

再对正式辅助字号日期布局做单变量试改：将标题和图形日历从同一个 VStack 表单行拆为各自的原生行，文字、字体、日期绑定、时区、标准字号和测试保持。`OutfitDateRows20260914.xcresult` 为 1 注册失败，固定两次都报 textClipped（10.757 / 10.258 秒），exit 65，结果门禁 exit 1；横向边界和可点击断言未失败。两份失败元素截图 `OutfitDateRowsProof20260914/63362445-004E-450F-9C18-263F3D4286D7.png` 与 `9904ECA8-889F-4388-B541-D591D9ABC798.png` 字节相同，SHA-256 `b016cc07abd381b9d3d4adf238333f04cb524e8d5c4e210c87cd063f150db8b1`，已查看均为“穿搭日期”所在的独立行。分行改变了布局但没有解决报告，已撤回；不据截图完整判定 SDK 误报。

重跑隔离对照使用 `DerivedData/OutfitDateAuditStart20260914/Probe.xcodeproj` 的 `Probe` scheme；正式对照使用既有 `ThenAppUITests/ThenAppUITests/testOutfitDateAtLargestText`。两者均使用独立结果包、`-parallel-testing-enabled NO -test-iterations 2 -test-repetition-relaunch-enabled YES`。隔离源码、原/试改 View 和正式 UITest 哈希清单为本机 `DerivedData/OutfitDateAuditEvidence20260914.json`，SHA-256 `2ff021c32e78c7e1d7d7c6c8008dc4c42136f9ec618dde3d8cf5cb192eede7bd`；这是本机诊断材料，非 CI 归档。

最终五份正式源码已重新核对，与上一轮 `OutfitPaginationEvidence20260914.json` 的已测试版本一致；本轮没有保留生产改动，也未重跑 Release。排除上述两个修复方向，日期裁切和取消详情对比度仍待定位，TEST-01 继续 in_progress；不以隔离对照抵扣完整产品或人工 VoiceOver/物理验收。

收尾：工具链、架构、两份相关文档 8 个本地链接及 `git diff --check` 通过。所有测试进程已终止，仅关闭并删除本轮 iPhone 17/iOS 26.5 专用设备 `ThenOutfitDateAuditStart20260914`（CD1CBB0C-83C2-4B76-BA04-E91DDFDBCB40），清单确认已不存在；已有设备与 OOTD 数据未清理，未提交/推送，整体目标继续 active。

## 2026-09-14时间线分页重入恢复

证据 `OUTFIT-PAGINATION-REENTRY-20260914-01`，对应 16-01 TEST-01、OUTFIT-ACC-006/010 的本地重读与删除子集。旧 `OutfitPlanViewModel.load()` 读取 `nextPage` 后未消费，加载过下一页后，再次进入页面执行读取仍走追加分支；按 ID 去重又保留旧行，导致已取消计划继续显示 active、已删除计划继续可见。

修复仅在请求开始取得追加意图后重置该意图。明确点击加载更多仍按原游标追加；页面重新读取则替换首屏，继续以真实存储事实为准。数据库、排序、删除、快照与编辑合同不变。

真实 SQLite 回归创建 51 条计划，验证首屏 50 条、第二页 1 条；分别取消或删除首屏计划，再模拟 SwiftUI 页面重新执行读取，逐项对照 Repository 当前页面与游标；取消路径再加载第二页，确认普通分页未退化。两组均检查原衣物保留。没有生产故障开关或预置测试数据。

| 本机结果包 | 实际结果 |
| --- | --- |
| `OutfitPaginationRed20260914.xcresult` | 15 注册、14 通过/1 失败/0 跳过，exit 65；新参数测试两组共 4 条断言失败，分别证实状态未更新和删除行残留；结果门禁 exit 1 |
| `OutfitPaginationGreen20260914.xcresult` | 26 注册、26 通过/0 失败/0 跳过，exit 0；15 状态 + 10 数据库（0.707 秒），正式保存→重启→历史快照→两种删除策略 UI（100.709 秒）；结果门禁 exit 0 |

重跑使用本文件首次保存恢复节的相同三个测试选择项、独立结果包和专用模拟器；新增分页参数用例包含在 `OutfitPlanViewModelTests` 中。本轮设备为 iPhone 17/iOS 26.5 `ThenOutfitPagination20260914`（49D1A461-64A7-4985-BBEB-4111262B1800）。分页重入故障在真实数据库支持的 ViewModel 测试中验证，正式 UI 回归验证已有用户旅程，未在 UI 中批量录入 51 条记录，不混淆两种证据。

本机源码清单 `DerivedData/OutfitPaginationEvidence20260914.json` 覆盖 ViewModel、状态测试、持久化、View 和 UITest 五份文件，SHA-256 为 `4a6e7cfa754aaacbe65eadb7f1b2cb7d5be319844319d5cb9b9efeab89cc520d`；修复前清单为 `OutfitPaginationRedEvidence20260914.json`。不代表全工作树或 CI 归档。TEST-01 仍 in_progress；最大字号日期裁切、取消详情对比度、人工 VoiceOver 和物理门禁未由本次回归关闭。

收尾：`scripts/validate-ios.sh build` exit 0，`OutfitPaginationBuild20260914.log` 记录工具链、架构与 Debug/Release 两次 BUILD SUCCEEDED；五份源码哈希与最终文件一致，三份相关文档 21 个本地链接及 `git diff --check` 通过。全部测试/构建进程已结束，本轮专用模拟器已关闭并删除且从清单消失；既有模拟器及 OOTD 数据未清理。未提交/推送，整体目标保持 active。

## 2026-09-14日期标题布局对照

证据 `OUTFIT-DATE-LABEL-CONTROL-20260914-01`，沿 TEST-01 核对正式页面与通过的隔离日历样例之间的差异。正式日期标题额外使用 `fixedSize(horizontal:false, vertical:true)`；本轮先测原布局，再仅移除此修饰器，各固定执行三次，每次重新启动测试进程。没有使用“失败后重试直到成功”，未过滤任何审计 issue。

| 原始结果 | 注册计数与重复执行结果 |
| --- | --- |
| `OutfitDateLabelControl20260914.xcresult` | 1 注册、0 通过/1 失败/0 跳过；三次重复均 textClipped，分别 13.612 / 10.680 / 10.717 秒，exit 65 |
| `OutfitDateLabelNatural20260914.xcresult` | 1 注册、0 通过/1 失败/0 跳过；三次重复仍 textClipped，分别 12.278 / 10.358 / 10.222 秒，exit 65 |

两包均被 `validate-ios-test-result.py` 拒绝（exit 1）。已查看自然布局三份 Element Screenshot：`BF50EF2E-EBE6-44F8-BA75-A75BD4F7F408.png`、`521E4B16-F116-437A-8A26-73F7F6B00847.png`、`27FC47C2-CEC5-4188-BD81-3FF3AF391402.png`，均位于本机 `OutfitDateLabelNaturalProof20260914`。三文件 SHA 相同（`9686a9b18e68ad199390c9948b3a6f5e721779a1b6cd2c72741dd1a160ce9573`），且与原日期报告的 `BA715820-7163-4A45-B98C-039326D311D8.png` 字节完全相同，均显示“穿搭日期”。单独移除 fixedSize 不改变失败或该元素截图，因此撤回该试改；不把截图完整推断成 SDK 误报，也不宣布原裁切已关闭。

重跑使用原日期专项测试与不存在的结果路径，在原正式 xcodebuild 参数上增加 `-test-iterations 3 -test-repetition-relaunch-enabled YES`；不增加 `-retry-tests-on-failure`。对照 View 源码分别保存为本机 `DerivedData/OutfitDateLabelControl20260914.swift` 与 `OutfitDateLabelNatural20260914.swift`，与未修改 UITest 的哈希清单为 `OutfitDateLabelEvidence20260914.json`，清单 SHA `05308dfb6d87d30bffc205294e7668c86f07a4f2e7d9ffa77fe1558a84250765`。这是本机诊断证据，不是 CI 归档。

最终产品代码与上一轮 `OutfitCreateRecoveryEvidence20260914.json` 的五个已测试源码哈希全部相同；本轮只有对照 Debug 测试，没有新增生产修复或重跑 Release。下一步不继续以标题固定高度为根因修改产品；日期标题审计和取消详情对比度仍为独立未关闭项，原日期横向越界修复及保存失败恢复保持。TEST-01 仍 in_progress，整体目标未完成。

收尾检查：工具链、架构、两份修订文档 6 个本地文件链接及 `git diff --check` 通过。全部测试进程已结束，仅关闭并删除本轮 `ThenOutfitDateLabel20260914`（E5316E97-7AB1-4932-9F7E-0D936DAA11BA，iPhone 17/iOS 26.5），设备清单确认不存在；既有设备和 OOTD 数据未清理。未提交/推送。

## 2026-09-14首次保存结果不明恢复

证据 `OUTFIT-CREATE-RECOVERY-20260914-01`，对应 16-01 TEST-01、OUTFIT-ACC-005/006/010 的保存失败/幂等与版本复核子集。首次创建在 SQLite 中提交后，响应丢失使 Editor 的 plan 仍为 nil；用户改草稿后再次保存使用 expectedRevision=nil，被已存在的同 ID 计划拒绝。原刷新只读衣橱，无法取回已提交计划，形成反复冲突。

修复：首次保存已尝试但本地会话尚未收到计划时，显式刷新读取会话固定 planID。仅找到初始 revision 1 时接回已提交计划，保留用户当前草稿，下一次明确保存使用新的更新命令；若已有后续版本，则保留草稿并提示重新打开核对。未找到计划时不抹去新建草稿，后续保存仍经过删除标记和事务版本校验；刷新只读，不为了确认结果重放写入。

真实 SQLite 验证覆盖：

1. 写入前返回存储错误，与提交后才丢失响应分别测试；每种再区分用户是否修改草稿。
2. 已提交且修改草稿的情况先复现再次保存冲突，再刷新、保存；最终只有同一 planID 的一条计划，修改后的摘要保留，没有重复创建。
3. 保存成功后另一次操作修改摘要或取消计划：刷新不能静默采纳 revision 2，后续保存被阻止，数据库并发结果保持不变。
4. 保存成功后另一操作删除计划：刷新和重试不复活计划，收到删除事实后清除内存内容；原衣物保留。
5. 既有删除响应丢失、删除明确冲突后复核及清理待重试测试继续运行。

Red：`OutfitCreateRecoveryRed20260914.xcresult` 13 注册、11 通过/2 失败/0 跳过，exit 65；新增的恢复与并发保护两个参数测试失败。`OutfitCreateRecoveryRedEvidence20260914.json` 留存修复前 ViewModel 与测试哈希。Green：`OutfitCreateRecoveryGreen20260914.xcresult` 共 25 注册、25 通过、0 失败/跳过，exit 0；14 项编辑状态 + 10 项数据库测试（2 suites，1.017 秒），正式保存→重启→历史快照→两种删除策略 UI（100.728 秒）。新增删除后不复活测试作为修复后的防回归检查；不把参数展开数重复累计为注册测试数。结果检查脚本确认非空全通过。

重跑命令：

```sh
xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug \
  -destination 'platform=iOS Simulator,id=<专用UDID>' \
  -skipPackagePluginValidation -disableAutomaticPackageResolution -parallel-testing-enabled NO \
  -only-testing:ThenAppTests/OutfitPlanViewModelTests \
  -only-testing:ThenAppTests/OutfitPlanRepositoryTests \
  -only-testing:ThenAppUITests/ThenAppUITests/testOutfitPlanSaveRestartSnapshotAndDeletion \
  -resultBundlePath DerivedData/<新名称>.xcresult test
python3 scripts/validate-ios-test-result.py DerivedData/<新名称>.xcresult
```

环境为固定 Xcode 26.6 / Swift 6.3.3、iPhone 17 / iOS 26.5 专用模拟器 `ThenOutfitCreateRecovery20260914`。5 份 ViewModel/状态测试/持久化/View/UITest 的本机源码快照为 `DerivedData/OutfitCreateRecoveryEvidence20260914.json`，SHA-256 `16eec2897ba454215b867ad26c62d24495e599783ebf8d8374e7a3a22525a371`。这只是所列文件与本次验证的对应记录，不是全工作树或 CI 归档。

失败控制位于测试用 Repository 适配器，写入由实际 GRDB/SQLite 完成；未增加生产故障开关，没有模拟 UI 的存储响应丢失。正式 UI 只验证常规组合旅程，不能替代故障注入状态测试，也不能关闭原最大字号无障碍失败。

收尾：`scripts/validate-ios.sh build` exit 0，`OutfitCreateRecoveryBuild20260914.log` 记录工具链、架构及 Debug/Release 两次 BUILD SUCCEEDED；5 个源码哈希与最终文件一致，三份相关文档 20 个本地文件链接及 `git diff --check` 通过。全部测试/构建进程终止，仅关闭并删除本轮 C1D91F9A-3047-48A2-A42D-1CA382450E86 专用模拟器，核对设备清单已不存在；既有设备及 OOTD 数据未清理。没有数据库/接口/页面结构变更，未提交/推送。TEST-01 仍 in_progress，原无障碍与完整发布门禁继续待验，整体目标保持 active。

## 2026-09-14辅助字号日期控件越界

证据 `OUTFIT-DATE-BOUNDS-20260914-01`，归 16-01 TEST-01 / OUTFIT-ACC-009。检查已选区 textClipped 时，在相同控件/最大字号的隔离 Form 中发现另一可视缺陷：紧凑 DatePicker 的中文日期及标签横向越界。正式 App 新增 `testOutfitDateAtLargestText` 复现控件 minX −3.33、maxX 405.33，超过 402pt 屏幕；标签 minX 同样 −3.33。不能把这项实际裁切归为审计误报。

隔离对照（无业务 Repository/照片/云调用）：

- `OutfitSelectedInsertion20260914.xcresult`：初始已有选择通过（11.479 秒），点击后插入组失败（19.317 秒，一条无 element 的 textClipped）；失败附件显示的是日期行，不能据此确认原已选名称报告的根因。
- `OutfitStableSection20260914.xcresult`：将条件移入稳定 Section 后仍为一通过/一失败（11.274 / 17.380 秒），不采用该试改。
- `OutfitWheelDate20260914.xcresult`：初始组通过，动态组因为页面中央的垂直拖动被日期滚轮接收，找不到下方选择按钮而失败；不把它记为布局修复通过，不采用滚轮。
- `OutfitCalendarDate20260914.xcresult`：同一测试改为完整标签与原生图形日历，两组通过（17.872 / 11.333 秒），原 contrast/textClipped 审计未过滤。

据此仅在产品辅助字号下使用原生日历，标准字号仍为紧凑日期控件；相同 Date/Gregorian calendar/原始时区绑定不变，不改保存范围或 schema。新增日期边界回归、辅助字号未来日期完整旅程，原标准/最大字号旅程保留。正式结果为部分修复，不能用隔离壳通过替代产品验收。

| 正式测试 | 实际结果 |
| --- | --- |
| `OutfitDateBoundsRed20260914.xcresult` | 1 注册、0 通过/1 失败/0 跳过，3 条边界断言失败，11.244 秒，exit 65 |
| `OutfitCalendarProduction20260914.xcresult` | 4 注册、1 通过/3 失败/0 跳过，exit 65。标准未来日期→选衣→保存→编辑→取消→删除及原审计通过（59.132 秒）；最大字号今日旅程失败于取消后场景 contrast（98.829 秒）；新增最大字号未来旅程已通过 2026-10-01 保存/日期浏览的功能断言，失败于取消后日期和场景两条 contrast（103.335 秒） |
| 同一正式包的日期专项 | 日期控件与标签边界、可操作性断言均通过，原 −3.33/405.33 越界未再出现；保留的整页审计报“穿搭日期” textClipped，因此该测试仍为失败（11.344 秒）。不把边界通过写成整项通过 |

已实际查看日期截图 `OutfitCalendarProductionProof20260914/ECD91AE8-3AA3-44B0-9577-F5D5961EABE2.png`：完整标签、月历和日期位于表单内；裁切报告元素附件 `BA715820-7163-4A45-B98C-039326D311D8.png` 的文字也完整可见。这是视觉观察，不足以关闭原始审计；本轮没有删除、过滤或降级任何审计断言。原已选名称/计数的两条 textClipped 在本轮两条最大字号完整旅程中未复现，不宣称永久关闭所有裁切问题。

正式重跑命令使用专用模拟器与新结果路径：

```sh
xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug \
  -destination 'platform=iOS Simulator,id=<专用UDID>' \
  -skipPackagePluginValidation -disableAutomaticPackageResolution -parallel-testing-enabled NO \
  -only-testing:ThenAppUITests/ThenAppUITests/testOutfitDateAtLargestText \
  -only-testing:ThenAppUITests/ThenAppUITests/testOutfitFutureDateEditCancelAndAccessibility \
  -only-testing:ThenAppUITests/ThenAppUITests/testOutfitAtLargestText \
  -only-testing:ThenAppUITests/ThenAppUITests/testOutfitFutureDateAtLargestText \
  -resultBundlePath DerivedData/<新名称>.xcresult test
python3 scripts/validate-ios-test-result.py DerivedData/<新名称>.xcresult
```

结果检查脚本如实拒绝正式 1/4 通过包（exit 1），不是空跑或跳过。`scripts/validate-ios.sh build` 返回 0，日志 `OutfitCalendarBuild20260914.log` 覆盖固定工具链、架构及 Debug/Release 两次 BUILD SUCCEEDED；本轮未修改领域/数据库/网络/文案，没有重复运行不受影响的领域测试。14 份 View/UITest 与隔离源码/工程的本机快照为 `DerivedData/OutfitCalendarEvidence20260914.json`，SHA-256 `2943ec30375b3cd0312effe0d9114b0a9d76862d451900763b9e63d98af2ed62`，只证明这些文件，不代表全工作树或 CI 归档。

当前关闭的是日期控件的实际横向越界；日期标题原始裁切报告、取消后对比度、完整人工 VoiceOver 与物理发布仍待验，TEST-01/REL-01 不改为 completed。

收尾：14 份快照与当前文件一致，三份相关文档 20 个本地文件链接及 `git diff --check` 通过。全部测试/构建进程已终止；仅删除本轮专用 `ThenOutfitInsertion20260914`（CA5F89E6-03DB-486A-AB19-FF249AD1CC1C，iPhone 17/iOS 26.5），关闭时已为 Shutdown，删除后核对设备清单确认不存在；既有模拟器及 OOTD 数据未清理。未提交/推送，整体目标保持 active。

## 2026-09-14删除冲突恢复验收

证据 ID：`OUTFIT-DELETE-CONFLICT-20260914-01`，对应 16-01 TEST-01、OUTFIT-ACC-005/006/010 的版本复核与删除子集。原实现第一次删除即缓存 `deletionRevision`，即使事务明确拒绝旧版本，刷新后仍复用旧 revision，导致重新确认也不能删除。

修复仅在 Editor 收到删除的 `OutfitPlanError.conflict` 时清除旧 revision 与 mutation 类型；刷新展示最新内容后，下次明确删除使用新版本和新幂等键。存储结果不明、取消异步调用或已提交但清理待重试时保持原意图。数据库、OpenAPI、界面布局及文案不变，没有迁移或兼容分支。

真实 SQLite 回归步骤：

1. 保存 revision 1 计划并打开详情；另一操作分别修改摘要或取消计划，使数据库成为 revision 2。
2. 旧详情删除被拒绝，内容和衣橱仍在；未经刷新再次删除仍被拒绝，不能越过版本复核。
3. 刷新取得 revision 2，核对最新状态后再次明确删除；计划及快照清空，原衣物保留，新的删除使用新的 mutation ID。
4. 另测删除已经提交但响应丢失：第一次返回错误，重试复用原 mutation ID，确认已删除，不恢复旧内容。
5. 原“删除已提交但清理待重试”测试继续验证只保留不透明清理身份、内容立即清空、重试仍用原 mutation ID。

| 证据 | 实际结果 |
| --- | --- |
| `OutfitDeletionConflictRed20260914.xcresult` | 修复前 10 项编辑状态测试：9 通过、1 失败；新增编辑/取消两个参数场景均在刷新后删除失败，共 4 issue，exit 65 |
| `OutfitDeletionConflictGreen20260914.xcresult` | 修复后 11 项编辑状态 + 10 项真实数据库测试通过（2 suites，0.638 秒）；保存→重启→历史快照→两种历史删除策略的正式 UI 旅程通过（100.150 秒）。结果包共 22 注册、22 通过、0 失败/跳过，exit 0；新结果检查脚本确认非空全通过 |

并发冲突与响应丢失在实际 Repository/SQLite 上由测试控制，未注入生产故障开关；UI 回归验证常规业务旅程，不宣称 UI 已自动制造并发冲突。环境是固定 Xcode 26.6 / Swift 6.3.3、专用 iPhone 17 / iOS 26.5 模拟器 `ThenOutfitDeletion20260914`。所有测试数据均临时生成，无用户照片或云调用。

执行命令：

```sh
xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug \
  -destination 'platform=iOS Simulator,id=<专用UDID>' -parallel-testing-enabled NO \
  -skipPackagePluginValidation -disableAutomaticPackageResolution \
  -only-testing:ThenAppTests/OutfitPlanViewModelTests \
  -only-testing:ThenAppTests/OutfitPlanRepositoryTests \
  -only-testing:ThenAppUITests/ThenAppUITests/testOutfitPlanSaveRestartSnapshotAndDeletion \
  -resultBundlePath DerivedData/<新名称>.xcresult test
python3 scripts/validate-ios-test-result.py DerivedData/<新名称>.xcresult
```

相关四份源码的 SHA 清单为本机 `DerivedData/OutfitDeletionConflictEvidence20260914.json`，清单 SHA-256 `baf67fc5c832e87092cab40e91b22c2ab02b204238d996082125bd1f327f695b`；其中 Editor 源码 SHA-256 `67b230c7305402d14e5b3616a876051b5a6f51777490be44df9f4ae8af90d50d`。最终四份源码哈希仍匹配，`OutfitDeletionConflictBuild20260914.log` 的 Debug/Release 均 BUILD SUCCEEDED，工具链、架构和 diff 空白检查通过。测试/构建已终止；清理时专用设备已处于 Shutdown，随后仅删除本次 `9030DB2A-ADB1-4ED7-A548-2CFEC59FDB3C`，保留原设备及 OOTD 数据，未提交/推送。这是本地检查点，不是远程 CI 或全项目发布；本次未重跑最大字号、照片安全矩阵或物理保护，原未通过项保持不变。

## 2026-09-14滚动状态与自动审计隔离复核

证据 ID：`OUTFIT-AX-SCROLL-20260914-02`；对应 16-01 TEST-01。本轮未改动产品配色、布局、字体、数据库或原审计断言，也未将失败过滤为通过。

| 结果包 | 操作与实际结果 |
| --- | --- |
| `OutfitContrastCards20260914.xcresult` | 无数据库/业务 ViewModel/照片的 SwiftUI 隔离壳，复刻详情文字、最大辅助字号、三张卡片和原生取消确认。圆角/直角两种卡片分别检查 active/cancelled，2 测试通过，33.148 秒，exit 0 |
| `OutfitContrastFresh20260914.xcresult` | 全新专用模拟器运行当前正式工程 `testOutfitAtLargestText`，完成选衣、保存、编辑、取消及删除；1 测试失败，67.341 秒，exit 65。原审计共 3 issue：已选名称和计数标题 textClipped、取消后场景文字 contrast；不能只保留功能步骤通过 |
| `OutfitContrastScrolled20260914.xcresult` | 同一隔离壳、相同文字和圆角卡片，仅改变取消后启动审计的滚动位置。顶部组通过（15.523 秒）；滚动后组失败，报告 `修改未来安排2BFB61` contrast。共 2 测试、1 通过/1 失败，27.629 秒，exit 65 |

上述结果包均用 `scripts/validate-ios-test-result.py` 复核注册计数，分别退出 0/1/1，没有空跑或跳过。环境为固定 Xcode 26.6 / Swift 6.3.3、iPhone 17 / iOS 26.5 专用设备 `ThenOutfitContrast20260914`；不使用既有 OOTD 数据或人物照片。

已实际查看正式工程失败附件 `OutfitContrastFreshProof20260914/9F67E54B-CF72-41D6-BDB7-F0AD9AB6F5D8.png` 和 `9E99AD6E-874B-4EF1-8185-C8E7B3D53987.png`：已选名称、标题在截图中完整。隔离失败元素 `OutfitContrastScrolledProof20260914/2CBC97AF-91BF-4FC1-A213-CA56FC390AF1.png` 与顶部通过截图 `3D564277-8E8B-48AF-9115-F945CE24D972.png` 显示相同黑字白底场景文字。视觉观察不代替自动裁切结论或完整无障碍验收。

新的诊断结论：滚动状态可在不含业务状态机和媒体的最小页面触发相同 contrast 报告，卡片形状和时区文案本身不足以解释失败。下一步检查审计滚动采样与 Form 内容尺寸更新，保留该最小复现作对照；不继续无依据地调整产品颜色/字号，也不把顶部通过当成整页通过。[Apple 官方审计实践](https://developer.apple.com/videos/play/wwdc2023/10035/)明确讨论了逐项调查误报；它不证明本项目全部失败可忽略。已选区 textClipped、人工 VoiceOver 与物理验收继续独立待验，ACC-009/TEST-01 未关闭。

隔离源码/工程位于本机忽略目录 `DerivedData/OutfitContrastCards20260914` 与 `DerivedData/OutfitContrastScrolled20260914`，不属于 ThenApp target；源码、工程、文案及正式 View/UITest 的 SHA 清单为 `DerivedData/OutfitContrastEvidence20260914.json`，清单 SHA-256 `23af0c2cf813b21b5d9dbdc64fc6a4dd5005de1cb707f8e38e9dce03bad05cb8`。这是本机诊断证据，不是 CI 归档。重跑使用新建专用模拟器和不存在的结果路径：

```sh
xcodebuild -project DerivedData/OutfitContrastScrolled20260914/Probe.xcodeproj \
  -scheme Probe -configuration Debug -destination 'platform=iOS Simulator,id=<专用UDID>' \
  -parallel-testing-enabled NO -resultBundlePath DerivedData/<新名称>.xcresult test
python3 scripts/validate-ios-test-result.py DerivedData/<新名称>.xcresult
```

正式工程复跑同样指定专用 UDID，使用 `-project app/ThenApp.xcodeproj -scheme ThenApp -skipPackagePluginValidation -disableAutomaticPackageResolution -only-testing:ThenAppUITests/ThenAppUITests/testOutfitAtLargestText`，其余参数同上。本轮只构建/运行了相应 Debug 测试，未重跑 Release、照片旅程或整个领域矩阵；原证据的范围不扩大。全部测试进程已终止，专用设备 `6BD4F817-7A74-4AC2-82A1-45DDA81D0681` 已关闭并删除，原 `ThenOutfitPlan20260913` 保持不动。工具链、架构及 diff 空白检查通过，未提交/推送。

## 2026-09-14 Woo 分类选衣检查点

顶部卡片收尾：`scripts/validate-ios.sh build` exit 0，工具链/架构及 Debug/Release 通过，日志 `OutfitDarkTrayBuild20260914.log`；`git diff --check` 通过。本次只修改 View 和文档，未改数据/接口/媒体/依赖。测试使用自建 `ThenOutfitTray20260914`（3C0291EB-A386-435E-BF1B-D49318B8F547，iPhone 17/iOS 26.5），全部进程结束后已关闭并删除，仅使用合成文字衣物，其他设备/数据未重置。未提交/推送，整体目标保持 active。

最新顶部卡片复验：`OutfitDarkTray20260914.xcresult` exit 0，标准分类/移除（104.796 秒）、最大字号分类/移除（71.748 秒）、标准未来日期/编辑/取消/删除（59.245 秒）三条 UI 全部通过，0 失败/跳过。标准完整旅程的选择、详情与取消后三处原 contrast/textClipped 审计通过。未重跑最大字号完整详情，原最大字号 contrast 失败仍保留，不能据局部通过关闭 ACC-009。

已选区由全宽灰色材质改为参考中的有边距黑底圆角卡片，白色文字/移除标记，系统保存仍是实际计划操作。实际查看 `OutfitDarkTrayProof20260914/39F6D208-B7C0-427C-8C53-018D3777795A.png`（标准）和 `3729AF0E-155C-4830-A03A-CB3FF8BCEA3B.png`（最大字号页内选择）。辅助功能树 `09F7532C-C118-4BD3-8996-C49268809E7D.txt` 中 tray 为完整 370×136.7pt 容器，计数和逐件移除按钮仍独立；此前 identifier 首项只覆盖计数文字，误低估固定区范围，现已修正。源码快照 `OutfitDarkTrayEvidence20260914.json` 只覆盖该 View，不代表全工作树验证。

收尾：`scripts/validate-ios.sh build` exit 0，Xcode 26.6 / Swift 6.3.3 / Go 1.26.5 版本检查、iOS 架构与 Debug/Release 构建通过；`git diff --check` 通过。构建日志 `OutfitSelectionBuild20260914.log` 保留既有 ThenTransport YAML 警告。5 份相关源码快照 `OutfitSelectionEvidence20260914.json` SHA-256 为 `99626513d425b17b2b3cf91d46f378acb90b8240d912d41ea7d9d47fe4f1a1cd`，非全工作树验证。测试和构建均已结束，本轮 5869A3D5 专用设备已关闭并删除，其他模拟器及 OOTD 数据未重置；未提交或推送。

最终结果 `OutfitSelectionFinal20260914.xcresult`：标准分类/顶部移除 101.394 秒、最大字号分类/页内移除 72.017 秒、含图/后台/照片移除 86.000 秒，3 条 UI 通过。最大字号原完整旅程仍有 2 条 contrast 失败（选择页无 element、取消详情原始时区），无 textClipped；4 UI 整体 exit 65，0 skip。没有忽略任何 issue，也不据此关闭既有标准字号 contrast 失败。

实际查看最终附件：`CD966B66-4AD7-414B-A5CF-1658296C6C35.png` 为顶部两件已选与下装筛选；`19B4918F-FCAE-4927-87D6-B59AFCB1F377.png` 为最大字号页内两件、完整占位及移除控件；`88208E93-769D-426C-A8AB-43117C36293A.png` 为带合成衬衫的两件计划。均位于本机 `DerivedData/OutfitSelectionFinalProof20260914`，不依赖短期下载链接。布局修复不等于完整视觉或无障碍通过。

沿用 16-01 已批准计划合同，按 Design 03 新核实的参考视频 33.4 / 35.48 秒实现分类网格与顶部已选区。参考的黑色已选区位于顶部，包含计数、缩略图和移除操作；当前原生版本采用系统材质，以真实“保存计划”完成业务。完整 Woo 页面、AI 结果和真实三维仍未完成。

复验步骤：

1. 在专用模拟器通过生产页面建立 4 件上装与 1 件下装，无图也必须可选；标准字号查看四列网格。
2. 选择上装，筛选下装再选择一件；顶部已选区保留两件名称、缩略图或占位、计数和逐件移除。
3. 从已选区移除被筛选隐藏的上装；回到全部类别，上装恢复未选择，下装仍选择。保存后终止并重启 App，计划只包含下装；删除计划不删除衣橱衣物。
4. 最大辅助字号重复以上过程（1 件上装 + 1 件下装），应为单列和页内已选区；文字完整、移除控件可操作，不缩小字号来适配。
5. 使用既有 synthetic-flat-shirt 执行含图多选、后台隐藏、移除照片和历史显示旅程；照片删除后不得显示旧图。

状态测试另核对跨类别选择顺序和 20 件上限：第 21 件拒绝，移除一件后允许加入，保存的是真实有序 UUID。无第二套选择状态、数据库表、网络接口或媒体副本。

初轮 `OutfitSelectionGrid20260914.xcresult` exit 0：9 项编辑状态测试通过（0.311 秒），标准/最大字号两条新 UI 旅程分别 103.518 / 72.937 秒通过。实际检查截图后发现无图占位符随字号放大被小框裁切，已改为在缩略图范围内等比缩放的 SF Symbol；用户文字继续随 Dynamic Type 变化。

后续 `OutfitSelectionRegression20260914.xcresult` exit 65：最大字号分类旅程和含图旅程通过（73.130 / 86.301 秒），占位图完整；原审计仍有 4 条失败（标准取消工具栏/无 element 的 contrast，最大已选标题 textClipped、详情时区 contrast）。标题截图完整也不抵扣失败。

编辑整体改为自然卡片的试验 `OutfitSelectionNaturalCards20260914.xcresult` exit 65：最大字号编辑容器实际宽 488.7pt，超过 402pt 屏幕；日期控件撑宽并使内容越界，测试准备视口期间已选被误移除，保存被领域规则拒绝。已撤回这一试改，恢复 Form 编辑，仅将大字号已选标题和移除控件放入同一行内的自然高度 VStack；只读详情继续自然卡片。试验失败保留，不按通过计数。

本次使用独立 iPhone 17 / iOS 26.5 模拟器 ThenOutfitPlanSelection20260914（5869A3D5-4529-42D3-9736-D8B94EB08FFE），只导入既有合成衬衫，不访问用户照片。测试和截图在本机忽略的 DerivedData 中，不是 CI 归档或真机发布证据。最终复验结果另行记录；ACC-009、TEST-01 不因交互测试通过自动关闭。

## 状态与结论

2026-09-14 新增 [16-02 实际穿着与穿后状态](../plan/16-02-实际穿着与穿后状态执行计划.md) 的待确认验收子集，尚未实现或运行。需求覆盖继续使用原稳定 ID；以下步骤和执行计划 WEAR-T01～11 对照，不另设第二份 checklist：

- ACC-001/002：同一计划保存及查看后次数仍为 0；明确实际确认后为 1；换件保留原计划，无计划/部分记录有明确标签；未来实际拒绝。“最后没穿”不产生事件，撤销后恢复待确认；已有实际时不能同时记未穿。
- ACC-004：所有穿后状态默认保持，只勾选一件待洗；注入第二个写入失败，事件/快照/计划结果/状态/收据均保持原状。冲突复核后成功，重启读到一致结果。
- ACC-005/006：纠正实际组成，计划不变；同日集合相似度 0.8 触发确认，允许明确第二次，幂等重试不重复；删除一条后次数从 2 变 1。纠正/删除不自动还原当前衣物状态，界面明确说明。
- ACC-010：同时存在计划和实际快照时，两种衣物删除策略覆盖两者；确认后新出现关联时拒绝旧影响确认。只删计划保留实际并清除来源；不含目标衣物的独立实际记录不能因来源计划被删而连带删除。图片替换/删除不倒灌新图。
- ACC-006/008/009：v4 含照片/计划/不透明删除标记升级保持原数据；混合时间线分页、跨时区日期、事务前后中断恢复、离线无新增权限；标准/最大字号完整旅程、深色/减少动态效果与后台遮罩。人工 VoiceOver、物理保护和现有对比度失败仍需独立关闭。

这些是可执行期望，不是通过证据。结构化反馈、月历、收藏与历史复用的完整验收不由 16-02 抵扣。

- 文档状态：`draft`
- 验收结论：`pending`
- 更新日期：2026-09-14
- 发布层级：本地核心 GA 必选

本文独立验证穿搭计划、实际穿着、穿后反馈和历史复用的用户结果。单功能通过不等于 OOTD 系统发布通过。

## 关联文档

- [`../prd/16-穿搭记录与反馈需求.md`](../prd/16-穿搭记录与反馈需求.md)
- [`../design/09-穿搭记录与反馈设计.md`](../design/09-穿搭记录与反馈设计.md)
- [`../plan/10-OOTD产品实施计划.md`](../plan/10-OOTD产品实施计划.md)
- [`10-OOTD产品系统验收.md`](10-OOTD产品系统验收.md)

当前 [16-01](../plan/16-01-穿搭计划与时间线执行计划.md) 已于 2026-09-13 获用户确认，进入实现；验收结果仍以实际证据判断。切片进入近期排期时，必须在同编号执行计划中映射本文场景并产出可复核证据。

## 需求覆盖

| 需求 | 验收场景 |
| --- | --- |
| `OUTFIT-REQ-001` | `OUTFIT-ACC-001` |
| `OUTFIT-REQ-002` | `OUTFIT-ACC-002` |
| `OUTFIT-REQ-003` | `OUTFIT-ACC-003` |
| `OUTFIT-REQ-004` | `OUTFIT-ACC-004` |
| `OUTFIT-REQ-005` | `OUTFIT-ACC-005` |
| `OUTFIT-REQ-006` | `OUTFIT-ACC-001`～`OUTFIT-ACC-006` |
| `OUTFIT-REQ-007` | `OUTFIT-ACC-007` |
| `OUTFIT-REQ-008` | `OUTFIT-ACC-007`～`OUTFIT-ACC-009` |

PRD 16 已为业务规则分配 `OUTFIT-RULE-001`～`OUTFIT-RULE-023` 并映射到上述需求簇。本文按需求簇判定功能验收；后续执行计划仍必须同时声明覆盖的规则 ID，规则映射变更时重新复核而不得复用已有 ID。

## 验收环境

- 最低 iOS 26 与主流尺寸模拟器；按用户决定先模拟器开发，物理保护/性能在后续真机验收，不能用模拟器抵扣。
- 具有无图、含图、归档、待洗、借出和已删除衣物的合成数据夹具。
- 覆盖离线、跨日/跨时区、低存储、强制退出和数据库写入失败。
- 最大辅助字号、VoiceOver、深色模式和 Reduce Motion。

## 验收场景

### OUTFIT-ACC-001 计划不等于实际穿着

- 覆盖需求：`OUTFIT-REQ-001`、`OUTFIT-REQ-006`；设计测试 `LOG-01`。
- 前置条件：已有一套可保存的结构化穿搭。
- 操作步骤：分别保存组合草稿、将其计划到未来日期，查看穿着次数和历史。
- 期望结果：只有计划记录，不产生 `WearEvent`、不增加穿着次数、不形成长期偏好。
- 边界情况：修改、取消或过期计划不得被转换为实际穿着。
- 验收证据：领域状态机测试、GRDB 事务查询和 UI 录屏。
- 结果：`pending`

### OUTFIT-ACC-002 实际结果和部分事实

- 覆盖需求：`OUTFIT-REQ-002`；设计测试 `LOG-02`～`LOG-05`。
- 前置条件：存在到期计划和可用替换衣物。
- 操作步骤：分别确认“按计划穿了”、“换了几件”、“穿了别套”、“未穿”、“无计划但穿了”和仅记录部分衣物。
- 期望结果：计划快照不被改写；实际穿着精确保存已确认事实；“未穿”不惩罚衣物。
- 边界情况：未确认的类别保持未知，不自动补齐成完整套装。
- 验收证据：用例测试、快照对比和 XCUITest。
- 结果：`pending`

### OUTFIT-ACC-003 结构化反馈可跳过、纠正和撤回

- 覆盖需求：`OUTFIT-REQ-003`；设计测试 `LOG-04`、`LOG-11`。
- 前置条件：已确认一次实际穿着。
- 操作步骤：跳过反馈，再填写冷热、舒适、场合和再穿意愿，然后纠正并撤回该证据。
- 期望结果：空值不等于负面；最新用户明确值生效；撤回后个性化特征可重建且不残留已撤回信号。
- 边界情况：快速连续修改、重复提交和杀进程不产生重复证据。
- 验收证据：特征重建测试、幂等测试和推荐解释审计。
- 结果：`pending`

### OUTFIT-ACC-004 穿后衣物状态原子提交

- 覆盖需求：`OUTFIT-REQ-004`；设计测试 `LOG-08`。
- 前置条件：实际穿着包含多件衣物。
- 操作步骤：逐件选择保持可用或标为待洗，分别注入成功和写入失败。
- 期望结果：成功时实际记录和衣物状态一次提交；失败时整体回滚并允许重试。
- 边界情况：衣物已被其他操作归档时提示冲突，不静默覆盖。
- 验收证据：GRDB 事务测试、版本冲突测试和恢复录屏。
- 结果：`pending`

### OUTFIT-ACC-005 历史快照与再次穿着

- 覆盖需求：`OUTFIT-REQ-005`；设计测试 `LOG-06`、`LOG-07`。
- 前置条件：历史穿搭中一件衣物已改名或不可用。
- 操作步骤：查看时间线/月历和详情，选择“再次穿这套”。
- 期望结果：历史保留当时结构化快照；图片已删除时无图降级；复用前重新检查当前衣物状态并说明冲突。
- 边界情况：已删除衣物按获批的占位/重建策略表示，不虚构当前可用性。
- 验收证据：快照迁移测试、当前/历史对比和 UI 录屏。
- 结果：`pending`

### OUTFIT-ACC-006 离线、中断、幂等与时区

- 覆盖需求：`OUTFIT-REQ-006`；设计测试 `LOG-12`～`LOG-14`。
- 前置条件：设备离线，时区和日界线夹具已准备。
- 操作步骤：创建计划和实际记录，在保存时杀进程/注入低存储，重启并重复确认，再切换时区。
- 期望结果：无网可完成核心流程；事务全成功或全回滚；重复操作不增加穿着次数；展示日期与原始时区语义一致。
- 边界情况：草稿无法恢复时明确说明已丢弃的未提交内容，不谎称保存成功。
- 验收证据：故障注入报告、时区属性测试和重启录屏。
- 结果：`pending`

### OUTFIT-ACC-007 视觉生成不是穿着事实

- 覆盖需求：`OUTFIT-REQ-007`、`OUTFIT-REQ-008`；设计测试 `LOG-09`、`LOG-11`。
- 前置条件：穿搭关联静态或动态 AI 结果。
- 操作步骤：查看、保留并删除生成结果，然后检查实际穿着和推荐偏好。
- 期望结果：浏览、拖动、保留和删除媒体均不创建 `WearEvent`，不自动成为长期偏好；删除人物图后历史使用模板或结构化拼图。
- 边界情况：迟到的生成结果不能回写已删除或已撤回的穿着证据。
- 验收证据：数据查询、偏好特征审计和删除链测试。
- 结果：`pending`

### OUTFIT-ACC-008 不打扰和最小生活上下文

- 覆盖需求：`OUTFIT-REQ-008`；设计测试 `LOG-15`。
- 前置条件：未授予通知、日历或位置权限。
- 操作步骤：使用应用内到期提示、跳过反馈，并检查分析/同步网络流量。
- 期望结果：核心记录不请求新权限；跳过不重复打扰；原始日程、精确位置和穿着正文不因核心功能离机。
- 边界情况：未来若开启推送、分析或同步，本场景保持阻断，直到目的分离和独立验收通过。
- 验收证据：权限录屏、网络抓包和通知计数检查。
- 结果：`pending`

### OUTFIT-ACC-009 可访问性与语义一致

- 覆盖需求：`OUTFIT-REQ-005`、`OUTFIT-REQ-008`；设计测试 `LOG-16`、`LOG-17`。
- 前置条件：最大辅助字号、VoiceOver、深色模式和 Reduce Motion 分别开启。
- 操作步骤：完成计划、实际确认、反馈、状态更新和历史复用。
- 期望结果：信息不裁切；关键控件有可理解标签和足够点击区；计划、实际和反馈的语义不只依赖颜色。
- 边界情况：无图、部分记录和错误状态与完整记录一样可操作。
- 验收证据：Accessibility Inspector 报告、真机录屏和 UI 测试。
- 结果：`pending`

## 证据与判定

最新检查点 `OUTFIT-20260914-PRIVACY`：`OutfitPrivacyRegression20260914.xcresult` exit 0，67 项领域/数据/VM 与 1 条完整删除旅程通过；该旅程执行两种删除策略，100.104 秒。新增参数化测试覆盖旧详情/草稿在 load/refresh/save/cancel 获知 notFound 后清除内容，清理待重试仍可恢复且不回填数据，重新加载不会提升旧草稿 revision。`OutfitPrivacyBuild20260914.log` Debug/Release 通过；工作树源码及基线指纹归 `OutfitPrivacyEvidence20260914.json`，未提交/推送。

ACC-009 继续 pending：标题改为卡片内原生标题行并保留 header trait；标准字号曾通过，但 `OutfitHeadingFinal20260914.xcresult` 复跑再次报告三处选择页 Contrast failed 和首次详情 Contrast nearly passed，67 项领域/数据/VM 仍通过。对失败附件进行像素复核后，将实际蓝字白底的“编辑计划”改为语义 primary 前景；`OutfitSemanticEdit20260914.xcresult` 的标准字号完整旅程及选择、首次详情、取消后详情三处审计通过（57.284 秒）。最大字号取消后仍有两条 textClipped，部分滚动位置还报告屏外文字对比度。没有忽略 issue，也没有将单次通过或业务回归抵扣整个无障碍验收。Docs 802 个本地 Markdown 文件目标检查通过（仅验证目标文件存在，不代表所有标题锚点或业务验收通过）。11-02 POC 准入与此缺口分别追踪。

16-01 当前开发检查点（2026-09-14）：`OutfitFinalChecks20260914.xcresult` 中 7 suites / 65 项领域、SQLite、ViewModel 和衣橱/图片回归通过；`testOutfitPlanSaveRestartSnapshotAndDeletion` 通过（101.713 秒）。整个 bundle 为失败：最大字号详情有两条 `Text clipped`，标准字号选择/详情各一条 `Contrast failed`，均未被 issue handler 忽略。相关场景继续 `pending`，不可将业务旅程通过抵扣无障碍验收。

`OutfitBuildBoth20260914.log` 记录 Debug、Release 两次 `BUILD SUCCEEDED`，架构检查通过。专用 iPhone 17 / iOS 26.5 模拟器 `ThenOutfitPlan20260913` 使用合成衣物完成减少动态效果下选择/保存/时间线回看，再在深色外观下查看详情、编辑为两件并保存；AX 树确认两件名称，截图 `OutfitDarkReducedMotion20260914.png` 留在本机 DerivedData。设置经 Simulator / Accessibility Inspector 实际开启，检查后恢复原值；不代表人工 VoiceOver、低电量、设备性能或真机 Data Protection 通过。

Accessibility Inspector 对本应用运行审计未返回可用报告；进程检查确认应用仍在运行，未发现本次 ThenApp 崩溃记录。此现象不作为自动检查误报的证明。下一步须复核自动失败截图与可复现的具体控件，保持原断言，不通过排除全部对比度/裁切检查获得绿色结果。

每份证据必须记录唯一证据 ID、commit/build、设备与 OS、环境、夹具版本、步骤/命令、持久 artifact 地址、时间、执行人、复核人、结果和限制。不得使用真实用户数据或依赖临时路径。

仅当 `OUTFIT-ACC-001`～`OUTFIT-ACC-011` 全部 `passed`，且相关系统组合场景独立通过时，本功能才可进入本地核心 GA。

## 2026-09-13手工计划切片验收映射

16-01 approved 合同覆盖 ACC-001 的计划不计实际、ACC-005 的最小快照/照片失效（复用/月历仍后续）、ACC-006 的计划离线/事务/时区、ACC-008/009 的最小权限与可访问性子集，以及补充 ACC-010 的彻底删除。完整功能结果仍 pending；16-01 已有生产领域/SQLite/页面实现，2026-09-14 数据层 50 项测试通过，模拟器 UI 验收进行中。需分别验证：同日多计划、IANA 时区与日界线、相同命令重试、删除后迟到写入、衣物改名不改历史、照片替换不冒充旧图、衣物彻底删除的占位/关联计划删除两条路径。完整 PRD 的实际穿着、反馈、复用/月历与发布门禁继续保留。

### OUTFIT-ACC-010衣物与计划彻底删除

覆盖 OUTFIT-RULE-021/022、Design LOG-10 与本切片已批准删除合同。分别选择保留无属性占位、删除受影响计划；在真实 SQLite 核查原名称/类别/版本/媒体引用/衣物关联均不可恢复，媒体删除意图同事务，失败可重试。删除计划本身不删除衣橱衣物，迟到创建/更新不得复活已删计划。结果 pending。本项补齐原验收 01～09 未单列的已批准删除要求，不改变用户合同。


## 2026-09-14计划数据开发验收

- 契约：16-01 approved；代码为当前未提交工作树，尚未发布。
- 环境：Xcode 26.6 / Swift 6.3.3，独立 iPhone 17 / iOS 26.5 模拟器；仅合成夹具。
- DATA-16-01：`OutfitPlanDataFinal20260913.xcresult`，命令为 `xcodebuild test` 选择 OutfitPlanTests / OutfitPlanRepositoryTests / WardrobeRepositoryTests / WardrobePhotoRepositoryTests / WardrobeViewModelTests，exit 0，50 tests / 5 suites。
- 已通过的计划子集：ACC-001 不生成实际穿着；ACC-005 改名快照不倒灌、媒体替换/移除清除旧引用；ACC-006 日期/时区、稳定分页、幂等、防复活、事务失败回滚；ACC-010 两种衣物删除策略及确认后关联竞态、计划删除保持衣橱。
- v3→v4 升级保留实际 SQLite 行、UUID、revision、ready 媒体元数据/hash 与文件字节；存储夹具不能替代图片解码与物理保护证据。
- UI、完整 PRD 实际穿着/反馈/历史复用、人工 VoiceOver、真机数据保护与发布指标未由上述测试验收。执行人 Codex，复核待定。


UI-16-01-PHOTO（2026-09-14）：`OutfitPhotoAndAudit20260914.xcresult` 的 `testOutfitPhotoMultiSelectionAndBackground` 通过，其他无障碍测试仍失败。已逐图检查真实合成衣物缩略图、照片移除后同一计划仅保留文字，以及 App 切换器中的整页隐私遮罩。附件 ID 归 16-01 证据段；该截图只证明本次模拟器显示/切后台行为，不证明物理数据保护或所有中断情况。最新数据/状态组合回归 `OutfitFinalChecks20260914.xcresult` 已输出 65 tests / 7 suites passed，UI 结果尚未收口，不能记整体验收 passed。

2026-09-14 诊断证据：独立 SwiftUI host 复现取消/滚动后一条 textClipped（无 element），产品旅程为两条；没有数据库、业务对象或真实媒体。原型、失败截图、颜色计数与未通过试验归 16-01 证据段；不据此直接声明 SDK 误报。三份选择页失败附件的字形/背景主要为不透明黑/白；首次详情“编辑计划”确认为 #0088FF / #FFFFFF，相关实际颜色问题已修复。

[CVS Health 的 XCTest 实践记录](https://github.com/cvs-health/ios-swiftui-accessibility-techniques/blob/main/iOSswiftUIa11yTechniques/Documentation/XCTestAccessibility.md) 列出了对比度和裁切误报的已观察限制；这是外部诊断线索，不证明本项目所有报告均为误报。颜色核对参考 [W3C 最低对比度说明](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)，并与真实显示、VoiceOver、动态字号分别验证；不照搬排除整个审计类别的示例。

`OUTFIT-20260914-SEMANTIC`：67 项领域/数据/VM 通过；最新标准字号完整旅程与三处审计通过（`OutfitSemanticEdit20260914.xcresult`）；最终 Debug/Release 与架构检查通过。关键源码和各证据的通过边界记录于 `OutfitSemanticEvidence20260914.json`，具体哈希归 16-01。最大字号、人工 VoiceOver 和物理发布项仍未抵扣，16-01 尚未完整验收。

## 2026-09-14只读详情自然高度修复

关联 16-01 TEST-01、OUTFIT-ACC-005/006/010 的详情/历史/删除子集。只读详情从 Form 改为 ScrollView/VStack 卡片；编辑继续原生 Form/DatePicker。场景、状态、日期/时区和快照不设固定文字高度，标题保留 VoiceOver header 语义；操作按钮保持原生语义与至少 44pt 高度。未改动数据库、状态机、照片读取/删除、后端或 API；回滚布局不清理任何 OOTD 数据。

测试使用本轮创建的 iPhone 17 / iOS 26.5（23F77），UDID `1076ADD2-6091-4F7B-98D7-F8AE45C5D32B`，初名 ThenOutfitAXScroll20260914，照片旅程前改为 ThenOutfitPlanScroll20260914；Xcode 26.6 / Swift 6.3.3。只输入合成衣物/计划，照片仅既有 synthetic-flat-shirt。不是用户图库或真机发布验收。

| 结果包 | 范围与真实结果 |
| --- | --- |
| `OutfitAXScrollDetail20260914.xcresult` | SwiftUI 独立样例，最大字号 active/cancelled 两项 contrast/textClipped 全通过，24.385 秒；原 Form 取消后的 textClipped 对照仍保留 |
| `OutfitScrollDetailProduction20260914.xcresult` | 产品标准字号完整旅程和三处审计通过，57.414 秒；最大字号取消后 1 contrast 失败，未出现原 textClipped；整体 exit 65 |
| `OutfitScrollDetailRegression20260914.xcresult` | 21 项领域/真实数据库/VM 测试通过（3 suites，0.505 秒）；保存/重启/改名快照/两种删除策略 UI 通过（99.613 秒）；最大字号仍 1 contrast 失败、未出现 textClipped；照片 UI 因设备名守卫跳过，整体 exit 65 |
| `OutfitScrollDetailPhoto20260914.xcresult` | 重启本轮更名设备后原守卫满足，含照片组合/后台遮罩/移除图片/历史文字/清理实际通过，1 UI、82.808 秒，exit 0；没有弱化守卫或抹去前次跳过 |

实际查看首轮产品标准字号、最大字号取消详情和失败元素截图：文字完整、卡片内容可滚动；最大字号首轮报告场景文字，复验报告原始时区。首轮元素图片 900×374，含 55,624 个不透明黑像素与 269,131 个不透明白像素，统计保存在 `DerivedData/OutfitScrollDetailProductionProof20260914/contrast-pixels.json`。这些观察支持已改善的布局，不等于关闭仍失败的对比度审计，也不宣称 SDK 误报。未修改审计类别、未忽略 issue、未缩小字号。

复跑使用 `xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug -destination 'platform=iOS Simulator,id=<专用UDID>' -skipPackagePluginValidation -disableAutomaticPackageResolution -parallel-testing-enabled NO -resultBundlePath DerivedData/<唯一名称>.xcresult test`。定向选择 `ThenAppUITests/ThenAppUITests/testOutfitFutureDateEditCancelAndAccessibility`、`testOutfitAtLargestText`、`testOutfitPlanSaveRestartSnapshotAndDeletion`；数据测试选择 ThenAppTests 的 OutfitPlanTests、OutfitPlanRepositoryTests、OutfitPlanViewModelTests。照片测试另选 `testOutfitPhotoMultiSelectionAndBackground`，须在启动前固定 ThenOutfitPlan 前缀设备名并导入合成衬衫，不能以跳过抵扣。

本轮产物与失败附件位于忽略的 DerivedData；可按命令/素材复跑，不是 CI 归档。代码与技术复核均为 Codex 自检，不冒充独立审核。最大字号对比度、人工 VoiceOver、物理保护与完整 PRD 的实际穿着/反馈/历史复用仍未完成，TEST-01 保持 in_progress、REL-01 blocked，整体目标继续 active。

照片实际截图已查看：`OutfitScrollDetailPhotoProof20260914/A849E44E-6089-44BD-AC3C-BA2E7C13EEF3.png` 为两件真实选择且仅一件带蓝色衬衫缩略图；`790386B0-D74B-4F15-8AE1-2ADAC33238AA.png` 为移除照片后同计划保留两件名称、没有旧缩略图；`673E7CF9-9150-4104-90BC-47A636CC5631.png` 为 App 切换器中不透明“内容已隐藏”。这些是当前详情布局的实际模拟器证据，不抵扣真机保护或 Woo 全量视觉复现。

构建收尾：`scripts/validate-ios.sh build` exit 0，工具链/架构及 Debug/Release 均通过，`git diff --check` 通过。关键源码证据文件 `DerivedData/OutfitScrollDetailEvidence20260914.json` SHA-256 `d18125eb03fe8612afcf9c48f39d496237cd0ee01daac58dc58f06c73141491e`；详情源码 SHA-256 `820ec95460f3d881d2692b166a70abb8b116db14de7376e0d729b66fbd33881f`。该快照只含 3 份相关文件，不证明全工作树验收；现有 ThenTransport YAML/module-map 警告仍在。测试/构建已终止，本轮 1076ADD2 专用模拟器已关闭并删除，其他模拟器及 OOTD 数据保持；未提交/推送。
