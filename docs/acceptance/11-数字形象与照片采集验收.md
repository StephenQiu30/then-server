# 数字形象与照片采集验收

## 当前完整Look验收的执行细则（2026-09-22）

11-05执行下表，图片输出由14-01执行；资产/性能/NFR测量统一见[Acceptance10](10-OOTD产品系统验收.md#非功能验收协议)。下表细化AVATAR-ACC-029～031，全部pending，不覆盖本文历史照片/工程微动结果。

| 场景 | 步骤 | 明确通过条件 |
| --- | --- | --- |
| ACC-029内容与默认离线 | 审核六套；清缓存干净安装断网，分别选三人物默认Look | 六套各有独立来源/视觉/预算结果；至少每人物一套图/GLB随包可用；非随包的其他内容诚实标待下载，不要求用网络才能开默认人物 |
| ACC-029真实三维 | 从图片进入GLB，连续旋转、缩放上下限、复位；原生辅助控件重复操作 | 正侧背为同一几何模型，固定姿势允许；图/模型/标签同版本；无裁切、无限缩放或伪造部件换衣；不存在骨架不是失败 |
| ACC-030持久化 | 保存、日期/收藏、编辑取消、另一次保存、重启详情；重复同命令、低存储和冲突 | 数据与页面一致；失败原值和草稿都在；一次提交一条；未生成草稿名称明确；计划/实际穿着/拥有计数无变化 |
| ACC-030竞态/坏包 | 快速A→B→C，A晚ready；载入中删除；坏hash/外部URI/超限纹理；WebContent终止 | 旧session/revision不发布；失败回当前版本图片；重建只恢复当前合法资源；不关闭校验求通过 |
| ACC-030删除 | 分别只删模型、删Look、删私人源，清理中杀进程后重启 | 按影响隐藏/清理，单删模型留图，源删除覆盖派生；共享母版/独立记录保留，旧回调不复活 |
| ACC-031主动输出 | 已确认且有权图→保存相册/系统分享；拒绝/取消/低存储/来源删除后重试 | 只有主动动作申请Add Only；无原始人物源/位置/设备/原文件名；失败不丢图；App可控临时副本清理，外部副本不虚报删除 |

画面录像必须能对照同一次操作的manifest/hash、Repository数据与加载事件；三维性能必须是真机。当前未生成六套正式资产，也未执行新Look旅程。

更新：2026-09-22。标准已按新设计替换，**新整套 Look 验收全部 pending**；没有真实 Tripo 产物、付费调用、App 新构建或最低真机证据。下文保留照片准备和工程微动已有结果，不扩大范围。

## 当前实现证据与缺口

工程 GLB 可离线观察/模块切换，模拟器微动与生命周期已有证据；正式人物画风、纹理、整套模型、Look 持久保存/恢复和生产照片门禁仍未通过。旧蓝色上装穿模与工程模型质量问题保留为当时不合格事实，不再要求先修独立服装才能验证当前静态 Look。

照片 POC 的 ImageIO 净化、临时会话、文件导入、声明前禁用系统选图、DEBUG 隔离、输入矩阵与模拟器测量已部分完成；真实 Vision 阈值/公平性、最低真机及完整安全仍不足。11-01 状态不因设计替换改变。

## AVATAR-ACC-029 完整 Look 与静态 GLB

- 覆盖 AVATAR-REQ-032/035～037/040/042、OOTD-REQ-017；执行 11-05。
- 干净安装、未登录、不授予新权限、断网：分别选择三套预设的默认完整 Look，图片可见，GLB 连续旋转、缩放/复位；再检查首批六套。
- 每套检查正侧背、连续中间角度、脸/发型/四肢/衣物与鞋、图片和模型一致性、裁切及来源权利；严重错误单套不通过。静态姿势允许，无骨架/眼部不作失败。
- 校验实际 GLB/hash、纹理与解码预算；坏 hash、外部 URI、未知必要扩展、越界 accessor/索引、超限纹理关闭失败，不关闭 validator 兜底。
- 同一版本图片先显示，模型失败保留图片，整套加载成功才切换；旧模型不能贴新标签。记录六套资产和设备数据，不要求旧 24 套笛卡尔积。
- 结果：pending。需正式资产、真实画布、网络/文件清单和最低真机记录；API 文档与工程夹具不抵扣。

## AVATAR-ACC-030 版本、保存恢复与资源生命周期

- 覆盖 AVATAR-REQ-034/041～043；执行 11-05，云派生与 14-01 联验。
- 保存日期/原时区和收藏、取消/保存编辑、终止重启、详情恢复；实际 GRDB 记录、图片 hash 与 GLB revision 一致。renderer ready 不能代替数据库提交。
- 快速 A→B→C、SQL/文件故障、低存储、坏下载、旧回调和删除并发不得覆盖新版本或出现假成功；有效结构化草稿可保存但不能标成已生成图。
- 删模型保留图；删 Look/私人来源清理可达私人图/模型/缓存，保留独立衣物、穿着事实和共享内置母版；重启清理与迟到结果不复活。
- 重复查看命中缓存不触发新生成。不可见/后台停止绘制；WebContent/context lost 恢复当前有效版本；旧纹理/几何/监听释放。
- VoiceOver/最大字号/Reduce Motion 完成同等旅程；最低真机按 Design 20 测首帧、拖动、内存、热与恢复，记录环境和样本数。
- 结果：pending。原内存 Model 测试和工程录屏不抵扣持久化或生产性能。

## AVATAR-ACC-031 主动 Look 图片输出

- 覆盖 AVATAR-REQ-044、OOTD-REQ-019；执行 14-01。
- 仅有使用权且已确认的 Look 图片可主动保存/系统分享；未生成、已删除或无权资源拒绝。原始本人照片、GLB 下载及视频不混入此入口。
- Add Only 在用户点击保存相册时按需申请；拒绝/取消不丢私有 Look。分享临时副本元数据净化，App 可控副本清理可恢复。
- 明确提示外部副本不受 App 删除控制；不会生成公开链接、自动发帖、WearEvent 或偏好信号。
- 结果：pending，需真实系统入口/权限/取消/失败和文件检查；模型未生成也必须可完成图片输出。

## AVATAR-ACC-026 无 Blender 与单一基线

覆盖 AVATAR-REQ-036/037。来源、版本、权利及原始完整 Look GLB 可追溯，无 Blender 导入并通过原生校验。当前基线固定 Design 01 的 2026-09-22 修订；具体样本/许可与设备待验。历史 MetaPerson 样例只证明过无 Blender 的格式和眼部输入，不是现行生产样本或商业授权。

当前结果：文档已替换，正式代表资产 pending；新静态 GLB 质量和运行合同由 ACC-029 覆盖。

## 退役与延期验收编号

| 原场景 | 当前处置 | 接续 |
| --- | --- | --- |
| AVATAR-ACC-010 | deferred | 原有限体型编辑延期；离线完整 Look 由 ACC-029 验证 |
| AVATAR-ACC-011、AVATAR-ACC-012、AVATAR-ACC-013、AVATAR-ACC-014 | superseded | 原角色参数事务/回退/历史删除改由 ACC-030，事实保护继续有效 |
| AVATAR-ACC-019、AVATAR-ACC-020、AVATAR-ACC-021、AVATAR-ACC-022、AVATAR-ACC-023 | superseded | 独立三视图/24 组合矩阵停止执行，ACC-029/030 接续 |
| AVATAR-ACC-024 | deferred | 视频合同归 Acceptance 15，不阻断静态模型观察 |
| AVATAR-ACC-027 | deferred，原结果 pending | 眨眼/轻微注视与正式眼部资产未通过，不能因延期改成 passed |
| AVATAR-ACC-028 | superseded | 原三人物模块工作室改由 ACC-029/030；旧内存态缺口仍待修复 |

编号只保留追踪，不复用。AVATAR-REQ-001～015 的照片/事实要求仍由 ACC-001～009 覆盖；旧 REQ-016 的禁止全部输出范围已由 REQ-044/ACC-031 替换；REQ-032/034～037 由 ACC-025/026/029/030 按适用层验证。现行人物/Look 新需求 REQ-040～044 映射 ACC-029～031。

## AVATAR-ACC-025 Three.js 灵动反馈

- 覆盖：`AVATAR-REQ-032`～`035`；执行：[11-04](../plan/11-04-Threejs角色灵动反馈执行计划.md)。
- 正常路径：角色就绪后可观察轻微起伏和重心摆动；左右拖动产生短暂方向倾斜并回正；实际换装变化产生一次短确认反馈，角度切换和重复相同配置不冒充换装动作。
- Reduce Motion：开启后角色保持稳定姿态，不自动起伏、不倾斜、不弹性确认；直接拖动、角度按钮、换装和静态回退仍可操作。
- 生命周期：进入后台后无持续动画帧；回到前台恢复当前人物、服装和 yaw，不补算后台时长；WebContent 终止可重建，退出释放资源。
- 边界：当前工程 GLB 缺少已验收眼睑 morph、面部骨架和 clips，本场景不要求也不得宣称眨眼、表情、挥手或行走；微动通过不能抵扣正式人物资产、穿模或最低真机性能。
- 当前结果：生命周期实现已通过 iPhone 17 / iOS 26.5 模拟器的覆盖页、后台 30 秒和 WebContent 终止重建；34.46 秒录屏 `/tmp/then-avatar-drag-change-20260915.mov` 覆盖待机、真实画布拖动、衣橱和换装完成；9.19 秒 `/tmp/then-avatar-reduce-motion-20260915.mov` 在系统 Reduce Motion 开启时两帧角色区域 YAVG=0/YMAX=0，关闭后可观察到待机帧差。专项 UI 验收在五秒拖动保持期间于 2026-09-15 16:45:45 +0800 启动系统设置，返回后 renderer 重新 ready，`/tmp/ThenAvatarInterruptedDrag-20260915-6.xcresult` 为 1 passed / 0 failed / 0 skipped。模拟器 ACC-01 完成，最低支持真机性能仍未完成，因此本验收保持 `in_progress`。
- 证据：相关自动测试 xcresult、正常/Reduce Motion 模拟器录屏，以及最低支持真机 Instruments/录屏。
- 历史证据：iPhone 17 / iOS 26.5 的 Debug 构建及 `AvatarStudioModelTests` 4 项通过，Woo 首页→衣橱→换装 UI 回归 1 项通过，均 0 失败/跳过。历史记录中的 13.638 秒录屏 `assets/acceptance/avatar-motion-v1/idle-simulator.mov` 及配套 README，在 2026-09-22 清理前的现有工作区均未找到，不能继续声称本机已持久化。保留当时的测试结论；需要复核画面时须恢复原证据或重跑，不用缺失 contact sheet、当前截图或文件名代替。本次没有删除该录屏，也没有重新执行或扩大动态验收。
- 结果：`in_progress`。模拟器的默认待机、Reduce Motion、拖动/换装动态、拖动中断、前后台恢复和 WebContent 重建均通过；最低支持真机的帧率、内存、发热与恢复仍待验收。

## 2026-09-15 Woo POC 输入包检查点

证据 ID：`AVATAR-MULTIVIEW-INPUT-20260915-01`，对应 11-03 开工检查与 `11-03-POC-01` 前置。项目已经形成三套人物各正/侧/背 9 张基础图、两上装/两下装/两双鞋 6 张商品图，以及浅色和高对比两套代表 recipe。文件、来源/权利与人工检查记录现位于私有 `frontend/assets/avatar-poc/woo-input-v1/README.md`，机器清单位于同目录 `manifest.json`；原 300 CNY/双供应商多视角对照预算未执行，随 11-03 退役，不作为新路线的调用授权。该目录只在本机持有，不随公开仓库交付，也不由 Go 服务或 Swagger 静态路由公开。

逐文件复核确认人物完整入镜、方向明确、白底、无 UI/文字/品牌；衣物单品完整、无人物和额外配件。15 个 manifest 条目的路径、PNG 尺寸、字节数和 SHA-256 与磁盘一致。输入均为原创合成，不含真实用户数据，也没有把 Woo 截图作为 Provider 输入。

2026-09-15 该检查点没有 FASHN 或 Google 测试凭据，地区和费用未通过账号核验；当时未调用外部供应商、支出为 0。因此本证据只关闭“可执行输入包”前置，不通过 `AVATAR-ACC-019`～`024`，不证明人物跨视角身份、服装一致性、24 套发布矩阵、App 页面、缓存或真机行为。

## 验收环境与证据规则

- 覆盖最低支持的真实 iPhone/iOS 26、主流尺寸模拟器、干净安装和已存在本地衣橱两类状态；2026-09-08 用户批准提高最低系统，验收结果保持 pending。
- 照片夹具只使用合成素材或取得书面授权的成年内部测试素材，覆盖单人、多人、背景人物、轮椅/辅助器具、宗教服饰、不同肤色与体型、遮挡、模糊、异常格式和超大图片；不得使用真实用户或生产数据。
- 覆盖无网络、所选 iCloud 项目不可读、低存储、锁屏、进程终止、权限撤回和删除竞态。
- 每份证据记录场景 ID、覆盖需求、commit/build、设备与系统、夹具版本、步骤或命令、结果、时间和复核人，并保存到 CI artifact、版本化测试报告或发布记录；不得把短期 `/tmp` 路径作为证据。
- 证据不得包含人物原图、可访问媒体 URL、对象 key、令牌、原文件名或其他真实敏感正文。

## 验收场景

### AVATAR-ACC-001 无照片、无权限与离线路径

- 覆盖需求：`AVATAR-REQ-001`、`AVATAR-REQ-008`、`AVATAR-REQ-011`、`AVATAR-REQ-012`。
- 设计依据：Design 04 `AV-01`、`AV-14`、`AV-18`。
- 适用层级：本地核心，首发必选。
- 前置条件：干净安装；未登录；无网络；相册和相机均未授权。
- 操作步骤：进入数字形象，选择风格化模板或完全停用形象；继续建立衣橱、获取结构化推荐、保存穿搭并查看历史。
- 期望结果：首次进入不请求照片权限；模板、单品拼图和完全停用均可选择；照片缺失不改变推荐资格、数量、保存和历史；所有视觉位置显示非尺码、非合身声明。
- 边界情况：从系统设置撤回已有权限、云端形象不可访问或设备离线时，既有结构化数据不丢失，也不出现循环劝说或不可用照片入口。
- 验收证据：XCUITest、权限状态截图、零非预期网络抓包、离线重启录屏及推荐输入审计。
- 结果：`pending`。

### AVATAR-ACC-002 本人、成年与渐进披露

- 覆盖需求：`AVATAR-REQ-002`、`AVATAR-REQ-004`、`AVATAR-REQ-013`。
- 设计依据：Design 04 `AV-02`、`AV-03`、`AV-07`。
- 适用层级：本人照片条件能力，生产必选。
- 前置条件：照片入口由受控开关对测试账号可见；尚未打开系统选择器。
- 操作步骤：查看照片说明；分别不确认、确认“不是本人”、确认“未满 18 周岁”和确认本人且 18+；在允许路径中打开并取消系统选择器。
- 期望结果：本地无照片核心不要求年龄声明；用户在首次选择本人照片或发起含本人照片生成前能看见照片可选、处理目的与位置、保留、删除、无照片替代和视觉边界；未明确确认本人且 18+ 时不能打开本人照片选择器、创建对应临时文件或任务；账号认证不等于年龄证明；声明策略版本变化后要求重新确认；取消后返回原上下文。授权虚构成人预设另按来源合同验证，不要求用户声明预设是本人。
- 边界情况：声明不能与营销、同步、分析或训练同意捆绑；不得用年龄推断、人脸识别或裁掉第三方来绕过确认；升级后不得对从未进入人物照片流程的无照片用户补弹声明。
- 验收证据：UI 元素顺序测试、沙盒前后差异、网络零请求记录和本地化/VoiceOver 录屏。
- 结果：`pending`。

### AVATAR-ACC-003 单张全身照与可选自拍

- 覆盖需求：`AVATAR-REQ-003`。
- 设计依据：Design 04 `AV-04`、`AV-08`。
- 适用层级：本人照片条件能力。
- 前置条件：使用获授权的单人成人全身照；准备合格与不合格自拍夹具。
- 操作步骤：只选择一张全身照并完成本地准备；分别跳过自拍、主动添加合格自拍和移除不合格自拍。
- 期望结果：一张全身照即可完成当前流程；不要求侧面、背面、视频、贴身衣着或额外角度；跳过或移除自拍不影响继续；自拍只服务用户明确选择的当前相似度目的。
- 边界情况：选择多张、重复选择、系统返回顺序变化和自拍处理失败时，不产生多份人物身份或覆盖上一稳定形象。
- 验收证据：领域输入快照、UI 测试、媒体 manifest 和文件系统差异。
- 结果：`pending`。

### AVATAR-ACC-004 中性质量门与安全解码

- 覆盖需求：`AVATAR-REQ-006`、`AVATAR-REQ-007`。
- 设计依据：Design 04 `AV-05`、`AV-06`、`AV-10`、`AV-14`、`AV-19`；当前 `11-01` 执行计划重点覆盖本场景的隔离 POC 部分。
- 适用层级：隔离 POC 与本人照片生产门禁。
- 前置条件：具备模糊、过曝、截断、强遮挡、多人、背景人物、镜像多人、MIME/魔数不符、损坏、超大像素和压缩炸弹夹具。
- 操作步骤：逐个输入夹具，记录稳定结果码、用户文案、资源峰值、临时文件和网络行为。
- 期望结果：每次只呈现最主要、可操作且中性的技术原因；多人或可能含第三方时停止处理，不自动裁剪继续；异常文件在预算内关闭失败；不输出年龄、性别、种族、体型、吸引力、健康或身体评价。
- 边界情况：Vision 不可用、低性能设备、系统内存压力或检测置信度不足时回退模板，不允许绕过门禁上传；不同用户群体的差异失败率必须单独报告。
- 验收证据：参数化测试、Vision 夹具报告、内存/时延报告、沙盒清理断言、公平性离线评测和双人复核记录。
- 结果：`pending`。局部实现证据：60 个相关测试（114 个参数化实例）在 iPhone 17 / iOS 26.5 Simulator 上 0 失败、0 跳过，通用 Debug/Release 构建通过；声明尺寸不符与字节超限均在 Vision 前关闭失败，照片准备管线和 ViewModel 覆盖阶段顺序、声明前拒绝、换图/不支持、异常、取消、跨 session、显式丢弃、scene 清理、迟到结果抑制和清理失败。模拟器缺少人体姿态权重时返回能力不可用且不输出人员/质量结论。macOS 原生同源码探针验证 9 个合成夹具可执行，人员数 0/1/2 与人工事实一致；截断覆盖 0.667、轮椅覆盖 0.889、欠曝曝光 0.343。运动模糊与正常样本锐度差距仅约 0.024，当前不能据此冻结阈值；完整异常格式/资源矩阵、公平性、双人复核和最低真机仍未通过。

### AVATAR-ACC-005 元数据净化、文件保护与目的隔离

- 覆盖需求：`AVATAR-REQ-005`、`AVATAR-REQ-009`。
- 设计依据：Design 04 `AV-09`、`AV-13`、`AV-21`。
- 适用层级：隔离 POC、本地照片准备与任何离机能力的共同门禁。
- 前置条件：含 GPS、EXIF、IPTC、设备型号、拍摄时间和原文件名的授权夹具；测试设备处于首次解锁前后状态。
- 操作步骤：完成端侧净化并检查输出元数据、方向、必要色彩、受保护目录、数据库、备份属性、日志和网络；分别选择一次性、本机复用和拒绝云处理。
- 期望结果：非必要元数据和原文件名均不存在；像素方向正确；媒体不存 SQLite BLOB 或绝对路径；敏感文件具有批准的数据保护和备份排除；本机复用同意不能扩展为云生成、同步、分析或训练同意。
- 边界情况：锁屏、后台快照、诊断、通知、Widget、Spotlight 和分享表面均不得泄露人物素材；未获单独同意时网络请求为零。
- 验收证据：ImageIO metadata dump、文件属性报告、数据库检查、网络抓包、日志扫描和系统表面截图。
- 结果：`pending`。

### AVATAR-ACC-006 一次性生命周期、异常恢复与删除链

- 覆盖需求：`AVATAR-REQ-010`、`AVATAR-REQ-011`。
- 设计依据：Design 04 `AV-11`、`AV-12`、`AV-15`、`AV-16`、`AV-17`。
- 适用层级：本地照片准备必选；云端派生启用时扩展为端到端门禁。
- 前置条件：建立含人物原图、净化图、mask、缩略图和本地参考的测试血缘；云端条件测试另含静态/动态结果、缓存与运行中任务。
- 操作步骤：在选择、净化、写入和确认阶段终止 App；分别让一次性流程成功、失败、取消和超时；删除数字形象；注入文件删除失败和迟到任务结果。
- 期望结果：启动只清理固定临时根目录的过期 session；一次性各终态均无可复用副本；删除后人物相关资产立即不可读并按血缘收敛；失败保持隐藏且显示删除中或失败；衣物、结构化搭配、实际穿着和反馈保留并降级为无图表示。
- 边界情况：删除与生成并发、重复删除、对象已不存在、断网和迟到成功回调均不得复活形象或误删其他 owner/衣橱数据；“已提交删除”不得显示成“已删除”。
- 验收证据：状态机与幂等测试、资产遍历清单、数据库/文件差异、迟到回调测试和 UI 录屏。
- 结果：`pending`。

### AVATAR-ACC-007 视觉边界、公平性与可访问性

- 覆盖需求：`AVATAR-REQ-007`、`AVATAR-REQ-012`。
- 设计依据：Design 04 `AV-18`、`AV-19`、`AV-20`。
- 适用层级：本地核心及所有照片/生成条件能力。
- 前置条件：准备经批准的多样性测试集；开启 VoiceOver、最大 Dynamic Type、提高对比度、深色模式和 Reduce Motion。
- 操作步骤：完成无照片和照片流程，查看模板、个人形象、静态/动态预览与历史降级；扫描所有相关文案。
- 期望结果：状态、原因和动作无需依赖颜色或动画；焦点顺序正确；说明不截断；每类视觉结果都明确不是尺码、合身、垂坠、透明度或颜色事实；不存在显瘦、遮缺陷、身体评分或人口属性评价。
- 边界情况：发现意外裸露、肤色改变、身份严重失真、辅助器具或宗教服饰被擦除时，照片/生成入口能够关闭并安全回退。
- 验收证据：Accessibility Inspector、真机录屏、字符串扫描、分组质量报告和人工双人复核。
- 结果：`pending`。

### AVATAR-ACC-008 本人照片生产启用门禁

- 覆盖需求：`AVATAR-REQ-002`～`AVATAR-REQ-010`、`AVATAR-REQ-012`。
- 设计依据：Design 04“实施顺序与发布门槛”“发布阻断条件”。
- 适用层级：条件生产能力，不属于无照片本地核心发布前提。
- 前置条件：隔离 POC 已完成；候选生产数据流、地域、供应商、保留期、删除 SLA、公平性阈值、性能预算和 feature flag 已形成批准记录。
- 操作步骤：复核本文件全部照片场景证据、隐私披露与真实抓包；演练关闭入口、停止新任务、恢复已接纳任务和删除既有数据。
- 期望结果：所有生产门禁一致且有效；开关关闭后不展示不可用入口；模板、拼图、衣橱、推荐、保存和历史继续工作；POC 通过不被写成生产批准。
- 边界情况：任一供应商、地域、训练、删除、公平性、安全或真实性门禁缺失时保持关闭，不允许以内测、灰度或用户自愿绕过。
- 验收证据：go/no-go 记录、供应商与隐私批准、feature flag/回滚演练、发布配置快照和跨文档追溯报告。
- 结果：`blocked`，当前尚未关闭生产供应商、地域、保留、删除、公平性和质量参数；不阻断无照片本地核心。

### AVATAR-ACC-009 OOTD 双目的、事实与首发输出边界

- 覆盖需求：`AVATAR-REQ-014`、`AVATAR-REQ-015`、`AVATAR-REQ-016`。
- 设计依据：Design 04 的目的分离、资产生命周期和非目标；Design 05 的 OOTD 草稿语义。
- 适用层级：本地 OOTD 输入与本人照片条件能力的共同门禁。
- 前置条件：同一张获授权的本人成人 OOTD 测试图；分别选择只拆分衣物、只建立人物参考、两者都选和两者都不选。
- 操作步骤：完成四种选择并检查人物资产、衣物/组合草稿、计划、实际穿着和所有下载/分享入口。
- 期望结果：人物参考与衣物拆分是两个独立目的并由用户逐项选择；未选择人物参考时不保留可复用人物源图、人体派生物或数字形象；默认只向衣橱交付待确认衣物草稿和组合草稿，不自动创建 `OutfitPlan` 或 `WearEvent`；原始人物照片不导出/公开；已确认 Look 图片的主动保存/系统分享另按 AVATAR-ACC-031，未经用户动作不出站。
- 边界情况：中途取消、只确认部分衣物、重启恢复和删除人物参考时，已确认衣物事实可保留，但未选择目的的数据不得借另一目的继续存在；原始人物照片不得借 Look 图片分享路径导出。
- 验收证据：目的选择 UI 测试、媒体/领域对象差异、草稿与计划/实际记录计数、文件/分享入口扫描和重启录屏。
- 结果：`pending`。

## 历史开发证据（按原代码与环境保留）

当前 then-app 工作树在 iPhone 17 / iOS 26.5 运行 AvatarStudioModelTests，结果包 `/tmp/ThenAvatarLifecycle-20260915-1.xcresult` 经 xcresulttool 确认 5 passed、0 failed、0 skipped；独立 Debug 与 test build 均通过。生命周期 UI 结果包 `/tmp/ThenAvatarBackground-20260915-1.xcresult` 确认覆盖页关闭、后台 30 秒和回前台渲染器恢复 1 passed、0 failed、0 skipped；`/tmp/ThenAvatarWoo-20260915-2.xcresult` 确认真正拖动画布、换装及两次 renderer ready 1 passed、0 failed、0 skipped。定点终止模拟器 WebContent 后 PID 从 25477 替换为 26134，角色重新显示。以上不覆盖真机性能、正式资产或眼部效果。

照片状态机检查点结果包 `/tmp/ThenAvatarRelated-20260915-1.xcresult` 为 60 passed / 0 failed / 0 skipped（114 个参数化实例）；排除明确依赖真机 Vision 权重的衣物探针和独立 UI target 后，`/tmp/ThenAppProductUnit-20260915-1.xcresult` 为 214 passed / 0 failed / 2 skipped，跳过项是既有真机 Data Protection 验证。Debug/Release 通用模拟器构建均通过；then-app `8e0210c` 的固定 Xcode 26.6 / Swift 6.3.3 CI 运行 34982809678 成功。完整 scheme 的 UI Runner 等待 Xcode worker 超时后中止，不能认领完整 XCUITest；该缺口归 IOS-09～10 与 REL-01。

IOS-09 入口实现已提交为 then-app `848b753`。iPhone 17 / iOS 26.5 的 `/tmp/ThenAvatarPOCUnit-20260915-4.xcresult` 为 7 passed / 0 failed / 0 skipped，其中新增启动模式与净化预览边界 2 项；`/tmp/ThenAvatarPOCUI-20260915-3.xcresult` 为 1 passed / 0 failed / 0 skipped，覆盖声明前无 picker、按钮禁用、确认后选图入口、模板退出和重新开始。真实模拟器截图 `/tmp/ThenAvatarPOCValidated-20260915-2.png` 显示披露及禁用态；generic Release build 通过且符号检查无 POC Host/View/loader。以上关闭 IOS-09，不关闭 IOS-10、TEST-05 完整矩阵、真机门禁或生产入口。

IOS-10 辅助功能增量已提交为 then-app `e5d7c52`，系统状态测试修正提交为 `a1f9c70`。POC 用户文案扫描在 String Catalog 中 0 缺失，JSON 无重复键且 Debug/Release 的 `xcstringstool` 编译通过；`/tmp/ThenAvatarPOCUI-20260915-6.xcresult` 为 1 passed / 0 failed / 0 skipped，覆盖后台返回 disclosure 和无照片出口。首次审计发现说明文字与禁用按钮在深色模式接近但未过阈值，改用语义主文本色后复测通过。视觉复核确认原 `/tmp/ThenAvatarPOCUI-20260915-5.xcresult` 的 dark/high-contrast 启动参数没有改变系统外观，因此不作为四环境证据；改用 `simctl ui` 设置并回读真实状态，最大辅助字号浅色 `/tmp/ThenAvatarPOCA11yLight-20260915.xcresult`、最大辅助字号深色 `/tmp/ThenAvatarPOCA11yDark-20260915.xcresult`、最大辅助字号提高对比度 `/tmp/ThenAvatarPOCA11yContrast-20260916.xcresult` 各为 1 passed / 0 failed / 0 skipped，深色及高对比度附件已人工核验。POC 没有 App 自有装饰性动画，处理反馈使用系统 `ProgressView`；未将未设置成功的 Reduce Motion 参数计为系统证据。真实选图后的处理中取消、replacement/unsupported 原因 UI、实际 Reduce Motion 系统设置记录、最低真机和生产门禁仍未通过。

TEST-05 增量已提交为 then-app `72a3c14`。仓库授权的合成成人夹具通过 `simctl addmedia` 进入现有 iPhone 17 Pro / iOS 26.5 图库，`/tmp/ThenAvatarPOCSelectionUI-20260916-2.xcresult` 为 1 passed / 0 failed / 0 skipped：三次均经过系统 PhotosPicker、文件导入和 ImageIO 净化，随后由 DEBUG 场景分析器稳定覆盖分析阶段取消、多人 replacement 与设备不可用 unsupported；两种失败终态分别只有一个中性原因，附件截图通过人工复核，测试结束后 App 沙盒 `tmp/ThenAvatarPhotoIntakePOC` 子目录为 0。完整照片相关结果 `/tmp/ThenAvatarPhotoRelated-20260916.xcresult` 为 63 tests / 117 parameterized instances passed、0 failed / 0 skipped，其中场景解析单元套件为 3 passed。模拟器系统 Reduce Motion 偏好回读为 1 后，`/tmp/ThenAvatarPOCReduceMotionUI-20260916.xcresult` 为 1 passed / 0 failed / 0 skipped，覆盖声明、入口、后台清理、模板出口和重新开始，测试后偏好恢复为 0。独立 generic Release 构建通过且二进制无 POC Host/View/场景参数符号。以上关闭 TEST-05，不抵扣真实 Vision 效果、阈值、公平性、最低真机或生产入口门禁。

TEST-01 已由 then-app `21b113f` 与 `825ca34` 关闭。前者将 PNG 固定 IHDR 尺寸预算前移到 ImageIO 建源之前；65,535 × 65,535 合成头部约 0.03 秒内以资源超限拒绝并清空会话。后者新增 918-byte 仓库自制 16×16 Bayer DNG：系统 UTType 判定为 RAW，picker 在传输前拒绝，sanitizer 独立以 `unsupportedFormat` 拒绝并清空会话。iPhone 17 / iOS 26.5 Simulator 聚焦套件为 10 tests / 17 parameterized instances passed；代表性 1024×1536 合成全身照按 POC 预算净化，两次有效采样的峰值物理内存增量为 19.95–26.28 MiB，低于 192 MiB 测试上限。完整模拟器套件为 223 tests / 364 parameterized instances passed、2 个真机保护检查 skipped、0 failed；固定 Xcode 26.6 / Swift 6.3.3 的 [CI 35005889994](https://github.com/StephenQiu30/then-app/actions/runs/35005889994) 同样为 221 passed、2 skipped、0 failed。测试仍只证明模拟器边界；最低真机内存、能耗、热状态和硬超时继续由 TEST-06 验证，测量值不转为生产常量。
