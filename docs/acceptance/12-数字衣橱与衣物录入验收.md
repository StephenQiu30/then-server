# 数字衣橱与衣物录入验收

更新：2026-09-23。12-01 与 12-03 的本地开发实现已有回归证据；12-05 手工属性的 DOMAIN/DATA/IOS/TEST 已完成，REL-01 pending；17-20/17-21 账号衣橱 CRUD 与四项确认属性的后端开发范围 completed；17-31 Web 无图衣橱数据核验本地开发完成；17-41 归档/恢复与状态筛选的服务端子集 completed。完整推荐、App 同步、自动属性、正式 3D、真机和人工发布未通过。

## 当前结论

- 无图衣物增删改、真实 SQLite、重启、快照与两种删除策略已有实现；单件图片选取、四项复核、原子保存、替换/移除、故障清理与恢复已有测试。
- 首版自动检测只认领明显人物/面部；零检出仍须确认当前图片的四项条件，检测失败不可人工绕过。含手部合成负样本零检出不等于自动安全，完整身体局部识别不是当前 MVP 已实现能力。
- 手工属性 12-05 已完成 App 开发与模拟器验收：四项均可未知并由用户确认，当前衣物和历史快照分离，图片生命周期与两种删除策略保持原子；发布验收仍待真机与人工 VoiceOver。
- 内置工程目录与真实衣橱独立；正式 3D 服装尚未通过穿模/画风/授权验收，见 [人物资产验收](11-数字形象与照片采集验收.md)。
- 账号下无图结构化衣物 CRUD 已完成后端开发验收：5 个 Huma operation、GORM/PostgreSQL owner 隔离、幂等、revision、分页和账号级联通过；App 主动同步仍未接入。
- Web `/wardrobe` 已以两组合成账号与真实服务验证无图衣橱、确认属性、冲突和两种删除历史策略；它不提供离线或推荐。
- 17-41 已以真实 PostgreSQL 验证归档/恢复、active/archived/all 与 availability 筛选、筛选游标 owner 隔离、revision 冲突；新计划/实际拒绝归档衣物，既有快照不被改写。客户端管理 UI、未来方案冲突展示和完整推荐仍 pending。
- 本页保留当前图片合同、12-05 手工属性、17-20 后端衣橱 API 和 17-31 Web 页的分层证据；开发通过不抵扣推荐、三维资产或发布门禁。

## 17-31 Web 无图衣橱核验

2026-09-23 在隔离 PostgreSQL/Redis、Go API 与 Next.js 开发和生产构建中，使用两组合成账号验证 `/wardrobe`；执行和命令见 [17-31](../plan/17-31-Web衣橱数据核验执行计划.md)。A 创建、刷新、编辑状态及四项可选属性；旧 revision 返回 409 后保留表单并重新加载。实际关联计划/实际记录各 1 条时，删除预览准确显示 1/1；“清除快照”后两条记录仍可读取且关联内容为 `null`，“删除关联历史”后衣物、计划、实际均返回 404。B 的衣橱为空，访问 A 衣物及历史返回 404。请求中断时可重试恢复，Cookie 清除后跳登录。

生成 OpenAPI 客户端无差异；前端 lint、typecheck、format、8 个 Node 测试、生产构建及生产依赖审计均通过。320px 无横向滚动，390px/1280px 人工布局检查；axe-core 4.12.1 的 WCAG 2 A/AA 扫描 0 violations、0 incomplete、26 passes。此为本地合成数据验证；公开账号发布、人工屏幕阅读器、App/真机、离线 Look 与推荐均未由此关闭。


## 12-05手工属性开发验收

`WARDROBE-ATTRIBUTES-SPEC-20260914-01` 于 2026-09-16 获确认，并按 [12-05 spec/checklist](../plan/12-05-手工衣物属性与确认来源执行计划.md) 完成开发。App `a4abbd08c69765f3d3ba7cc689ae2435446d8586` 注册 `ootd_wardrobe_attributes_v1`，在当前衣物和计划快照中写入正式度、保暖感受、雨天/步行适用四项 nullable 受限值；未知没有来源，已知值唯一表示用户确认。没有条件探表、历史回填、模型推断或 OpenAPI 变更。

| 证据 | 实际结果 | 认领边界 |
| --- | --- | --- |
| `ThenWardrobeAttributesData-20260916-4.xcresult` | 43 个逻辑测试：42 通过、1 物理保护跳过、0 失败 | v4 真实升级/整体回滚、非法值拒绝、revision/完整输入幂等、图片原子保存/替换/移除、快照不倒灌、redacted trigger 与两种删除策略 |
| `ThenWardrobeAttributesAllUnit-20260916-2.xcresult` | 227 个逻辑测试：225 通过、2 物理保护跳过、0 失败 | 所有模拟器兼容单元回归；Vision probe 依 CI 合同排除，不能认领真机 Vision |
| `ThenWardrobeAttributesUI-20260916-7.xcresult` | 1/1 通过、0 失败/跳过，iPhone 17 Pro / iOS 26.5 Simulator | 四项填写、取消、单项清除、重启、旧快照保持、计划与衣物删除的真实 UI 链路 |
| `ThenWardrobeAttributesAccessibility-20260916-6.xcresult` | 1/1 通过、0 失败/跳过；系统深色、App 固定浅色、Accessibility XXXL、Reduce Motion | 四项可访问、对比度与文本裁切审计 0 问题；人工 VoiceOver 仍待发布验收 |
| Debug / Release generic simulator build | 两种配置均通过；String Catalog JSON 与 diff 格式检查通过 | 编译与资源完整性；不代表签名真机或发布构建 |
| GitHub Actions | 最终提交 [CI 35003359683](https://github.com/StephenQiu30/then-app/actions/runs/35003359683) 全绿；功能提交 [CI 35002675606](https://github.com/StephenQiu30/then-app/actions/runs/35002675606) 亦通过 | 固定 Xcode 26.6 / Swift 6.3.3 的 Debug 构建和模拟器兼容测试；CI 不运行 UI、Vision probe 或真机检查 |

本片通过 WARDROBE-ACC-001/003/005/006/008 的手工属性子集。一次包含全部 Vision probe 的本地运行曾因模拟器无法创建推理上下文及旧 migration 期望出现 3 个失败；migration 期望已修复，兼容套件随后 0 失败，Vision 失败没有冒充通过。结果包位于本机临时目录，可能被清理；远端 CI 是长期代码回归证据，UI/真机边界需要按相同用例重跑。REL-01 仍等待真机 Data Protection、锁屏/低存储/最低设备性能和人工 VoiceOver。

## 验收环境与证据规则

- 覆盖干净安装、空衣橱、已有历史穿搭、离线、权限拒绝、低存储、进程中断和数据库升级环境。
- 使用合成衣物图片、合规商品图夹具与脱敏数据库 fixture；不得使用真实用户衣物、订单、地址或生产数据。
- 覆盖无图、单件图片、OOTD 多件草稿、疑似重复、图片丢失和现实状态冲突。
- 每份证据记录场景 ID、需求 ID、commit/build、schema/fixture 版本、环境、步骤、结果、时间与复核人，并存入 CI artifact、版本化报告或发布记录；不得依赖短期 `/tmp` 路径。
- 媒体证据只保存哈希、元数据摘要、血缘/manifest 和不可访问性结论，不保存真实敏感素材或可访问 URL。

## 验收场景

### WARDROBE-ACC-001 无图最小保存与稳定身份

- 覆盖需求：`WARDROBE-REQ-001`、`WARDROBE-REQ-002`、`WARDROBE-REQ-009`。
- 设计依据：Design 05“场景一”“场景八”。
- 适用层级：本地核心，首发必选。
- 前置条件：空衣橱；无网络；相机和照片权限未授权。
- 操作步骤：从“今日”和“衣橱”分别新增无图衣物，只填写名称、类别和当前状态后保存；重启 App，再编辑和筛选。
- 期望结果：三项最小信息可保存，图片、品牌、价格、材质和尺码均不是必填；两个入口写入同一对象语义；每件衣物获得稳定全局业务 ID，不使用文件名、哈希、Photos identifier 或本地顺序作为跨端身份；重启后数据存在。
- 边界情况：名称或类别缺失、重复点击保存、权限拒绝和离线均关闭失败或幂等恢复，不产生半记录、重复记录或不可用空入口。
- 验收证据：领域验证测试、GRDB Repository/事务测试、XCUITest、重启录屏和网络零请求记录。
- 结果：`pending`。

### WARDROBE-ACC-002 OOTD 与批量草稿逐项确认

- 覆盖需求：`WARDROBE-REQ-003`、`WARDROBE-REQ-004`、`WARDROBE-REQ-007`、`WARDROBE-REQ-014`。
- 设计依据：Design 05“场景一”“场景六”。
- 适用层级：本地核心；自动识别部分按批准能力条件启用。
- 前置条件：准备包含多件衣物、遮挡和叠穿的合成 OOTD，以及五项批量处理夹具。
- 操作步骤：从 OOTD 形成多件草稿，逐项修正、保存、跳过；在第三项原子提交前注入低存储并重启后继续。
- 期望结果：草稿不是已录入衣物；每项独立确认，已确认项不因后续失败回滚；遮挡细节保持未知并标记需要补拍，不生成隐藏细节作为事实；恢复或丢弃未确认草稿的行为明确，重试不重复已确认项。
- 边界情况：识别失败、退出、重复回调、草稿顺序变化和部分图片丢失时，已确认衣物稳定，未确认项目不进入推荐事实。
- 验收证据：批量状态机测试、GRDB 原子事务测试、低存储/杀进程故障注入、草稿与正式记录计数及 UI 录屏。
- 结果：`pending`。

### WARDROBE-ACC-003 建议、确认值与未知分离

- 覆盖需求：`WARDROBE-REQ-003`、`WARDROBE-REQ-004`。
- 设计依据：Design 05“场景二”。
- 适用层级：本地核心的数据语义；识别供应方式可替换。
- 前置条件：模型建议颜色为蓝色、正式度为休闲，用户改为蓝绿色、商务休闲；另有无法判断材质和保暖的样本。
- 操作步骤：保存用户修正，模拟模型版本升级和重新处理；再参与筛选与推荐准备。
- 期望结果：建议值、来源、置信度、用户确认值和未知状态可区分；重新处理只产生新建议，不覆盖确认值；未知属性不以默认值补齐，也不作为硬过滤事实。
- 边界情况：用户撤销确认、模型不可用、建议 schema 升级和旧建议字段缺失时，结构化衣物仍可查看、编辑和保存。
- 验收证据：领域映射测试、GRDB migration/Repository 测试、模型版本 fixture、ViewModel 测试和 UI 截图。
- 结果：`pending`。

### WARDROBE-ACC-004 疑似重复由用户决定

- 覆盖需求：`WARDROBE-REQ-001`、`WARDROBE-REQ-008`。
- 设计依据：Design 05“场景四”。
- 适用层级：本地核心。
- 前置条件：两件外观近似但状态、历史或来源不同的白色 T 恤。
- 操作步骤：触发疑似重复提示，分别选择保留两件、取消新增和明确合并；对保存操作重复提交。
- 期望结果：系统只提示不自动合并；选择保留时产生两个稳定 ID，状态和历史互不覆盖；取消不改变原衣物；合并仅在用户明确确认范围后执行且可验证。
- 边界情况：图片相同、图片缺失、离线、并发编辑或模型相似度变化均不得触发隐式覆盖或删除。
- 验收证据：Repository 唯一性/幂等测试、并发测试、UI 对比录屏和合并影响清单。
- 结果：`pending`。

### WARDROBE-ACC-005 现实状态、历史快照与未来冲突

- 覆盖需求：`WARDROBE-REQ-005`、`WARDROBE-REQ-006`、`WARDROBE-REQ-011`。
- 设计依据：Design 05“场景三”。
- 适用层级：本地核心，首发必选。
- 前置条件：同类衣物分别处于可穿、待洗、借出、已打包和归档；已有历史搭配和未来计划。
- 操作步骤：在普通和匹配旅行场景请求候选；编辑名称、状态和归档；打开历史和未来计划。
- 期望结果：普通新推荐只使用可穿衣物；已打包仅在用户明确匹配场景中可用；当前编辑不改写历史最小文字快照；未来计划显示状态冲突，由用户选择替换、恢复状态或保留计划，不静默替换。
- 边界情况：状态在推荐与保存之间变化、衣物离线编辑或同时在另一页面更新时，版本复核阻止无提示保存；历史始终与当前状态分离。
- 验收证据：领域过滤属性测试、GRDB 并发/版本测试、历史快照断言和端到端 UI 旅程。
- 结果：`pending`。

### WARDROBE-ACC-006 移除照片但保留衣物与无图历史

- 覆盖需求：`WARDROBE-REQ-009`、`WARDROBE-REQ-010`。
- 设计依据：Design 05“场景五”。
- 适用层级：本地核心；存在云端条件任务时扩展为跨端删除。
- 前置条件：单品含规范化图、前景图、缩略图、本地特征、历史引用和条件任务引用。
- 操作步骤：选择“移除照片但保留衣物”，注入一次删除失败后重试；查看详情、推荐、历史和关联任务。
- 期望结果：照片立即隐藏，全部直接派生媒体与特征进入删除范围；衣物名称、属性、状态和历史引用保留；历史降级为文字、色块或无图占位，绝不保留已删除的图片快照；引用该图的条件任务被阻断、取消或清理。
- 边界情况：对象已不存在、进程终止、迟到结果、重复删除和部分清理失败时，照片不重新可见，也不误删其他衣物或历史事实。
- 验收证据：媒体血缘遍历、数据库/文件差异、任务竞态测试、幂等删除报告和 UI 录屏。
- 结果：`pending`。

### WARDROBE-ACC-007 商品图主动确认与零隐式上传

- 覆盖需求：`WARDROBE-REQ-012`、`WARDROBE-REQ-013`、`WARDROBE-REQ-019`、`WARDROBE-REQ-021`。
- 设计依据：Design 05“场景七”“场景十”。
- 适用层级：本地核心输入边界；任何云端用途另行验收。
- 前置条件：合规商品图、含订单号/地址/价格/页面文案的购物截图和含 GPS/EXIF 的图片夹具。
- 操作步骤：主动选择图片，分别确认和否认“该衣物属于本人衣橱”；完成基础录入、浏览、筛选和状态维护并抓包、扫日志与持久化字段；尝试系统分享、文件导出和公开链接。
- 期望结果：未确认拥有时不创建衣物；确认后先在本机移除可识别人物、价格、订单、地址和无关页面内容，只保存必要衣物工作图和用户确认事实；无法安全净化时不保存原图；原文件名和元数据不成为衣物事实；基础操作无图片、属性或标签上传，也不默认公开素材。Look 图片主动输出按 AVATAR-REQ-044 单独验收，不由衣物录入自动触发。
- 边界情况：来源不明、图片含人物或多个重叠主体时，提供换图或无图新增，不自动保存人物区域，也不扩大照片权限；系统分享表、文件协调器和深链不能绕过首发限制。
- 验收证据：网络抓包、metadata/log/persistence 扫描、净化前后差异、UI 测试、沙盒文件清单和分享/导出入口扫描。
- 结果：`pending`。

### WARDROBE-ACC-008 离线、中断与可访问性

- 覆盖需求：`WARDROBE-REQ-002`、`WARDROBE-REQ-009`、`WARDROBE-REQ-014`。
- 设计依据：Design 05“场景六”“场景八”“场景九”。
- 适用层级：本地核心，首发必选。
- 前置条件：飞行模式；图片缺失与损坏样本；最大 Dynamic Type、VoiceOver 和深色模式开启。
- 操作步骤：新增无图衣物、编辑属性、筛选、修改待洗/可穿状态；在保存和媒体写入窗口杀进程并重启；完成重复比较。
- 期望结果：核心路径完全离线；事务完整提交或回滚，无悬空媒体；图片丢失只降级无图，不删除衣物；文本替代、状态、错误和主要操作均可读可操作且不只依赖颜色。
- 边界情况：数据库初始化失败、低存储和辅助字号下弹窗空间不足时，提供保留数据的恢复路径，不自动清空或隐藏衣橱。
- 验收证据：故障注入、GRDB 事务测试、重启文件清单、XCUITest、Accessibility Inspector 和真机录屏。
- 结果：`pending`。

### WARDROBE-ACC-009 输入模式与 OOTD 双目的安全隔离

- 覆盖需求：`WARDROBE-REQ-015`、`WARDROBE-REQ-016`、`WARDROBE-REQ-018`。
- 设计依据：Design 05 的输入流程、端侧质量门和领域边界；Design 04 的本人照片规则。
- 适用层级：本地 OOTD/衣物输入核心与人物照片条件能力的共同门禁。
- 前置条件：准备本人成人单人 OOTD、多人 OOTD、单件衣物图和商品图夹具；同一 OOTD 可分别选择衣物拆分和人物参考目的。
- 操作步骤：逐种模式输入夹具；在 OOTD 路径不确认本人且 18+、选择多人图、只选衣物拆分和同时选择人物参考；检查净化输出与资产血缘。
- 期望结果：本人 OOTD、单件衣物和商品图使用三套明确规则，一种模式的允许内容不扩展到另一种；本地无照片核心不要求年龄声明，但 OOTD 选图前确认本人且 18+；多人阻断并先完成元数据净化；未选择人物参考时不保留可复用人物源图、人体派生物或数字形象。
- 边界情况：用户在确认中途改变目的、只保存部分衣物或重启恢复时，目的状态仍明确；不得以衣物草稿存在为由延长人物素材生命周期。
- 验收证据：输入模式参数化 UI 测试、质量结果码、metadata dump、媒体血缘/文件差异和零非预期上传抓包。
- 结果：`pending`。

### WARDROBE-ACC-010 两分钟首次价值与事实创建边界

- 覆盖需求：`WARDROBE-REQ-017`、`WARDROBE-REQ-020`。
- 设计依据：Design 05 的 OOTD-first 快速添加、批量原子提交与领域关系。
- 适用层级：本地核心产品门禁。
- 前置条件：空衣橱；获授权的本人成人 OOTD 或等价合成输入；从用户主动开始录入计时。
- 操作步骤：形成至少两件衣物草稿，逐项确认并原子保存组合草稿；在另一轮只完成自动识别或留下未确认/半保存草稿；随后分别主动保存为计划和主动确认某日实际穿着。
- 期望结果：只有两分钟内至少两件衣物确认成功且组合草稿原子保存成功才计为首次价值完成；未确认草稿、半保存和仅自动识别均不计入；OOTD 导入本身不创建 `OutfitPlan` 或 `WearEvent`，只有后续明确保存计划或确认日期与“实际穿了”才创建对应事实。
- 边界情况：第二件保存失败、组合事务失败、重复提交、杀进程和跨日时，不得虚报激活、生成半组合或误计实际穿着；重试不重复已确认衣物。
- 验收证据：计时口径测试、GRDB 原子事务/故障注入、草稿/衣物/组合/计划/实际记录计数和端到端录屏。
- 结果：`pending`。

## 需求覆盖矩阵

| 需求 | 验收场景 |
| --- | --- |
| `WARDROBE-REQ-001` | `WARDROBE-ACC-001`、`WARDROBE-ACC-004` |
| `WARDROBE-REQ-002` | `WARDROBE-ACC-001`、`WARDROBE-ACC-008` |
| `WARDROBE-REQ-003` | `WARDROBE-ACC-002`、`WARDROBE-ACC-003` |
| `WARDROBE-REQ-004` | `WARDROBE-ACC-002`、`WARDROBE-ACC-003` |
| `WARDROBE-REQ-005` | `WARDROBE-ACC-005` |
| `WARDROBE-REQ-006` | `WARDROBE-ACC-005` |
| `WARDROBE-REQ-007` | `WARDROBE-ACC-002` |
| `WARDROBE-REQ-008` | `WARDROBE-ACC-004` |
| `WARDROBE-REQ-009` | `WARDROBE-ACC-001`、`WARDROBE-ACC-006`、`WARDROBE-ACC-008` |
| `WARDROBE-REQ-010` | `WARDROBE-ACC-006` |
| `WARDROBE-REQ-011` | `WARDROBE-ACC-005` |
| `WARDROBE-REQ-012` | `WARDROBE-ACC-007` |
| `WARDROBE-REQ-013` | `WARDROBE-ACC-007` |
| `WARDROBE-REQ-014` | `WARDROBE-ACC-002`、`WARDROBE-ACC-008` |
| `WARDROBE-REQ-015` | `WARDROBE-ACC-009` |
| `WARDROBE-REQ-016` | `WARDROBE-ACC-009` |
| `WARDROBE-REQ-017` | `WARDROBE-ACC-010` |
| `WARDROBE-REQ-018` | `WARDROBE-ACC-009` |
| `WARDROBE-REQ-019` | `WARDROBE-ACC-007` |
| `WARDROBE-REQ-020` | `WARDROBE-ACC-010` |
| `WARDROBE-REQ-021` | `WARDROBE-ACC-007` |
| `WARDROBE-REQ-022` | `WARDROBE-ACC-011` |
| `WARDROBE-REQ-023` | `WARDROBE-ACC-012` |
| `WARDROBE-REQ-024` | `WARDROBE-ACC-013` |
| `WARDROBE-REQ-025` | `WARDROBE-ACC-014` |
| `WARDROBE-REQ-026` | `WARDROBE-ACC-015` |
| `WARDROBE-REQ-027` | `WARDROBE-ACC-016` |
| `WARDROBE-REQ-028` | `WARDROBE-ACC-017` |
| `WARDROBE-REQ-029` | `WARDROBE-ACC-018` |
| `WARDROBE-REQ-030` | `WARDROBE-ACC-018` |
| `WARDROBE-REQ-031` | `WARDROBE-ACC-018` |
| `WARDROBE-REQ-032` | `WARDROBE-ACC-019` |
| `WARDROBE-REQ-033` | `WARDROBE-ACC-019` |
| `WARDROBE-REQ-034` | `WARDROBE-ACC-019` |

### WARDROBE-ACC-018 账号下无图结构化衣物 CRUD

- 覆盖需求：`WARDROBE-REQ-029`～`031`；执行：[17-20](../plan/17-20-结构化衣橱账户API执行计划.md)。
- 前置条件：两个隔离账号，分别持有有效会话；只使用合成名称与 UUID，不提供照片或真实用户数据。
- 操作步骤：创建同一账号衣物并重复提交；用同 ID 提交不同内容；分页列出和读取；按当前/过期 revision 更新和删除；用另一账号访问同一 ID；最后删除账号并查询数据库。
- 期望结果：同内容创建幂等，冲突不改写；分页排序稳定且有界；来源不可修改，更新 revision 加一；过期修改/删除返回冲突；跨账号统一 404；运行时 OpenAPI 不接受 owner、照片或资产字段；账号删除级联清理。
- 边界情况：无会话清理过期 Cookie；非法 UUID/枚举/名称/limit 在 Service 前或业务校验处失败；不存在或跨 owner 的游标/单项不泄露资源存在性。
- 验收证据：领域/Service/HTTP/OpenAPI 自动测试、真实 PostgreSQL schema/事务/约束测试，以及实际进程或远程 CI 结果。
- 验收结果：`passed（后端开发范围）`。功能提交 `cd6b63de60e3d09473e96c9a06beddd1bf27e6c8`；本机 unit/race、真实 PostgreSQL services、实际 API 进程均通过；GitHub Actions [CI 34969171732](https://github.com/StephenQiu30/then-server/actions/runs/34969171732) 的 Go quality、PostgreSQL integration、OCI container 全绿。App 尚未接入，不能认领跨设备同步完成。

### WARDROBE-ACC-019 账号衣橱用户确认属性

- 覆盖需求：`WARDROBE-REQ-032`～`034`；执行：[17-21](../plan/17-21-衣橱确认属性API执行计划.md)。
- 前置条件：两个隔离账号；使用合成 UUID/名称，准备四项全非空、部分非空、全未知及越界枚举请求。
- 操作步骤：创建含属性衣物并重复提交；只改变一项属性再次创建；读取/分页；按当前 revision 完整更新并清除原属性；重建 Repository 后读取；提交过期 revision、请求来源和数据库越界值。
- 期望结果：四项值与未知原样保存；非空响应逐项显示 `user_confirmed`，未知返回 NULL；请求不能提交来源；属性参与幂等比较；PUT 原子完整替换并 revision +1；跨 owner、冲突或约束失败不产生部分写入。
- 验收证据：Service/HTTP/OpenAPI 自动测试、真实 PostgreSQL nullable/CHECK/重读测试、实际 API 二进制属性 HTTP 生命周期和远程 CI。
- 验收结果：`passed（后端开发范围）`。功能提交 `db24d4cf58db807f068c3bfc425f28d099aac7d6`；本机 `go test ./...`、`go vet ./...`、`go mod verify`、完整 race、真实 PostgreSQL services 和实际二进制 integration 均通过；OpenAPI 3.1.2 / API 0.9.0 保持 24 个 operation。GitHub Actions [CI 35008800654](https://github.com/StephenQiu30/then-server/actions/runs/35008800654) 的 Go quality、PostgreSQL integration、OCI container 全绿。App 尚未接入，不能认领同步或完整产品发布通过。

## 单件图片当前开发验收证据

以下是 2026-09-15 已记录的实际结果，环境为现有 iPhone 17 / iOS 26.5 Simulator `CB37F50E-D231-4FE1-A1C8-B461DDB6B506`。本次文档同步没有重跑。

| 证据 | 实际范围和结果 | 可认领范围 |
| --- | --- | --- |
| ThenWardrobeMVPContract20260915-01.xcresult | 3 suites、25 tests，25 通过、0 失败/跳过；真实 CPU Vision 与复核/编辑/原子保存 | 零检出仍 needsConfirmation；任一条件缺失、检测失败或人物/面部检出不能保存 |
| ThenWardrobeImageContract20260915-02.xcresult | 8 suites、83 tests，82 通过、1 物理 Data Protection 跳过、0 失败 | IMAGE-01 模拟器范围完成；三格式/八方向、预算、元数据、文件身份/竞态、复核、原子保存/替换/删除/重启 |
| ThenWardrobePhotoUI20260915-06.xcresult / ThenAppUIFull20260915-01.xcresult | 系统 PhotosPicker 1/1、完整 UI 8/8 通过，0 失败/跳过 | IOS-01 开发范围完成；新增→系统选图→四项复核→保存→重启→移除→删除；对比度/文字截断未过滤 |
| THEN-WARDROBE-PHOTO-NETWORK-20260915-01 | 提交 9736b3c，ThenWardrobePhotoNetwork20260915-03.xcresult 1/1 通过；nettop 31 条进程采样 TCP/UDP 收发均为 0 | TEST-01 模拟器范围完成；不能扩展为真机系统抓包或永久零网络保证 |

内容检测负证据仍有效：含手部合成图 people=0/faces=0；Vision 手部四组探针零检出、默认推理上下文失败、通用分类器没有判别力。它们未进入生产，不能宣称自动识别所有身体部位。首版通过的是“明显人物/面部阻断 + 当前图片四项人工确认”契约；MediaPipe 为后续增强候选，非本片阻断。

Debug 与通用 Release Simulator 构建已有通过结果。Release 包 43 文件、29,055,921 bytes；不包含测试素材/测试标记，可执行 SHA-256 `17216489b035f536b390b7a286c07a539b5f5b87700cacce9aa433e1e982341f`。网络 CSV SHA-256 `88e6a564b1a0499a536d49a4caa0b645bb51ac92885ccea2ff9316e482d197e7`；2026-09-22 已将完整证据包从 then-app 的 DerivedData 移至 then-server 私有 `frontend/assets/acceptance/wardrobe-photo-network-20260915/`，CSV、evidence.json 与 xcresult 字节保持一致，不随公开仓库交付。其余结果包原在 `/tmp`；本机证据不保证长期保留，丢失时须按相同用例重跑，不能仅凭文件名认领当前版本通过。

记录的 App CI `34885280097` 通过标准构建/测试，不预置相册、不覆盖上述完整图库旅程。Instruments Network 不支持 Simulator 的失败记录不计通过。WebKit 为隔离本地 Three.js 使用 non-persistent store、avatar scheme 白名单和 CSP；静态扫描没有业务网络调用只是辅助证据。

REL-01 仍要求真机网络、Data Protection、锁屏、低存储、最低设备性能与人工 VoiceOver。正式 Woo 画风、生产 GLB、推荐与云生成不由图片测试抵扣。

## 当前完整 Look 与二维选衣验收

2026-09-22：WARDROBE-ACC-011、WARDROBE-ACC-012、WARDROBE-ACC-013、WARDROBE-ACC-014、WARDROBE-ACC-015、WARDROBE-ACC-016、WARDROBE-ACC-017 原模块服装/rig/即时换装合同为 superseded，旧 pending 不改为 passed。现行来源分离、快照/保存与失败由下列场景及 AVATAR-ACC-029/030 接续；上文真实衣橱和账号验收证据保持其原范围。

### WARDROBE-ACC-020 二维衣橱与来源分离

- 覆盖 WARDROBE-REQ-035/036；内置参考只读，有来源标识，不写入用户衣橱。用户手工/单件照片建档保持原确认规则；无图仍能管理/推荐。
- 内置完整 Look 零录入离线可选；新选衣只改草稿/托盘，不冒充已更新图片/模型，不自动调用 Provider。
- 逐项选取/替换/移除/取消、无支持图片/数量超限/类别不支持明确提示；不自动补默认衣物或丢弃鞋/外套。
- 证据：真实库计数/来源、原生交互、网络记录；结果 pending。

### WARDROBE-ACC-021 选衣版本与生成快照

- 覆盖 WARDROBE-REQ-036/037；提交时复核选衣 ID/revision/图片、冻结快照；完成后模型只属于对应 Look 图版本，不反写单品属性或真实几何。
- 选衣修改、新图/旧模型、源删除、任务晚到、取消和重启分别验证；结果不得冒充新组合，保存不增加拥有/穿着事实。
- 真实衣物照片删除清理可达派生图/GLB但不擅自删除独立结构化衣物和历史；共享内置母版不受个人删除影响。
- 证据：快照、媒体血缘、文件/数据库差异和故障恢复；结果 pending。
