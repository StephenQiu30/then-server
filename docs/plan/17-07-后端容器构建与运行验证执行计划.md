# 后端容器构建与运行验证执行计划

## 状态与追踪

- 契约状态：`approved`；承接用户当前“按项目规范和 SOP 实现服务”的授权，落实已批准单 module / 单 binary / 单镜像设计的本地构建验证，不授权生产发布。
- 执行状态：`completed`。按 design → PRD → plan 完成清理所有权修复；Go、真实数据库、镜像重建及容器复验全部通过。
- 上游：[PRD 17](../prd/17-云端生成与任务管理需求.md) CLOUD-REQ-006、[Design 01](../design/01-技术选型.md)、[Design 02](../design/02-后端架构.md)。
- 依赖：17-01 的 Go 运行子集及已通过的数据库/退出测试；17-01 的 Swift Client 阻断独立保留，不能据此发布完整产品。

## 范围契约

使用既有 Go 1.26.5 编译根 main.go，构建一个静态 Linux binary。多阶段构建的最终 scratch 镜像仅包含 binary、CA 证书及模块清单；默认 UID/GID 65532，不包含 shell、Go 工具链、源码、环境文件或测试照片。容器显式设置 HTTP_ADDR，默认行为沿用现有配置；api 可用，worker/all 继续拒绝启动。

本地测试以只读 rootfs、cap-drop ALL、no-new-privileges、256 MiB 内存、64 PID、1 CPU 运行。预算只作为本次健康 API 的测试限制，不作为生产容量承诺。实际镜像连接隔离 PostgreSQL，验证 live/ready、SIGTERM 退出及非法角色拒绝。

不改 OpenAPI、业务数据、迁移、app 模块或云能力开关；不引入 Compose 集群、管理后台、第二个二进制、自动 migration 或线上镜像推送。

## 逐文件影响面

| 文件 | 输入、输出与职责 | 验证 |
| --- | --- | --- |
| backend/Dockerfile | 锁定 builder digest，go.mod/go.sum 解析与静态编译；输出单一 scratch runtime | 实际 docker build、镜像配置与运行测试 |
| backend/.dockerignore | 构建上下文只允许 go.mod/go.sum/main.go/openapi.yaml/internal 源码；排除测试及其余文件 | 显式 COPY，最终镜像文件审查 |
| backend/tests/container_integration_test.go | container build tag；显式镜像输入，创建隔离 DB/API，验证受限运行与退出 | 实际 Docker，不以 HTTP mock 替代 |
| backend/README.md / .github/workflows/ci.yml | 构建、验证与测试命令，记录适用范围 | 命令与源码一致 |

## 实施任务

| ID | 任务 | 状态 | 完成证据 |
| --- | --- | --- | --- |
| 17-07-01 | 构建与自动验证入口、容器验证 | completed | BE-CONTAINER-20260908-02：失败创建清理、真实数据库、镜像重建与容器复验通过，旧遗留资源确认清除 |
| 17-07-02 | 文档、证据与限制同步 | completed | Design 01/02、README、计划索引、acceptance 17 已同步 |

## 失败、资源与回滚

测试镜像名显式传入，缺失直接失败，不自动拉取任意后端镜像。PostgreSQL 使用 17-01 相同固定 digest 与随机合成凭据。API 与 DB 由测试清理，仅本地 loopback 共享网络用于隔离明文 DB 连接，不推广到生产。生产 DB 仍要求 verify-full。

本切片未写业务数据；回滚为停止测试容器、移除本次本地镜像及回退构建文件。测试镜像不发布。正式发布仍需完整 SBOM/许可证/漏洞、生产 TLS/容量/可用性及 Swift 契约构建门禁，本次模块清单不是完整 SBOM。

## 完成判定

backend 验证脚本、既有真实 DB 测试、实际镜像构建及容器测试通过；确认镜像非 root、只读运行和 SIGTERM 退出；记录镜像 ID、平台、命令和限制。只关闭本地构建切片，不改变 17-01 与生产云验收状态。

## 完成证据

见 [BE-CONTAINER-20260908-01](../acceptance/17-云端生成与任务管理验收.md#17-07-本地容器构建与运行验证)。镜像 ID 为 `sha256:12305b8287eacb40d572c5face3efcad14da753bf022a7f67777608dd825f0f1`，linux/arm64，9,631,796 bytes；不是远程 registry digest，也未验证 linux/amd64。

## 当前实施任务影响面

设计已记录于 Design 02，需求细化于 PRD 17；本次直接落实既有失败清理契约，契约 approved 不变。

- 新增 tests/container_support_test.go，限定 integration 或 container 测试：统一在构造返回非空句柄后注册 t.Cleanup，再将结果/错误返回调用方。独立 context 有界释放，失败使测试失败。
- 修改两个真实容器测试文件复用此所有权逻辑，移除重复释放；无生产代码、API、schema 或依赖版本变更；已有测试依赖的 direct 标记由 go mod tidy 校正。
- 增加真实启动失败回归：使用已构建镜像触发不可能满足的启动等待，检查返回错误后所属容器确实被清理。
- 复验旧测试资源、Go/race、真实数据库及容器成功/失败流程；Docker 故障与代码缺口分别记载，不用重试掩盖失败。

## 最新完成证据

BE-CONTAINER-20260908-02 记录本轮设计、PRD 回写、计划实施及验收结果。最新镜像 ID `sha256:1df65829832abf17c9f27f7f072b194e997a82732fcb2f93c589398f23c9693e`。一次模块代理 EOF 导致构建失败；原配置再次执行构建成功且最终容器用例通过，失败历史不删除。17-01 的 Swift 阻断保持独立，不因本切片通过而关闭。
