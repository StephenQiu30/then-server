# Go 工程结构与测试分层执行计划

## 状态

- 契约状态：approved。依据：2026-09-16 用户要求调研标准 Go 工程结构、规范 backend 目录，并解决 test 与业务代码边界不清。
- 执行状态：completed（本地验证完成；远程 CI 随本次推送复核）。

## 依据与决定

- [Go 官方模块布局](https://go.dev/doc/modules/layout)给出 server project 的 `cmd`/`internal` 组织；本项目采用用户确认的 `cmd/then-server`，让进程入口稳定且可发现。
- [Go 官方测试教程](https://go.dev/doc/tutorial/add-a-test)与 [`testing`](https://pkg.go.dev/testing) 约定测试文件使用 `_test.go` 并与被测包同目录，由 `go test` 单独编译。包内单元/契约测试因此保留。
- 通过 GitHub、Firecrawl 与 Context7 交叉核对 Kubernetes、Grafana、Prometheus、Loki、CloudWeGo 和 Go 官方资料；研究与取舍固定在 [Design 24](../design/24-Go后端工程结构研究与规范.md)。
- 当前保持一个模块化单体，采用 `command → bootstrap → application/adapter → domain`；application 按业务域拆分，adapter 按技术拆分。
- 真正需要修复的是 `backend/tests` 曾把本机依赖、实际进程和镜像测试放在同一个目录及 package。它们拆为独立 suite，根目录不再放 Go 文件。

## 目录合同

```text
backend/
├── cmd/then-server/main.go
├── internal/
│   ├── bootstrap/
│   ├── domain/
│   ├── application/{account,privacy,wardrobe,outfitplan,wearevent,media,mediaworker}/
│   ├── adapter/{httpapi,postgres,objectstore,messagequeue}/
│   └── platform/
├── tests/
│   ├── services/       # -tags=services，本机真实依赖
│   ├── integration/    # -tags=integration，实际二进制与隔离容器
│   ├── container/      # -tags=container，已构建 OCI 镜像
│   └── internal/       # 测试 suite 共享夹具
└── architecture_test.go
```

不创建 `controller/dao/entity` 的 Java 式镜像目录，不增加空 `pkg`、`common`、第二个 module 或 test 微服务。`cmd/then-server` 对应当前真实 binary，不是占位目录。

## Checklist

| ID | 工作 | 状态 | 完成条件 |
| --- | --- | --- | --- |
| 17-24-RES-01 | 核对官方 Go 布局、测试模型和成熟仓库实践 | completed | 决策引用官方一手资料，区分官方规则与社区模板 |
| 17-24-CMD-01 | 建立 cmd 与 bootstrap 进程边界 | completed | main 只导入 bootstrap；Docker、CI 与 integration 构建同一 command |
| 17-24-DOM-01 | 业务与技术包二次拆分 | completed | application 按领域、adapter 按技术拆分，旧横向大包删除且无兼容 alias |
| 17-24-TEST-01 | 拆分外部测试 suite | completed | services/integration/container 独立目录和 package，公共容器资源归 tests/internal |
| 17-24-ARCH-01 | 固化生产与测试边界 | completed | 架构测试拒绝生产源码导入 testing/httptest/testcontainers，并拒绝测试回流根目录或错误 tag |
| 17-24-CI-01 | 同步 CI、README、AGENTS 与 Design | completed | 所有活动入口使用 `./tests/...`，规范说明包内 `_test.go` 的工具链边界 |
| 17-24-VAL-01 | 执行格式、unit、vet、race 和三层 suite 验证 | completed | 格式、module、vet、unit、race、services、integration 通过；本地镜像完成受限 container 验收 |

## 验证合同

```sh
cd backend
gofmt -l .
go mod verify
go vet ./...
go test ./... -count=1
go test -race ./... -count=1
go test -race -tags=services ./tests/... -count=1
go test -race -tags=integration ./tests/... -count=1
go test -run '^$' -tags=container ./tests/... -count=1
```

只有显式构建本地镜像并设置 `THEN_BACKEND_TEST_IMAGE` 后，container 运行验收才记为通过。目录调整不改变 API、数据库 schema 或用户数据。

2026-09-16 本地验证：`gofmt -l .` 无输出，`go mod verify`、`go vet ./...`、`go test ./... -count=1`、完整 race、services 与 integration 均通过；`then-server:codex-20260916` 构建为 manifest list `sha256:f8d5641e463ca3a69259d63d923c1863f1b20abe9532fa32183f99b14aec61c8`，受限 container 测试通过。远程 CI 结论在推送后单独记录。
