# 完整穿搭图与按需三维生成执行计划

## 状态与授权

更新：2026-09-24。设计契约 `approved_with_gates`，执行 `in_progress（后端 DATA-01）`。用户本轮授权本地优先的图片/三维验证路线；本轮只实现 Provider 无关的任务业务逻辑，不调用外部 Provider、不购买额度、不启用真实照片或发布。

追踪：Design 07/10/20 → TRYON-REQ-008～010、AVATAR-REQ-043/044、CLOUD-REQ-007～009、OOTD-REQ-018/019 → TRYON-ACC-011/012、AVATAR-ACC-031、CLOUD-ACC-012、PRIVACY-ACC-011、OOTD-SYS-ACC-018。与 11-05 共享 Look/revision，不再建平行渲染集/试穿结果域。

共用规则覆盖TRYON-REQ-001～006及TRYON-RULE-001～016的当前图片/模型适用部分，CLOUD-REQ-001～006与CLOUD-RULE-001～024的认证、接纳、状态、额度和删除部分；各自映射TRYON-ACC-001～008与CLOUD-ACC-001～012。视频专属路径仍deferred，不在本片实现。

## 交付旅程

用户选人物/衣物 → 查看用途与费用并确认 → 提交图片任务 → 离开并恢复 → 核对和保存完整图 → 主动生成立体 → 等待仍可使用图片 → GLB ready 后旋转 → 再次查看命中缓存 → 改衣物形成新图版本 → 删除及迟到结果清理。只需图片的用户可在图片阶段结束，并主动保存/系统分享图片。

## 输入与功能边界

- 人物来自有权处理的内置虚构成人参考或经 11-01 准备的本人照片；真实本人入口需原生产照片门禁，不能借合成测试开放。
- 图片本地 POC 默认使用确定性的 `fixture` Provider；视觉质量验证只允许调用登记过的本地图片服务，最多四参考：一个人物加至多三件支持类别衣物。超过范围明确说明，不静默删项或自动变成多轮付费生成。
- 模型只从用户已确认的图生成，固定走本地 `img2threejs` → Three.js/材质烘焙 → 静态 GLB 导出与校验；无骨架、眼部、parts、retarget 和视频调用。核心工具若只产生代码工厂而不能导出带纹理 GLB，则该样本只能记录为模型 POC 不通过，不能用云 Provider 补齐。
- 本地 Codex CLI 只可作为受控开发编排器，不能直接充当图片 Provider；没有可返回图片字节的本地服务合同时，图片路线停在 fixture。
- Provider 终端服务许可、可用地域、保留/删除、价格/账号额度待核实；获准的合成素材 POC 与真实用户上线分开，不用免费网页体验替代 API 生产资格。

### 本地低成本 POC 路线（2026-09-24）

- 只运行现有 `then-server`，复用现有开发 PostgreSQL、Kafka 和 MinIO；不创建第二套数据库、第二个 Go module 或新的服务入口。
- image：`fixture` 用于单元/集成测试；可选 `local-image` 适配器只接收本地服务返回的图片字节或受控本地文件，不允许公共 URL、未定义的 Codex 工具调用或云端回退。
- model：worker 在临时目录调用固定版本 `img2threejs`，保存输入 hash、工具版本、spec/factory hash、GLB hash、纹理/面数/字节预算和校验结果；任务完成前不发布资产。
- local POC 的供应商费用为 0；仍记录本机计算时间、失败和资源限制。Codex 使用远程模型账户、托管 img2threejs 转换器或远端 GLB 插件时，按实际账户/额度重新计费并重新走准入。
- 真实 Provider/本地图片服务合同和代表资产仍未获批；因此不创建 Provider adapter、不发起外部回调，也不开放真实素材。当前 DATA-01/WORKER-01 已先落地 Provider 无关的 GORM 任务、租约、提交状态和结算记录，后续仍需真实 PG/HTTP 验收及 Provider 准入后才能执行外部 worker。

## GATE-01 只读供应商核验记录

核验日期：2026-09-23。仅查阅供应商官方文档和条款，未创建供应商账号、调用 API、上传素材、购买额度或接入真实用户数据。以下结果用于决定是否可以进入 `POC-01`，不构成供应商准入或上线授权。

| 核验项 | 官方资料确认 | 本项目结论 |
| --- | --- | --- |
| Tripo API 调用形态 | [Quick Start](https://platform.tripo3d.ai/docs/quick-start) 要求服务端使用 Bearer API key，任务异步返回 `task_id`；[FAQ](https://platform.tripo3d.ai/docs/faq) 说明下载链接只有短时有效期。 | 未来 adapter 必须只在服务端持有密钥，接收任务后立即把结果收取到受控对象存储；不能把供应商临时 URL 当业务媒体地址。当前不实现调用。 |
| Tripo 资产与权利 | [资产安全说明](https://www.tripo3d.ai/help/privacy-policy/is-my-asset-safe) 提供 API 侧定时删除和默认私有设置；[Terms](https://www.tripo3d.ai/terms) 与[商业使用说明](https://www.tripo3d.ai/help/privacy-policy/how-to-use-tripo-models-commercially) 区分免费/付费计划的资产和商业使用条款，并说明付费用户输入/输出不用于模型训练。 | 仍需供应商负责人登记具体付费计划、商业许可、区域、保留/删除配置与合同版本；仅凭公开条款不能通过 `GATE-01`。免费网页体验不能替代生产 API 资格。 |
| Volcengine / Seedream | 官方 [Seedream 发布记录](https://docs.volcengine.com/docs/LakeAIService/FeatureReleaseRecord2026?lang=en) 列出 Seedream 5.0 系列及北京、上海地域；[API Key 实践建议](https://docs.volcengine.com/docs/IAM/APIKeyAccessKeyPracticeRecommendation?lang=en) 建议使用专用密钥并避免写入代码、仓库或客户端。 | 候选地域和密钥边界已有公开依据，但本项目仍缺实际账号、区域选择、价格/额度、数据保留与删除合同；不能据此开启计费任务。 |
| 本地素材与配置 | 当前仓库的 `frontend/assets/avatar-poc/woo-input-v1` 仍是旧 POC PNG，标记为非生产资源；2026-09-23 逐个读取 `then-app/ThenApp/Resources/AvatarStudio/Models/*.glb` 的 GLB JSON chunk，现有 8 个文件均为 `images=0`、`textures=0` 的 JSON+BIN 无纹理分件资产；仓库没有获批的带纹理静态 GLB，后端也没有启用 Provider 凭据或计费开关。 | `GATE-01`、`POC-01` 保持 `pending`；现有工程 GLB 不作为完整 Look/模型 POC 输入；`DATA-01` 只实现 Provider 无关记录和 API，不创建 Provider 调用或猜测供应商字段。离线 Look 与本地衣橱记录继续按已完成后端范围运行。 |

核验退出条件：补齐测试账号、地域、预算/额度、素材授权、服务许可、保留/删除与合同版本后，才能在隔离账号和合成素材范围内登记 `GATE-01`；通过后才进入代表样本和至少 12 组输入的 `POC-01`。在此之前保持 image/model 接纳关闭。

### 当前统一配置与费用边界（2026-09-24）

`backend/internal/platform/config` 是生成相关运行配置的唯一解析入口。默认 `GENERATION_MODE=off`、`GENERATION_ENABLED=false`、`GENERATION_PROVIDER_CALLS_ENABLED=false`，本地开发、单元测试和 CI 不会向第三方图片/模型服务发起请求，也不会产生供应商生成费用。`GENERATION_MODE=local` 只允许 fixture 或回环 `GENERATION_LOCAL_IMAGE_ENDPOINT`，货币/外部预算必须为零；`remote` 仍是有门禁的后续意图。预算、配额、并发、最大提交次数、Provider 超时和保留期集中由 `GENERATION_*` 配置读取；缺少启用所需的边界值时启动失败关闭。

当前把 `GENERATION_PROVIDER_CALLS_ENABLED=true` 作为显式高风险动作，并在供应商准入、代表素材 POC、真实 worker 和删除合同完成前拒绝启动。未来通过门禁后，该开关可能触发按供应商账户计费的请求；`GENERATION_MAX_BUDGET_MINOR_UNITS` 是产品侧预算上限，不等于供应商免费或最终账单承诺。163 邮件只有完整 163 配置且实际调用账号验证/找回接口才会发送，本切片的配置测试不发邮件；网易账号是否存在套餐或配额费用需按实际账号条款确认。

## 数据与 API 合同

Go 侧实现两种任务用途 image/model、Look/revision 与媒体引用、输入快照、owner、Provider/model/参数、外部任务 ID、同意收据、费用/额度及删除状态。客户端不提交 owner，不以签名 URL 作为业务 ID。任务身份、mutation 幂等和模型内容去重分别定义，后者按 owner+Look/revision+image hash+模型参数。

提交任务返回 202 和可查询 ID，认证/输入/同意/配额不合格明确拒绝；查询只见本人任务；取消和删除各自幂等。具体 operation、字段和错误码在同一 Huma 类型定义中实现，运行 `/openapi.json` 后生成实际 Client；此计划不新建另一份接口文件或空服务层。App 只接所需认证、上传、任务和媒体 API，完整跨设备衣橱同步不作为本片前置。

PostgreSQL 事务写任务、额度占用和 Outbox；Kafka 唤醒现有 Go worker。外部调用在事务外，租约与 fencing 防并发。获取 external task ID 后持久化并查询；提交超时是否已接纳未知时先对账，若供应商不能可靠查询/幂等，则暂停自动重提并显示可处理状态。

结果收取限制来源、重定向、大小和时限，图片净化/GLB 校验后落私有 MinIO；随后事务发布媒体和任务。旧 Look revision、已撤回同意或删除状态拒绝发布；失败留下可清理追踪，不复活。用户配额结算与供应商实际费用分开，不能承诺取消退款。

## App 与输出

SwiftUI 管理选衣草稿、真实任务阶段、取消/失败/重试，GRDB 保存必要任务引用与 Look 版本。前台退避轮询，后台由持久任务继续处理；返回页面恢复，不需要推送或 WebSocket。已确认图可独立保存，GLB 下载失败回图，命中缓存不创建新任务。

图片写入相册仅在用户点击时申请 Add Only；系统分享使用临时净化副本并清理 App 可控副本，不添加公开 URL。检查资产/人物/衣物的输出权利，告知外部副本不能由 App 远程撤回。下载/分享不生成 WearEvent 或偏好，社区发帖仍独立。

## Checklist

| ID | 状态 | 完成判定 |
| --- | --- | --- |
| 14-01-SPEC-01 | completed（文档） | 两类用途、版本、费用/删除与验收合同明确 |
| 14-01-LOCAL-POC-01 | pending | `fixture` 图片替身与本地 `img2threejs`→GLB 导出/校验合同在现有 then-server worker 中可复现；不创建新数据库，不调用云 Provider |
| 14-01-GATE-01 | pending | 具体测试账号、调用预算、素材授权与适用服务许可/地域核实；实际调用前关闭，不在本轮执行 |
| 14-01-POC-01 | pending | 单代表→至少 12 组图片输入；完整参考与全部失败/费用记录；模型质量单独测，不合格明确 no-go |
| 14-01-DATA-01 | in_progress（接纳/查询/取消/状态与结算持久化已实现） | 已落地 Provider 无关的任务身份、按用途约束的输入快照、owner/purpose 作用域幂等/去重分类、纯领域接纳编排（重放/内容去重、fail-closed 检查、任务+临时额度占用+单一 Outbox 意图），以及 GORM 任务/额度/输出记录、事务接纳、owner 隔离分页查询、取消请求、提交状态/租约/fencing 和成功或失败结算事务。隔离真实 PostgreSQL 已验证参数规范化回放、幂等/内容去重、额度预留、取消、跨账号隔离和任务/输出/额度原子结算；HTTP Handler 已用 Huma 实际路由测试覆盖会话、202 接纳、本人查询/分页/取消与关闭/冲突错误映射。生成模式仍默认关闭，真实 PG+HTTP 全链路、Provider 与故障矩阵验收待后续。 |
| 14-01-WORKER-01 | in_progress（租约/fencing/提交状态/有限退避/结算持久化已实现） | 已落地生成任务的获取/续租/释放、到期恢复递增 fencing token、旧 worker 条件写入拒绝，`in_flight`/`unknown`/`accepted` 提交状态、显式未受理对账、external task ID、非终态 Provider 状态观察，已知未受理后的 capped exponential `next_attempt_at` 与最大提交次数门禁，以及验证后输出元数据与成功/失败/取消/过期结算的原子持久化；真实 submit/query/fetch、Provider/object-store 适配器、私有产物校验发布编排仍待后续 |
| 14-01-DELETE-01 | in_progress（撤销/清理 manifest/源与账号级联已实现） | 已实现任务删除 HTTP 合同、访问撤销、迟到输出拒绝、对象版本与 Provider task 目标快照、有限清理重试、源媒体→任务与图→模型级联、账号注销等待生成清理完成；真实对象存储/Provider/cache 删除 worker 与外部回执仍 pending |
| 14-01-IOS-01 | pending | 必要 App 认证/媒体/任务 Client、真实状态恢复、图片先用与按需模型 |
| 14-01-EXPORT-01 | pending | 已确认图主动保存/系统分享、元数据与临时副本清理、外部删除说明 |
| 14-01-TEST-01 | pending | 真实 PG/Kafka/MinIO 故障恢复和 Provider 受控账单验证，图片/模型/当前版本一致 |
| 14-01-REL-01 | pending | 真实照片准入、终端服务许可、最低真机/隐私、独立开关、费用止损与关闭演练 |

## 必测失败与退出

重复点击/重投只得到一个有效任务；Provider 已接纳但响应丢失不盲重提；图片失败不丢草稿；模型失败保留图；A 版本任务晚到不覆盖 B；删除后迟到结果不发布；对象写入成功但数据库失败可回收；worker/队列中断可恢复；跨账号素材/任务/签名读取被拒绝；配额耗尽不新建外部任务。

实际集成的技术通过、模型质量通过、供应商账单通过和生产许可分别记录。代表性小样本不替代真实人群质量；本片未通过不阻断已合格的离线 Look 或真实衣橱。详细价格只引用 Design 20，避免多处金额漂移。

## 执行顺序、依赖与交付物

质量标准引用 `OOTD-NFR-001～013`。App 认证由 [17-30](17-30-App账户与云端访问执行计划.md) 提供，Look 持久化/渲染由 [11-05](11-05-三维人物与造型闭环执行计划.md) 提供；POC、领域合同与后端测试替身可先行，不因账号 UI 未完成而调用真实照片。

| 顺序 / 既有任务 ID | 依赖 | 操作 / 责任 | 可审查交付与退出标准 |
| --- | --- | --- | --- |
| 1 / GATE-01 | 用途和账号明确 | 产品/供应商负责人登记素材来源、服务许可、地域、处理/删除、价格版本及 POC 总预算 | 准入记录完整，未满足的模式关闭；不因文档 approved 自动执行费用动作 |
| 2 / POC-01 | GATE-01 | 产品+资产按 PRD 14 先代表、再 ≥12 输入组，固定评分表/参数/最多 2 次计费尝试/样本 | 全量尝试台账、可开放类别/组合、失败样例与 no-go 原因；图片与 GLB 分别判定，未通过不扩大测试费用 |
| 3 / DATA-01 | 11-05 的 Look 合同；POC 确认实际字段 | 后端定义最小任务/版本/血缘/配额和去重事务；Huma 输入输出/错误，运行时 Client 契约验证 | 同意/owner/版本/额度检查在接纳前；同幂等键返回同任务，改输入冲突；真实 PG 约束与跨账号用例通过 |
| 4 / WORKER-01 | DATA-01；POC 供应商合同 | 后端实现 submit/query/fetch、有限退避、租约/fencing、未知提交对账、私有产物校验发布 | 中断任何阶段能找到原任务；不确定受理时停止新计费 POST；已明确未受理才按最大次数和 capped exponential 窗口重试；对象成功/DB 失败可追踪回收；不是无操作 stub |
| 5 / DELETE-01 | WORKER-01；已有 19-06 可靠删除 | 后端扩展源→图→模型血缘、取消、撤回、账号删除和晚到产物处理 | 访问先关，所有可达副本/版本清理可重试；删号不先级联删除清理依据；用户配额与供应商成本各自结算 |
| 6 / IOS-01 | DATA/WORKER/DELETE；17-30；11-05 | iOS 接入实际所需生成/媒体 Client；冻结输入、确认费用、提交与恢复、核对图、显式模型、版本详情 | App 杀进程/断网/401后能恢复原任务，A 旧任务不覆盖 B；图片先用，模型错误降级；无第二套 DTO |
| 7 / EXPORT-01 | IOS-01 的已确认图；权利信息 | iOS 实现净化输出、Photos Add Only/系统分享与临时副本生命周期 | 拒绝/取消/低存储仍保留图，导出无敏感元数据；不导出 GLB 或人物采集源，不自动创建公开帖子 |
| 8 / TEST-01 | 步骤 3～7 | 后端+iOS+验证按故障矩阵联验，真实依赖与受控 Provider 分别跑 | OpenAPI/事务/画布/任务/账单五类证据可互相对照；TRYON-ACC-011/012、CLOUD-ACC-012、AVATAR-ACC-031适用项通过 |
| 9 / REL-01 | TEST；真实照片及生产门禁 | 产品+发布核对关闭/止损/清理、最低真机和披露，按实际合格范围启用 | 图片与模型可独立放行；任一入口缺权利/地域/删除/预算则保持关闭；本地核心无回归 |

建议每 1～2 个工程人日检查一个端到端结果：契约接纳→替身输出→真实 worker 恢复→App 图片→按需模型。沿 Design 20 的初估在代表 POC 后重估，不把 6～10 人日旧估算当作供应商许可、账号客户端和生产质量的总工期。

## 文件职责与输入输出

以下路径相对 `then-server`，iOS 行明确标出 `then-app`。拟新增项只在对应步骤实际开发时创建；文件名沿当前 `*_contract.go / *_routes.go / *_handlers.go`，不建立空 provider 平台或第二套服务入口。

| 位置 | 输入 → 输出 / 所有权 | 副作用和失败责任 |
| --- | --- | --- |
| `backend/internal/application/generation` | owner、输入快照、用途/同意、额度、命令 → Look 版本与任务状态；已实现 Provider 无关领域合同和任务应用服务 | 应用用例控制合法转换和去重，拒绝无效状态；不包含 HTTP、GORM 或供应商 SDK 类型；执行仍由后续 worker 承接 |
| `backend/internal/adapter/postgres/generation.go`、`generation_worker.go` | 合法接纳/查询/取消命令、worker 租约命令 → 事务结果与 Outbox 待办 | GORM 记录/唯一约束、任务+额度+Outbox 原子提交；状态 revision、owner 隔离和租约 fencing 条件写入，Provider 不在事务内调用 |
| `backend/internal/adapter/httpapi/generation_contract.go`、`generation_routes.go`、`generation_handlers.go` | 当前会话+实际 API 请求 → 202/查询/取消及稳定错误 | Huma 唯一 schema；owner 取会话，非法请求不接纳；日志只记脱敏关联 ID |
| `backend/internal/adapter` 下供应商语义包（拟新增） | 已批准输入句柄/参数 → 外部受理 ID、查询事实、临时输出 | 唯一外部调用位置；默认关闭可能计费 POST 的盲重试；超时/未知受理必须可区分；供应商密钥只在服务端 |
| 现有媒体应用、`adapter/objectstore` 与 generation worker | 输出流/血缘 → 私有固定版本资产、清理结果 | 来源/重定向/解码与字节限制，校验后发布；先写对象后事务失败保留回收依据；不得直接交付供应商临时 URL |
| `backend/internal/adapter/messagequeue/kafka.go`、`backend/internal/bootstrap/application.go` | 已提交 Outbox / 运行配置 → 唤醒实际 worker、关闭和恢复 | 复用现有进程角色；短消息仅携带 ID。长时间外部等待不占数据库事务、不长时间阻塞单条 Kafka 消费；持久任务定时对账保证重启后继续 |
| `then-app/ThenApp/Features/Avatar`、实际 Cloud Client、`Data/Avatar` | 用户选择/会话/服务端事实 → 可恢复草稿、任务/版本和页面 | 网络与存储在 Repository/adapter，View 只发命令；账号隔离；轮询生命周期与后台实际任务分开 |
| `then-app/ThenApp/Services/Photos` 与实际图片输出 adapter | 有权输出的已确认图 → 系统相册/分享临时副本 | 单次净化、按需权限；失败/取消清理 App 可控副本，不扩大照片库读取权限 |

具体 API operation/record 字段在 DATA-01 实现，本文不手写 OpenAPI 文件；能力开关、并发/额度/货币预算、最大尝试数、外部时限和保留期必须有真实配置、边界测试及上线前确定值。缺预算配置按关闭处理。

## 必测故障矩阵

| 故障注入点 | 必须观察到的结果 | 主要证据 |
| --- | --- | --- |
| 接纳事务前后终止，客户端重复提交 | 未提交不占额；已提交返回同 job；无第二 Outbox 意图 | PG 事务/唯一约束、HTTP 与 App 恢复 |
| Provider 已受理但响应丢失 / ID 未落库 | 持久不确定状态，供应商幂等/对账找回；无可靠恢复则人工核对，不自动新提交 | 实际 POST 次数、受理 ID 与账单；不能用只读 poll 次数代替 |
| 两 worker/重投/租约过期 | 过期执行者不能发布；同一任务无重复结算，新的执行者只恢复现有外部工作 | Kafka+PG 故障注入和 fencing 断言 |
| 外部输出过期、重定向越界、坏图/GLB、写库失败 | 不发布不合格资源；安全地址/解码失败分类；已写孤儿有清理追踪 | 受控输出服务、MinIO 固定版本清单和日志 |
| A 生成时编辑 B，或撤回同意/删源/删号 | A 不成为当前 B；删除后的产物只清理；账号访问先失效 | revision/owner 对照、UI、私有媒体拒绝与清理回执 |
| 429、额度竞争、成本上限、关闭入口 | 退避不刷请求；总占用不超上限；停止新提交，已接纳任务继续对账/清理 | 配额台账、外部计费次数、关闭恢复演练 |
| App 无网/被杀/切账号、坏缓存、相册拒绝 | 同账号恢复，其他账号不见前人内容；同版本取回不重新付费；图片仍可用 | 真机/模拟器旅程、网络事件与 GRDB 差分 |

### 验证与回退操作

在 `backend` 运行 `go vet ./...`、`go test -race ./... -count=1` 与受影响真实依赖/进程测试（`-tags=services`、`-tags=integration`）；测试复用隔离 PG/Kafka/MinIO 与唯一前缀，不清空共享库/队列。运行当前入口 `go run .` 读取实际 `/openapi.json`，在 `frontend` 以 `THEN_OPENAPI_SCHEMA=<本次实际地址> npm run openapi` 后做类型/测试校验；App Client 遵循 17-30 的生成决定，相关 iOS build/test 与真机结果单独记录。

故障矩阵先用替身完成，再在 GATE 已通过和预算内做少量受控真实调用，计费记录脱敏入证据。回退关闭 image/model 新任务接纳，不删 PG 任务、Outbox、额度账或清理回执；worker 保留查询/清理能力，用户保留已确认图和本地记录。未经许可不得在回退中改换供应商、地域或重建计费任务。本轮未运行这些命令或调用。
