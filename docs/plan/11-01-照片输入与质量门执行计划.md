# 照片输入与质量门执行计划

## 状态

- 契约状态：`approved`，2026-08-30。
- 平台修订：2026-09-08 用户明确批准最低 iOS 26，本文后续历史 iOS 18 条件统一由 iOS 26 替代；执行与任务状态不变，不扩大照片 POC 范围。最低真机须按新基线重新选择，旧系统验证不抵扣新基线证据。
- 执行状态：`blocked`，2026-09-13；IOS-01～05 的领域、临时会话、导入与净化实现及各自隔离测试已完成；后续 IOS-06 受 SEC-01 素材/预期标签准入阻断，17-01 已解除 App 编译阻断，完整 Host 尚未实现，真机验收待可用设备。完整 POC 未通过。

本切片已获准作为端侧隔离 POC 与可复用处理内核实施，不代表生产照片个性化能力获批。Release 中不得存在可达入口；后续生产切片必须基于本切片证据重新批准阈值、支持矩阵、内容安全与媒体生命周期。

## 追踪关系

- PRD：[`../prd/11-数字形象与照片采集需求.md`](../prd/11-数字形象与照片采集需求.md)
- 覆盖的需求 ID：在本切片隔离范围验证 `AVATAR-REQ-001`～`AVATAR-REQ-010` 的相关边界，不关闭这些需求的完整产品验收；生产用户流程仍需后续执行计划与 acceptance 证明
- design：[`../design/04-数字形象与照片采集设计.md`](../design/04-数字形象与照片采集设计.md)，共同约束见 [`../design/01-技术选型.md`](../design/01-技术选型.md)、[`../design/03-OOTD产品总体设计.md`](../design/03-OOTD产品总体设计.md) 与 [`../design/11-OOTD权限隐私与安全设计.md`](../design/11-OOTD权限隐私与安全设计.md)
- 产品计划：[`10-OOTD产品实施计划.md`](10-OOTD产品实施计划.md) 的 M0 隔离研究样机
- acceptance：[`../acceptance/11-数字形象与照片采集验收.md`](../acceptance/11-数字形象与照片采集验收.md) 的 `AVATAR-ACC-002`～`AVATAR-ACC-008`（仅其中 POC 子集）；细化设计映射为 design 04 的 `AV-02`～`AV-10`、`AV-12`、`AV-14`、`AV-20`、`AV-21`。本切片不关闭 [`../acceptance/10-OOTD产品系统验收.md`](../acceptance/10-OOTD产品系统验收.md) 的任何发布场景

## 切片目标

1. 在不触碰生产资料、生产数据库和云端的前提下，验证一张全身照从系统单项选择到安全解码、净化、质量分析、预览与清理的完整端侧管线。
2. 建立可复用的纯 Swift 领域状态、质量策略、处理用例和 PhotosUI/ImageIO/Vision 适配边界，为后续生产照片准备切片提供经过测量的输入。
3. 用合成素材和取得书面授权的成人测试素材确定最低设备上的文件/像素/内存预算、Vision 阈值、误拒率、误收率、时延和失败恢复表现。
4. 证明取消、替换、失败、后台切换、进程中断和下次启动不会留下未受控原图、净化图、敏感内存或网络流量。
5. 保持无照片路径可见且可退出；任何测试失败都不得促使实现请求更宽相册权限、绕过门禁或启用生产入口。

## 非目标

- 不把照片入口接入正式数字形象页、旧 Profile、Today、Ledger、Calendar、Travel、Life 或其他生产旅程。
- 不实现系统相机、自拍增强、OOTD 衣物分割、风格化形象生成、AI 试穿、动态预览、分享、导出或跨设备同步。
- 不实现长期本机复用，不创建 `AvatarProfile`、`AvatarSourceAsset` 等生产持久化记录，不新增或修改 GRDB migration。
- 不修改 `backend/openapi.yaml`，不实现 Go API、账号、对象存储、RabbitMQ、Redis、供应商或任何网络上传。
- 不推断或记录身份、年龄、性别、种族、肤色、情绪、吸引力、身体尺寸、健康、尺码或真实合身度。
- 不宣称已完成裸露/性化内容识别。当前没有获批的端侧内容安全能力，因此即使技术质量 POC 达标，生产照片入口仍保持关闭。
- 不在本切片固定生产阈值、生产保留期限或生产设备支持矩阵；这些是 POC 输出，不是实现者可主观填写的常量。

## 影响面确认

| 范围 | 本计划影响 | 约束 |
| --- | --- | --- |
| iOS | 是 | SwiftUI + Observation、ImageIO、Vision、PhotosPicker、受控临时文件、Swift Testing/XCUITest |
| Xcode 工程 | 是 | 手工 PBXGroup；新增源码与 Tests Resources 必须显式加入 `project.pbxproj` |
| GRDB/本地 migration | 否 | 不新增表、Record、Repository，不读取 `local_profiles` 或旧账本 bootstrap |
| OpenAPI/Go/PostgreSQL | 否 | 不修改契约、后端代码或服务端 migration |
| RabbitMQ/Redis/对象存储/供应商 | 否 | POC 全程零网络，不预建云资源或 adapter |
| 媒体生命周期 | 是 | 仅随机受保护临时 session；无长期资产，取消/失败/后台/启动必须清扫 |
| 生产入口 | 否 | `#if DEBUG` + `--then-avatar-photo-intake-poc`；Release 不可达 |

## 用户可见契约

本切片只对内部测试人员和获授权参与者提供 DEBUG 测试壳，正式用户不可见。

1. 进入测试壳先看到“照片可选”、仅限年满 18 周岁的本人照片、仅在本机处理、不做量体/尺码判断、结束即清理和“改用风格化形象”说明。
2. 测试人员未主动确认“我是照片中的本人且已年满 18 周岁”时，不得打开系统照片选择器，不创建临时 session。
3. 每次只允许选择一张图片。使用 SwiftUI `PhotosPicker`，不得请求全图库权限、枚举图库或新增 `NSPhotoLibraryUsageDescription` 来扩大访问。
4. 处理成功时只展示重新编码后的净化预览，不展示或复用原始选择结果。
5. 门禁不能继续时，每次只显示一个中性、可操作的主要原因，并提供“换一张”和“使用风格化形象”。不得显示身体评分或人口属性。
6. 取消、离开、改用模板或发生错误后，界面回到可重新开始的安全状态；旧任务迟到结果不得重新显示照片。
7. 界面必须支持 Dynamic Type、VoiceOver、深色模式、提高对比度和 Reduce Motion；焦点顺序为状态、原因、恢复动作。

## 页面、交互与状态契约

### DEBUG 测试壳

- 入口位于 `#if DEBUG` 编译边界内，只能由 `--then-avatar-photo-intake-poc` 启动参数进入。
- 入口绕过旧 `AppEnvironment` 和账本数据库初始化，不挂到旧 Profile 或正式 Root 导航。
- Release 构建不得响应该参数，也不得出现通往测试壳的导航、按钮、URL scheme 或 deep link。

### 状态

`AvatarPhotoIntakeState` 是纯 Swift、`Sendable` 的封闭状态，至少包含：

```text
disclosure
  ├─ 未确认/退出 ─> templateFallback
  └─ 确认 ─> awaitingSelection
awaitingSelection
  ├─ 取消 ─> disclosure
  └─ 选中 ─> importing ─> sanitizing ─> analyzing
analyzing
  ├─ pass/passWithWarning ─> review
  ├─ needsReplacement ─> replacement
  ├─ unsupported ─> unsupported
  └─ 失败/取消 ─> recoverableFailure 或 disclosure
review/replacement/unsupported/recoverableFailure
  ├─ 换一张 ─> awaitingSelection
  └─ 使用模板 ─> templateFallback
```

- ViewModel 使用 `@MainActor @Observable`，通过初始化器接收精确依赖。
- ViewModel 拥有一个可取消的结构化任务；新选择、退出、scene 变化或显式取消会取消旧任务。
- 每次运行使用不可复用的 generation token。只有当前 token 的结果可以改变 UI，防止取消后的迟到结果覆盖新状态。
- `review` 仅持有受控净化预览句柄，不持有 `PhotosPickerItem`、原始 `Data`、绝对路径或 Photos 标识。

### 质量结果与主要原因

结果固定为：

- `pass`：当前 POC 策略认为技术输入可用。
- `pass_with_warning`：可继续评测，但存在一个非阻断技术提醒。
- `needs_replacement`：质量或第三方隐私风险阻止继续。
- `unsupported`：格式、资源、设备能力或安全解码不支持。

稳定主要原因码按以下优先级选择，最多返回一个：

1. `unsupported_format`
2. `unsafe_or_corrupt_input`
3. `resource_limit_exceeded`
4. `multiple_people`
5. `no_person_detected`
6. `person_not_fully_visible`
7. `person_obscured`
8. `image_too_blurry`
9. `image_exposure_unusable`
10. `device_capability_unavailable`
11. `analysis_failed`
12. `quality_warning`

原因码只描述当前技术流程，不得包含身体部位尺寸、面部特征、身份或人口属性。映射规则必须是纯函数并接受显式注入的 POC 阈值；不得把 Vision 默认置信度直接解释为生产质量标准。

## 领域与数据契约

### 领域类型

- `AvatarPhotoIntakeState`：测试壳状态。
- `AvatarPhotoInputHandle`：受控 session 与随机输入 ID，不暴露外部 URL、文件名或 Photos identifier。
- `SanitizedAvatarPhotoHandle`：净化图的受控 session/asset ID、像素尺寸和获批渲染属性，不含绝对路径。
- `AvatarPhotoTechnicalSignals`：当前调用内的人员数量、边界覆盖、遮挡/分割可用性、模糊与曝光等技术信号；只在内存中传递。
- `AvatarPhotoQualityAssessment`：结果、一个主要原因码和本地化恢复动作。
- `AvatarPhotoQualityPolicy`：从技术信号到 assessment 的确定性映射；阈值由 POC 配置注入。

领域层不得导入 PhotosUI、Vision、ImageIO、GRDB、UIKit、网络或旧生活管理类型。

### 临时 session

为避免 `loadTransferable(Data.self)` 对大文件的全量内存加载，本切片固定使用受控文件式导入：

1. 系统 picker 只提供一次性 file representation。
2. `AvatarPhotoTemporarySessionStore` actor 在固定 App 私有临时根目录创建随机 session；目录和文件使用 `FileProtectionType.complete` 并排除备份。
3. 输入先核验文件属性、UTType/魔数和像素头，再复制到随机 `original_import` 名称；不得保留原文件名或外部绝对路径。
4. ImageIO 使用 `shouldCache=false` 检查图像源，通过缩略图路径降采样，规范方向与色彩空间后重新编码为获批输出格式。
5. 输出采用元数据白名单；不得复制 GPS、EXIF、IPTC、TIFF、原文件名、创建日期、设备/镜头、编辑历史或 Photos 标识。
6. 生成净化图后立即删除 `original_import`。净化图只存活到当前测试会话结束、取消、失败、模板回退、后台安全清理或下次启动清扫。
7. 所有路径操作必须以固定根目录和解析后的 descendant 校验为边界，不接受 UI、测试素材或调用方传入任意删除路径。

本切片不写 SQLite，不创建长期媒体资产，不把图片写入剪贴板、系统相册、Widget、Spotlight、通知、诊断导出或分享面板。

## API 与系统集成契约

### iOS 文件边界

实现应按以下职责落位；允许在代码评审中微调文件拆分，但不得合并领域、平台和 UI 边界：

- `Features/Avatar/Domain/AvatarPhotoIntakeModels.swift`
- `Features/Avatar/Domain/AvatarPhotoQualityPolicy.swift`
- `Features/Avatar/UseCases/PrepareAvatarPhotoUseCase.swift`
- `Features/Avatar/Presentation/AvatarPhotoIntakeViewModel.swift`
- `Features/Avatar/Presentation/AvatarPhotoIntakePOCView.swift`
- `Services/Photos/SystemAvatarPhotoPicker.swift`
- `Services/Imaging/ImageIOAvatarPhotoSanitizer.swift`
- `Services/Imaging/VisionAvatarPhotoAnalyzer.swift`
- `Services/Imaging/AvatarPhotoTemporarySessionStore.swift`
- `App/Debug/AvatarPhotoIntakePOCHost.swift`

Photos picker、sanitizer、analyzer 与 session store 都通过精确协议注入用例。系统/框架错误在适配层映射为稳定领域错误。

当前 Xcode 工程使用手工 PBXGroup。所有新增 Swift、测试和 fixture manifest 必须显式加入 `project.pbxproj` 的正确 group、Sources 或 Tests Resources；保持一个生产 `ThenApp` module。

### 不受影响的系统

- OpenAPI/Go/队列/对象存储：不适用；本切片禁止网络和云端数据流。
- GRDB：不适用；不新增 migration、表、Record 或 Repository，不读取 `local_profiles`。
- 认证、同意服务端记录和配额：不适用；没有离机处理。
- 相册权限说明：不新增；系统 Photos picker 使用用户明确单项选择。

## 权限、隐私与安全

1. 测试素材只允许使用合成图片或取得书面授权的年满 18 周岁成人本人素材；fixture manifest 记录稳定 ID、来源/授权依据、预期标签和哈希，不记录真实姓名或不必要属性。
2. 未确认 18+ 本人声明前不得打开 picker；不得使用视觉模型猜测年龄或验证身份。
3. 多人结果必须停止，不得自动裁掉第三方后继续。
4. Vision 只输出当前处理所需的瞬时技术信号；不持久化 observation、关键点、人脸框、embedding、遮罩或身体描述。
5. 日志、OSLog、分析、trace、崩溃附加信息和测试报告不得包含图片字节、绝对路径、原文件名、Photos 标识、哈希、质量信号明细或可还原个人特征的文本。
6. POC 不发网络请求。抓包或网络 spy 发现任何图片、元数据或状态外发即判定失败。
7. scene 进入 inactive/background 时先移除可见预览并释放敏感图像缓存，再依赖全局隐私遮罩保护系统快照；遮罩不能替代内存和文件清理。
8. 当前无获批内容安全检测能力，测试壳只能用于受控素材；该缺口是生产入口的硬阻断，不得在 POC 中以简单分类器或供应商 SDK 临时填补。

## 错误、降级与恢复

| 场景 | 行为 | 禁止行为 |
| --- | --- | --- |
| picker 取消 | 清理空 session，返回 disclosure，保留模板路径 | 把取消显示为错误或申请更宽权限 |
| iCloud 项目离线不可读 | 显示可重试原因，允许换图/模板 | 自动枚举图库或长期等待无取消 |
| 格式、魔数、尺寸或解码失败 | 返回 `unsupported` 对应主原因并清空临时字节 | 尝试按扩展名强行解码 |
| Vision 不可用或异常 | 返回 `device_capability_unavailable` 或 `analysis_failed` | 绕过质量门进入 review |
| 多人/零人/截断/遮挡 | 按优先级显示一个 replacement 原因 | 自动裁剪第三方或输出身体描述 |
| 低存储/写入失败 | 不产生可用句柄，清理部分文件并可重试 | 回退不受保护目录或只登记半成品 |
| 用户换图或取消 | 取消旧任务、清理旧 session、抑制迟到结果 | 旧结果覆盖新状态 |
| App 强制终止 | 下次 DEBUG POC 启动清扫固定根目录残留 | 恢复并自动展示敏感预览 |
| 进入后台 | 清除预览/缓存并安全收口 session | 仅盖遮罩但继续持有图片 |

所有失败都保留“使用风格化形象”出口，不接触推荐、衣橱或历史数据。

## 兼容、迁移与回滚

- 最低 iOS 26、Swift 6.3.3、SwiftUI + Observation 和严格并发设置不变。
- 没有数据库、OpenAPI、后端或媒体 schema 迁移；不存在历史数据升级和服务端兼容窗口。
- 回滚时移除 DEBUG host/launch argument 与新增 POC 文件及测试资源即可；固定临时根目录由当前或回滚版本的启动清扫删除。
- Release 不产生用户数据，因此回滚不得要求清库、删除账号或修改旧 GRDB migration。
- 若 POC 发现最低设备无法满足预算、不同群体误拒明显、ImageIO/Vision 行为不稳定或清理不可证明，则保持生产入口关闭并允许整切片停止，不以放宽安全边界换取通过。

## 可观测性与指标

生产日志和产品分析不适用。本切片只在隔离测试壳与测试进程中采集：

- 各阶段 p50/p95 时延、峰值内存、能耗/热状态和取消收口时间。
- 每类 fixture 的结果码、预期/实际差异、误拒和误收。
- 元数据净化字段清单、沙盒前后文件差异和零网络断言。
- 设备型号、系统版本、构建号和阈值配置版本。

报告不得嵌入图片、真实姓名、绝对路径或质量信号原始正文。长期证据使用可复核 CI artifact/评测报告引用，不以短期 `/tmp` 路径作为完成证据。

## 测试与验收映射

| 需求/设计场景 | 功能验收 | 本切片验证 | 必要证据 |
| --- | --- | --- | --- |
| `AVATAR-REQ-002`、AV-02/07 | `AVATAR-ACC-002`、`AVATAR-ACC-008` | 18+ 本人声明前不可进入 picker；拒绝后模板出口可用 | ViewModel 测试、XCUITest、零临时文件断言 |
| `AVATAR-REQ-003`、AV-04/08 | `AVATAR-ACC-003`、`AVATAR-ACC-008` | 一张全身照是唯一输入；不要求自拍或额外角度 | UI/状态测试、调用 spy |
| `AVATAR-REQ-004`、AV-02 | `AVATAR-ACC-002` | 用途、位置、保留、删除、无照片替代在选择前可见 | XCUITest、最大字号/VoiceOver 证据 |
| `AVATAR-REQ-005`、AV-09 | `AVATAR-ACC-005`、`AVATAR-ACC-008` | 白名单重编码移除 GPS/EXIF/IPTC/TIFF/原名等 | ImageIO metadata dump、参数化测试 |
| `AVATAR-REQ-006`、AV-06 | `AVATAR-ACC-004`、`AVATAR-ACC-008` | 零人/多人停止且不自动裁剪继续 | 授权 fixture 测试、文件清理断言 |
| `AVATAR-REQ-007`、AV-05 | `AVATAR-ACC-004`、`AVATAR-ACC-007`、`AVATAR-ACC-008` | 只输出技术结果与一个主要原因 | policy 单元测试、字符串/日志扫描 |
| `AVATAR-REQ-001/008/009` | `AVATAR-ACC-001`、`AVATAR-ACC-005` | 测试壳隔离；模板出口；零推荐读取、零网络、零云同意复用 | 依赖图审查、network spy、Release 不可达测试 |
| `AVATAR-REQ-010`、AV-10/12/14 | `AVATAR-ACC-006`、`AVATAR-ACC-008` | 当前 session 的取消、失败、异常启动和后台清理 | 故障注入、沙盒 diff、真机中断证据 |
| AV-20/21 | `AVATAR-ACC-007`、`AVATAR-ACC-008` | 无障碍、Reduce Motion、后台快照与敏感表面 | XCUITest、Accessibility Inspector、真机录屏 |

测试至少包含：

- sanitizer：HEIC/JPEG/PNG、方向/色彩、损坏、MIME/魔数不符、动画/RAW、不受支持格式、超文件/像素、压缩炸弹和元数据夹具。
- policy：四种结果、全部原因优先级、阈值边界和“一次只返回一个主要原因”。
- Vision：单人、零人、多人、截断、遮挡、模糊、曝光、轮椅/辅助器具、宗教服饰和多样成人授权素材；报告分组失败率，不把未校准常量当作事实。
- ViewModel/use case：声明、picker 取消、处理中取消、换图、迟到结果、服务异常、scene 变化、模板回退和资源清理。
- 工程/UI：DEBUG 启动参数、Release 不可达、最大辅助字号、VoiceOver 顺序、深色/高对比度、Reduce Motion。
- 真机：真实 Photos picker 选择/取消、iCloud 离线、Data Protection、最低设备内存/性能、后台快照和进程中断恢复。模拟器结果不能替代这些证据。

## 发布、feature flag 与关闭路径

- 本切片没有生产 feature flag；使用更严格的 `#if DEBUG` 与显式启动参数双门禁。
- Release 构建中生产导航不可达，OpenAPI/URLSession spy 为零，照片能力不计入首发功能。
- POC 完成后必须形成 go/no-go 结论。任何安全、内容边界、公平性、最低设备、净化或清理门槛未通过时结论为 no-go，后续生产照片入口不得创建。
- 即使 POC go，仍需另建生产“本地照片准备”执行计划，固定阈值、支持矩阵、用户入口、媒体生命周期、真机验收和独立关闭路径。

## 待本计划产出的参数

2026-09-13 状态修正：基础内核可在已批准范围内独立实施，但完整 POC 仍受 17-01 App 构建、系统 file representation 生命周期接入、成人/Vision 素材和最低设备证据制约；不能继续将“没有阻断实施的未决问题”作为整体结论。以下是 POC 必须产出、由后续生产切片批准的参数，不得静默转为生产常量：

1. 获批文件大小、总像素、输出像素、峰值内存和阶段超时预算。
2. Vision/图像技术信号阈值、原因优先级校准结果与最低设备支持矩阵。
3. 多样成人测试集的误拒/误收门槛及公平性关闭阈值。
4. 裸露/性化内容安全的获批端侧方案或其他可证明生产边界。
5. 后续是否启用一次性生产照片准备；长期本地复用、相机、自拍和云端仍分别另行评审。

## 2026-09-13纯领域内核实现检查点

本次时间盒 60 分钟：完成 IOS-01/02 的纯领域代码和 TEST-02；若超时或发现上游冲突，记录未完成项，不放宽质量语义。本轮不接入 PhotosPicker/ImageIO/Vision、POC Host、生产入口、GRDB、文件/网络；不采集素材，也不把合成数字信号测试当作真人检测效果。

- 领域代码仅使用 Swift/Foundation 值类型与 Sendable，显式 nonisolated；App 保留 MainActor 默认和 Swift 6 严格并发，不改生成 Client 或 target 数量。
- 技术信号的覆盖、可见性、清晰度、曝光可用性使用当前适配调用内的 0～1 分数，nil 表示缺失，不解释为身体测量或生产质量。阈值同域、必须显式注入且校验有限值；缺失、负人员数或非有限/越界信号不能通过。
- 优先返回已知技术原因；设备不可用和分析失败仍按原固定优先级处理。assessment 只带四类结果、一个稳定原因及对应恢复动作，无用户原文、图片或路径。
- 文件为既定的两个 Domain Swift 文件、ThenAppTests 中的质量策略测试，并同步手工 PBXGroup/Sources；不增加 package、生产模块或转发层。
- `scripts/validate-avatar-photo-domain.sh` 仅为该纯领域子集提供可重复验证：原文件按 App 相同 MainActor/严格并发设置编译，iOS Simulator SDK 类型检查，宿主 Swift Testing 执行同一测试文件。临时测试产物保存在忽略的 DerivedData，结果不能代替 xcodebuild、iOS 测试或真机报告。
- 11-01-IOS-11 工程完整构建及 REL-01 仍受 17-01 生成 Client 阻断；只登记本轮新增文件，不把局部编译记为整体通过。

## 开工检查

| ID | 任务 | 状态 | 依赖/阻断 | 完成证据 |
| --- | --- | --- | --- | --- |
| `11-01-DOC-01` | 完成本执行计划的范围准入评审，统一范围契约、任务、证据并明确不解锁生产照片能力 | completed | 已批准 PRD 11 与 design 04 | 本文契约状态为 `approved`，且范围、非目标、执行任务与 Release 不可达约束已统一记录 |
| `11-01-DOC-02` | 运行工具链与工作区预检，记录 Xcode 26.6、Swift 6.3.3、最低 iOS 26、现有未提交改动和当前可用 destination | completed | `11-01-DOC-01` | 2026-09-13 工具链通过；已审查并保留当前未提交修改；showdestinations 成功，有 iOS 26.5 模拟器，无已连接 iOS 真机；App 的 17-01 编译阻断仍存在 |
| `11-01-SEC-01` | 批准测试素材清单：只使用合成图或有书面授权的 18+ 成人本人素材，记录稳定 fixture ID、授权依据、预期标签与哈希 | pending | 隐私负责人/素材来源 | `app/ThenAppTests/Resources/AvatarPhotoIntake/manifest.json` 评审记录；仓库和报告不含真实姓名、网图或生产照片 |
| `11-01-DOC-03` | 将生产硬阻断、无 GRDB/OpenAPI/网络边界写入本切片代码评审说明 | completed | `11-01-DOC-01` | 本轮逐文件实现约束见下节；不新增入口、数据、权限或运行依赖 |

## 实现任务

| ID | 任务 | 状态 | 依赖/阻断 | 完成证据 |
| --- | --- | --- | --- | --- |
| `11-01-IOS-01` | 在 `Features/Avatar/Domain` 建立纯 Swift `Sendable` 状态、受控句柄、技术信号、四类 assessment 与稳定主要原因码 | completed | `11-01-DOC-02` | 2026-09-13：见 AVATAR-POC-DOMAIN-20260913-01；原文件 iOS SDK 类型检查、宿主 Swift Testing 10 项通过，含 4095 原因组合及四项阈值边界；不代表 App 集成或 Vision 效果 |
| `11-01-IOS-02` | 实现 `AvatarPhotoQualityPolicy` 纯函数：阈值显式注入，按本计划固定优先级只返回一个主要原因 | completed | `11-01-IOS-01` | 2026-09-13：见 AVATAR-POC-DOMAIN-20260913-01；原文件 iOS SDK 类型检查、宿主 Swift Testing 10 项通过，含 4095 原因组合及四项阈值边界；不代表 App 集成或 Vision 效果 |
| `11-01-IOS-03` | 实现 `AvatarPhotoTemporarySessionStore` actor：固定私有根、随机 session、`complete` 文件保护、排除备份、descendant 校验、幂等清理和启动残留清扫 | completed | `11-01-DOC-02` | 2026-09-13：AVATAR-POC-SESSION-20260913-01；actor 与 Foundation 适配实现，11 项 iOS Simulator Swift Testing 通过；complete 保护请求已编码，真机保护等级/锁屏和备份效果仍必须由 TEST-06 验收 |
| `11-01-IOS-04` | 实现系统单项 PhotosPicker 适配与受控 file representation 导入；声明前不可调用，不使用 `loadTransferable(Data.self)` 全量加载 | completed | `11-01-IOS-01`、`11-01-IOS-03` | 2026-09-13：AVATAR-POC-FORMATS-20260913-01；类型准入、受控复制及取消/迟到/清理实现完成；10 项测试/22 个实例和真实五文件矩阵/选择前取消共 2 项 UI 测试通过。仅关闭本服务实现；完整声明页、iCloud/真机/RAW 和 POC 发布验收仍归 IOS-09、TEST-01/05/06 与 REL，不据此通过 |
| `11-01-IOS-05` | 实现 ImageIO sanitizer：先校验文件属性、UTType/魔数和像素头，`shouldCache=false`，缩略图降采样、方向烘焙、色彩规范与元数据白名单重编码 | completed | `11-01-IOS-03` | 2026-09-13：AVATAR-POC-IMAGING-20260913-01；7 项编解码测试含 11 个参数用例通过；修复自动 EXIF 和写入后排除备份丢失；同步 ImageIO 无法硬中断，完整 TEST-01/06 仍未通过 |
| `11-01-IOS-06` | 实现 Vision analyzer：只返回人员数量、边界覆盖、遮挡/分割可用性、模糊与曝光等瞬时技术信号 | blocked | `11-01-IOS-01`、`11-01-SEC-01` | SEC-01 尚无覆盖人员检测/质量校准的获批素材和预期标签；现有五份色块仅支持传输/编解码。实现仍须不返回或持久化 Vision observation、关键点、人脸框、embedding、遮罩或人口属性；取消可及时结束 |
| `11-01-IOS-07` | 实现 `PrepareAvatarPhotoUseCase` 管线：导入→净化→分析→策略，阶段间检查取消，所有成功/失败/取消路径清理原图和半成品 | pending | `11-01-IOS-02`～`11-01-IOS-06` | 结构化并发测试、阶段调用顺序、故障注入与资源清理断言通过；无无主 `Task` |
| `11-01-IOS-08` | 实现 `@MainActor @Observable` ViewModel：精确依赖、单一有主任务、generation token、换图/取消/scene 变化和迟到结果抑制 | pending | `11-01-IOS-07` | 状态机测试覆盖声明、选择、处理、review、replacement、unsupported、失败和模板回退；旧 token 不能更新 UI |
| `11-01-IOS-09` | 实现 DEBUG POC View/Host：选择前披露、18+ 本人确认、净化预览、一个中性原因、换图/模板/取消；由启动参数进入并绕过旧 AppEnvironment | pending | `11-01-IOS-04`、`11-01-IOS-08` | DEBUG 可进入；Release 对启动参数无响应且无生产导航入口；未修改旧 Profile/Today/Ledger/Calendar/Travel/Life |
| `11-01-IOS-10` | 补齐 String Catalog、VoiceOver 状态—原因—动作顺序、Dynamic Type、深色/高对比度、Reduce Motion 和后台敏感预览/内存清理 | pending | `11-01-IOS-09` | 本地化键扫描、无硬编码用户文案、无障碍测试和 scene 故障注入通过；隐私遮罩之外已释放图像缓存 |
| `11-01-IOS-11` | 从首个新增文件开始同步维护 `project.pbxproj` 的 group、Sources、Tests Sources 与 fixture Resources，保持每步可增量编译和单一业务 module（仅 ThenTransport 为已批准技术例外） | pending | `11-01-DOC-02` | 累计 6 份产品 Swift、5 份测试及 5 个合成夹具/manifest 已登记到原业务/测试 target；2026-09-13 的 17-01 仅新增已获准 ThenTransport。真实 Debug/Release 和测试 target 构建通过；本任务随未实施的 IOS-06～10 持续登记，尚未整体完成 |
| `11-01-SEC-02` | 建立边界守卫：禁止 POC 引用 GRDB/`local_profiles`/旧 AppEnvironment、URLSession/OpenAPI、分享/剪贴板/相册回写或分析 SDK | pending | `11-01-IOS-11` | 静态扫描与依赖 spy 为零；`backend/openapi.yaml`、migration、Info.plist 权限键和旧 Feature 无本切片业务改动 |

## 测试与验收任务

| ID | 任务 | 状态 | 依赖/阻断 | 完成证据 |
| --- | --- | --- | --- | --- |
| `11-01-TEST-01` | 为 sanitizer 建立参数化测试：HEIC/JPEG/PNG、旋转/色彩、损坏、MIME/魔数不符、动画/RAW、超文件/像素与压缩炸弹 | pending | `11-01-IOS-05`、`11-01-SEC-01` | 测试结果、峰值内存、失败时间、输出 metadata dump；所有临时目录测试后为空 |
| `11-01-TEST-02` | 为质量策略建立四类结果、全部原因、固定优先级、阈值边界与单一原因属性测试 | completed | `11-01-IOS-02` | 2026-09-13：见 AVATAR-POC-DOMAIN-20260913-01；原文件 iOS SDK 类型检查、宿主 Swift Testing 10 项通过，含 4095 原因组合及四项阈值边界；不代表 App 集成或 Vision 效果 |
| `11-01-TEST-03` | 运行 Vision fixture 矩阵：单人、零人、多人、截断、遮挡、模糊、曝光、轮椅/辅助器具、宗教服饰和多样成人素材 | pending | `11-01-IOS-06`、`11-01-SEC-01` | 按 fixture ID 的预期/实际、误拒/误收和分组失败率报告；报告不含图片或个人描述 |
| `11-01-TEST-04` | 为 use case/ViewModel 建立声明、picker 取消、处理中取消、换图、旧结果迟到、每阶段异常、低存储、scene 变化和模板回退测试 | pending | `11-01-IOS-07`、`11-01-IOS-08` | Swift Testing 通过；每条路径断言任务取消、敏感句柄释放、文件清空和状态可恢复 |
| `11-01-TEST-05` | 建立 DEBUG POC XCUITest：声明前无 picker、无照片退出、处理中取消、单一中性原因、最大辅助字号、深色/高对比度与 Reduce Motion | pending | `11-01-IOS-09`、`11-01-IOS-10` | XCUITest 结果、截图/录屏和元素顺序；Release 启动参数不可达测试通过 |
| `11-01-TEST-06` | 在最低支持真机验证 PhotosPicker 选择/取消、iCloud 项目离线、Data Protection、后台快照、强制终止恢复、峰值内存/能耗/热状态与 p50/p95 时延 | pending | 可用 iOS 26 真机；`11-01-TEST-01`～`11-01-TEST-05` | 设备/系统/构建/阈值配置完整的真机报告；模拟器结果不得替代。当前无真机 destination 时保持 pending，不可豁免 |
| `11-01-SEC-03` | 执行抓包、日志/trace、沙盒、剪贴板、分享、Widget、Spotlight、通知、备份与后台快照检查 | pending | `11-01-IOS-10`、`11-01-SEC-02`、`11-01-TEST-06` | 零网络、零敏感日志、零长期副本、零未批准表面；元数据和原文件名不出现在进程外或报告中 |
| `11-01-TEST-07` | 双人复核原因文案与 fixture 结果，检查无羞辱性身体描述、人口属性、身份结论或尺码/真实合身承诺 | pending | `11-01-TEST-03`、`11-01-IOS-10` | 字符串扫描、人工复核记录与需要修正的稳定原因码清单 |

## 发布与回滚任务

| ID | 任务 | 状态 | 依赖/阻断 | 完成证据 |
| --- | --- | --- | --- | --- |
| `11-01-REL-01` | 运行 `scripts/verify-toolchain.sh`、`scripts/validate-ios-architecture.sh`、相关 Swift Testing/XCUITest、generic Debug build 与 Release build | pending | `11-01-IOS-11`、自动化测试完成 | 命令、退出码、构建号和可长期复核的 CI/xcresult 引用；已知警告单独列出且不得掩盖新增警告 |
| `11-01-REL-02` | 形成 POC 参数报告并回写 design、产品计划、acceptance 与本文：文件/像素/内存/超时预算、阈值版本、最低设备矩阵、误拒/误收和公平性风险 | pending | `11-01-TEST-06`、`11-01-TEST-07` | 上游文档 diff 与批准记录；未达标项保留为生产硬阻断，不把测量值静默写成产品事实 |
| `11-01-REL-03` | 执行 Release 不可达和回滚演练：关闭/移除 DEBUG host 后无入口，临时根可清扫，不影响旧数据库、OpenAPI 和现有旅程 | pending | `11-01-REL-01` | Release UI/启动参数测试、沙盒 diff、`git diff` 边界审查与回滚步骤记录 |
| `11-01-REL-04` | 产出 go/no-go 结论；只有所有必需证据完成且无安全/公平性/设备阻断时，才允许起草后续生产“本地照片准备”执行计划 | pending | `11-01-REL-02`、`11-01-REL-03`、`11-01-SEC-03` | 评审结论、阻断项和下一切片准入记录；无论结论如何，当前生产照片入口保持关闭 |

## 完成判定与后续计划

本计划只有在所有必需任务和证据完成、`11-01-REL-04` 给出正式结论后，执行状态才能标记 `completed`。POC 得出 no-go 不妨碍计划在证据和关闭结论完整时完成，但生产入口必须继续关闭。以下能力不得借实现便利顺带加入：

- 生产数字形象入口与正式 Root 导航。
- 内容安全生产方案、一次性生产媒体生命周期和固定阈值。
- 长期本机复用、GRDB schema、相机与自拍。
- AI 试穿、云上传、OpenAPI、Go worker、对象存储、供应商和删除 SLA。

`11-01-REL-04` 之前不得创建上述能力的实现任务；若 POC 结论为 no-go，应记录原因并保持无照片模板/单品拼图路线，不以放宽安全或隐私边界关闭本计划。

## 本轮证据：AVATAR-POC-DOMAIN-20260913-01

- 2026-09-13，执行者 Codex；基线提交 `2bc97280796791fa3d20b6407e01b7a8e403b0fa` 加当前未提交工作树。原有修改保留；新增纯领域文件和测试，未修改旧 Feature、生产入口、数据库、OpenAPI、依赖或生成代码。
- Red：验证入口在领域文件尚未存在时报告缺少两个 Swift 输入，不能误判已有实现。Green：补齐领域代码后，`scripts/validate-avatar-photo-domain.sh` exit 0，10 项 Swift Testing 通过；包含 4095 个原因组合、每项阈值的相等/相邻上下界、每项信号的缺失/NaN/无穷/越界、配置校验和非 MainActor 任务调用。
- 四个 `0.5` 仅为测试中的显式合成阈值，没有默认生产参数；本轮没有图片、Vision 预测、授权成人样本或误拒/误收测量。
- `plutil -lint app/ThenApp.xcodeproj/project.pbxproj`、`scripts/validate-ios-architecture.sh` 通过。三份新文件仅加入现有 ThenApp / ThenAppTests，没有新增 target、模块或 package。
- `scripts/validate-ios.sh build` 实际执行后 exit 65；新增领域文件已被 Xcode 编译任务读取，39 行错误全部位于生成的 Client.swift（MainActor converter / Decodable 隔离），与 17-01 已知阻断一致。Debug 未通过，Release 与 XCUITest 未执行。
- Swift Testing 宿主测试及 iOS SDK 类型检查只关闭当前纯逻辑子集；IOS-11/REL-01、AVATAR-ACC 完整场景与整体目标仍未完成。没有真机、隐私数据或生产数据访问。

## 2026-09-13临时Session实施契约

本轮时间盒 60 分钟，目标为 IOS-03 的目录/文件保护请求、范围校验、幂等清理与故障测试；真实锁屏和备份验证仍归 TEST-06，不伪造真机通过。

- `Services/Imaging/AvatarPhotoTemporarySessionStore.swift` 为唯一会话所有者 actor。仅由 POC composition 注入可信私有临时容器，实际根目录固定追加专用名称；每次会话、暂存文件使用 UUID，删除接口只接受已登记的会话/文件引用，不接受外部路径字符串。
- 新 owner 在首次使用前清扫专用根目录；有活跃会话时拒绝再次启动清扫。清理失败不继续建立新会话；本轮不把初始化接入旧 App 或旧数据库。
- 目录和新建空暂存文件先请求 complete 保护及排除备份，成功后才返回引用。暂存引用不等于已净化资产，不产生 `SanitizedAvatarPhotoHandle`；真正解码、大小/格式校验及输出完成仍归 IOS-04/05。
- 返回平台适配层使用的文件 URL 前，必须验证已登记会话、固定根的路径分量、真实目录及普通文件；拒绝 symlink、目录伪装和未知句柄。UI/ViewModel 只持有领域句柄，不得调用平台文件路径方法。
- 创建中途失败时清理该会话的部分产物；清理失败返回独立稳定错误并保留可重试状态。底层错误映射为无路径/文件名的稳定服务错误；不吞掉清理失败，不回退未保护目录。
- 窄文件系统协议仅用于 Foundation 操作和可取消的故障注入，不引入新持久化框架。测试只操作随机临时容器及合成字节，覆盖根/会话/文件 symlink、相邻目录、重复清理、模拟重启、保护失败、低存储/部分创建和删除重试。
- 使用独立 iOS Simulator 测试进程执行原代码和 Swift Testing；不启动生产 App、不打开历史 SQLite。模拟器属性结果不能证明真机锁屏保护或备份排除。Xcode 工程仍同步登记，完整 App 构建继续单独检查。

## 本轮证据：AVATAR-POC-SESSION-20260913-01

- 2026-09-13，Codex 执行，基线 `2bc97280796791fa3d20b6407e01b7a8e403b0fa` 加当前工作树。只新增 Imaging session service、对应 Swift Testing、独立模拟器验证脚本与现有 target 文件登记；未启动旧 App 或读取历史数据库。
- `THEN_AVATAR_TEST_SIMULATOR=<专用已启动模拟器ID> scripts/validate-avatar-photo-session.sh` exit 0：iPhone 17 / iOS 26.5（23F77）的独立测试进程执行 11 项测试通过，原代码以 Swift 6、MainActor 默认与 Complete Strict Concurrency 编译，没有额外生产 module 或 package。
- 通过内容：专用根/随机 session/文件身份、排除备份属性、普通文件存在性、单文件及会话幂等清理、模拟进程重建后启动清扫、相邻目录不变、根/会话/文件 symlink 与 hardlink 拒绝、低存储及保护步骤故障注入、创建/清理双重失败、删除重试及底层错误脱敏。删除失败后禁止读取旧文件 URL 或继续创建文件，防止未完成清理的会话恢复使用。
- 测试只包含随机测试目录与少量合成字节；所有测试容器在结束时删除，模拟器未加载 ThenApp/旧 AppEnvironment。脚本临时构建目录自动清理；专用模拟器保留用于后续同一 POC，不复用用户的旧 App 数据。
- **保护证据边界**：目录创建/设置使用 `.protectionKey: FileProtectionType.complete`，文件使用 `.completeFileProtection` 和 `.withoutOverwriting`；模拟器实际创建文件并读回排除备份属性，但不报告可用于证明真机保护等级的文件属性。真实设备锁定后不可读、备份排除、后台快照及故障恢复仍 pending，归 TEST-06/SEC-03。未通过这些门禁前不能认领完整 POC go。
- `scripts/validate-ios-architecture.sh`、`plutil -lint app/ThenApp.xcodeproj/project.pbxproj` 通过。`scripts/validate-ios.sh build` exit 65，新 service 已进入 Xcode 编译，39 行编译错误均位于原生成 Client.swift；Release、XCUITest 未执行，17-01 阻断仍未解决。
- 开工的 missing-source Red 及并发检查发现不可跨域持有 FileManager 后，改为无状态 Foundation 适配器，由会话 actor 串行拥有操作；没有使用 `@unchecked Sendable`、关闭并发检查或添加平台兼容分支。增强删除失败禁止复用后相应测试更新并复验通过。
- IOS-03 completed 仅指其代码和隔离模拟器检查完成；TEST-06、IOS-11、REL-01～04、全部完整功能验收和总目标没有因此完成。

## 2026-09-13ImageIO净化实施契约

时间盒 60 分钟：实现 IOS-05 的已导入受保护文件→净化文件内核，以程序生成小尺寸色块验证格式、方向、sRGB、元数据隔离、输入/输出上限与失败清理。系统 PhotosPicker/file representation 接入仍归 IOS-04，不用测试里的文件写入代替该能力。

- `AvatarPhotoInputHandle` 增加声明格式枚举（JPEG/PNG/HEIC），不含路径、原名、Photos identifier。ImageIO adapter 按会话引用读取已登记原图，仅成功输出 `SanitizedAvatarPhotoHandle`。协议与句柄属于 Domain；ImageIO/UTType/CoreGraphics 只留在 Services/Imaging。
- 输入预检可复用：普通文件/受控路径、文件字节上限、声明格式/容器头与 ImageIO 类型一致、单帧、正像素尺寸、乘法溢出安全的总像素上限。使用 `kCGImageSourceShouldCache=false`，不先完整解码再判断像素上限。
- 文件字节、源像素、输出边长、显式 RGBA raster 字节、编码输出字节和阶段时长由调用方注入，没有生产默认值。时长检查位于原生调用前后；不能强行抢占同步 ImageIO 调用，因此硬超时、真实峰值内存和支持预算仍由 TEST-01/06 实测，不以应用层检查宣称满足。
- 缩略图强制从像素生成并应用方向变换，再绘制到新 sRGB、8-bit RGBA 上下文。输出固定为 PNG 且不传入源 metadata；方向已经烘焙，不写回源 orientation/EXIF/GPS/IPTC/TIFF。生成的像素尺寸、位深与标准色彩属性是唯一允许的渲染信息。
- 输出先在既有 session 分配受保护空文件，完成编码和输出大小复核后立即删除 original_import，再返回净化句柄。取消、超时、校验或写入失败清理整个 session；清理失败单独返回稳定错误，不能误报成功或泄露底层路径。
- ImageIO 实测会自动生成不含源值的 EXIF chunk；为符合当前零 EXIF 条件，编码后仅保留固定 PNG 的 IHDR/sRGB/IDAT/IEND 与原 CRC，不解析像素或引入编解码依赖。输出再次设置 complete/排除备份，失败清理会话后才报错；不得仅依赖预创建文件的属性。
- 合成夹具只包含几何色块和明确标为 SYNTHETIC 的测试元数据；没有人物或真实用户数据。来源、文件 hash、预期类型/场景写入既定 manifest，并登记到 Tests Resources。素材准入只关闭此合成编解码子集；成人/Vision 公平性素材仍未具备，不标 SEC-01 整体完成。
- 不依赖完整 App、网络或数据库；继续在已确认存活的专用 iOS 26.5 模拟器独立运行原源码与 Swift Testing。新增文件同步 PBXGroup/Sources/Resources；完整 xcodebuild、真机与 Release 门禁独立保留。

## 本轮证据：AVATAR-POC-IMAGING-20260913-01

- 2026-09-13，执行者 Codex，基线 `2bc97280796791fa3d20b6407e01b7a8e403b0fa` 加现有工作树。新增 `ImageIOAvatarPhotoSanitizer`、Domain 格式/协议、合成生成器、夹具与测试；修正 session store 写入后保护步骤，保持单一生产 module、现有依赖和无照片生产入口。
- Red → Green：首次 3 种合法格式测试发现 ImageIO 自动产生 EXIF（ColorSpace/PixelXDimension/PixelYDimension）；以 PNG chunk 白名单修复后，严格断言 GPS/EXIF/IPTC/TIFF 全部不存在。随后新增输出属性断言，3 种格式均复现排除备份属性丢失；由 store 在写入完成后重新设置保护，复测全部通过。保护失败与保护后清理失败均注入验证，不把清理失败表示为成功。
- 固定工具链检查通过：Xcode 26.6、Swift 6.3.3、Go 1.26.5。独立原源码测试启用 Swift 6、Complete Strict Concurrency、MainActor 默认隔离与 Approachable Concurrency，不修改生成代码或 App 并发设置。
- `THEN_AVATAR_TEST_SIMULATOR=<专用已启动ID> scripts/validate-avatar-photo-session.sh imaging`：iPhone 17 / iOS 26.5（23F77）模拟器，7 项 Swift Testing 全通过，含 11 个参数化用例。覆盖 5 个 manifest 哈希/实际类型/帧数，JPEG/PNG/HEIC 方向 6 的 64×32 输入→16×32 sRGB PNG、零敏感元数据/测试标记、排除备份属性、成功移除原图、格式伪装、截断、动画 PNG/未批准 TIFF、文件/像素/输出字节/阶段时限、预先取消及失败无会话残留。
- 同模拟器 `scripts/validate-avatar-photo-session.sh session`：12 项测试通过，含写后保护失败和复合清理失败恢复。`scripts/validate-avatar-photo-domain.sh`：10 项通过，含 4095 原因组合；领域 handle 增加格式后无回归。
- `scripts/validate-ios-architecture.sh` 与 PBX `plutil` 通过；新增两个源码及测试素材仅进入正确既有 target。`scripts/validate-ios.sh build` 实际 Debug 构建 exit 65；39 条 error 均位于 OpenAPI Generator 的 `GeneratedSources/Client.swift`，新增 sanitizer 已进入 SwiftCompile 且无自身诊断。未运行完整 App、Release、XCUITest 或真机。
- PBX 引用检查还发现 HEAD 中已有 UITests 指向未定义的 Frameworks phase；删除该悬空引用（原本无 framework 条目），未新建空 phase/target。现有三 target 的 build phase 引用及本轮 Sources/Resources 所属检查通过。
- 测试源、生成器与 `app/ThenAppTests/Resources/AvatarPhotoIntake/manifest.json` 为可复现长期证据入口；不以临时日志路径作为永久证据。测试预算只服务当前色块夹具，不是生产性能参数。
- 未完成：真实 RAW/压缩炸弹矩阵、处理中取消和同步 C 调用硬时限/峰值内存、PhotosPicker/iCloud 输入、Vision/公平性、真机锁屏及实际备份、完整 App 集成。TEST-01、SEC-01、TEST-06 及完整 AVATAR-ACC 保持 pending；不得用 TIFF 拒绝替代 RAW，也不得用已取消任务替代运行中取消。无业务数据/schema 变更；回滚时成组移除本 POC 源码/测试/资源登记，保留已有历史数据与入口。

## 2026-09-13文件式导入实施契约

- 当前任务 IOS-04；本次 60 分钟检查点先完成系统 file representation 回调内可调用的导入器与真实文件测试，再接 SwiftUI PhotosPicker。完整 IOS-04 仍以系统单选、声明门禁、取消、iCloud 失败和无标识泄漏为完成条件。
- `Services/Photos/AvatarPhotoFileImporter.swift` 接收系统临时 URL、声明格式与本次成年本人确认；通过初始化器注入同一 session store 与显式 POC limits。未确认或任务已取消时，在读取源文件/创建 session 前停止。外部路径仅在平台适配器调用栈中存在，不进入 Domain、UI、日志或返回值。
- 系统源文件始终归系统所有：只读、不移动/删除、不复制原文件名或属性。先用已实现 ImageIO 预检文件、格式/魔数和像素头，再创建随机受保护 `original_import`，通过固定小缓冲分块复制；限制累计字节、阶段时限并在每块之间检查取消。源文件打开禁止最终路径 symlink，核对普通单链接文件身份/大小/时间以拒绝预检后替换或复制中变化；复制后在私有文件上再次预检并重新保护，成功后才返回 opaque handle。
- 任何失败、取消或保护失败都只清理本次已创建会话；清理失败单独报错且不发布可用句柄。不创建全局 store、外部路径注册表、默认允许的声明状态或不可取消的无主任务。
- 不假设 PhotosPicker 的 Transferable 导入闭包继承 TaskLocal，也不在回调结束后才读取系统临时 URL；具体系统适配必须有实际回调生命周期证据。文件夹具调用不冒充真实选图/iCloud/系统取消验收。
- 此次没有产品行为、数据 schema、权限、依赖或 UI 框架变更。更复杂的导入配置/全局上下文或替换 SwiftUI PhotosPicker 均不在本次实施内。

## 本轮证据：AVATAR-POC-IMPORT-20260913-01

- 2026-09-13，Codex 执行，基线 `2bc97280796791fa3d20b6407e01b7a8e403b0fa` 加当前工作树。新增 `Services/Photos/AvatarPhotoFileImporter.swift` 与 `AvatarPhotoFileImporterTests.swift`，复用现有 store/sanitizer 和固定合成夹具；没有 SwiftUI 界面、Photos 标识、全图库权限、网络、数据库或新依赖。
- iPhone 17 / iOS 26.5（23F77）专用模拟器：`THEN_AVATAR_TEST_SIMULATOR=<已启动ID> scripts/validate-avatar-photo-session.sh importer` 6 项 Swift Testing、13 个参数化用例通过；没有 cancelled/skipped 测试。工具链与 Swift 6/完整并发/MainActor 默认隔离保持基线。
- 真实文件断言覆盖：未声明/预先取消时不读源、不创建根目录；JPEG/PNG/HEIC 原始字节复制及后续净化/原图删除/会话删除，源文件字节不变；随机目标名称与排除备份；源最终路径 symlink、硬链接、缺失、格式伪装和文件超限在建会话前失败。
- 在文件预留完成处注入取消、源内容变化和存储错误，私有半成品会清理；清理本身失败单独报告，阻止新会话，模拟下次启动清扫可恢复。取消注入运行在结构化子任务，避免把测试 runner 本身取消而漏掉清理断言；最初 runner 出现 cancelled 的结果未计为通过证据。
- 另以固定种子的原创 512×256 噪声色块在测试内生成合法 PNG（无人物/用户数据），确认编码结果超过两个 64 KiB 缓冲，分块复制后字节完整且可净化为 32×16。该临时合成输入自动清理，不用于 Vision 公平性或生产性能校准。
- helper 新增 importer 模式；原 session 12 项及 imaging 7 项回归通过。PBX 正确登记到原 ThenApp/ThenAppTests Sources，仍只有既有三个 target。`scripts/validate-ios-architecture.sh`、`plutil`、文件引用/文档链接及 `git diff --check` 通过。
- 实际 `scripts/validate-ios.sh build` Debug exit 65，39 条 error 全部来自既有 `GeneratedSources/Client.swift` 并发隔离冲突；新 importer 已进入 SwiftCompile，无自身诊断。完整 App、Release、XCUITest 未运行。
- 仍待验证：SwiftUI PhotosPicker 的真实 file representation 接入及回调依赖绑定、声明页面、系统取消/iCloud 离线；多个缓冲之间的运行中取消、实际写盘中断/磁盘满、最低设备硬时限/峰值内存和保护效果。文件原位变化检查是输入一致性守卫，不宣称抵御同进程恶意并发篡改；实际系统源生命周期需独立证据。IOS-04 保持 in_progress，TEST-04/05/06 与完整 AVATAR-ACC 保持 pending。

## 本轮证据：AVATAR-POC-TRANSFER-20260913-01

- 本次仍执行 IOS-04，不创建第二套生产模块、全局导入服务或 TaskLocal 配置方案。通过真实 SDK 探针验证系统文件传输的上下文边界，弥补直接调用 importer 的测试盲区。
- 可复现源码：`scripts/probes/AvatarPhotoTransferProbe.swift`（命令行）与 `AvatarPhotoPickerProbe.swift`（临时 SwiftUI 探针）；入口 `THEN_AVATAR_TEST_SIMULATOR=<专用已启动ID> scripts/probe-avatar-photo-transfer.sh provider|picker`。它们不进入 ThenApp/ThenAppTests Sources，不生成业务句柄、不读取旧数据库；只有合成固定 token 与布尔结果。picker 模式仅向专用模拟器添加仓库红蓝色块，安装 `org.then.internal.picker-probe`；不是 11-01 完整 DEBUG Host。
- Xcode 26.6 / Swift 6.3.3，iPhone 17 / iOS 26.5（23F77）实测：直接 `Transferable.init(importing:contentType:)` 得到 `representationScope=true, callbackScope=true`；通过 `NSItemProvider.loadTransferable` 得到 `representationScope=true, callbackScope=false`。正式脚本复现同样结果。此处 getter 捕获值与回调里重新读取 TaskLocal 是两个不同观察，不把前者通过推广成 PhotosPicker 会话注入/隔离安全证明。
- 结论：只测直接文件导入会漏掉系统回调上下文差异；回调内动态读取 TaskLocal 不可作为现有适配的依赖来源。不得用默认全局 store/预算或返回未经复制的临时 URL 掩盖丢失上下文。Apple [TaskLocal](https://developer.apple.com/documentation/swift/tasklocal) 的继承规则和 [FileRepresentation](https://developer.apple.com/documentation/coretransferable/filerepresentation) 文件生命周期说明是 API 依据；本探针只证明当前记录的平台行为。
- 真实 SwiftUI PhotosPicker 探针已编译、安装、启动，单项选择器打开并显示合成色块和系统最小访问说明；没有请求全图库权限。当前 UI 工具可打开按钮，但系统网格无可用 AX 项，坐标点击报告窗口不可用，重新获取窗口后仍未完成点击。因此 **选中后的 PhotosPicker 回调、源文件可用性布尔结果和系统取消均未验证**，不可记录为单选成功或 IOS-04 完成。
- 下一步必须把实际 PhotosPicker 回调的配置绑定、操作取消/迟到回调、源文件生命周期及文件清理一起验证；NSItemProvider getter 的一次捕获成功、直接 import 测试和临时探针启动均不能替代。现有 17-01 App 编译阻断保持，完整测试壳、真机与最终 go/no-go 均未通过。

## 2026-09-13系统选图XCUITest检查点

继续 IOS-04，使用已批准测试方式 XCUITest 验证真实系统回调：在临时目录建立仅包含既有 SDK 探针和 UI 测试的隔离工程，使用专用模拟器与已导入的原创色块。该工程无正式 ThenApp 代码、生成 Client、数据库或依赖，不能替代 App 编译、完整 DEBUG Host 或 TEST-05/06。目标是取得选择后的 callbackScope/sourceExists 结果并验证取消后无回调，不使用坐标工具失败作为系统功能失败结论；本轮不据此采用 TaskLocal 依赖方案。

## 本轮证据：AVATAR-POC-TRANSFER-UI-20260913-01

- 已执行 `THEN_AVATAR_TEST_SIMULATOR=<专用已启动ID> scripts/probe-avatar-photo-transfer.sh uitest`。正式脚本生成临时诊断工程并运行真实 XCUITest：Xcode 26.6 / Swift 6.3.3；iPhone 17 / iOS 26.5（23F77）；`xcodebuild test` exit 0，xcresult summary 为 2 passed、0 failed、0 skipped。ThenApp 正式工程 target/依赖未改动，没有旧 App/数据库启动。
- 测试 1：打开系统 PhotosPicker，取消后选择按钮再次可操作，状态仍为 `Ready: synthetic fixture only`，没有收到选择结果。
- 测试 2：分别通过异步 `loadTransferable` 和返回 Progress 的 completion-handler 接口，每种连续两次选择。四次均为 `representationScope=true, callbackScope=false, sourceExists=true, fixtureMatched=true, correctOperation=true`；每次使用新操作序号，未复用上一操作的捕获值。
- 源素材身份以已登记合成 PNG 的 SHA-256 校验，回调最多读取 65,537 字节且不把字节/路径/哈希打印到 UI；错误选中其他图片不能通过断言。这里只验证系统回调，不调用正式 importer，不把有限读取示例复制为产品的 Data 全量加载方案。
- iOS 26.5 此系统网格没有可用 AX cell；诊断仅在专用 iPhone 17 的已知合成素材位置使用 XCUITest 坐标，截图与素材哈希同时留证。该坐标不是正式产品测试选择器，不适用于任意图库、设备或系统布局；脚本校验专用模拟器名称/机型。生产旅程仍须建立自己的稳定场景与真机证据。
- 初始探针类的 MainActor 标注引起 XCTest 继承初始化器隔离冲突，已改为 nonisolated 测试类、逐个 UI 方法显式 MainActor；App 仍保持原并发基线。早期 AX cell 查询失败结果没有计入通过证据。
- 源码和重跑入口位于 `scripts/probes/AvatarPhotoPickerProbe.swift`、`AvatarPhotoPickerProbeUITests.swift`、`make-picker-test-project.py` 与 `scripts/probe-avatar-photo-transfer.sh`。脚本将本地 xcresult/日志保留到忽略的 DerivedData 并卸载诊断 App/runner；本次本地结果目录为 `DerivedData/AvatarPhotoPickerUITests.dURdr5`。本文记录环境、观察和源码哈希，不依赖初次探索用的临时目录；这不是远程 CI 或发布构建证据。
- 尚未关闭：运行中取消、Progress/continuation 注册竞态、迟到回调、cleanup 失败、系统源生命周期与正式 importer 接线、iCloud 离线、真机保护/性能及完整 DEBUG Host。IOS-04、TEST-04/05/06、整个 AVATAR-ACC-003 仍未完成。已解除的是此前 UI 工具不能取得真实选择回调这一诊断障碍，不能据此宣称 App 构建恢复。

### 下一步适配约束

以下细化已批准的 IOS-04 实现，不增加产品范围或新生产入口：

1. 依赖由服务初始化器注入，每次选图创建独立操作；SwiftUI 保留系统单选，Domain/后续 ViewModel 不接收 Photos identifier、系统 URL 或原图字节。
2. 若使用 private task-scoped 工厂值衔接 Transferable 的静态声明，只允许在构造表示时捕获本次操作的不可变引用；回调内不动态读取 TaskLocal。无工厂上下文、已关闭操作或结果不属于当前操作时，在读取/复制或交付前明确拒绝；不存在全局默认 store/预算、旧上下文重试或兼容分支。捕获必须与取消、过期拒绝测试一起提交，不能仅靠本次四次成功证明所有竞态。
3. 文件回调必须等待受控导入完成，不能返回尚未复制的系统 URL。每次操作明确持有文件复制任务；取消同步阻止后续创建任务/会话，并取消已持有任务，异步收口等待它退出和清理后再返回。现有 ImageIO 同步调用仍不能宣称硬中断。
4. completion-handler 路径明确持有 Progress，continuation 只能恢复一次；覆盖取消先于注册、同步完成先于 Progress 赋值、取消后的迟到结果、复制完成后交付前取消及清理失败。UI 移除/换图不能依靠回调继承 TaskLocal 或父 Task 取消状态。
5. 用可控制的文件 provider/回调测试验证资源及取消竞态，再把同一适配器接入系统探针；仅验证独立复制器、静态源码或探针启动都不足以关闭 IOS-04。

- 本次源码 SHA-256：`scripts/probes/AvatarPhotoPickerProbe.swift` = `ab06c14b51658f513dd1fbdd50b24ebe623c78f5f838e2ba888a5619adadb55d`。

- 本次源码 SHA-256：`scripts/probes/AvatarPhotoPickerProbeUITests.swift` = `d29e895b86792d659ebbd1d8e0abe98cae09ff7948c04888ec6ec9bca03c3e9d`。

- 本次源码 SHA-256：`scripts/probes/make-picker-test-project.py` = `887179beecfc62e64035ff205cf093e9d78ab038cd75ee8aaeae7d7bea9f0ee5`。

- 本次源码 SHA-256：`scripts/probe-avatar-photo-transfer.sh` = `2f68587075bdccf4bb06ab31fb5aa0a67c1b8ec543654ed54b1dcd2ddb6fd94b`。


## 2026-09-13系统适配执行检查点

本次 60 分钟时间盒限定 IOS-04：把真实系统 FileRepresentation 接到既有 importer，落实取消/迟到结果/错误清理的所有权，再以同一原源码通过模拟器单选闭环。仍只使用已登记合成色块，不新增生产入口、数据库、网络、依赖、兼容分支或生产 module。下面是本轮增量证据；前文 TRANSFER/TRANSFER-UI 的源码哈希与限制保留为各自历史快照。

## 本轮证据：AVATAR-POC-PICKER-20260913-01

- 实现：`SystemAvatarPhotoPicker.swift` 注入 store/limits，声明检查先于系统加载；单次操作持有 Progress、continuation 与受控复制任务。private TaskLocal 只在表示构造时绑定/捕获本次操作；缺少上下文不能交付，已关闭操作不能再复制。FileRepresentation 只声明 HEIC/JPEG/PNG，收到的源 URL 在回调内等待复制和校验，不进入领域结果。真实系统源格式/转码矩阵仍未全部实测。
- 取消与交付：取消先同步关闭操作并取消已登记复制/Progress；晚登记的 Progress 立即取消，continuation 只恢复一次。异步收口等待已有复制任务退出，复制成功但尚未交付的会话也要删除。结果必须同时匹配操作 ID 与实际复制句柄；交付在锁内转移所有权并释放操作持有的任务/Progress，之后调用方负责清理及过期 token 处理。清理失败优先返回 cleanupFailed，失败 store 禁止继续读写，启动恢复测试使用同一固定根。
- SDK 错误核验：缺少上下文的 FileRepresentation 错误被 CoreTransferable 包装为 TransferableError，原错误不会直接保留；测试按“拒绝交付且零私有文件”断言，服务从持有的复制任务恢复存储/清理错误，未知 provider 错误映射为 sourceUnavailable。没有打印系统错误正文、媒体路径或用户标识。
- `THEN_AVATAR_TEST_SIMULATOR=<专用已启动ID> scripts/validate-avatar-photo-session.sh picker` exit 0：原源码以 iOS SDK、Swift 6 Complete/MainActor/Approachable 设置编译，9 项 Swift Testing 通过，7 组参数化合计 15 个用例加 2 个单实例，共 17 次。覆盖声明/预取消、同步完成与重复回调、取消先于 Progress 赋值、provider 不回调仍可取消、复制中取消、复制后交付前取消、迟到回调、三格式完整导入净化清理、错误操作/伪造句柄、无工厂上下文、存储不足、清理失败优先及重启清扫；每例检查系统合成源字节保持不变。
- `THEN_AVATAR_TEST_SIMULATOR=<专用已启动ID> scripts/probe-avatar-photo-transfer.sh adapter-uitest` exit 0：隔离壳直接引用原 domain/session/sanitizer/importer/picker 文件，不复制一套实现，不启动 ThenApp/旧数据库。Xcode 26.6 / Swift 6.3.3，iPhone 17 / iOS 26.5（23F77），xcresult 为 2 passed、0 failed、0 skipped。两次真实 PNG 选择均 `fixtureMatched=true, backupExcluded=true, protectionAttribute=unavailable, clean=16x32, originalRemoved=true, rootEmpty=true, freshSession=true`；选择前取消返回 Ready。当前布局坐标和哈希守卫仅用于专用模拟器合成图库，不是完整产品旅程。
- 失败记录：第一次隔离壳编译因 throwing `&&` 表达式缺少外层 try 失败，修复为先读取属性再比较；之后的合并保护断言两次失败。拆开观测确认备份排除为 true，而模拟器缺少 `.protectionKey`，没有得出“complete 保护失败”或“真机保护通过”的结论。产品 complete 请求保持原样；未知/非 complete 的已提供属性仍使探针失败。两轮失败不计入通过次数。
- 本地 UI 证据为 `DerivedData/AvatarPhotoPickerUITests.OELXa6/result.xcresult` 与 build.log，首次属性断言失败保留在 `DerivedData/AvatarPhotoPickerUITests.t27Zwt`。目录被 Git 忽略，脚本/结果摘要/下列源码哈希用于复核；这不是远程 CI 或永久发布制品。诊断 App 与 runner 已由脚本卸载，专用模拟器保留。
- 工程：新增 1 份服务与 1 份 Swift Testing 正确登记到既有 ThenApp/ThenAppTests，合计本 POC 6 份产品文件、5 份测试；工程仍只有 ThenApp、ThenAppTests、ThenAppUITests。脚本 picker 模式以及 adapter-uitest 模式不修改正式工程或绕过其生成插件。
- 验收边界：IOS-04 保持 in_progress。真实系统其他格式/iCloud 离线、声明页面、UseCase/ViewModel 的 generation token/scene 清理、Vision 素材与阈值、真机 Data Protection/实际备份/资源、完整 DEBUG/Release 与 TEST-04/05/06 均未关闭。照片 POC 不是本地 3D 首版，整个项目目标仍 active。
- 本轮不涉及数据迁移、后端/API 或正式用户行为变更；回滚应成组移除新适配器、测试及 PBX 登记并恢复对应脚本入口，不触碰已有临时存储实现、历史数据或其他工作区改动。

- 最终检查：`scripts/validate-ios.sh build` exit 65；其工具链与架构检查先通过，实际 Debug 编译仍为生成 `Client.swift` 的 39 条既有并发隔离错误；新增适配器参与编译且没有新增诊断，Release/完整 App Tests 未执行。独立适配器测试与 UI 工程成功不抵扣 IOS-11/REL-01。

### 本检查点源码 SHA-256

| 文件 | SHA-256 |
| --- | --- |
| `app/ThenApp/Services/Photos/SystemAvatarPhotoPicker.swift` | `48fa1289b5b8a233a69f0065297e372303f9509a834c8d9b8f7eb39c64351f75` |
| `app/ThenAppTests/SystemAvatarPhotoPickerTests.swift` | `68ae8bd1390bc77118ed2be8dc0cdafc6d36a33fa1f1233e96584676451fccc2` |
| `scripts/validate-avatar-photo-session.sh` | `124fdf1dbc91d77028f48d9240bb2add1b302db70b88b58d3b97fcca9be9f0a6` |
| `scripts/probes/AvatarPhotoPickerAdapterProbe.swift` | `4eb94cacd1fd40dd15a4339fd67b9f2db665bef0f393e8b01b674c55f7d7217d` |
| `scripts/probes/AvatarPhotoPickerProbeUITests.swift` | `e731936058f7f1c20e896cd14348be22c5a5a11512b075f4f655d5b6363b0bdb` |
| `scripts/probes/make-picker-test-project.py` | `a3a202396930b5c7544d538071966f1c56e70fe198bb89836ae11e864b46e812` |
| `scripts/probe-avatar-photo-transfer.sh` | `177c1cace9af36062e7001153579c9725cfc2d4e82c7f07fed1af100c5789895` |


## 2026-09-13系统格式矩阵执行检查点

继续 IOS-04，本次 60 分钟时间盒核验真实 PhotosPicker 的 HEIC/JPEG/PNG、动画 PNG 与未批准 TIFF 文件交付。使用仓库既有 manifest 和五份合成色块，不新增真人素材或 Vision 校准。独立 `.image` 诊断表示先有界读取系统源，核对 manifest 哈希、类型与帧数，再调用原系统适配器；这个诊断读取仅存在于 scripts 探针，不进入产品导入器。合法格式必须保留选中源身份并完成净化/删除；动画和未批准格式必须返回 unsupportedFormat 且不残留会话，不能把系统转码或通用 sourceUnavailable 当成契约通过。矩阵在脚本本次新建的专用 iPhone 17 / iOS 26.5 模拟器运行，结束仅删除本次创建的设备，既有模拟器与 App 数据不变。结果将记录在本节后续证据，执行前不预填 passed。


## 本轮证据：AVATAR-POC-FORMATS-20260913-01

- 缺陷复现：原适配器对真实系统 TIFF 选择返回 sourceUnavailable；FileRepresentation 不支持该类型而没有结果，被错误归类成源读取失败。已增加系统声明类型准入检查，位于本人声明/预取消之后、创建操作/调用 provider 之前；空类型列表、仅笼统 image 或仅 RAW/GIF/TIFF 的声明均不能走加载或生成私有会话，返回 unsupportedFormat。允许类型收到后仍执行既有实际字节/魔数/帧数和资源预检，不按扩展名接收、不增加转码/兼容路径。
- 回归：`THEN_AVATAR_TEST_SIMULATOR=<既有专用已启动ID> scripts/validate-avatar-photo-session.sh picker` exit 0，10 项 Swift Testing，共 22 个实例；新增 5 个未批准/未知声明类型用例验证正确错误、provider 未启动及零会话，其余取消/重复/迟到/清理/交付用例仍通过。
- 真实矩阵：`scripts/probe-avatar-photo-transfer.sh matrix-uitest` exit 0。脚本新建 iPhone 17 / iOS 26.5（23F77）模拟器，顺序导入原 manifest 的五份合成文件；隔离壳引用正式适配源码及 manifest，先通过独立 image 表示在回调内最多读取 1,000,001 字节验证系统源哈希/类型/帧数，拒绝未知素材，再调用正式 adapter。此有限诊断读取不进入产品、不替代受控 importer，也不用于真人素材。
- xcresult summary：2 passed、0 failed、0 skipped；测试一为选择前取消，测试二包含下面五项按序执行。每次系统源身份均与 manifest 匹配；连续拒绝后仍能接受合法格式，不需要重启或清库。

| 合成文件 | 系统类型 / 帧数 | 实际适配结果 | 文件清理 |
| --- | --- | --- | --- |
| unsupported-tiff | public.tiff / 1 | rejected，unsupportedFormat | 根为空，无产品导入 session |
| animated-png | public.png / 2 | rejected，unsupportedFormat | 根为空，无可用句柄 |
| metadata-heic | public.heic / 1 | accepted，源与副本类型/哈希一致；净化为 16×32 | 原图删除、备份排除 true、会话根为空 |
| metadata-jpeg | public.jpeg / 1 | accepted，源与副本类型/哈希一致；净化为 16×32 | 原图删除、备份排除 true、会话根为空 |
| metadata-png | public.png / 1 | accepted，源与副本类型/哈希一致；净化为 16×32 | 原图删除、备份排除 true、会话根为空 |

- 真实模拟器中的保护等级属性仍为 unavailable；不把备份排除属性等同于真机文件保护或真实备份效果。合法三次使用不同 session；无界原图 Data 读取没有进入产品代码。
- 首轮矩阵的诊断坐标按导入正序编写，而新建图库实际以相反顺序展示；源哈希断言及时失败，未把误选视为通过。根据五次实际文件身份校正固定位置后重跑，仍保留逐项源哈希守卫。坐标只适用于该新建机型/系统及合成布局，不扩展为任意图库的产品自动化。
- 本地失败记录 `DerivedData/AvatarPhotoPickerUITests.zdzu5Q`；修复后记录 `DerivedData/AvatarPhotoPickerUITests.B8Mjqk/result.xcresult` 与 build.log。脚本结束关闭并删除各自创建的模拟器；当前设备清单已确认无残留矩阵设备，既有模拟器不受影响。产物目录被 Git 忽略，本文命令/环境/观察/源码哈希是可重跑记录，不是远程 CI 或永久发布制品。
- `scripts/validate-ios.sh build` 实际复编仍 exit 65；工具链、架构检查通过，39 条 error 均来自生成 Client.swift 的已有并发隔离冲突；本轮适配器没有新增诊断，Release/完整 App Tests 未执行。未改生成代码、并发基线或正式 target 数量。
- 保持未完成：iCloud 离线/下载取消、真实 RAW/压缩炸弹和大图资源矩阵、完整声明/UseCase/ViewModel/DEBUG Host、VoiceOver/scene/中断恢复、Vision 授权集与真机门禁。IOS-04/TEST-04/05/06、完整 AVATAR-ACC、11-01 go 与整个项目目标未关闭；这些照片结果不抵扣本地 3D 首版。
- 变更只修复既有格式拒绝规则，无数据/API/schema 或生产入口变化；回滚同步恢复服务类型准入与测试/探针变更，保留已登记失败证据，不能将已知 TIFF 错误重新列为通过。

### 格式检查点源码 SHA-256

| 文件 | SHA-256 |
| --- | --- |
| `app/ThenApp/Services/Photos/SystemAvatarPhotoPicker.swift` | `045f01d4278a87007147fbc86dcdb906bdd7cb8a67547dcd6d5cae5e2a1507dd` |
| `app/ThenAppTests/SystemAvatarPhotoPickerTests.swift` | `955aa6bdb3f098c995b135141d18125efa62d76a780572e56e7e87f95f9a2cb7` |
| `scripts/probes/AvatarPhotoPickerAdapterProbe.swift` | `48f6a340d786069a2fa3c050679edb9462c172b54261d5b4ac74b244d80c0185` |
| `scripts/probes/AvatarPhotoPickerProbeUITests.swift` | `74e5c2ff2d5c3b5124663a59d12e37ee952320b25cf04553786d59da26620a98` |
| `scripts/probes/make-picker-test-project.py` | `e2702aaec1077b177ee827a6674a8906e1899b3c4f38835e6f415ceaae797989` |
| `scripts/probe-avatar-photo-transfer.sh` | `2e7275da423a62c18b4e6176e7005ebe9852a578086ec5d8dac9b8ce734bd1a8` |

素材登记说明：manifest 和生成脚本的用途文案同步涵盖同一 11-01 内的系统传输测试；五份素材字节、ID、哈希与授权依据均不变，不扩展至 Vision 校准或真人数据。


## 2026-09-13依赖与执行状态复核

之前检查点把 IOS-04 的“任务不关闭”与完整 Host/iCloud/真机门禁混写，产生了计划依赖循环：IOS-07 依赖 IOS-04，IOS-08 依赖 IOS-07，IOS-09 Host 又依赖 IOS-04/08，因此不能要求 IOS-04 在 IOS-09 Host 完成之后才允许完成。本次修正任务归属，不变更用户行为、接口、资源或隐私契约，也不降低完整 POC 的任何退出条件。

- IOS-04 completed：仅确认其既定平台服务实现与已经运行的 Swift Testing/真实系统 UI 子集，口径与 IOS-03/05 一致；不会证明声明页、完整状态机、真实 iCloud、真机保护或发布构建通过。
- IOS-09/10、TEST-04/05 继续负责完整声明/换图/scene/迟到结果/无障碍与测试壳；TEST-01/06 继续负责 RAW/异常资源、真实 iCloud、最低设备和硬件验收。所有完整 AVATAR-ACC 与 REL 仍 pending，既有故障证据和测试要求保留。
- 下一项实现是 IOS-06，实际前置 SEC-01 尚未完成。五份已登记色块只能验证导入和编解码，不能用于证明人员检测、遮挡或公平性；不将其冒充 Vision 准入素材。IOS-06 改为 blocked，执行状态同步 blocked，无任务 in_progress。
- 当前复核 `xcrun xcdevice list` 没有 available 的物理 iOS 设备；曾登记的 iPhone 16 显示 available=false，不能当作当前可测试设备。完整 App 仍受 17-01 已知生成 Client 冲突阻断，不能以隔离探针抵扣。
- 恢复条件：SEC-01 的适用素材/授权依据/预期标签获批后继续 IOS-06；17-01 的架构决策关闭后接入完整 Host；取得可用目标真机与受控 iCloud 测试条件后执行相应硬件用例。各前置独立记录，不要求等到全部齐备才恢复已具备条件的任务。

前文各轮的 in_progress 和缺口描述是当时检查点；当前执行状态与任务状态以文件头和任务表为准。11-01 blocked 不代表整个项目目标完成，也不授权启用生产照片能力。

## 2026-09-13App编译前置关闭

17-01 已按用户确认的 ThenTransport 技术模块例外修复生成 Client 隔离问题。正式 App Debug/Release、测试 target 构建通过；本切片原五组 Swift Testing 在真实 ThenAppTests 宿主内 45 项全部通过，与 3 项 transport 测试合计 48 项。结果见 [THEN-TRANSPORT-20260913-01](../acceptance/17-云端生成与任务管理验收.md#2026-09-13生成client正式工程验收)。仅替代前文各历史检查点的 App 编译失败结论，不把隔离壳 UI 或旧页面 UI 回归当作照片 Host 验收。

IOS-11 继续随 IOS-06～10 的新增文件登记保持 pending；11-01 整体仍 blocked 于 SEC-01/Vision 素材和预期标签，完整 Host、性能/隐私真机及 REL 门禁仍未完成。没有增加生产照片入口或变更媒体协议。

## 2026-09-13选图交接同步回调复核

12-03 新衣物适配器测试复现同步完成早于 Progress 返回的取消丢失；检查 SystemAvatarPhotoPicker.swift 发现同样的 finished 分支。按已批准 IOS-05 的所有权/取消合同修复：先在 SystemAvatarPhotoPickerTests.inlineCompletion 的原同步 nil/错误参数中补充 Progress 必须取消断言，再修正服务登记时机，保留人物声明、临时会话与删除协议。此为已有处理内核缺陷修复，不开启生产照片入口；SEC-01、完整 Host 与真机门禁不改变。

`WardrobeAvatarTransferRegressionRed20260913.xcresult` 真实复现 inlineCompletion 的两个参数 Progress 未取消；SystemAvatarPhotoPicker 将登记条件从 loading 且未 finished 改为 loading，取消/交付阶段仍不夺回所有权。`WardrobeSelectionFullRegression20260913.xcresult` 的原人物选图 10 项通过，包括补充断言；全量 145 注册、141 passed、2 前景诊断 failed、2 physical skipped。首次方法短名筛选包 WardrobeAvatarTransferRed20260913 实际执行 0 项，不计为证据。完整命令、环境与失败范围归 12-03；本修复没有改变人物照片内容准入或解除本计划其余门禁。
