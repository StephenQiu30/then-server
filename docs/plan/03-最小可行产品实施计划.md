# MVP 实施计划

> **历史计划。** 本文记录旧记账、日历和出行 MVP 的实施状态，不再作为当前进度；当前计划见 [`10-OOTD产品实施计划.md`](10-OOTD产品实施计划.md)。

## 状态

`in_progress`，2026-08-11。当前范围已收敛：预算、长期原始轨迹、票据原图、Time Sensitive、国内地图适配和商业化均不实施；最小 Xcode 工程与依赖边界已完成。账务月度概览、月份切换、canonical root 流水、本地搜索、六个月支出趋势、资金账户/分类/行程关系组合筛选、信用卡还款专用入口、OCR 会话候选、日历缓存、日历事件到出行计划本地闭环、路线/提醒/导航交接、导航失败显式复制、实际行程、“今天”只读组合、五 Tab、本地权限中心、行程—交易关联与消费复盘三态、结构化导出、分层删除、异常导出清理、低存储失败关闭、只读诊断与关于、简体中文本地化基线、后台隐私遮罩、App 隐私清单和数据库/导出文件保护操作已落地。当前 `zh-Hans` 共享 Scheme 仍为 88 项 Swift Testing 与 16 项真实 XCUITest 共 104/104 通过，结果包为 `/tmp/then-p0-localization-tests.ssaANh/Result.xcresult`；新增日历完整访问前置说明专项 XCUITest 另以 1/1 通过，结果包为 `/tmp/then-p0-localization-permission-ui.xPEk59/Result.xcresult`。每个 UI 测试使用独立的真实 GRDB 文件目录，无效存储 UUID 已验证闭合失败且不回退业务界面；五个一级页面、快速记账和导航失败卡片已通过原生描述、特征、点击区域、元素检测、对比度与裁切审计，最大辅助字号深色测试也已通过。阶段 3 仍保持进行中，因为 EventKit、MapKit、通知、Apple 地图真实打开和 Core Location 前后台/恢复/耗电尚缺真机证据；阶段 4 已取得快速支出到部分退款、账本月报/流水/搜索、信用卡还款、支出更正到完整撤销审计历史、纯文字地址计划、合成全天日历事件到计划、导航失败复制降级，以及 Journey 无消费确认、消费关联、解除、重关联和删除后账务保留的真实 UI 证据；阶段 5 已完成保护操作、低存储失败关闭和 457 个生产界面键/5 个系统键的本地化漂移门槛，继续补最低 iOS 18 真机文件保护、VoiceOver、减少动态效果、签名候选聚合隐私报告与异常场景验收。

## 关联文档

- [`../prd/03-最小可行产品需求.md`](../prd/03-最小可行产品需求.md)
- 旧生活管理 03–09 号设计已从工作树删除；详细设计保留在 Git 历史，当前数据处置边界见 [`../design/12-OOTD技术债清理与迁移设计.md`](../design/12-OOTD技术债清理与迁移设计.md)。
- [`../acceptance/03-最小可行产品系统验收.md`](../acceptance/03-最小可行产品系统验收.md)

## 实施原则

- 先交付单设备、本地优先的 P0 完整闭环；账号与多设备同步固定进入 P1。
- 每个阶段都形成可运行的垂直切片，不先铺满空目录、空接口和空表。
- 先完成领域模型、数据库和系统能力 POC，再扩展页面数量。
- 记账正确性、活跃行程可靠落盘和权限降级优先于视觉动效和智能建议。
- OpenAPI 与 PostgreSQL schema 只有在关联设计批准后才加入业务定义。
- 穿搭、社交、共享、预算、票据附件、长期原始轨迹、国内地图适配、特殊通知、商业化和云 OCR 不进入本计划。

## 阶段与发布切片

```mermaid
flowchart LR
    D["0. 决策冻结"] --> F["1. 工程与数据基础"]
    F --> A["2. 本地记账闭环"]
    A --> T["3. 日历与出行闭环"]
    T --> L["4. 今天与生活关联"]
    L --> H["5. 本地 MVP 加固"]
    H --> B["6. 真机 Beta 与发布"]
    B -. "P0 发布后" .-> C["P1. Go 后端与同步"]
```

### 阶段 0：范围冻结（已完成）

目标：固定当前边界，避免 schema、接口和 UI 为不实施能力留下占位。

已确认：

1. P0 单设备本地优先，首笔正式交易前确认本币；只支持同币种转账。
2. 正式交易通过退款、冲销和原子更正处理，不直接改写历史分录。
3. 原始轨迹只保留到摘要确认或行程丢弃，票据图片只保留在 OCR 会话内。
4. Apple 地图按系统能力尽力提供路线与交接，不设置城市/交通方式覆盖门槛，不接国内地图供应商。
5. 不实现预算、Time Sensitive、订阅、付费、免费容量或权益分层。

退出条件：上述决定已写入 PRD、设计、计划和验收，且不存在相关空入口、空表、空接口或临时 SDK。文档范围冻结已满足；静态仓库边界由阶段 5 的可重复脚本持续守护，运行时文件/网络与深链证据在阶段 6 收口。

### 阶段 1：工程与数据基础

目标：建立最小可构建 App、集中本地数据库、依赖容器和真实系统能力 POC。

- 创建 Xcode 工程、单 ThenApp module 和测试 target。
- Exact 固定 GRDB 与 Swift OpenAPI packages，提交 Package.resolved。
- 建立 App/Core/Features/Services/Data 的最小真实代码边界。
- 建立 DatabasePool、集中 DatabaseMigrator、领域 ID/时间/金额/坐标值对象。
- 统一数据库 Data Protection 应用函数：每次打开显式设置目录，再设置主文件/WAL/SHM 为 `completeUntilFirstUserAuthentication`；Debug UI 测试路径复用同一实现。自动化用委托真实文件系统的记录器断言每个保护操作，真机再检查实际属性和锁屏语义。
- 冻结已经执行的 migration identifier；`v6_relax_manual_location_coordinates` 在旧版地点坐标三列仍为 `NOT NULL` 时原子重建地点表并保留既有数据、外键与索引，在当前结构上为空操作；`v7_add_journey_expense_review_state` 只追加 Journey 消费复盘三态并按既有关系安全回填。
- 建立历史 `v1...v5` schema 夹具：先插入带坐标地点和关联计划/路线/提醒，再执行全部最新 migration，断言旧数据不变、三列可为空、`foreign_key_check` 与 `integrity_check` 通过，随后实际保存纯文字目的地。
- 建立 App Router、权限状态和脱敏错误体系。
- 完成 EventKit、UserNotifications、Core Location、MapKit 和 Vision 真机 POC。

退出条件：工程可构建测试；空数据库创建/迁移通过；每个系统能力有真机证据和降级结论。

### 阶段 2：本地记账闭环

目标：无需账号和网络完成可靠个人账本。

- 账户、分类、交易和分录本地 schema。
- 首次离线初始化 LocalProfile、系统账户、默认现金账户和内置分类；发布首笔正式交易前确认本币。
- 完成现金、银行卡、电子钱包、信用卡与一级收入/支出分类的创建、归档、恢复和选择器；状态切换保持父子分类始终一致，余额始终从 posted 分录按账户正常方向重建。
- 支出、收入、转账、期初、退款、冲销和原子更正领域用例。
- 在快速记账中提供“还款”展示选项，复用 `transfer` 领域命令并把来源约束为资产、目标约束为信用卡负债；不新增交易种类或报表口径。
- 为退款、冲销和更正实现 canonical root head compare-and-swap、当前 replacement 解析、有效退款累计、稳定命令标识与原子幂等回读。
- 快速记账、流水、详情、账户/分类和基础月报。
- 余额与月度收支可重建查询、月度/时区边界测试。
- 在同一只读快照中生成所选月份合计、分类排行与截至该月的六个月支出趋势；零月份补齐，不新增报表缓存表。
- 增加资金账户、收入/支出分类、行程关联状态与现有本地搜索词的组合筛选；流水匹配 current replacement，月报按命中 canonical root 的全部正式分录保持退款、冲销与更正净额。
- OCR 候选与确认流程；识别不达标时保持手动入口并关闭非必要入口。

退出条件：所有账务不变量和 [`../acceptance/03-最小可行产品系统验收.md`](../acceptance/03-最小可行产品系统验收.md) 的本地记账场景通过。

### 阶段 3：日历与出行闭环

目标：从日历或手动计划到提醒、导航和实际行程。

- 日历授权、来源选择、完整扫描和事件快照。
- 重复事件、全天、时区、事件移动/取消/删除处理。
- 地点确认、TripPlan、RouteEstimate 和 DepartureReminder。
- 完成事件到计划的本地垂直切片：从真实 `GRDBCalendarRepository` 持久化的 occurrence 构建编辑器 seed，覆盖普通事件来源值、地点确认和全天事件显式时间门禁。
- 使用真实 SQLite 仓储断言 TripPlan 字段来源、`event_trip_links` 的 occurrence ID/source version、提醒期望状态和 CalendarOccurrence 不变；再以失效 occurrence 注入证明地点、计划、提醒和关系全部回滚。
- 在计划保存后再次提交同一 occurrence 的标题、地点与时间新版本，断言 CalendarOccurrence 产生 pending revision，而 TripPlan、LocationSnapshot、DepartureReminder 与 EventTripLink 保留用户采用时的值和 source version。
- 追加 `v8_add_trip_plan_revision_review`：为 TripPlan 增加乐观版本和标题来源字段，为 revision 增加解决时间与状态一致性约束；历史标题安全回填为用户值，空库、v7 升级、重复执行、外键与完整性都必须测试。
- 扩展 Calendar/TripPlan Repository：读取来源差异与关联计划；同一 occurrence 重复建计划闭合失败；“保留当前计划”只解决所见 revision；“采用到计划”在一个 SQLite 事务中校验 occurrence/link/plan 三重版本，更新计划与关系、使旧路线失效并解决 revision。
- 增加统一来源变化处置页和计划编辑确认页；日程事件与 Today 待处理入口复用同一状态。采用前必须重新确认事件地点文本、到达/出发时间、路线与提醒；陈旧页面显示重新加载，不静默重试。
- 自动化覆盖无关联事件、单一关联计划、历史重复关系、忽略、采用、再次扫描、陈旧 plan/source version、事务故障、路线失效、提醒拒绝与 UI 可达性；Debug fixture 只能证明本地产品流，真实 EventKit 变化通知仍由真机验收。
- 自动化只覆盖编辑器、领域和真实 SQLite 边界；EventKit 真实授权、系统事件变化通知到可见页面和系统通知 request 仍在最低 iOS 18 真机上取证。
- Apple 地图路线/调起和无结果/打开失败降级，不设置覆盖率门槛。
- 为打开失败降级增加可注入的 `DestinationClipboard` 系统边界：ViewModel 保存失败所属 TripPlan，只在用户点击后按“非空地址 → 地点名称”规则写入，并显示成功状态；不自动读写剪贴板。
- 失败卡片使用原生 `Section`/`Label`/`Button` 和 SF Symbol，同时展示错误、目的地和复制操作；不只依赖红色。联合开始地图失败时先展示已保存 Journey，关闭后保留该计划的复制降级。
- 添加 fake 剪贴板的 ViewModel 测试，覆盖失败前零写入、地址优先、空白地址回退名称、成功文案和新失败清理旧成功态；再以仅 Debug 导航失败夹具运行真实 GRDB XCUITest，断言 handoff 失败、复制入口和返回计划。Release 可执行文件必须不包含夹具参数。
- `v4_create_journey_recording` 集中定义 Journey、TrackSegment 和 TrackPoint，以部分唯一索引限制每设备一个未收口行程，并验证状态、segment/point 序号、坐标和 manifest 约束。
- App 级 `JourneyRecordingService` 使用 When In Use、iOS 18 live location updates 与可见后台活动；实现开始、暂停、新 segment 恢复、样本先落盘后质量投影、定位拒绝/低精度降级和启动恢复。
- 结束固定经过 `finalizing → reviewing`，冻结 sequence/point count/SHA-256 manifest 并计算摘要；确认或丢弃在同一 SQLite 事务中清理所有原始点与片段，任意故障不得宣称已完成。
- 使用 fake location adapter 和故障注入覆盖重复开始、权限拒绝、单调点序、暂停/继续新段、收尾幂等、无点摘要、确认/丢弃清理、清理回滚与冷启动恢复。
- 真机验证锁屏、后台、低精度、权限撤回、系统杀进程和耗电。

退出条件：不依赖第三方回传完成计划—导航—Journey 闭环，所有高风险真机场景有证据。

### 阶段 4：今天与生活关联

目标：把两条独立能力组织为一个产品。

- `TodaySnapshotService` 只读组合今日事件、下一次出发、月度收支摘要、唯一未收口 Journey 和真实待处理项；不创建首页汇总表，各块失败独立降级。
- 把根导航完整为固定“今天—账本—日程—行程—我的”五 Tab；行程 Tab 同时显示计划、未收口状态和已完成摘要，我的 Tab 不留伪账号/云备份入口。
- `v5_create_life_links` 只建 `transaction_journey_links`；关系引用 posted canonical root，同 owner 交易 root + Journey 唯一，只允许用户确认并幂等创建。
- 新增前向 `v7_add_journey_expense_review_state`，为 Journey 持久化 `pending | no_expense | has_expense`；既有正式关系回填 `has_expense`，其余只回填 `pending`，不修改已执行的 `v4`，并用历史 `v1...v6` 升级夹具证明既有 Journey、关系与账务不变。
- 行程结束后提供记账/关联已有交易/显式“本次无消费”分支；标记无消费、创建关系和解除关系必须在显式事务中同步维护复盘状态。汇总从关联 root 的 posted 分录净影响重建，解除最后一条关系后回到待复盘且不删除任一端。
- 待处理只展示已持久化的 CalendarOccurrence pending revision 与 `finalizing | reviewing` Journey；OCR、文件导入和同步冲突不留空分组。
- 全局新增面板和无权限/无数据空态使用系统控件、SF Symbols 和语义样式，不依赖自定义组件才能完成主流程。

退出条件：事件—计划—Journey—交易主闭环端到端通过；解除/删除关系不破坏两端实体。

### 阶段 5：本地 MVP 加固

- 实现版本化 JSON 导出：在一次 SQLite 一致性读取中导出账户、交易、分录、确认计划、Journey 摘要和生活关系，金额使用最小货币单位，排除日历来源身份、通知 request、导航诊断、票据图片和原始轨迹。
- 用委托真实文件系统的记录器断言每个导出随机子目录和最终 JSON 都收到 `complete`，数据库目录、主文件、WAL 与 SHM 都收到 `completeUntilFirstUserAuthentication`。Simulator 不可靠返回保护属性，最低 iOS 18 真机必须检查实际属性并完成锁屏读写门槛。
- 实现受保护临时导出目录、系统分享完成回调与成功/取消/失败清理；生成失败不得修改源数据或留下半成品。
- 把文件数据写入纳入可替换文件系统边界；用实际写出合成部分文件后抛出 `fileWriteOutOfSpace` 的故障注入，断言容量不足映射为稳定领域错误、随机目录完全删除、源数据库计数不变、用户看到脱敏可重试提示，随后正常导出仍能成功。P0 不实现分批或多文件降级。
- 实现日历缓存清理：先删除本地 EventTripLink，再原子清理 revision、occurrence、series、scan 和 source，保留计划、提醒、Journey 与账务。
- 实现已收口 Journey 删除：级联清理轨迹和交易—行程关系，保留 TripPlan、交易与分录；未收口记录继续走完成/丢弃状态机。
- 实现本机全量删除协调器：停止定位、取消本 App 出发提醒、原子清空全部用户表、重新 bootstrap 新本地资料，并让根环境切换到新 identity。
- 在根视图按 scene phase 覆盖无业务正文的隐私遮罩，确保 `inactive/background` 的系统任务切换快照不显示敏感内容。
- App 环境启动时清扫固定导出根目录；创建新导出前再次清扫，失败时阻止生成并交给现有可重试错误入口展示。
- 提供本地数据风险说明，不虚构云备份。
- 将“诊断与关于”收敛为只读发布元数据：用纯值模型从 App bundle 读取名称、版本与构建号，显示固定的“单设备本地”和“诊断收集未启用”状态。缺失 bundle 字段降级为“未知”，不增加日志、诊断包、剪贴板或网络边界。
- 为发布信息模型增加缺字段和完整字段单元测试；扩展真实 App XCUITest 滚动到诊断与关于区域，断言 `0.1.0 (1)`、单设备本地与诊断收集未启用，并纳入“我的”最大辅助字号及原生无障碍审计。
- 以 Xcode 编译器导出的生产字符串建立 `Localizable.xcstrings` 简体中文基线，移除空键并保留类型正确的位置参数；手工审核 `InfoPlist.xcstrings` 中 App 名称和日历、相机、定位权限用途说明。两份目录加入 ThenApp target Resources，不创建 `.strings` 或第二份目录。
- 新增 `scripts/validate-ios-localization.sh`：验证两份目录唯一存在且 `sourceLanguage = zh-Hans`、无空键、可由 `xcstringstool` 编译、项目资源引用唯一；通过 Xcode 导出的生产字符串逐键比对目录，并可对候选 `.app` 断言 `zh-Hans.lproj` 中的 `Localizable.strings` 与 `InfoPlist.strings` 均已打包。使用独立临时副本删除一个合成键，证明漂移门槛稳定失败。
- 完成数据库迁移失败、低存储、断网和异常恢复。
- 隐藏账号和冲突入口，但保留协议边界；不创建云附件或原始轨迹入口。
- 扩展 `scripts/validate-ios-privacy.sh`，阻断生产 Swift 源码中的控制台、统一日志、signpost 和标准输出/错误入口；在独立临时副本中注入合成敏感日志，证明验证器稳定非零退出且不修改工作区。
- 新增 `scripts/validate-ios-motion.sh`，阻断 P0 生产 Swift 源码中的显式/隐式 SwiftUI、UIKit 和 Core Animation 动画入口；在独立临时副本中注入合成 `withAnimation`，证明验证器稳定非零退出。真机仍需开启系统“减少动态效果”完成核心旅程。
- 新增 `scripts/validate-p0-scope.sh`：精确比对 19 张本地表、空 OpenAPI paths、无服务端业务 DDL、SwiftPM 锁定依赖和禁止生产入口/SDK 词项；以合成禁止入口验证脚本能稳定非零退出。

退出条件：导出清单与排除清单可自动断言；三层删除均有前后数据库不变量；全量删除故障注入可证明回滚，成功后旧 profile 不可访问且新资料回到本币待确认状态；异常导出目录启动/再次导出清扫可自动断言；只读诊断与关于信息可见且不引入任何生产诊断数据流；分享清理、后台快照遮罩与系统权限不变有模拟器或真机证据。

### 阶段 6：真机 Beta 与发布

- 在最低支持设备和目标系统版本完成核心旅程。
- 使用脱敏或合成数据验证 OCR、长列表和轨迹。
- 完成 VoiceOver、Dynamic Type、减少动态效果和锁屏隐私。
- 使用 `XCUIApplication.performAccessibilityAudit()` 覆盖五个一级页面与快速记账页：先修复全部可定位的对比度、裁切和命中区问题；描述、特征、点击区域、对比度和元素检测不得过滤。另以最大辅助字号深色外观测试覆盖同一页面；只有该测试通过且源码无固定字号时，才按设计文档的确定规则记录并过滤 Xcode 26 `.dynamicType` 和精确匹配系统搜索框的 `.textClipped`。元素为空或未匹配规则的问题直接阻断候选发布。
- 对 iOS 26 长列表启用系统 `tabBarMinimizeBehavior(.onScrollDown)`，密集滚动容器设置 hard scroll edge；重跑零过滤对比度审计。
- 在最大辅助字号测试滚动到“我的”底部后，点击无障碍值为“已折叠”的当前“我的”Tab，断言系统重新显示“今天”Tab，再进入快速记账；以此覆盖 Tab Bar 最小化后的确定恢复路径。
- 将“今天”和“账本”的 `.searchable` 固定为常驻导航栏抽屉，消除底部浮动搜索栏遮挡列表内容；修改后先重跑零过滤对比度审计，再运行最大辅助字号深色测试。
- 增加仅 Debug 编译的无障碍目的地启动入口，复用真实 `TodayView`、`LedgerView`、`CalendarView`、`TripPlanListView` 和 `MyView`；UI 审计逐页重启真实 App、断言无 Tab Bar 后执行。默认启动的五 Tab 导航测试继续独立运行，Release 构建检查参数不可达。
- 对比度等原生像素审计固定在逐页冷启动后的稳定初始视口执行；不在滚动到“删除本机全部数据”后把已经离屏的 SwiftUI 复用节点当作可见像素问题。底部数据控制继续由默认模式的确认/取消测试和最大辅助字号深色测试滚动至可点击状态并留存截图；滚动到诊断与关于区域后只零过滤执行元素检测、命中区、描述、特征和文字裁切审计，并用导航栏与标签栏安全边界断言完整可见。
- 精简“我的”本地保留规则文案，并为四个说明 `Label` 启用垂直自适应尺寸；重跑文本裁切审计和最大辅助字号测试，应用正文问题不得过滤。
- 将快速记账的票据隐私说明从 Section footer 移到照片/相机操作之前，精简文案并允许垂直增长；重新执行快速记账零过滤对比度审计与文本裁切审计。
- 为票据隐私说明增加独立真实 XCUITest：分别以标准字号和最大辅助字号滚动到完整文案，断言其位于照片选择与相机操作之前，在该视口执行零过滤文本裁切审计并分别留存截图。
- 把 `LedgerAmountText.accessibilityFormatted` 接入月报、交易行、余额、退款、行程消费和 Picker 选项，并为正金额补同一货币全称格式；将账本 UI 搜索测试同步到统一提示“搜索账目”，再使用有既有交易数据的完整 Scheme 验证可读标签与测试隔离。
- 将“今天”月报统计口径从系统 Section footer 移入正文，使用主色 footnote 并禁用年份千位分组；在已有月报和交易数据状态下重跑零过滤对比度审计。
- 为 XCUITest 增加每测试 UUID 存储参数：Debug App 将其映射到独立 Application Support 子目录并继续使用正式 GRDB/migration，测试辅助方法把参数加入全部 App 启动且同一测试重启保持一致；验证参数无效时启动失败、正常数据库不受影响，并检查 Release 二进制不包含该入口。
- 完成依赖、安全、隐私清单、日志、诊断包和删除证据。
- 将唯一 `PrivacyInfo.xcprivacy` 加入 App target 资源，声明 P0 无追踪、无离机收集和 App 自有源码无 Required Reason API；检查 GRDB 等运行时依赖的自带清单并验证发布包聚合结果。
- 对候选发布包递归枚举并逐份校验 App 与嵌入依赖的隐私清单，固定记录相对路径和数量；再对签名 Archive 生成 Xcode Organizer 聚合隐私报告并人工复核。以后新增网络、分析、崩溃上报或同步时先回到设计与 PRD 更新声明。
- 在 `-AppleLanguages (zh-Hans)` 下运行共享测试与核心 XCUITest，确认五个一级入口、快速记账、日历权限前置说明和“我的”诊断信息仍显示审核后的简体中文；Release build、Analyze 与 Archive 必须携带可编译的两份中文资源。P0 不以机器翻译或未验收英文目录替代该门槛。
- 新增 `ThenAppUITests`，把它加入共享 Scheme；先以无破坏测试覆盖 App 启动、五 Tab、“我的”本地模式、日历缓存清理确认/取消和全量删除确认/取消，再逐步扩展四条核心旅程。
- 通过小规模 beta 确认记账耗时、行程恢复、路线成功/失败降级和电量目标。
- 冻结发布范围；未通过能力通过 feature flag 关闭，不用文案掩盖。

### P1：Go 后端与同步

仅在 P0 发布并完成账号、合规、云地域和同步范围专项批准后执行，不作为 P0 发布条件：

- Sign in with Apple、会话、设备和账号删除设计落地。
- 依据批准领域模型在 Repository GORM record 中声明初始业务结构。
- 依据批准接口目录写入 Huma operation 与类型 tag，完成运行时 OpenAPI 验证。
- 实现单进程 Go HTTP、Repository、Service、contract test 和同进程 Outbox。
- 实现 mutation push、按用户串行分配的 cursor pull、tombstone、conflict staging 和稳定分页 snapshot reset。
- 完成 Testcontainers PostgreSQL、GORM schema、越权与幂等测试。

退出条件：多设备、长期离线、冲突、删除和信封加密验收通过；本地模式不受影响，且图片/原始轨迹不进入同步。

## 任务追踪

| 任务 | 状态 | 依赖 | 验证方式 |
| --- | --- | --- | --- |
| 完成 PRD、总体、信息架构、领域、功能、同步和安全初稿 | completed | 技术基线 | 文档交叉链接和一致性审查 |
| 产品负责人收敛 P0 范围 | completed | 设计初稿 | 六项关闭决定已同步到文档 |
| 创建最小 Xcode 工程与依赖边界 | completed | 阶段 0 | clean build/test、Package.resolved、OpenAPI 插件生成编译通过 |
| 修复旧版地点表更新兼容 | completed | 历史 `v3` 已执行数据库、纯文字地址 PRD | `v6_relax_manual_location_coordinates` 已追加；历史夹具保留带坐标地点、计划、路线、提醒、账务与 Journey，真实模拟器 v5 旧库升级后可保存纯文字计划且第二次冷启动不重复迁移，外键和完整性检查通过 |
| 完成本地数据库初始模型与 migration | completed | 领域模型批准 | `v1` 账本、`v2` 日历缓存、`v3` 出行计划、`v4` Journey/轨迹三表、`v5` 生活关联、`v6` 地点兼容修正、`v7` Journey 消费复盘三态及 `v8` 计划来源处置版本均已通过空库与历史库测试；后续只追加已批准的有序 migration，绝不原地改变已执行 identifier 的升级语义 |
| 持久化 Journey 消费复盘状态 | completed | 行程与生活关联已落地 | `v7`、显式无消费命令、关系创建/解除的原子状态转换与三态 UI 已落地；真实 v6 历史库证明既有关系回填 `has_expense`、无关系只回填 `pending` 且账务不变，模拟器端到端完成无消费→新建并关联 8.88 支出→解除回到待复盘→重关联→删除 Journey 后账务仍可搜索 |
| 建立 P0 范围负向验证器 | completed | 范围冻结、当前 schema/契约/依赖 | `scripts/validate-p0-scope.sh` 已证明 19 张本地表、空 OpenAPI paths、无服务端业务 DDL、11 个锁定 SwiftPM package 及无禁止入口/SDK；独立临时副本中的合成“订阅方案”入口以退出码 1 被拒绝。文件系统、网络、深链和真实 UI 仍由验收场景收口 |
| 建立 P0 零生产日志验证门槛 | completed | 安全设计、隐私清单验证器 | `scripts/validate-ios-privacy.sh` 已阻断 ThenApp 生产 Swift 源码中的控制台、统一日志、signpost、`CFShow` 与标准输出/错误入口；当前源码和 Archive 通过，独立副本注入 `print("synthetic amount: 1234")` 后以退出码 1 拒绝，日志为 `/tmp/then-ios-privacy-log-negative.ZohCRF/result.log`，测试 target 的无障碍诊断不进入生产扫描范围 |
| 建立 P0 减少动态效果静态门槛 | completed | 可访问性设计、当前无自定义动效 | `scripts/validate-ios-motion.sh` 已阻断 ThenApp 生产 Swift 源码中的 SwiftUI、UIKit 与 Core Animation 自定义动画入口；当前源码通过，独立副本注入合成 `withAnimation` 后以退出码 1 拒绝，日志为 `/tmp/then-ios-motion-negative.BNsOt7/result.log`。该门槛不替代最低 iOS 18 真机开启“减少动态效果”的核心旅程录屏 |
| 验证数据库与导出文件保护操作 | completed | AppDatabase、结构化导出 | 统一启动保护修复逻辑与 Debug UI 测试路径已复用；记录器证明数据库目录、主文件/WAL/SHM 收到 `completeUntilFirstUserAuthentication`，导出目录/JSON 收到 `complete`，14 项专项与共享 Scheme 104/104 通过，证据为 `/tmp/then-file-protection-targeted.V0PjaX/Result-3.xcresult` 和 `/tmp/then-p0-diagnostics-full.29ghCZ/Result.xcresult`。Simulator 不作为实际属性证据；最低 iOS 18 真机属性、首次解锁、锁屏行程写入和导出不可读归入阶段 6 发布门槛 |
| 验证低存储导出失败关闭 | completed | 结构化导出、可测试文件边界 | 注入真实部分文件后抛出的系统 `fileWriteOutOfSpace`；稳定映射为 `insufficientStorage`，随机目录清空、源数据库不变、界面显示脱敏重试提示，随后正常导出成功且不实现分批导出。19 项专项和共享 Scheme 104/104 通过，证据为 `/tmp/then-file-protection-targeted.V0PjaX/Result-low-storage.xcresult` 与 `/tmp/then-p0-diagnostics-full.29ghCZ/Result.xcresult` |
| 完成账务领域与页面垂直切片 | in_progress | 数据基础 | 本位币确认、账户/分类生命周期、支出/收入/转账/还款选择器、折叠流水/详情、本地搜索、posted 余额、月份切换、基础月报、货币全称无障碍名称和 OCR 会话候选均已实现；快速支出→部分退款→月报/搜索、信用卡还款→正式 `transfer`、支出更正→完整撤销三条真实 XCUITest 随 88 项 Swift Testing 与 16 项 XCUITest 在共享 Scheme 中 104/104 通过，截图已人工复核。OCR 真实样本与最低 iOS 18 真机仍待完成 |
| 完成退款、完整冲销与原子更正 | completed | 三类正式交易、canonical root 规则 | 领域命令、root CAS、幂等回读、额度规则、SQLite 原子回滚、折叠流水、详情历史与三类页面操作已随当前 88 项 Swift Testing 通过；真实 UI 已将新建 20.00 支出更正为 18.00 并修改说明，再完整撤销，确认“更正后的版本”“更正冲销”“当前版本，已撤销”与全部操作入口关闭；全量结果为 104/104，截图位于 `/tmp/then-p0-journey-expense-full-attachments.EuMqSi/7D947838-FFD9-4CDD-B6B1-602C437FA9EA.png` |
| 完成账务趋势与组合筛选 | completed | canonical root 查询、生活关联 | 已实现纯值筛选条件、current replacement/root 关系匹配、同一 GRDB 只读快照中的月报与六个月趋势、零月份补齐、归档账户/分类历史选择、原生筛选 Sheet、可见条件摘要、空态和一键清除。新增 2 项 Swift 测试覆盖跨月零值、退款/冲销/更正净额、归档项、已关联/未关联、旧展示字段不命中、跨 owner 隔离、非法 kind 闭合失败和陈旧查询防覆盖；新增 1 项真实 GRDB XCUITest 覆盖组合筛选、趋势与清除。共享 Scheme 104/104 与原生无障碍审计通过，证据为 `/tmp/then-p0-diagnostics-full.29ghCZ/Result.xcresult` |
| 完成 OCR POC 与候选流程 | in_progress | Xcode 工程、合成样本 | Vision 合成图、字段解析、低置信度、重复提示、候选采用前后零写入、OCR 来源、相机拒绝降级和零图片持久化已自动验证；独立 XCUITest 又证明票据隐私说明在标准与最大辅助字号下完整可读、位于图片操作之前且零文本裁切；真实清晰/模糊/缺字段样本、相机/照片选择器系统界面与最低 iOS 18 真机仍待验证，准确率不足可关闭入口且不阻塞 P0 |
| 完成日历扫描与来源变化 | in_progress | EventKit POC | `v2` 本地缓存、Keychain HMAC 身份、Full Access 状态、来源默认不选中、30/90 展示窗口与 60/180 匹配窗口、完整事务、全天/时区/revision/移出窗口/缺失/取消/权限失效及忽略所见版本均已编译并由 5 项日历集成测试覆盖；真实重复例外、授权同意/拒绝/撤回、系统变化通知和界面仍需真机证据 |
| 验证日历事件到出行计划本地闭环 | completed | 日历缓存、计划编辑器、`v3` 关系表 | 4 项真实 GRDB 跨域集成测试覆盖普通/全天 occurrence seed、保存门禁、时间/地点字段来源、本地 EventTripLink/source version、日历快照不变、无效 occurrence 全事务回滚，以及计划保存后的来源重扫隔离；合成全天事件 XCUITest 覆盖来源选择、完整扫描、地点确认、具体时间确认、保存和行程列表回读。TripPlanning 专项 15/15、新旧来源处置 UI 专项 1/1、共享 Scheme 104/104 通过，证据为 `/tmp/then-revision-adoption-targeted.xcresult`、`/tmp/then-revision-review-ui-4.xcresult` 和 `/tmp/then-p0-diagnostics-full.29ghCZ/Result.xcresult`；真实 EventKit/通知界面继续归真机验收 |
| 完成来源变化对比与显式处置 | completed | 日历 revision、本地事件关系、计划编辑 | `v8_add_trip_plan_revision_review`、单 occurrence 单计划写前门禁、计划/事件/采用版本三重校验、连续 revision 校验、保留当前与显式采用命令、旧路线失效、数据库提交后提醒协调，以及“今天/日程”统一处置和编辑确认页均已落地。15 项 TripPlanning 测试覆盖采用成功、陈旧版本、历史重复关系和全事务回滚；真实 GRDB XCUITest 完成 v1 扫描→建计划→v2 重扫→旧值/新值对比→重新确认目的地→采用→行程回读，专项 `/tmp/then-revision-review-ui-4.xcresult` 与共享 Scheme `/tmp/then-p0-diagnostics-full.29ghCZ/Result.xcresult` 均通过。真实 EventKit 变化通知仍保留为真机门槛 |
| 完成路线、提醒和外部导航 | in_progress | 日历缓存切片、MapKit/通知 POC | `v3` 计划数据、地点确认、15 分钟 MapKit 路线快照、手动出发降级、普通本地通知状态和 Apple 地图单向交接已实现；15 项 TripPlanning 专项测试覆盖零自动剪贴板写入、地址优先、空白地址回退名称、联合开始保留 Journey 和新失败清理旧状态。真实 GRDB XCUITest 已验证失败卡片、显式复制成功、普通导航不会误启动 Journey，以及同页面无障碍审计；共享 Scheme 104/104、Release clean build/analyze/archive 与测试参数泄漏检查通过，证据为 `/tmp/then-navigation-copy-ui-11.xcresult`、`/tmp/then-p0-diagnostics-full.29ghCZ/Result.xcresult` 和 `/tmp/then-release-diagnostics.Yj7DJQ/ThenApp.xcarchive`。仍需真机路线、通知权限、Apple 地图真实打开/失败、系统剪贴板和返回 App 证据 |
| 完成实际行程与异常恢复 | in_progress | 定位 POC、数据库 | `v4` 三表、App 级记录服务、When In Use/Keychain/Core Location adapter、样本先落盘、新 segment 恢复、manifest 收尾、原子清理和恢复界面已落地；7 项 Journey 专项与 1 项联合导航测试随共享 Scheme 104/104 通过。仍需锁屏、前后台、低精度、撤权、杀进程、真实移动和耗电真机证据 |
| 完成今天与跨领域关联 | in_progress | 账务和行程 | Today 只读组合、固定五 Tab、`v5` canonical-root 行程关联、`v7` 消费复盘三态、局部降级与双向可见均已实现；共享 Scheme 104/104 通过，XCUITest 已逐一打开五 Tab、从合成全天事件创建计划，并完成 Journey 无消费确认、消费关联、解除、重关联和删除后账务保留；最大辅助字号深色模式下五入口、本地数据控制和最小化 Tab Bar 恢复路径仍可达。真实 EventKit、VoiceOver、减少动态效果和最低 iOS 18 真机证据待完成 |
| 完成结构化导出与本地分层删除 | in_progress | 阶段 4 本地数据闭环 | JSON 白名单、受保护随机临时文件、分享回调/关闭清理、启动与再次导出清扫、容量不足和普通写盘故障回滚、日历缓存清理、completed Journey 删除、全量删除回滚/新 identity、定位/提醒协调和后台遮罩均已实现；9 项本地数据、1 项遮罩策略与相关真实 UI 测试随共享 Scheme 104/104 通过，系统分享取消后可再次导出，任务切换卡片只显示“于是 / 内容已隐藏”，Journey 删除后账务仍可搜索，截图均已复核。分享成功到第三方目标、真机文件保护语义与系统权限不变证据待完成 |
| 完成只读诊断与关于 | completed | P0 零生产日志与“我的” | `AppReleaseInformation` 已从 bundle 读取并规范化 App 名称、版本和构建号，缺失字段安全降级为“未知”；“我的”显示单设备本地、诊断收集未启用和零诊断数据流说明。5 项发布/权限模型测试、真实 App 可见性、标准字号可视区无障碍门槛和最大辅助字号深色截图均通过；共享 Scheme 104/104、Release clean build/analyze/archive、隐私/范围/动画门禁与 Release 测试参数缺失检查通过，证据为 `/tmp/then-diagnostics-unit.TCC91f/Result.xcresult`、`/tmp/then-p0-diagnostics-full.29ghCZ/Result.xcresult` 和 `/tmp/then-release-diagnostics.Yj7DJQ/ThenApp.xcarchive`。未增加日志、分析、崩溃上报、诊断包、剪贴板或网络能力 |
| 完成简体中文本地化基线 | completed | 已批准 P0 文案、Xcode 工程 | `Localizable.xcstrings` 的 457 个界面键与 `InfoPlist.xcstrings` 的 5 个系统键全部为已审核 `zh-Hans` 值并各自只进入 ThenApp Resources 一次；`scripts/validate-ios-localization.sh` 完成目录唯一性、格式参数、Info.plist 一致性、Xcode 编译器重新提取、目录编译和候选 App 资源检查。共享 `zh-Hans` 测试 104/104、日历权限前置说明专项 1/1、Release build/analyze/archive 均通过；Release Archive 为 `/tmp/then-p0-localization-release.I7GGUy/ThenApp.xcarchive`。独立副本删除“今天”后以 `missing=["今天"]` 被阻断，日志为 `/tmp/then-ios-localization-negative.LWn7ie/result.log`；P0 仍不声明英文或其他语言受支持 |
| 完成本地 P0 发布 | pending | 阶段 1–5 | 本地 MVP 验收 |
| P1 账号与云同步专项批准及实现 | pending | P0 结果、合规和云方案 | 条件同步验收 |
| 完成安全、隐私、无障碍和 beta | in_progress | 功能冻结 | 五个一级页面+快速记账的 Xcode 原生无障碍审计、最大辅助字号深色模式、任务切换隐私遮罩、独立真实 UI 测试数据库、递归隐私清单审计、P0 零生产日志与零自定义动画正负向门槛已有证据；当前 Release Archive `/tmp/then-release-diagnostics.Yj7DJQ/ThenApp.xcarchive`、Analyze `/tmp/then-release-diagnostics.Yj7DJQ`、两份嵌入隐私清单及 Debug 日历夹具字符串缺失检查通过，随后补 VoiceOver/减少动态效果真机录屏、正式签名候选、Organizer 聚合隐私报告和 beta 证据 |

## 测试策略

| 层 | 重点 |
| --- | --- |
| 领域单元 | 分录、退款/冲销、更正、状态机和时间规则。 |
| SQLite 集成 | migration、事务、约束、轨迹批写、异常恢复和查询性能。 |
| 系统适配器 | 协议 fake 验证业务；EventKit/Location/Notification/MapKit/Vision 真机验证。 |
| UI | 快速记账、事件到计划、开始/结束 Journey、行程到消费、权限降级。 |
| Go 单元/HTTP（条件） | Service、Handler、错误、认证、优雅退出。 |
| PostgreSQL（P1） | schema、原子账务命令、越权、幂等、cursor、tombstone、Outbox。 |
| API（P1） | Spectral、oasdiff、Swift 生成编译、kin-openapi contract test。 |
| 安全隐私 | 敏感日志、Keychain、文件保护、临时文件、导出、删除和第三方清单。 |

## 风险与缓解

| 风险 | 缓解 |
| --- | --- |
| P0 范围过大 | 账号、预算、国内多地图、票据附件、长期轨迹、特殊通知、商业化和穿搭均不进入当前实现；优先四段主闭环。 |
| 账务正确性返工 | 先批准分录/更正语义并完成领域测试，再写页面。 |
| 日历来源不稳定 | 完整扫描、来源与 overlay 分离、模糊匹配人工处理。 |
| 后台轨迹丢失或耗电 | App 级 owner、先落盘、真机长行程和自适应采样 POC。 |
| MapKit 路线不可用 | 允许手动出发时间、复制地址和返回计划；不以供应商扩展或覆盖率承诺阻塞发布。 |
| 同步拖慢首版 | P0 固定本地模式；云同步只在 P1 专项批准后启动。 |
| 单 schema 生产演进受限 | 初始业务模型一次批准；首次复杂生产变更前重新评估迁移。 |
| 视觉设计缺失 | 系统流程批准后再对四个关键场景进行三方向视觉探索，不提前固化 token。 |

## 发布与回滚

- 每个阶段保持可关闭的 feature flag；关闭后不得遗留无法访问的用户数据。
- 本地 schema 一旦进入用户构建只通过有序 GRDB migration 演进，不重建用户数据库。
- OpenAPI 破坏性变更必须提供兼容版本或升级路径。
- 当前开发 PostgreSQL 变化直接更新 GORM record 并重建隔离数据库；需要保留数据时先建立新的迁移计划。
- MapKit、OCR 或同步失败可切回本地手动路径；当前不切换第三方地图或云 OCR。
- 活跃 Journey、未同步账务和待确认草稿在版本升级/回滚前有兼容验证。
