# 17-33 Web 实际穿着数据核验执行计划

## 状态与范围

- 契约：`approved`；执行：`local_development_complete`（2026-09-23；公开发布门禁未关闭）。
- 依据用户指定的 `then-server` 后端与 Web 页面先行范围，消费已完成的 [17-23](17-23-账号实际穿着API执行计划.md) API。
- 需求：[PRD 16](../prd/16-穿搭记录与反馈需求.md) `OUTFIT-REQ-013`～`015`；设计：[Design 27](../design/27-Web实际穿着数据核验设计.md)；验收：[Acceptance 16](../acceptance/16-穿搭记录与反馈验收.md) `OUTFIT-ACC-013` 的 Web 子集。

本片展示和纠正用户明确确认的实际事实，不凭计划到期、页面访问或 Look 生成自动创建实际事件；不抵扣 App 离线记录与反馈。

| ID | 状态 | 完成条件 |
| --- | --- | --- |
| `17-33-DOC-01` | `completed` | 固定 Web 的来源、重复/状态确认、快照与非目标。 |
| `17-33-UI-01` | `completed` | `/wear` 可主动创建、分页回看、纠正、删除；两类逐件确认及相似事件二次确认。 |
| `17-33-TEST-01` | `completed` | 生成客户端无漂移；表单/候选测试及前端质量命令通过。 |
| `17-33-BROWSER-01` | `completed` | 隔离真实服务覆盖事实和失败矩阵、两账号、移动/键盘/axe。 |
| `17-33-REL-01` | `pending` | 本地证据独立提交；公开入口另过账号发布门禁。 |

失败不产生半条实际事件或无确认待洗；回滚只撤 Web 入口，不删现有后端记录或 API。

## 本地开发证据（2026-09-23）

- 环境：隔离 Compose `then-wear-1733` PostgreSQL/Redis，Go API `127.0.0.1:18080`、Next 开发页 `127.0.0.1:3114`；两组合成账号，测试结束销毁隔离数据。实际运行 `/openapi.json` 重新生成客户端无差异。
- 浏览器：A 选待洗外套和可穿衬衫，未逐件确认时阻止实际保存；明确确认后保存部分记录，只有选中待洗的衬衫状态变化。相同日期和衣物再次保存先收到一条相似候选，主动二次确认后才有第二条事件。新建来源计划并主动确认一件衣物后计划 `active → completed`，删唯一关联事件回到 `active`。外部纠正让旧 revision 返回 409，本地草稿保留，主动重载取回外部版本。B 列表为空、读取 A 事件 404；断网时可重试，Cookie 失效转登录，同一浏览器切换 B 后不显示 A 数据。23 条列表按 20+3 稳定分页。
- 质量：lint、typecheck、format、15 项 Node Test、Next 生产构建、生产依赖审计通过；320/390/1280px 的 `scrollWidth=clientWidth`，键盘 Enter 可打开删除确认，Escape 可关闭。axe-core 4.12.1 WCAG2 A/AA：0 violations / 0 incomplete、25 passes。截图捕获未成功，未据此宣称人工视觉截图或屏幕阅读器验收。远端 CI、生产 TLS/邮件/隐私与 App 真机未在本片运行。
