# 单件图片导入与媒体生命周期执行计划

## 2026-09-14外部删除后的编辑内容清理

本缺陷已修复：Red 新增两组参数共 11 issue；Green 的 15 项编辑/照片状态测试 + 1 条正式新增/编辑/重启/删除 UI，共 16 注册全部通过，0 失败/跳过。Debug/Release、工具链和架构检查通过。String Catalog 增加“衣物已删除”；已提交清理重试仍可用。详见 acceptance 12 `WARDROBE-DELETED-EDITOR-20260914-01`；IOS-01 继续 in_progress，完整图片安全/无障碍/发布未由此抵扣。

沿已批准的删除/媒体生命周期与失败恢复契约处理缺陷：旧衣物编辑页写入时收到明确 `WardrobeError.notFound`，原先只显示错误，仍保留原名称、图片和候选确认。本轮在 `WardrobePhotoEditingTests.swift` 用真实 SQLite 和已登记合成编码图复现无图修改/带图修改两条路径；在 `WardrobeViewModel.swift` 清除明确已删除衣物的本地展示与候选，终止保存/照片/删除请求；`WardrobeView.swift` 对已知不存在的衣物只保留说明与关闭，不显示无依据的清理重试。已有删除提交后清理失败仍保留精确重试身份；照片缺失或普通 I/O 错误不等同于衣物删除。无 schema、API、供应商或权限变更，继续归现有 IOS-01，不新增切片或改变 IMAGE/发布未完成状态。

## 状态与授权

- 契约状态：`approved`，2026-09-13；承接用户全流程实施、Woo 优先与页面复现授权，细化已批准 PRD 12 的单件图片范围，不新增云流量或旧数据兼容。
- 执行状态：`in_progress`。
- 当前检查点：不同像素换图/重启已验证；新增手部合成图复现了人物/面部漏检，独立手部请求的四组对照也未检出，修复候选不能准入。IOS-01 保持 in_progress，IMAGE-01 的身体部位及完整安全矩阵保持 pending，未认领完成。
- 依赖：12-01 的衣物语义和数据库实现已通过自动回归；其物理发布验收未通过不阻止本隔离 POC。11-01 的净化/选图服务仅为后续复用输入，其人物检测门禁不冒充已通过。

### 2026-09-15身体部位检测技术复核

本轮没有修改生产检测器。先用 `VNClassifyImageRequest` 对手部衣物、平铺短袖、红色毛衣和完整成人四张既有原创合成图做隔离诊断：默认设备仍报 `Failed to create espresso context`；显式 CPU 可运行，但四图的高分标签近乎相同，`people` 分数分别约为 0.0357、0.0353、0.0353、0.0353。该分类器既不能区分手部衣物与纯衣物，也不能稳定区分完整成人，不能作为安全阈值或生产兜底；临时诊断代码已移除。

GitHub 与 Firecrawl 交叉核对后，Google 官方 [MediaPipe Hand Landmarker iOS 指南](https://developers.google.com/edge/mediapipe/solutions/vision/hand_landmarker/ios) 确认其支持静态图片 `detect(image:)`、输出 21 个手部关键点，并要求 `MediaPipeTasksVision` 与独立 `.task` 模型；官方当前安装方式为 CocoaPods。仓库 [podspec](https://github.com/google-ai-edge/mediapipe/blob/master/mediapipe/tasks/ios/MediaPipeTasksVision.podspec.template) 声明 iOS 15、静态 framework 和 Apache 许可，[项目许可证](https://github.com/google-ai-edge/mediapipe/blob/master/LICENSE) 为 Apache-2.0。

当前 Then App 只有 Swift Package Manager。为非必填照片增强直接加入 CocoaPods、二进制 framework 和模型会同时改变依赖管理、包体、供应链与模拟器验证面，因此本切片不盲目接入。后续只有在独立 POC 同时满足以下检查后，才可把 MediaPipe 提升为生产候选：

- [ ] 在不引入第二套项目依赖管理的接入方式上有官方或可审计证据；否则先批准项目级依赖变更。
- [ ] 官方模型文件、版本、下载来源、许可证、SHA-256 和 App 包体增量固定。
- [ ] 当前手部夹具检出，平铺短袖/红色毛衣不误报，完整成人仍由现有人物/面部门禁拒绝。
- [ ] iPhone 17 / iOS 26.5 模拟器可重复运行；默认/CPU 选择、超时、取消和初始化失败均返回不可用，不映射为无人。
- [ ] 至少补齐手、臂、腿、脚、局部躯干、多人、遮挡、玩偶/海报和两件重叠矩阵，再评估真机性能与误报。

在这些条件完成前，`12-03-IMAGE-01` 继续 `pending`。页面仍允许无图创建衣物；选择图片时保留用户逐项确认和已实现的人物/面部拒绝，但产品文案与验收不得把它描述为“已自动确认无人物”。不采用肤色、矩形、通用标签低阈值或失败后放行等兼容处理。

## Spec与影响面

手部检查点收尾：`scripts/validate-ios.sh build` 的版本/架构和 Debug/Release 均通过，`git diff --check` 通过；manifest v3 四张素材实际字节/hash 匹配，最新 Release App 不含素材目录或合成图。仅测试 target 增加失败复现与能力探针，生产代码未修改。所有测试/构建已结束，本轮 EA42A0AF 专用模拟器已关闭并删除；未触碰其他设备或用户照片，未提交/推送。构建通过不能抵扣 2 项内容检查失败，后续从真实身体部位检测方案继续，不反复跑已无差异的四组对照。

本轮内容门禁结果：`WardrobeHandsPOC20260914.xcresult` 的 1 项手部 CPU 探针失败，512px 图返回 hands=0。扩展对照 `WardrobeHandsDevices20260914.xcresult` exit 65，7 注册测试中原有 5 项通过，身体部位拒绝用例与手部 POC 失败，共 7 条 issue；手部请求在 512px/1254px × CPU/system 四组均 hands=0，原产品 people/faces 仍为 0。未改变预期或忽略 issue，不把这套手部请求接入生产。仅追加素材、复现/POC 测试和文档；IMAGE-01 的身体部位自动拒绝缺口明确保留，现有人工确认不是自动安全结论。

手部样本复现：`WardrobeHandsRed20260914.xcresult` 虽 exit 0，但定向选择缺少 Swift Testing 方法括号，实际运行 0 项，不计证据。完整分析套件 `WardrobeHandsReproduction20260914.xcresult` exit 65，6 注册测试、手部用例 3 条失败；原分析实测 people=0/faces=0，进入 needsConfirmation 且可以人工确认。该问题不能按“检测无人”处理。先通过仅测试 target 的 `handRequestProbe()` 验证固定 revision 1、maximumHandCount=2 的 Vision 手部请求及显式 CPU；其后若可运行，再给现有 observations 增加必填 hands 并逐调用点更新，人物/面部/手部任一检出均拒绝，任一请求失败仍整体不可用。属于落实既定身体部位拒绝规则的缺陷修复，不新增框架、上传或生产精度承诺。

2026-09-14 内容边界检查点：编码/元数据已有参数化测试，本轮在当前 IOS-01 验收中补充“只有成人手部与衣物、没有脸或躯干”的原创合成样本。先视觉核对/登记真实文件哈希，再通过既有文件导入→净化→CPU 人物/面部请求→复核状态机验证。预期内容包含人物身体部位，不能宣称 catalog 安全；检测结果按实测记录，零检出仅能进入仍需人工确认的状态，不能将该样本当安全衣物保存。若未自动检出，保留为 IMAGE-01 安全矩阵缺口，不伪造分类器结果或降低检测失败门禁。只增加测试资源/诊断与验收证据，不扩展产品行为、provider 或上传；本轮不重复已通过的格式矩阵。

Design 05「单件图片持久化与恢复」→ PRD 12 WARDROBE-REQ-001/002/009/010/012/015 → 本计划 → WARDROBE-ACC-006/008/009 的相应子集。Woo 页面以 Design 03 逐页映射为最终 UI 目标，存储测试不能抵扣页面复现。

1. 单件图仍非必填；只使用主动选择且能安全形成单件素材的图片，人物/多件重叠按原规则换图、选主体或无图处理。完整图片入口在处理与生命周期联调后提供。
2. 规范化图和缩略图有稳定照片组 ID、用途、摘要与相对路径，原始输入只临时持有；本机无上传。照片组同删，衣物事实独立。
3. importing → ready → deleting 在 SQLite 持久登记。文件写入/移动和数据库提交分步，只有 ready 可见；换图失败保留原 ready，成功后清理旧图，重启清理未完成阶段。
4. 移除照片或衣物先事务持久删除意图，再清理文件；失败隐藏图片并保留重试依据。读取坏图/丢图返回不可用，衣物仍存在。
5. expectedRevision 防止过期编辑；同一命令 ID 可重试，不能覆盖新版本。路径由随机 ID 与固定文件名派生，不接受删除路径，拒绝符号链接与硬链接。
6. 隔离 POC 仅在测试 target 运行，已完成；DATA-01 已接入正式 OOTDSchema/启动与 Data 层。本轮按用户“先使用模拟器进行开发”接通原生 PhotosPicker、图片预览、单件复核、原子保存和移除；完整人物/身体部位/多件安全矩阵与真机发布门禁仍未通过，不宣称生产图片准入完成。OpenAPI、后端与依赖不变。

## Checklist与职责

| ID | 状态 | 依赖 | 交付与完成判定 |
| --- | --- | --- | --- |
| 12-03-DOC-01 | completed | 用户当前路线 | Design 05→PRD 12→本计划，数据/失败/删除与 POC 边界明确 |
| 12-03-POC-01 | completed | DOC-01 | app/ThenAppTests/Support/WardrobeMediaLifecycleProbe.swift：真实 GRDB 意图日志与受保护文件的隔离实现；app/ThenAppTests/WardrobeMediaLifecycleTests.swift：中断点/重启/损坏/幂等/过期操作/删除测试；14 项通过，只在测试 target 编译；见本文件最终证据 |
| 12-03-DATA-01 | completed | POC-01 | 生产 Data/Services 与 OOTDSchema 追加 migration；衣物 revision、查询、图片/衣物删除和重启恢复接通；现有衣物保持 |
| 12-03-IMAGE-01 | pending | POC-01 | 复用受控选图/净化原理，明确衣物类型/尺寸预算与人物/多件拒绝；生成规范化图/缩略图，临时源清理；真实合成衣物矩阵与格式/元数据故障验证 |
| 12-03-IOS-01 | in_progress | DATA-01、IMAGE 已实现的选图/净化/复核/原子保存检查点 | SwiftUI Woo 衣橱图片网格/详情选换移除、同状态截图复核；确认保存/取消/错误/离线及原生辅助功能 |
| 12-03-TEST-01 | pending | IMAGE-01、IOS-01 | 完整生成 Client/照片/衣橱回归，真实图片新增→换图→重启→移除/删除、文件清单、Debug/Release 与零外联证据 |
| 12-03-REL-01 | pending | TEST-01 | 真机保护/锁屏/低存储/中断与 VoiceOver 等组合验收，更新 acceptance 与产品索引；未通过保留阻断 |

同一时间最多一项 in_progress。本文件统一承载 spec 与 checklist，不另建第二份。

### 2026-09-14不同图片替换验收

沿用现有保存/取消/删除合同，不改变用户输入或同意规则。此前系统换图旅程重复选择同一张蓝色短袖，尚不能证明不同像素内容切换。本轮新增一张原创红色长袖合成衣物，先人工查看并登记来源、尺寸、哈希和预期标签；只作为测试资源，不进入 App 或默认衣橱。

逐文件任务：`Resources/WardrobeContentProbe/manifest.json`、`README.md` 登记新样本；`ThenAppUITests.swift` 扩展既有系统选图旅程，使用下述种子脚本准备专用模拟器，图库 index 0/1/2 分别为蓝色短袖/成人/红色长袖。验证蓝图保存与重启、红图候选丢弃与取消不改变蓝图、确认并保存后重启显示红图、移除后无照片且衣物保留，最后正常删除测试衣物。截图与衣物区域颜色计数用于区分真实图片，不以控件存在或不同照片 ID 代替视觉验收。

仅扩充 IOS-01 验收；实际人物拒绝、单件复核开关、零外联与真机等门禁保留。出现实际缺陷则先记录复现并修改对应生产责任层，不增加 UI 测试后门、双写、重置当前数据库或替代分类器。

首次执行发现图库按实际文件日期展示红色长袖/蓝色短袖/成人，导入顺序不足以保证索引。增加 `scripts/seed-wardrobe-photo-simulator.py`：仅接受已启动的 ThenWardrobePhotoUI 测试设备，在可丢弃副本上设固定、递增的 2030 年合成日期后导入；校验复制前后内容哈希，不修改仓库原图，不自动清空设备。仅在本轮新建的纯测试模拟器重建夹具。图片区可视检查使用实际边界，不以不可交互图片的 isHittable 判定可见。

本检查点结果：`WardrobeDistinctPixelsRetry20260914.xcresult` exit 0，15 项 Swift Testing 注册测试与 1 项真实系统选图 UI 旅程通过，0 失败/跳过；UI 耗时 147.137 秒。六个衣物预览区域的颜色断言与保留截图共同证明蓝图→红图候选→丢弃恢复蓝图、已复核红图取消后重启仍蓝图、显式保存后重启为红图。人物候选仍拒绝且保存禁用；移除照片后重启无预览，衣物继续存在，最后按原生删除流程清理测试衣物。详细命令、实测比例和失败边界见 acceptance 12 的 2026-09-14 不同图片替换章节。

- [x] 原创红色长袖素材登记来源/哈希/预期，真实 CPU 人物/面部检测覆盖该图。
- [x] 固定日期种子脚本实跑，实际图库截图核对蓝色短袖/成人/红色长袖顺序。
- [x] 不同像素替换、丢弃/取消、重启和移除完整系统 UI 旅程通过。
- [x] 保留人物拒绝、逐项重新复核与后台遮罩证据。

本轮只扩充测试资源、UI/检测测试与验收工具；没有修改产品行为、schema、API 或生产依赖，回滚时成组撤回新增测试资源及相应测试步骤即可，不删除现有 OOTD 数据。完整安全矩阵、VoiceOver、离线/零外联和真机发布检查仍未完成，IOS-01 继续 in_progress，IMAGE-01 pending，整片和整体目标不关闭。

收尾检查点：工具链/架构及 Debug、Release 构建通过，最新 Release 包无三张合成图或测试资源目录，源 fixture 字节数/哈希均符合 manifest，`git diff --check` 通过。关键源码证据与具体限制见 acceptance 12；全部执行进程已终止，仅关闭并删除本轮专用模拟器 0401FE8D，未提交/推送。

### 2026-09-13换图与拒绝路径联调

上一轮为 progress：原子保存、真实系统选图链路、无障碍修复和 9 项 UI 复验已改变实现与证据。本轮继续 IMAGE-01，时间盒 90 分钟；沿用 Design 05 的当前候选/旧图隔离、取消与失败规则及 PRD 12 WARDROBE-REQ-009/010/015，先补已有照片的实际替换和人物拒绝证据，不把清理/真机或 Woo 网格门禁提前关闭。

逐文件计划：ThenAppUITests.swift 扩展现有系统选图旅程，覆盖有效候选重新复核、取消候选、含人物图拒绝、取消已复核编辑与原图重启保留，再成功换图及移除；只提取当前重复的系统图库点选与四项复核步骤。素材复用 WardrobeContentProbe 的两张已有原创图，专用空白模拟器先导入虚构成人、再导入衬衫，让第一格是衬衫、第二格是成人；实际观察画面后再判定。发现生产缺陷则在 WardrobeViewModel/View/对应服务根因处修复并补领域用例。app/README 与 acceptance 12 同步前置条件、实际结果和不足；不新增后端/OpenAPI/schema 或生产测试开关。

本轮代码审核发现读取抛错会跳过 existingPhoto 赋值：有效数据库记录存在，但图片文件类型/权限等错误使页面丢失移除目标。`WardrobePhotoReadFailureRed20260913.xcresult` exit 65，7 个注册测试中该用例失败；新增“normalized.image 被目录占据”参数实际触发 4 个断言失败（照片 ID、清理重试 ID、revision 和数据库记录未移除）。修改 ViewModel 在读取媒体字节前保留经验证的元数据，并继续以取消/编辑代次守卫避免迟到回写；文件检查和删除规则不放宽。已有文件缺失参数保留，目录障碍须明确清除后重试，不能递归删除不属于照片的内容。

`WardrobePhotoReadFailureFixed20260913.xcresult` exit 0，25 注册、24 通过、1 物理保护跳过；编辑 Suite 与照片 Repository 全部可运行测试通过。后续进一步在同一编辑实例先成功读图、再破坏文件重读，要求缓存图片清空但照片 ID 保留，防止以旧缓存掩盖读取失败；一并进入系统换图回归。此修复不改 schema、不改底层文件类型拒绝或递归删除边界。

`WardrobePhotoReplacementJourney20260913.xcresult` exit 0，**8 注册、8 passed、0 failed、0 skipped**（编辑状态 7 项 + 扩展的系统相册旅程 1 项，参数展开另计）。同一编辑实例读图后再遇到缺失/错误类型，缓存清空、精确照片 ID 保留；删除意图与清理重试通过。实际 UI 覆盖新增、已存图复开、后台遮罩、换图重新复核、丢弃候选、虚构成人拒绝、取消已复核编辑后重启保留、重新选图保存与重启、移除及不复活。未选择的复核开关逐项断言为 0，不能沿用之前的同意。

环境为新建专用 `ThenWardrobePhotoUIReplacement20260913`，UDID `A6C80F6D-D781-40A5-8433-79E5CCF0F7D7`，iPhone 17 / iOS 26.5（23F77），Xcode 26.6 / Swift 6.3.3。图库只新增既有两个合成素材，成人先导入、衬衫后导入；截图实际确认第一格衬衫、第二格虚构成人，其余为系统默认风景图。`DerivedData/WardrobePhotoReplacementAttachments20260913` 的 gallery-before-select-1 与 person-rejected-old-photo-retained 已查看：候选人物未显示为衣物，原衬衫保留，错误可见且保存禁用。

命令：`xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug -destination 'platform=iOS Simulator,id=A6C80F6D-D781-40A5-8433-79E5CCF0F7D7' -parallel-testing-enabled NO -skipPackagePluginValidation -disableAutomaticPackageResolution -only-testing:ThenAppTests/WardrobePhotoEditingTests -only-testing:ThenAppUITests/ThenAppUITests/testWardrobePhotoPickerJourney -resultBundlePath DerivedData/WardrobePhotoReplacementJourney20260913.xcresult test`。结果在忽略的 DerivedData，非 CI 归档；丢失后按本节前置条件重跑。

证据边界：成功换图使用同一张合成衬衫，证明重新复核/保存/恢复操作和状态链；不独立证明不同像素图的最终视觉切换，照片 UUID/文件替换由既有真实 Repository 测试佐证。图片拒绝只证明这张明确虚构成人素材；身体局部、多人、遮挡、不同图片替换、系统选择器直接取消、完整 VoiceOver/iCloud/零外联仍待验证。本轮未重跑全部 160 项单元或其余 8 项 UI；上一轮前景 2 失败和物理保护 2 跳过继续保留，不把相关子集通过写成全绿。IMAGE-01 仍 in_progress，全项目目标保持 active。

### DATA-01逐文件与本轮检查点

时间盒 120 分钟：完成生产数据协议、迁移与真实 SQLite/文件回归，之后保留图片处理/UI/真机门禁。当前合同由 Design 05「生产数据合同」细化，沿用 PRD 12 已批准生命周期，不改变人物/云输入规则。

- Features/Wardrobe/Domain/WardrobePhotoModels.swift：编码结果、用途、稳定元数据、保存结果与精确照片 Repository 协议；无 GRDB/文件路径访问。
- Data/Database/OOTDSchema.swift：追加 v2 媒体迁移、单个 ready 约束、删除保留清理事实；v1 不改写。
- Data/Wardrobe/WardrobePhotoPersistence.swift：同一 pool 下导入/提交/读取/恢复/按组删除；受保护文件与路径/哈希校验，错误只暴露稳定领域码。
- Data/Wardrobe/GRDBWardrobeRepository.swift：精确照片协议实现、衣物删除同事务登记、启动恢复与清理状态；继续单一 actor owner。
- ThenAppTests/WardrobePhotoRepositoryTests.swift：v1 真实衣物升级、正式 Repository 图片保存/更换/重启/移除/删除、SQL 失败与清理故障、坏图和路径拒绝；同原衣物/照片/API/状态机一起回归。
- project.pbxproj：同步登记当前职责的 Sources/Tests；POC 保留 test-only，不把 probe 当作生产服务。

按 POC 完成判定进入正式 Data 实现，新增 production schema 与媒体服务，但不开放图片 UI 或网络。

## 迁移、恢复与回滚

POC 仅在测试自建临时目录使用独立 SQLite schema；不访问 App 私有目录、用户图库或生产库，测试完成清理夹具。运行中断模拟在故障点直接抛出，重新创建存储 owner 后恢复；与真实进程 kill/最低设备测试分别记录。

进入 DATA-01 后追加 ootd_v2 媒体 migration，保留 v1 已有衣物。数据回滚不能清库或由旧代码误删媒体；保留新 schema 并以修复版本恢复，未清理意图保持可重试。当前回滚 POC 只成组移除测试登记与 probe/test 文件，不触碰其他未提交改动。

## 完成判定

全部 checklist 及必要图片安全/媒体/真机/UI 证据通过才完成本片。通过 POC 不代表图片入口或 Woo 试穿/真实三维已交付，不关闭整体目标。

## 首轮隔离验证

2026-09-13 `WardrobeMediaPOCInitial20260913.xcresult`：9 项 Swift Testing 全部通过，包括 4 个提交前中断点、提交后换图、2 种删除范围、真实 SQL trigger 拒绝、过期 revision、5 种坏文件/链接、根路径拒绝与取消。原工程 test target 编译通过；schema 和逻辑仅在测试夹具中运行。补充审查发现 fileExists 可能把悬空符号链接视为不存在，下一轮以悬空链接和实际清理障碍复现并修复，未关闭 POC-01。

## 最终隔离证据与下一步

路径 Red/Green：`WardrobeMediaPOCPathRed20260913.xcresult` 的悬空文件链接读取测试失败；改为检查目录项本身，并且仅对明确 no-such-file 返回 missing。[Apple fileExists 文档](https://developer.apple.com/documentation/foundation/filemanager/fileexists(atpath:)) 说明该方法跟随最后一段链接、无法确认也返回 false，因此不能据此认定目录项已被清理。`WardrobeMediaPOCVerified20260913.xcresult` 的 13 项测试通过，包含悬空目录、实际未知文件阻碍清理和写第一份文件后取消。中间 `WardrobeMediaPOCFixed20260913` 因测试宏的 throwing 布尔表达式编译失败，仅修正该表达式，保留失败包。

最终改为与生产一致的 DatabasePool/WAL，并增加实际 PRAGMA、schema 无 BLOB 和数据库不含媒体字节断言。`DerivedData/WardrobeMediaPOCWALRegression20260913.xcresult` exit 0：80 项注册、79 passed、1 physical skipped、0 failed；其中媒体 14 项全部通过，其余为现有照片/生成 API/衣橱/ViewModel。展开参数后的设备计数不混入顶层计数。命令为 `xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug -destination 'platform=iOS Simulator,id=8D9A4405-1089-4C4D-913B-AF4D39486714' -parallel-testing-enabled NO -skipPackagePluginValidation -disableAutomaticPackageResolution -only-testing:ThenAppTests -resultBundlePath DerivedData/WardrobeMediaPOCWALRegression20260913.xcresult test`。

环境：Xcode 26.6 / Swift 6.3.3，ThenMediaPOC20260913 专用 iPhone 17 / iOS 26.5（23F77）模拟器；基于 HEAD 2bc97280796791fa3d20b6407e01b7a8e403b0fa 的未提交工作树。执行与技术复核为 Codex，自检不冒充独立发布审核。源码 SHA-256：probe `c1b99603ea6f0e6786241a2e7b54482fbdee5fea7a8230b01e984ebf8c4a51f0`，tests `702ab3cddd3a0a252b264db7ace1d4145cb128f44d19832bca7028abe49d27ea`。结果在忽略的 DerivedData，仅为本地证据，未上传 CI；丢失后须按命令重跑。

POC-01 completed，允许进入 DATA-01 的生产实现；整个 12-03 仍 in_progress。当前 POC 是单一存储 owner、真实 SQLite/文件加确定性中断点；不证明多进程并发、真实断电/kill、图片净化或质量、真机保护/锁屏/性能、完整删除 UI 或 Woo 页面。输入是纯合成标记字节，并非人物或衣物图片；1 MiB 仅为存储测试上限，不能转为生产图像预算。在上述 POC 检查点，正式 OOTDSchema、App 启动和照片入口尚未变更；后续 DATA-01 变更见下节。project.pbxproj 目标依赖图检查确认两份新 Swift 仅进入 ThenAppTests，未进入 ThenApp/ThenTransport。

收尾检查：`scripts/validate-ios-architecture.sh` 通过，21 项 `scripts/tests` 测试通过，`git diff --check` 通过；仅关闭并删除本轮专用模拟器 8D9A4405-1089-4C4D-913B-AF4D39486714，未清理其他已启动设备。全部测试进程已确认终止；未提交或推送当前工作树。

## DATA-01实现与缺陷检查点

正式 `ootd_v2_wardrobe_photos` 已追加到唯一 OOTDSchema；原 v1 不变。GRDBWardrobeRepository 持有原 DatabasePool，接入照片保存/按用途有界读取/精确照片 ID 移除/衣物同事务删除登记/启动恢复与待清理查询。WardrobePhotoPersistence 没有独立数据库、全局状态或网络；文件校验和源路径均在 Data 层。质量只接收上游显式 catalog_ready，不由存储判定或升级成 render_ready。

`WardrobePhotoDataInitial20260913.xcresult` 首次编译因 guard 的第二个 throwing 调用缺 try 失败，补齐后 `WardrobePhotoDataRun20260913` 的 14 项正式 Repository 测试通过。继续审查发现原有目录被新导入意图错误认领，以及跨组 photo/thumbnail ID 没有联合防重；新增两项复现测试后 `WardrobePhotoOwnershipRed20260913` 真实失败（4 个断言），分别证明预存目录被删和冲突命令改变了照片/revision。修复为意图登记前只读预检并保留已有目录、事务内交叉检查两类 ID；不增加历史兼容分支。

`WardrobePhotoDataRegression20260913`：97 项单元注册、96 passed、1 physical skipped、0 failed；生产媒体 17 项通过，包含 v1 衣物升级、规范化图/缩略图身份与元数据、同命令 8 次并发重试、旧删除重试不触及新图、删除失败/重启、三个真实持久中断快照、6 种文件异常、路径登记篡改和链接根。另新增物理媒体 `.complete`/备份排除测试，模拟器明确 skipped；当前 iPhone 16 经 `devicectl list devices` 复核仍 unavailable。完整 UI 回归和最终构建结果另记，不用这些单元结果抵扣图片安全或真机通过。

完整回归 `WardrobePhotoFullRegression20260913` 在浅色普通字号发现衣物行“上装 · 待洗”对比度失败；导出的整页和 issue 文本已检查，状态确实在清晰可见区域而非滚动栏下。将其 subheadline 的前景由 secondary 改为语义 primary；沿用现有 contrast/textClipped 测例，不忽略 issue、不缩减字号和测试范围。该回归失败保留，新修复证据单独记录。


`DerivedData/WardrobePhotoVerified20260913.xcresult` 完整回归 exit 0：106 项注册、104 passed、2 physical skipped、0 failed；包含 96 项 Swift Testing 与 8 项 UI。两项跳过分别是数据库与媒体物理文件保护，不能算通过。浅色普通字号的保存后整行 contrast/textClipped 通过，导出图已查看，名称与状态完整位于内容区。深色最大字号与最终 Debug/Release 构建继续单独记录。


`DerivedData/WardrobePhotoDarkLargest20260913.xcresult` exit 0：2 项 UI 通过、无失败/跳过。先用 simctl 将本轮专用设备设为 dark / accessibility-extra-extra-extra-large，再使用相同 xcodebuild 参数，以 `-only-testing:ThenAppUITests/ThenAppUITests/testWardrobeCreateEditRestartAndDeleteJourney` 和 `-only-testing:ThenAppUITests/ThenAppUITests/testWardrobeAccessibilityAtLargestText` 执行。保存后列表与错误表单截图均已查看，实际为深色最大字号；完整行清晰可见，审计通过不代替真机 VoiceOver。

最终全量命令：`xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug -destination 'platform=iOS Simulator,id=F42DF127-A343-4E61-805B-E4DB170975D0' -parallel-testing-enabled NO -skipPackagePluginValidation -disableAutomaticPackageResolution -resultBundlePath DerivedData/WardrobePhotoVerified20260913.xcresult test`。环境为 ThenPhotoData20260913 专用 iPhone 17 / iOS 26.5（23F77），Xcode 26.6 / Swift 6.3.3；基于 HEAD 2bc97280796791fa3d20b6407e01b7a8e403b0fa 的未提交工作树，执行与技术自检 Codex，无独立人工发布审核。结果在忽略的 DerivedData，未上传 CI，丢失须重跑。

最终源码 SHA-256：

| 文件 | SHA-256 |
| --- | --- |
| WardrobePhotoModels.swift | 80387ea982539bdace8cd68ef82a760a09664f9c1e011c67f6ee7278e9320ddb |
| OOTDSchema.swift | fb1a5b20c6249145fd97d93b22ba6578f386b5a8d0a7a817ab3dc687711b48c7 |
| WardrobePhotoPersistence.swift | 64297e5ce830e889d184c9a17df9d3fea46d85ca15214903d5fa6c1ba7166c00 |
| GRDBWardrobeRepository.swift | 629cb319f80e929499f29e4df09426bb3c8cd63db22220e067ef769e56259ce3 |
| WardrobePhotoRepositoryTests.swift | bb2daa692a47c6f9c50be7f62fb8603fefabae9a85a56c518c7a929a0b23393d |
| WardrobeView.swift | 99af77f9413c413f429a2baf6128b78d81da0e7a9fe1a4c204e93c67f645e5e7 |

数据测试输入是合成标记字节；只验证保存/完整性和生命周期。没有证明其可解码、人物检测或单件衣物质量；IMAGE-01 必须以真实编码的合规图像验证。单一 actor/数据库事务证据不等同恶意同进程 TOCTOU、真实 kill/断电、低存储、硬件加密、零外联测量或 Woo 图片页面验收。


DATA-01 收尾：`scripts/validate-ios.sh build` exit 0，工具链、架构检查、Debug 和 Release 构建均通过；21 项脚本测试通过，`git diff --check` 通过。已有 ThenTransport YAML 资源规则/module-map 与 AppIntents 元数据警告仍记录，不宣称无警告。DATA-01 completed；下一项 IMAGE-01 pending，整个 12-03 保持 in_progress，尚未开放图片入口或完成整片发布验收。未提交或推送。

全部测试与构建进程已确认 exit；仅关闭并删除本轮创建的 F42DF127-A343-4E61-805B-E4DB170975D0 专用模拟器，未操作其他设备或用户数据。


## IMAGE-01编码与缩略图检查点

2026-09-13，时间盒 90 分钟：按 Design 05「单件图编码合同」实现无副作用编码阶段及测试，然后继续人物/单件规则、临时输入和真实图像联调。此为已批准输入/净化规则的实现细化，不新增上传、商品截图或人物模式，不改变 PRD 12 的保存门禁。

- WardrobePhotoModels.swift：新增独立衣物源格式、待检查编码对与精确准备协议；不赋予质量通过状态。
- Services/Imaging/ImageIOWardrobePhotoPreparer.swift：显式预算、签名/格式/单帧/完整性、方向变换/sRGB/缩略图、取消与错误；不接文件路径或数据库。
- Services/Imaging/PixelOnlyPNG.swift：抽出现有 PNG 编码后的元数据清理算法，人物和衣物处理共用；原头像错误由适配器映射保留。
- ImageIOAvatarPhotoSanitizer.swift：仅委托共用清理函数；原人物会话与净化合同不变。
- ThenAppTests/WardrobePhotoPreparerTests.swift：原创几何衣物和元数据实际编码、方向/颜色/两种尺寸/拒绝预算/取消/两图保存删除的真实验证。
- project.pbxproj 与独立 probe 编译脚本：登记当前实际依赖，不增加新 module 或第三方框架。

编码阶段通过不关闭整个 IMAGE-01；人物/重叠门禁、选图临时源生命周期与设备预算仍须继续，不把待检查图片直接传入 catalog_ready 保存。


### IMAGE-01编码阶段实际证据

2026-09-13 `WardrobeEncodingInitial20260913.xcresult` exit 0，17 项注册通过（衣物编码 10 项、原人物净化 7 项）；首次编译运行即通过，无伪造 Red。进一步补上八种 EXIF 逐像素方向映射及不放大小图两项，`WardrobeEncodingRegression20260913.xcresult` exit 0：110 注册、108 passed、2 physical skipped、0 failed。当前衣物编码 12 项通过；参数展开数与顶层注册数分别统计。真实损坏 PNG 拒绝测试会产生系统 ImageIO 错误日志，不是测试失败。

证据命令：`xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug -destination 'platform=iOS Simulator,id=0596F2EA-7F1C-452C-9C08-AC2ED0A75B1D' -parallel-testing-enabled NO -skipPackagePluginValidation -disableAutomaticPackageResolution -only-testing:ThenAppTests -resultBundlePath DerivedData/WardrobeEncodingRegression20260913.xcresult test`。这轮只跑单元/真实本地 Repository，未重复 UI，不能宣称本轮 Woo 页面验收。专用 ThenGarmentEncoding20260913、iPhone 17 / iOS 26.5（23F77），Xcode 26.6 / Swift 6.3.3；HEAD 2bc97280796791fa3d20b6407e01b7a8e403b0fa 上未提交工作树；执行与技术自检 Codex，非独立发布审核。

- JPEG/PNG/HEIC 与声明格式一致后解码；TIFF/动画拒绝，空字节、截断和错误声明失败。非零起点 Data 的 HEIC 输入通过，不假设字节索引从零开始。
- 240×320 原创几何 T 恤，非对称红袖、P3 色彩、透明底、合成元数据；生成 120×160 / 30×40，旋转后宽高相应交换。八种方向逐像素匹配期望映射；透明背景白底、sRGB、无 EXIF/GPS/TIFF/IPTC 及原始标记；小图不放大。它不是人物或真实拍摄数据，不能用于校准 Vision 质量阈值。
- 输入、源像素、两图输出、阶段时限与非法配置/取消通过。当前低尺寸测试预算不能替代 Design 05 候选 48 MP / 2048 px 的实际设备峰值/耗时验收。
- 实际编码图经显式测试夹具确认后，写入正式 Repository，关闭/重新开库可读取并由 ImageIO 解码；删除衣物后两图及目录消失。代码未提供自动 catalog_ready 判断，不把该测试确认带入产品。
- `THEN_AVATAR_TEST_SIMULATOR=0596F2EA-7F1C-452C-9C08-AC2ED0A75B1D scripts/validate-avatar-photo-session.sh imaging` exit 0，7 项原人物净化探针通过；其原会话删除、类型与异常语义保持。独立脚本与 picker 临时工程的源清单同时登记 PixelOnlyPNG。
- `python3 -m unittest discover -s scripts/tests -p '*test.py'` 21 项通过；架构与 diff 检查通过。最终 Debug/Release 构建结果另记。

结果包在忽略的 DerivedData，只是本地证据，未归档 CI，丢失须按命令重跑。IMAGE-01 仍 in_progress：待选图临时输入生命周期、人物/多件规则的隔离验证与联调、真实设备预算；IOS-01 仍 pending，未开放图片保存入口或认领 Woo 视觉复现。

最终 `scripts/validate-ios.sh build` exit 0：Debug、Release、工具链与架构均通过。已有 ThenTransport YAML/module-map 与 AppIntents 元数据警告未变化，不能宣称无警告。当前编码与共用工具源码 SHA-256：

| 文件 | SHA-256 |
| --- | --- |
| ImageIOWardrobePhotoPreparer.swift | 85363bc08b12bc359cbc699fa15e6acef536a47089ed23cff26f45d586a394c6 |
| PixelOnlyPNG.swift | ca604632dc6c6e2ca1814759d781295eb687a704f9998ee826a22bd411a3d2be |
| ImageIOAvatarPhotoSanitizer.swift | e9085274e5debdb41392057f6b2fed088e717425be23e1e42efee78a490b4943 |
| WardrobePhotoModels.swift | 3379974ab74d00b638576af6a094a397791c1906a36f27e2b2a7f98fd2fb3443 |
| WardrobePhotoPreparerTests.swift | 02b29ed739e8bec3d2cc32070ac596c8c35b346ab7fa5c05333a73b126943f32 |

本轮测试、独立探针、脚本测试与构建全部已终止；仅关闭并删除本轮专用 0596F2EA-7F1C-452C-9C08-AC2ED0A75B1D，未操作其他设备。工作树未提交或推送，整体目标保持 active。


## IMAGE-01文件接收与检测隔离检查点

2026-09-13，90 分钟时间盒：推进受控系统文件接收和有标签的合成图 Vision 观察，不降低保存准入；沿用 Design 05→PRD 12→本计划范围。

- Services/Photos/WardrobePhotoFileImporter.swift：provider 回调有效期内受限文件读取，核对路径/打开描述符身份、大小与变更，及时关闭；调用已通过衣物编码器，仅返回待检查两图，无原图落盘/删除或网络。
- ThenAppTests/WardrobePhotoFileImporterTests.swift：真实图像文件→净化对、原文件保持、大小/类型/链接/空/截断/取消拒绝与资源关闭；不把文件调用等同真实 PhotosPicker 通过。
- 原创合成人物 fixture：只给隔离检测使用，逐图核实预期人数/遮挡、记录提示词来源/文件摘要；不作为真实用户照片或产品头像。
- Vision 探针仅测试 target：记录当前运行环境的实际人框/前景能力，不将实例数冒充衣物数，失败与 unavailable 如实记录。生产分析服务需相应证据后再接入。

IMAGE-01 保持唯一 in_progress；本轮不预填系统选择器/人物检查/真实设备门禁通过。


素材准入子集：已在检测运行前查看 synthetic-one-adult 与 synthetic-flat-shirt，两者只有原创合成内容，标签/生成输出 ID/实际尺寸/字节/hash 固定在 `app/ThenAppTests/Resources/WardrobeContentProbe/manifest.json`；用途和生成约束见同目录 README。人物图只有一个完整成人形象，平铺衣物无人；均有 SYNTHETIC TEST 标识。它们只进入测试资源，11-01 完整素材/阈值仍未完成。

`WardrobeContentInitial20260913.xcresult` 首次编译失败于测试 closure 内宏展开的 throwing 目录读取；改为先读取清单再断言，未修改生产逻辑或移除断言。新增真实 2 MiB 单件图读取、输入恰好上限、Int.max 不溢出场景后，使用新结果包继续运行；不得将该编译失败称为业务 Red/Green 证明。


`WardrobeContentRun20260913.xcresult`：文件导入 8 项通过，含三种格式、2 MiB 实际图片/恰好上限、链接/目录/空/缺失/非文件、声明/动画、时限/取消与 Int.max；Vision 默认请求六个参数用例均报 `com.apple.Vision Code=9 Could not create inference context`，整次 exit 65。没有得到人数，不能把异常映射成零人或准入通过。测试使用预先登记标签，没有更改标签来匹配返回值。

为区分素材/归一化与平台差异，新增 `scripts/probes/WardrobeVisionHostProbe.swift`，仅作 macOS 主机对照：使用相同衣物编码器、512 px 工作图及 VNRequest revision（human 2、face 3、foreground 1），两个素材六项实际计数分别为 1/1/1、0/0/1，与标签一致；原始输出位于 DerivedData/WardrobeVisionHost20260913/result.log。该入口捕获逐项错误并输出，因此进程 exit 0 本身不代表成功；本次已逐项核对实际六行 count，无错误行。它不是 iOS 真机验收。

诊断依据为锁定 SDK 的 VNRequest 计算设备接口，以及 [Apple 人物检测](https://developer.apple.com/documentation/vision/vndetecthumanrectanglesrequest)、[前景实例接口](https://developer.apple.com/documentation/vision/vngenerateforegroundinstancemaskrequest)。默认自动设备失败与主体类型无关；进一步对照只在 probe 中使用系统明确支持的 CPU stage，不使用 deprecated usesCPUOnly，也不引入生产自动 fallback。默认请求失败仍保留单独用例和结论。

当前 iPhone 16 经 devicectl 再核对 unavailable，已向用户请求可用 iOS 26 iPhone 以补实机观察；其他文件/选图实现可以继续，整体目标不因此关闭或缩小。


最终模拟器矩阵：`WardrobeContentRegression20260913.xcresult` exit 65，120 注册、116 passed、2 failed、2 physical skipped；失败为默认 Vision 和明确 CPU 对照两项参数化测试，合计 8 个参数失败。CPU 模式下两图的人物/面部四次计数均匹配 1/1、0/0，分别约 0.157/0.083/0.067/0.068 秒；前景请求没有可用 CPU stage，明确返回 unsupportedCPUStage。默认六次仍 Code 9；未忽略、删除或改为 expected failure。普通衣物/编码/生成 Client/照片与媒体回归通过，不能据此将整体测试报告写成全绿。

推断边界：原生 macOS 相同请求成功、模拟器 CPU 人物/面部成功，说明问题与该模拟器运行配置/可用推理设备有关，而非已证明素材不存在人物；尚未证明底层系统具体缺陷或 iOS 真机结果。没有启用强制 CPU 的生产兼容处理，前景分割也没有用矩形/色块算法替代。当前真机不可用与人体多场景矩阵不足仍限制能力接入；系统 PhotosPicker/用例/UI 尚待完成。

完整回归命令：`xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug -destination 'platform=iOS Simulator,id=1DC968DC-E61D-416B-AAFF-825B35F3C0F2' -parallel-testing-enabled NO -skipPackagePluginValidation -disableAutomaticPackageResolution -only-testing:ThenAppTests -resultBundlePath DerivedData/WardrobeContentRegression20260913.xcresult test`。环境为本轮专用 ThenContentProbe20260913，iPhone 17 / iOS 26.5（23F77），Xcode 26.6 / Swift 6.3.3，HEAD 2bc97280796791fa3d20b6407e01b7a8e403b0fa 的未提交工作树；执行/技术自检 Codex，非独立发布审核。结果均在忽略的 DerivedData，未上传 CI，丢失须重跑。

该次主机对照属于历史隔离证据，原独立入口已按“仓库不保留 scripts 内容”的决定退役。当前共用探针位于 `app/ThenAppTests/Support/WardrobeVisionProbe.swift`，由 ThenAppTests 直接编译；后续以带 `-resultBundlePath` 的 Xcode 测试和结果摘要为事实，不用主机结果替代 iOS 验收。


用户随后明确“先使用模拟器进行开发”：停止等待设备连接，IMAGE-01 继续推进模拟器中可执行的真实人物/面部分析、单件复核、选图及保存/删除。前景分割的缺失保留为能力限制，不创建假的成功结果；物理测试移至后续发布验收，原 PRD 隐私/单件规则保持。下一轮不重复请求连接设备，除非用户主动提供或进入真机验收阶段。

收尾构建 `scripts/validate-ios.sh build` exit 0，Debug、Release、工具链和架构通过；21 项脚本测试通过，diff 检查通过。Release App 包内检查没有 WardrobeContentProbe 或两张合成 fixture；探针、人工合成标签和图片均限测试资源/源码。已有 ThenTransport YAML/module-map 与 AppIntents 警告保留。最新主机对照 final.log 六项 counts 符合原标签，exit 0；仍不抵扣两项 iOS 模拟器检测测试失败。

当前源码 SHA-256：

| 文件 | SHA-256 |
| --- | --- |
| WardrobePhotoFileImporter.swift | 4d7ed556bc9bea44dfc68bbe87eac78273b2d025dae812bfee07a81a6dafd924 |
| WardrobePhotoFileImporterTests.swift | 2e5aed8cc26486627037f327279578d6dacebaf974734888cbab062d6c6687cd |
| WardrobeVisionProbe.swift | 0526fa478b6580684374be014696e38a71037ecdf6c0df50ac791282f01efecd |
| WardrobeVisionHostProbe.swift | e12c8b505e61f51999b495ef1b15059ddd8b3afe7e395406c1fc15882e574333 |
| WardrobeVisionProbeTests.swift | e9fbb230dca20f09f2a082f573195a6e0dde1aa1eb1e269b9357e36750b7ec04 |

本轮所有测试、主机诊断与构建进程已确认终止。清理时专用模拟器已为 Shutdown；确认状态后仅删除本轮创建的 1DC968DC-E61D-416B-AAFF-825B35F3C0F2 专用模拟器，不改动其他设备、生产数据或已有修改；未提交或推送。

## IMAGE-01选图交接与复核实施

2026-09-13 继续按模拟器开发；上一轮为 progress（导入器、检测证据及用户排期决定已落地），整体目标未完成。沿用 approved 单件契约，不扩展照片目的/云能力。当前时间盒 120 分钟推进系统交接与复核规则，IMAGE-01 保持唯一 in_progress。

逐文件职责与验收：

- Services/Photos/SystemWardrobePhotoPicker.swift：PhotosPickerItem → 受控 FileRepresentation → WardrobePhotoFileImporter → WardrobePreparedPhoto；显式 limits/总时限，operation 独占任务、Progress 和 continuation，取消/超时/迟到/重复/错属结果可验证；无原图副本或人物声明复用。
- Features/Wardrobe/Domain/WardrobePhotoReview.swift：当前图片候选、检测结论、用户明确单件/无人物/拥有/完整确认及 catalog_ready 写入值；替换/拒绝/失败使旧确认失效，不访问图库、文件、数据库或网络。
- ThenAppTests/SystemWardrobePhotoPickerTests.swift：真实 FileRepresentation 与 PNG/JPEG/HEIC、元数据和源字节保留；确定性 provider 回调、取消/超时/重复/并发错属、包装错误与一任务限制；测试夹具只来自既有合成目录。
- ThenAppTests/WardrobePhotoReviewTests.swift：检出人物/面部、分析失败、缺少确认、替换候选/迟到结果、稳定保存 ID 与质量边界；后续与实际检测服务及 ViewModel 联调独立记证据。
- project.pbxproj：仅登记上述当前实际源码与测试；不加入新包/模块，不将 Vision probe 移为生产服务。
- Design 05、Acceptance 12 与本计划：同步真实测试结果和未交付 UI/性能/真机边界。OpenAPI、后端、migration 不变；回滚仅移除本轮独立适配器/复核及对应登记，保留已有衣物/媒体数据。

首次 WardrobeSelectionInitial20260913 因缺少 SwiftUI import（PhotosPickerItem 作用域）编译失败，补齐模块导入后 WardrobeSelectionRun20260913 真实运行 19 项：复核 8 项通过；provider 同步 nil/错误的两个参数复现 Progress 未取消。原因是完成回调早于 start 返回时 finished=true，适配器丢弃了尚未交接的 Progress。修正为只按 loading/cancelled/delivered 所有权保存或取消 Progress，不以完成回调先后决定所有权；回归另记。

继续 IMAGE-01 的逐文件任务：Services/Imaging/VisionWardrobePersonAnalyzer.swift 新增精确人物检测服务，接净化规范图、显式字节/像素/阶段时限与 compute device 配置，固定已验证 request revision，返回计数或明确失败；不做分割、属性识别、CPU 自动回退或写库。Domain/WardrobePhotoReview.swift 增加该精确分析协议。ThenAppTests/VisionWardrobePersonAnalyzerTests.swift 用原有人工标注两张合成图验证真实 CPU 人物/面部结论、预算/取消/坏图拒绝，并串联真实文件接收→复核→Repository 保存/读取/删除。该串联不是 PhotosPicker 系统 UI 或全场景检测验收，默认 Vision/前景探针的原失败继续保留。


### 选图、分析与复核实际证据

- `WardrobeSelectionFixed20260913.xcresult` exit 0，系统选图 11 项与复核 8 项通过；随后增加并行错属选择测试并进入完整回归。三个真实 FileRepresentation 格式均返回规范图对；取消/迟到/重复/错属/无回调超时不交付。该结果不是实际 PhotosPicker 页面点选或 iCloud 下载验收。
- `WardrobeSelectionAnalysis20260913.xcresult` exit 65：19 项交接/复核通过，5 项新分析测试在文件接收处失败，共 10 个参数报 unsafeInput。先前夹具直接把安装 bundle 文件当作 provider 文件；后续运行时诊断确认其 link count=2，正确触发了生产导入器的硬链接拒绝。测试改成从已校验哈希的合成 bundle 读字节、写入独立测试临时文件来模拟 provider 生命周期，保留原有生产硬链接拒绝，不放宽源准入。
- `WardrobeAnalysisProvider20260913.xcresult` exit 0，5 项实际分析/生命周期测试通过：CPU 成人 1 人/1 面、衣物 0/0；预算/格式/尺寸/取消拒绝；规范图→真实检测→当前人工确认→实际 Repository 保存/同 ID 重试→重开→读取→移除，衣物保留。原图为既有合成素材，无用户照片。512 px/字节等为该测试显式配置，不冒充最低设备生产预算。
- `WardrobeAvatarTransferRed20260913.xcresult` exit 0 但实际 0 项执行：未带参数签名的 Swift Testing 方法筛选没有匹配，**不是通过证据**。改用完整 Suite 后 `WardrobeAvatarTransferRegressionRed20260913.xcresult` exit 65，15 项中原人物 adapter 同步 nil/错误两参数复现 Progress 丢失；5 项分析仍通过且输出 bundle link count=2。修正人物 adapter 同一登记条件，保留本人声明/临时会话/清理，不新建通用抽象或兼容分支。临时 link-count 诊断已从最终测试源码移除。

最终 `DerivedData/WardrobeSelectionFullRegression20260913.xcresult` exit 65：**145 项注册、141 passed、2 failed、2 physical skipped**。本轮 12 项选图、8 项复核、5 项真实分析测试均通过；原人物选图 10 项包含新 Progress 断言通过，既有编码/文件/Repository/生成 Client/ViewModel 回归均保留。顶层结果不能改写为完整通过。

两个失败注册项仍为 WardrobeVisionProbeTests 默认与 CPU 诊断，各两张图的前景参数失败（总 4 个参数）：默认 Code 9，CPU unsupportedCPUStage。本次同一进程中的默认人物/面部四个参数计数正确，与前轮默认六项失败不同；仅证明本次进程观察，尚不能区分热缓存/执行顺序/环境差异，不把它宣称为默认设备选择已经根治。产品分析显式配置 CPU，无失败后切换逻辑。零检出还必须通过当前图片的用户单件/拥有/无人物/完整确认。

完整命令：`xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug -destination 'platform=iOS Simulator,id=0A0E4178-AD3F-4E28-977B-2BF667EC3A86' -parallel-testing-enabled NO -skipPackagePluginValidation -disableAutomaticPackageResolution -only-testing:ThenAppTests -resultBundlePath DerivedData/WardrobeSelectionFullRegression20260913.xcresult test`。环境 ThenWardrobeSelection20260913，iPhone 17 / iOS 26.5（23F77）、Xcode 26.6 / Swift 6.3.3；HEAD 2bc97280796791fa3d20b6407e01b7a8e403b0fa 的未提交工作树。执行/技术复核 Codex，不是独立发布审核；本地忽略目录中的结果包未归档 CI，丢失需重跑。

IMAGE-01 继续 in_progress：完整安全/单件矩阵、系统相册实际交接和页面生命周期、生产参数仍须推进；IOS-01 尚未接入选图与复核服务，也没有本轮 Woo 页面截图验收。当前业务 View 不新增网络/文件访问，无 OpenAPI/后端/schema 变更。CPU 服务不执行前景分割，不把它包装成已成功抠图。同步失败 Progress 的根因修复与生命周期回归不抵扣真机保护/低存储/性能或真实 3D 验收。

最终源码 SHA-256（本检查点）：

| 文件 | SHA-256 |
| --- | --- |
| SystemWardrobePhotoPicker.swift | 272500d79869a36aff7c42e94ea439b23b6c7b4bcde12b5a1ef039644b7da146 |
| SystemAvatarPhotoPicker.swift | 60fcb23834e2e5734bb00a1fc4b25619fc5f58d09da64442a3b2834c3689c355 |
| WardrobePhotoReview.swift | 47639f53c7aecda7ba2bb5cb7c959747c53056a7d35a947f3d153a2d8d54826d |
| VisionWardrobePersonAnalyzer.swift | f3861ff14d81e033cba095b6b97627c307c7c88ea84cc471ee0c109642d0c300 |
| SystemWardrobePhotoPickerTests.swift | 62ad4a39c40fe6f6efbef9bd444e60f9a70567a75983e7226020bb0a8a18805c |
| WardrobePhotoReviewTests.swift | 4e74099236e73c80941d329f018fa017e0d1903d04ba0f8ab2f34834fa4ad8a0 |
| VisionWardrobePersonAnalyzerTests.swift | 71a15e6c70c405c39d0ebf66edb0ed7b9afc3b827ccbcffc2b447df4d787bcbd |
| SystemAvatarPhotoPickerTests.swift | 8553cb0f315b635341a609411638a26bdcf358b420af367706d6abb5d132fcb3 |

收尾 `scripts/validate-ios.sh build` exit 0，Debug/Release 真实构建成功；`scripts/validate-ios-architecture.sh` 与 21 项脚本测试通过。现有 ThenTransport YAML/module-map 与 AppIntents 警告未在本轮消除，构建成功不表示完整验收通过。

最终 Release App 检查不包含 WardrobeContentProbe 与两张合成图资源。所有本轮测试/构建/脚本进程均已取得终止结果；确认专用 ThenWardrobeSelection20260913（0A0E4178-AD3F-4E28-977B-2BF667EC3A86）为 Shutdown 后仅删除该设备，未操作其他设备。diff 检查通过；未提交、未推送，保留已有工作树改动。整体目标保持进行中。

## 页面联调的原子提交修正

2026-09-13 上轮为 progress（25 项新增测试及同步回调缺陷修复），本轮继续模拟器页面联调。前置发现 create/update 与 savePhoto 分两次提交，会留下半完成衣物。沿用 approved 保存/失败/删除行为，先修正这一根因，再接页面，不用失败后删除衣物的补偿方案。

临时将 DATA-01 退回 in_progress、IMAGE-01 pending，确保同一计划只有一项执行中；原 DATA-01 证据保留，新增原子保存通过后恢复状态。时间盒 120 分钟推进原子保存及页面组合用例，未通过项目全范围验收不关闭目标。

逐文件：Domain/WardrobePhotoModels.swift 增加衣物与照片一次保存的精确输入/输出与 Repository 方法；Data/Database/OOTDSchema.swift 追加 ootd_v3_unattached_photo_imports，重建照片表允许 importing/deleting 无 itemID、ready 必须绑定，保留数据和索引；Data/Wardrobe/WardrobePhotoPersistence.swift 在同一最终事务写衣物与照片，重用既有路径/意图/清理；Data/Wardrobe/GRDBWardrobeRepository.swift 接入新用例与 Data 内部 Record。ThenAppTests/WardrobePhotoRepositoryTests.swift 更新合法 migration 列表，新加 WardrobeItemPhotoSaveTests.swift 验证真实 SQL 拒绝/文件失败、旧图字段保持、同命令重试、新增不可见中间态、v2 数据升级和无绑定导入恢复；project.pbxproj 只增加该测试登记。

v3 回滚不清除 OOTD 数据：保留新 schema，以修复版本恢复；旧二进制将按已存在 newerDatabase 机制拒绝，不引入双 schema 兼容。OpenAPI/后端不变。本次不是已发布产品字段变化，属于已批准“同一次保存”行为的实现修正，不新增用户确认请求。

`WardrobeAtomicPhotoInitial20260913.xcresult` 26 注册、25 通过、1 物理保护跳过，原子保存与原照片 Repository Suite 通过。DATA-01 恢复 completed，IMAGE-01 恢复 in_progress，继续同一切片的系统照片/页面生命周期联调，不提前认领 IOS-01 Woo 网格验收。

### 原生照片编辑与完整回归检查点

本轮追加 `ootd_v3_unattached_photo_imports`，v1/v2 不改写；新照片在 importing 阶段可以尚未关联衣物，ready 仍强制关联衣物。衣物字段与照片 ready 在同一个 SQLite 事务发布，失败不留下新衣物或改写原衣物；不是创建失败后删除衣物的补偿。真实 v2 数据升级测试保留原字段、照片记录与文件，回滚不自动降级或擦除现有 OOTD 数据。

- `WardrobeAtomicPhotoInitial20260913.xcresult`：新增 8 项原子保存测试与原照片 Repository 回归，含 SQL trigger 失败、同命令重试、旧图替换失败、过期/错属命令、无关联导入恢复及 v2 → v3 升级。
- `WardrobePhotoEditingRegistered20260913.xcresult` exit 0，11 项通过（新增编辑用例 7 项、原 ViewModel 4 项）：复核前不保存、人物/坏像素拒绝、迟到和取消结果、失败保留草稿后重试、坏图仍可删除、清理障碍重试。前一次 `WardrobePhotoEditing20260913` 只运行原有 4 项；注册脚本失败后已更正登记，不能用该前次结果证明新用例运行。
- `WardrobePhotoPickerJourney20260913` 因远程 PhotosUI Image 没有 AX tap 动作失败；根据实际画面与元素 frame 点击该合成素材格中心。`WardrobePhotoPickerTileJourney20260913` 发现测试点击了 Toggle 外层标签而非原生 switch，业务保持三个值为 false、正确阻止确认；修正为点击真实 switch 并逐项断言值。同期修复选图后编辑框仍持有焦点、键盘遮挡复核的问题。
- `WardrobePhotoReviewJourney20260913.xcresult` exit 0，1 项真实 UI 旅程通过：PhotosPicker 选择本地合成衬衫 → 净化/CPU 分析 → 用户复核 → 原子保存 → 终止/重启读图 → 移除 → 再次重启不复活 → 删除衣物。
- `WardrobePhotoUIFullRegression20260913.xcresult` exit 65：**169 注册、163 passed、4 failed、2 physical skipped**。160 项单元中 156 通过，两个原前景诊断仍失败；9 项 UI 中 7 通过，普通和最大字号两项失败。照片旅程新增后台/前台恢复已通过，两个无障碍用例共记录 9 个问题，不是 9 个 UI 用例全部失败。

本轮完整回归明确复现照片区标题/按钮对比度、Label 文字裁切和必填错误被新照片区推后的问题；修改 WardrobePhotoPickerButton 的可伸展语义色标签、WardrobeEditorView 的标题与错误位置。保留 `.contrast`/`.textClipped` 检查，不过滤问题、不缩小最大字号。第一次定向命令遗漏测试类名导致 `WardrobePhotoAccessibilityFix20260913` 实际执行 0 项，只是构建成功；纠正命令后的真实结果另记。

完整回归环境：专用 `ThenWardrobePhotoUI20260913`，iPhone 17 / iOS 26.5（23F77），UDID `2CD8DD55-379E-441C-B4A3-1A851DFFDA68`；Xcode 26.6 / Swift 6.3.3。仅通过 `xcrun simctl addmedia <专用UDID> app/ThenAppTests/Resources/WardrobeContentProbe/synthetic-flat-shirt.png` 预置既有合成素材，图库其余 6 张是系统默认风景图。本轮点选素材已通过截图检查；重跑必须创建专用空白模拟器并让该合成衬衫成为最新图，不能在含真实私人照片的图库上运行此测试。

完整命令：`xcodebuild -project app/ThenApp.xcodeproj -scheme ThenApp -configuration Debug -destination 'platform=iOS Simulator,id=<上述专用UDID>' -parallel-testing-enabled NO -skipPackagePluginValidation -disableAutomaticPackageResolution -resultBundlePath DerivedData/WardrobePhotoUIFullRegression20260913.xcresult test`。结果在忽略的 DerivedData，只是本地证据，丢失后按同样前置条件重跑，不依赖临时日志作为长期证据。

`WardrobePhotoUIFullAttachments20260913` 的 restored-after-restart 截图可见完整衬衫；background-snapshot-review 截图只见不透明“内容已隐藏”卡片。两图已经实际查看。该截图证明本轮模拟器快照状态，不等于全设备锁屏/录屏保护、完整 VoiceOver、离线或零外联测量。512px/CPU 配置是明确的模拟器开发组合，尚非最终 Woo 生成图预算；不把未实现的前景分割或完整单件安全矩阵算作通过。Woo 网格/真实 3D/AI 生成仍未交付。

联调逐文件：Services/Photos/WardrobePhotoPickerButton.swift 封装原生选择控件及选择协议，使用 Design 05 显式开发预算，返回净化图+真实检测结论；Domain/WardrobePhotoReview.swift 增加精确选择结果/协议；Presentation/WardrobeViewModel.swift 接异步候选、确认、原子保存、已保存图读取与独立确认移除、迟到/取消保护；Presentation/WardrobeView.swift 增加照片区、原生复核/失败/清理文案与 .task 生命周期。图片显示经 Services/Imaging/WardrobePhotoImage.swift 使用 ImageIO 解码规范图到 SwiftUI Image，不在 View 读文件/Photos/网络。App/OOTDAppModel 与 ThenApp 使用同一 Repository 的衣物/照片协议组合，不新增全局服务。Localizable.xcstrings 收录新文案；测试 WardrobePhotoEditingTests.swift 使用真实 Repository 与受控选择覆盖选择/换图/取消/确认/保存失败/删除，XCUITest 接系统图库与真实合成素材操作，编译与既有旅程回归。project.pbxproj 登记新职责源码/测试，Docs 同步真实证据。


### 原生照片编辑最终复验

`WardrobePhotoAccessibilityFixRun20260913.xcresult` 真实执行 2 个 UI 用例，颜色与错误位置修复生效，Label 裁切仍失败；最终将选图控件改为可多行增长、最小 44pt 的原生文字按钮，保留系统 PhotosPicker。没有关闭无障碍审计或改小字号。

`WardrobePhotoUIAccessibilityRegression20260913.xcresult` exit 0，**9 注册、9 passed、0 failed、0 skipped**，实际含系统合成照片旅程。新增测试前置守卫：只有 `SIMULATOR_DEVICE_NAME` 以 `ThenWardrobePhotoUI` 开头才进入照片用例，否则明确跳过；还必须按前述步骤预置最新合成衬衫。该守卫不替代素材准备，也不意味着普通模拟器上的跳过等同通过。

命令使用前述完整 xcodebuild 参数，加 `-only-testing:ThenAppUITests`，resultBundlePath 改为 `DerivedData/WardrobePhotoUIAccessibilityRegression20260913.xcresult`。本轮是受影响的全部 UI 复验，未重跑未变化的 160 项单元；单元仍引用完整结果的 **156 passed / 2 failed / 2 physical skipped**，不能把两份结果改写为单个“全部通过”结果包。

最终附件目录 `DerivedData/WardrobePhotoUIAccessibilityAttachments20260913` 中普通与最大字号错误页、重启照片页与后台遮罩图均已实际查看：错误先于照片区、按钮文字与标题可读，照片保持完整比例；后台卡片不显示照片。模拟器图不是 Woo 视觉还原完成的证据，也不证明未执行的深色图片页、VoiceOver、真实换图 UI、iCloud 或零外联测量。

源码检查点 SHA-256：OOTDSchema `c6edeba68648a2e632583669af885fc38f50057a7d3cd8953d8172a80bb2a32e`；WardrobePhotoPersistence `f080fe4f2a961f85326eded609b1b0e8919a1b3d99ccc52bd41ef73f7473794d`；WardrobeViewModel `c29332e0ed93fdabccb64843198e4a7aa356d928a2896d9069f51801441ae181`；WardrobeView `a2316e0463f405f371dc0b7a00d6e18b8a04d2d483ffc7c87c8560f7799f6c2c`；WardrobePhotoPickerButton `b73ae559d5d48970559551e0f7c28be7ecf64a79e2dc9cc563674c330a531d14`；ThenAppUITests `c0789d47a866c2ecfea50974177ff5194f5aa2070aeca488b50a81a65c42f7e9`。均为 HEAD `2bc97280796791fa3d20b6407e01b7a8e403b0fa` 上未提交工作树，执行与复核为 Codex 自检，不冒充独立审核。


本轮收尾：`scripts/validate-ios.sh build` exit 0，工具链、架构与 Debug/Release Simulator 构建全部通过；`python3 -m unittest discover -s scripts/tests -p '*_test.py'` 21 项通过，`git diff --check` 通过。Release App 资源检查未包含 synthetic 图或 WardrobeContentProbe；OpenAPI 符号链接仍只指向 backend/openapi.yaml。已有 ThenTransport YAML/module-map 和 AppIntents 警告未扩展处理，本轮未修改后端/OpenAPI、未提交或推送。专用模拟器结束时已为 Shutdown，随后仅删除本轮拥有的该设备，未操作其他设备。

IMAGE-01 继续唯一 in_progress：下一步完善人物/身体部位/多人/重叠矩阵、真实换图与取消 UI、正式参数和离线/零外联证据，再进入 IOS-01 Woo 网格及同状态视觉验收；物理保护/最低设备资源/完整 VoiceOver 留待后续真机发布验收。整片和全项目目标均未标记完成，本轮为 progress。


换图补验收尾：`scripts/validate-ios.sh build` exit 0，Debug/Release、工具链与架构通过，`git diff --check` 通过；Release App 未包含合成 fixture。当前源码 SHA-256：WardrobeViewModel `cd657b4fad6c8ef709869694a67ed11b7db77a21327a696ea7d9f31b6f834532`；WardrobePhotoEditingTests `84519928a97cfc2946d0efcbaff6df49d6d4a79f24fb7627534d234fdc3b3c37`；ThenAppUITests `0e3cba92afa45f5feb3261a1e20ebd3e9cc8f7380477cc0e3e0cf982bddbc849`。仍为原 HEAD 上未提交工作树，未提交/推送；执行和技术复核为 Codex 自检。专用模拟器收尾时 Booted，已关闭并仅删除本轮该设备，其余设备未操作。原完整回归及发布门禁状态不变，本轮为 progress。

## 2026-09-13IOS分类网格时间盒

当前 120 分钟推进 IOS-01：实现真实缩略图分类浏览、无图可编辑、revision 刷新与后台清空，再验证真实 Repository 和模拟器标准/最大字号及照片旅程。IMAGE-01 暂回 pending（未完成）；IOS-01 依赖 DATA-01 与已实现的选图/净化/复核/原子保存子检查点，完整 IMAGE 安全矩阵继续阻断整片完成。只调整独立任务顺序，不削弱验收。

- [x] SwiftUI 分类网格、搜索/筛选和原生编辑入口。
- [x] 有界缩略图读取，移除/更新/退出不残留旧图；真实 Repository 与迟到 clear/cancel 子测试通过。
- [x] Repository/状态测试与模拟器标准、大字号、照片旅程：最终 21 注册全通过，见 acceptance 12 分类网格证据。
- [x] 同步实际证据和未通过门禁；整个目标保持 active。

初始数据检查：`WardrobeGridInitial20260913.xcresult` exit 0，9 注册测试全部通过（含动态参数展开 11 次）；新增真实类别/状态/名称筛选和真实 PNG 缩略图读取→清空→未知衣物→重读→移除检查。仅编辑 Suite，不代表全部单元或 UI 已通过。

网格影响与回滚：本次只修改 WardrobeView/受限缩略图 ViewModel、现有编辑 Suite/UI 测试和对应文档；没有 schema、依赖、OpenAPI 或服务端修改。回滚浏览实现只恢复原列表展示，不删除衣物、照片或已应用 migration；测试数据清理由专用模拟器中的原生删除完成，不触及用户当前 OOTD 数据。

最终检查点：`WardrobeGridFinal20260913.xcresult` exit 0，10 编辑/缩略图 + 11 UI = 21 注册全部通过，0 失败/跳过；参数展开 24 次。常规四列、辅助字号单列、真实衬衫和后台遮罩截图已人工检查。IOS-01 尚有完整 VoiceOver、离线/零外联与媒体矩阵验收，因此继续 in_progress；IMAGE-01 的未完成矩阵保持 pending，TEST/REL 不提前关闭。复现命令、源码哈希、首轮失败和修复详情统一见 acceptance 12。

网格构建收尾：`scripts/validate-ios.sh build` exit 0，工具链、架构检查与 Debug/Release 两次构建通过；Release App 不包含 WardrobeContentProbe/AvatarPhotoIntake 或两张合成图。`git diff --check` 通过。原有 ThenTransport YAML/module-map 与 AppIntents 警告继续记录；无新增依赖、API 或迁移。专用模拟器 0374FF54-8B2B-4F3A-86B6-C218DFB16772 已关闭并删除，其他设备不变。未提交/推送，保留既有工作区修改。
