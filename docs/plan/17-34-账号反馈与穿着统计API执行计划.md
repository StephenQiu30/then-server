# 17-34 账号反馈与穿着统计 API 执行计划

## 状态与范围

- 契约：`approved`；执行：`completed`（后端切片，2026-09-26）。后端数据/API 是 [BL-HISTORY-02](../../BACKLOG.md#历史复用统计与扩展反馈) 的第一片；Web 页面接续 17-35。符合用户指定的 `then-server` 后端先行顺序。
- 依据：[PRD16](../prd/16-穿搭记录与反馈需求.md)、[Design 28](../design/28-账号反馈与穿着统计设计.md)、[Design 22/23](../design/22-后端数据库设计.md)、[16-03](16-03-结构化穿后反馈与证据重建执行计划.md)、[17-23](17-23-账号实际穿着API执行计划.md)。验收归 [Acceptance 16](../acceptance/16-穿搭记录与反馈验收.md) 的账号反馈子集。

## 检查点

| ID | 状态 | 完成条件 |
| --- | --- | --- |
| `17-34-DOC-01` | `completed` | 固定云/本地事实范围、可撤回反馈、去重穿着统计与日历口径。 |
| `17-34-DATA-01` | `completed` | 反馈与命令回执 FK/校验，事件行锁下事务写入，事件/账号删除级联。 |
| `17-34-API-01` | `completed` | GET/PUT/DELETE 反馈与 GET 穿着统计四项 operation，owner/401/404/409 和运行时 OpenAPI。 |
| `17-34-TEST-01` | `completed` | 单元、完整 Go/race/vet/module，隔离真实 PostgreSQL 与运行中 HTTP，校验边界和重算。 |
| `17-34-REL-01` | `completed` | 本切片证据回写并独立 Git 提交，交付工作树干净；公开发布另过门禁。 |

## 本地验证证据

2026-09-23 使用隔离 PostgreSQL 18、Redis 与实际 Go 进程 `127.0.0.1:18084`。OpenAPI 3.1.2 / API 0.18.0 共 102 个 operation；从运行服务生成 `frontend/src/api` 客户端。合成账号 HTTP 旅程通过注册→衣橱→实际事件→反馈保存/相同命令重试→旧 revision 409→反馈撤回→真实穿着统计仍为 1→匿名 401。真实 PostgreSQL race 专项覆盖双账号隔离、反馈纠正/撤回与再建、事件改期前后重算、事件/账号删除级联与回执清理；17-23 既有实际事件回归同过。完整 `go test -race ./... -count=1`、`go vet ./...`、`go mod verify`、前端 lint/typecheck/format/15 tests 均通过。当次未运行远端 CI、公开发布、真机或 App 同步。

2026-09-26 在隔离交付工作树复测当前代码：`go test -race -tags=services ./tests/services -run '^TestOutfitFeedbackPersistenceAndStatistics$' -count=1`、`go mod verify`、`go vet ./...` 通过。测试仅在本机现有 PostgreSQL 的 `postgres` 库建立并清理临时 schema，没有新建数据库。包含此实现的 `f23c281` 在 [CI 36208387693](https://github.com/StephenQiu30/then-server/actions/runs/36208387693) 的 Go race、PostgreSQL 集成、OCI 容器及前端任务均通过；该次远端工作流未执行 `services` 专项，故真实反馈数据库场景以本机复测为证。交付提交由独立工作树完成，不覆盖主工作树中的并行改动。公开发布、真机及 App 同步仍待各自验收。

本片不改 `then-app`，不自动上传离线事实，不预建偏好缓存或假造推荐学习。17-35 在 API 稳定后实现可操作 Web 反馈与统计核验页；BL-HISTORY-02 直到页面及其验收完成仍为 `in_progress`。
