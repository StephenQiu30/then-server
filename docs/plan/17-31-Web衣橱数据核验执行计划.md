# 17-31 Web 衣橱数据核验执行计划

## 状态与合同

- 契约状态：`approved`，依据用户 2026-09-23 对 then-server 后端优先、Web 页面验证、App 后续实现的明确范围。
- 执行状态：`local_development_complete`（2026-09-23；公开发布门禁未关闭）。
- 需求：[PRD 12](../prd/12-数字衣橱与衣物录入需求.md#web-数据核验入口2026-09-23)；设计：[Design 25](../design/25-Web衣橱数据核验设计.md)；验收：[Acceptance 12](../acceptance/12-数字衣橱与衣物录入验收.md)。

本片只实现账号无图衣橱 Web 页，消费已有 17-20/17-21 API。推荐、计划/实际记录页面、图片、离线 Look、App 和生产公开发布不在本片。

| ID | 状态 | 完成条件 |
| --- | --- | --- |
| `17-31-DOC-01` | `completed` | PRD/Design/Plan/Backlog 固定路线、字段、删除影响和非目标。 |
| `17-31-UI-01` | `completed` | `/wardrobe` 列表/分页与创建编辑表单，四项属性可清除为未知；删除前读取影响并二次确认策略。 |
| `17-31-TEST-01` | `completed` | 生成客户端无差异，纯映射测试、lint/typecheck/format/build/生产依赖审计通过。 |
| `17-31-BROWSER-01` | `completed` | 真实 PG/Redis/API/Next 验证两账号、刷新、409、删除影响/策略、401/网络恢复及移动布局；删除对话框键盘触发及取消。 |
| `17-31-REL-01` | `pending` | 开发证据已回写并提交；生产公开入口保留账号发布门禁。 |

失败时保留旧表单和历史；删除仅由本人明确确认，服务端 revision/摘要仍是最终判断。回滚只撤销 Web 入口，已完成的衣橱 API 与数据库事实不回退。

## 本地开发证据（2026-09-23）

- 环境：隔离 Docker Compose 项目 `then-wardrobe-1731` 的 PostgreSQL/Redis，Go API `127.0.0.1:18080`，Next 开发及生产构建 `127.0.0.1:3114`；仅使用合成账号与衣物，测试结束销毁隔离数据。
- 生成：从运行中 `/openapi.json` 执行 `npm run openapi`，`frontend/src/api` 无差异。`npm run lint`、`npm run typecheck`、`npm run format:check`、`npm test`（8/8）、`npm run build` 和 `npm audit --omit=dev --audit-level=high`（0）通过；生产启动后 `/wardrobe` 与本人 API 复核通过。
- 浏览器：A 创建衣物并刷新、编辑可穿/待洗和属性；服务端外部修改使旧 revision 保存得到 409，表单保留并可主动重新加载。删除影响实际显示计划 1/穿着 1；默认策略使两条历史仍返回 200 且 `items[0].content=null`，另一策略使衣物/计划/实际均返回 404。B 列表为空、读取 A 衣物及历史均为 404。模拟衣橱请求中断时出现重试，解除中断后恢复；清除 Cookie 后跳转 `/login`。
- 视觉与辅助：320px `scrollWidth=clientWidth=320`，390px 与 1280px 布局人工截图检查；生产页 axe-core 4.12.1 WCAG 2 A/AA 扫描 0 violations / 0 incomplete / 26 passes；删除对话框可用键盘触发及 Escape 取消。未进行人工屏幕阅读器或生产 TLS/邮件/隐私运营验收。
