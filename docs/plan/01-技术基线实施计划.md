# 技术基线实施计划

> **历史计划。** chi、pgx 和单一 `schema.sql` 等旧基线已被 OOTD 技术基线替代；当前计划见 [`10-OOTD产品实施计划.md`](10-OOTD产品实施计划.md)。

## 关联文档

- [`../design/01-技术选型.md`](../design/01-技术选型.md)
- [`../design/02-后端架构.md`](../design/02-后端架构.md)
- [`../acceptance/01-技术基线验收.md`](../acceptance/01-技术基线验收.md)
- 历史 `backend/openapi.yaml`、`backend/schema.sql` 与 migration 目录均已删除；当前见 [17-15](17-15-OpenAPI文档门户与Umi生成执行计划.md)。

## 范围

把已确认的技术选型转化为可构建、可测试的 iOS 与 Go 最小工程。此计划不实现记账、日历或出行业务，也不提前选择云厂商；国内地图与云 OCR 已明确不进入当前实现。

## 里程碑

1. 固定工具链、依赖和架构边界。
2. 创建可构建的 iOS 原生工程和可启动的单进程 Go 服务。
3. 打通 OpenAPI 生成、Swagger、PostgreSQL schema 和 contract test。
4. 对日历、定位、通知、导航和 OCR 做真机技术验证。
5. 在 P0 PRD 与领域模型批准后进入业务开发。

## 任务

| 任务 | 状态 | 依赖 | 验证方式 |
| --- | --- | --- | --- |
| 记录固定技术版本、排除项和待业务决策 | completed | 无 | `docs/design/01-技术选型.md` 评审 |
| 写入版本文件与 `scripts/verify-toolchain.sh` | completed | 技术基线 | 脚本精确检查 Xcode、Swift、Go 和 Go module |
| 写入 OpenAPI 版本并验证 Swift 生成 | completed | 技术基线 | OpenAPI 解析与 Generator 1.13.0 实际生成 |
| 创建 `ThenApp.xcodeproj`，最低 iOS 18，启用 Swift 6 严格并发 | completed | Apple Team 可后补 | build settings、模拟器 clean build/test 已通过 |
| Exact 添加 GRDB 与三项 Swift OpenAPI Package，提交 `Package.resolved` | completed | Xcode 工程 | Package 解析、clean build、生成 Client/Types 编译通过 |
| 建立 App/Core/Features/Services/Data 的最小真实代码边界 | in_progress | Xcode 工程 | App/Core/Features/Data 已落地；Services 随首个系统能力 POC 建立 |
| 创建唯一 `backend/main.go` 和分层最小服务 | pending | `backend/go.mod` | `go test ./...`、启动/优雅退出测试 |
| 固定 chi、pgx、kin-openapi、OTel 依赖并生成 `go.sum` | pending | Go 最小服务 | `go mod tidy`、`go mod verify`、`govulncheck` |
| 嵌入 OpenAPI 与 Swagger UI 固定资产 | pending | Go HTTP 服务 | 开发环境访问 `/swagger/` 与契约 hash 对比 |
| 配置 Spectral、oasdiff、Swift 生成与 Go contract test | pending | CI 方案 | CI 从干净 checkout 全部通过；覆盖关键 OpenAPI 3.1 语义 |
| 用 `postgres:18.4` 验证空库 schema 初始化 | pending | 初始领域数据模型 | Testcontainers 集成测试、schema inspect |
| 用 Atlas Community 演练已有库 `public` schema dry-run 和人工确认流程 | pending | 非空专用测试库 | 保存 diff、非目标 schema 隔离、DROP 阻断与回滚证据 |
| EventKit 全量窗口、变更通知与权限撤回 POC | pending | Xcode 工程、真机 | 真机验收记录 |
| Core Location 主动行程与后台活动 POC | pending | Xcode 工程、真机 | 锁屏、切后台、杀进程与耗电证据 |
| UserNotifications 本地出发提醒 POC | pending | Xcode 工程、真机 | 锁屏、时区改变、权限拒绝验收 |
| MapKit 路线、Apple 地图调起和手动降级 POC | pending | Xcode 工程、真机 | 正常路线、无路线、离线、打开失败和复制地址 |
| Vision/VisionKit 中文票据 OCR 基准 | pending | 脱敏或合成样本集 | 字段准确率、低置信度和手输兜底报告 |
| 确认账号、云厂商和观测供应商 | pending | 运营主体、成本、PRD、合规评估 | 独立设计决策与合同/配额核对 |

## 实施顺序

先创建最小 iOS 和 Go 工程，再打通契约与数据库，最后做系统能力 POC。业务 Feature 只能依赖已验证的 Service/Repository 协议，不应在 POC 前直接绑定第三方 SDK。

Go 依赖只在出现真实 import 时写入 `go.mod`；不得为了“看起来完整”提交未使用依赖。Swift 依赖则在 Xcode 工程创建时以 Exact requirement 落地，由 `Package.resolved` 固定解析结果。

## 风险与缓解措施

- Xcode 26.6 CI 需要 macOS Tahoe 26.2 或更高版本：创建 CI 前确认 runner 镜像，不使用 beta Xcode。
- iOS 18 的位置、日历和通知行为无法只靠模拟器证明：每个 POC 保留真机系统版本、设备与证据。
- 单一 `schema.sql` 不能表达所有复杂线上数据转换：P0 限制 Atlas Community 管理的对象范围，首次复杂变更前重新评审。
- MapKit 可能无法返回部分路线：保留手动出发时间、复制地址和返回计划，不以覆盖率或第三方地图选择阻塞发布。
- 中文票据 OCR 准确率未知：建立脱敏基准集，所有结果必须人工确认。

## 发布与回滚

- 技术基线文档与版本文件可以通过 Git 回滚；依赖升级必须同时回滚锁文件。
- iOS 工程首次发布前可以调整本地 schema；一旦有用户数据，只能通过 GRDB 有序 migration 演进。
- PostgreSQL 空库可重建；非空环境变更前必须有备份/PITR、dry-run、数据转换、验收与回滚计划。
- 云厂商未批准前，不发布依赖其密钥、SDK 或隐私清单的构建；当前构建不得包含国内地图或云 OCR SDK。
