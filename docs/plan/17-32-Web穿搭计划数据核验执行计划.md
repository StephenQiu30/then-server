# 17-32 Web 穿搭计划数据核验执行计划

## 状态与范围

- 契约状态：`approved`；执行状态：`local_development_complete`（2026-09-23；公开发布门禁未关闭）。
- 依据用户 2026-09-23 明确的 `then-server` 后端与 Web 页面先行范围。
- 需求：[PRD 16](../prd/16-穿搭记录与反馈需求.md) `OUTFIT-REQ-010`～`012`、`OUTFIT-RULE-001/011`；设计：[Design 26](../design/26-Web穿搭计划数据核验设计.md)；验收：[Acceptance 16](../acceptance/16-穿搭记录与反馈验收.md)。

本片消费已有 17-22/17-23 API，以 Web 证明账号计划事实；不改变后端合同，不实现 App、实际穿着、反馈或离线能力。

| ID | 状态 | 完成条件 |
| --- | --- | --- |
| `17-32-DOC-01` | `completed` | PRD/Design/Plan/Backlog 固定 Web 范围、事实边界和失败行为。 |
| `17-32-UI-01` | `completed` | `/plans` 创建/列表/分页/编辑、取消、未穿/恢复、删除和不可穿确认，历史清除占位可读。 |
| `17-32-TEST-01` | `completed` | 运行时 OpenAPI 再生成无差异，纯映射测试及 lint/typecheck/format/build/生产依赖审计通过。 |
| `17-32-BROWSER-01` | `completed` | 隔离 PG/Redis/API/Next 覆盖创建不产实际、两账号、409、状态与删除、401/网络恢复、移动与键盘对话框。 |
| `17-32-REL-01` | `pending` | 本地证据已回写并独立提交；公开入口须另行通过账号发布门禁。 |

失败不覆盖历史快照；计划是意图，只有后续独立的主动实际事件才增加穿着事实。回滚撤销 Web 入口，不删除现有计划 API 或测试数据。

## 本地开发证据（2026-09-23）

- 环境：隔离 Docker Compose 项目 `then-plan-1732` 的 PostgreSQL/Redis，Go API `127.0.0.1:18080`，Next 开发及生产构建 `127.0.0.1:3114`；两组合成账号，测试结束销毁隔离数据。
- 契约与质量：运行中 `/openapi.json` 重生成 `frontend/src/api` 无差异；lint/typecheck/format、11 项 Node Test、生产 build 与 `npm audit --omit=dev --audit-level=high`（0）通过。初次浏览器发现同名 `/outfit-plans` 页面遮住 API 列表，页面改为 `/plans` 后实际 API 返回 JSON、生产构建复核通过。
- 浏览器：A 选可穿/待洗衣物，未逐件确认时阻止保存，确认后创建且 `/wear-events` 仍为空；外部更新造成旧 revision 保存 409，草稿保留，重新加载后编辑成功。未穿/恢复、取消和永久删除状态及后端事实相符。衣物删除选择清除快照后计划仍在，`content=null` 显示占位并禁止直接编辑。B 列表为空，读 A 计划 404；网络中断可重试，清除 Cookie 后跳登录。21 条列表按 20+1 分页加载。
- 页面：320px 无横向溢出，390/1280px 截图人工检查；axe-core 4.12.1 WCAG 2 A/AA 0 violations / 0 incomplete，开发页 24 passes、生产页 23 passes。确认对话框可操作；人工屏幕阅读器、生产 TLS/邮件/隐私与 App 真机不在此证据范围。
