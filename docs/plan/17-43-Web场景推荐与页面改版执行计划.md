# Web 场景推荐与页面改版执行计划

状态：approved / local_complete，release_pending。2026-09-26 用户选定 Product Design 方案 3，并明确要求接入真实衣橱推荐。依据 Design 06、PRD 13 `RECOMMEND-WEB-001`～`003`；本计划只覆盖 Web 与其必须的本人推荐 HTTP 读路径，不改变 iOS 本地引擎。

## 文件职责

- `backend/internal/application/wardrobe/recommendation.go`：输入校验、真实衣橱候选与稳定理由；不包含 HTTP 或数据库实现。
- `backend/internal/adapter/httpapi/wardrobe_{contract,routes,handlers}.go`：受保护 operation、请求/响应合同与错误映射；沿用当前 wardrobe service 和会话。
- `frontend/src/components/layout/`、`src/app/`、`src/components/recommendation/`：共享导航、场景首页、真实推荐结果与现有业务页版式；只消费生成客户端，不在页面内镜像推荐规则。
- `frontend/public/images/`：方案 3 的独立原创示例影像；不得展示为本人衣物。

## Checklist

- [x] 核对现有设计、需求、Web 页面、衣橱合同和所选视觉稿，固定真实/示例边界。
- [x] 服务端推荐规则、认证 HTTP 合同、无解/未知/状态和 owner 测试。
- [x] 从实际运行的 OpenAPI 生成 Web 客户端，完成场景选择、结果和主动保存路径。
- [x] 全局导航与首页/账户/衣橱/计划/实际页视觉统一；响应式与可访问性检查。
- [x] 前后端静态/单测、真实依赖和浏览器关键旅程；回写 Acceptance 13 与项目根目录 `design-qa.md`。

本地证据见 [Acceptance 13 的 Web 切片](../acceptance/13-穿搭推荐验收.md#2026-09-26-web-场景推荐与真实衣橱切片) 和 [设计 QA](../../design-qa.md)。远端 CI、正式账号与公开发布保持待验；本计划不认领 iOS 13-02 的锁件换件、离线推荐或发布性能门禁。

验收时分别说明功能单测、真实 PostgreSQL/进程、浏览器、远端 CI 与产品发布结果；示例照片和 HTTP 200 不抵扣推荐可用性。
