# 后端服务启动与健康契约执行计划

> 本文保留后端启动、故障与历史 Swift 生成验收过程。2026-09-14 最新接口和 schema 规则已由 [17-15](17-15-OpenAPI文档门户与Umi生成执行计划.md) 替代：Huma 运行时生成 OpenAPI，GORM 集中 AutoMigrate，App 不再保留 ThenTransport 或跨仓库契约输入。

## 2026-09-14测试空跑门禁修复

沿用本切片已批准的 iOS 验证入口与失败传播范围，修复 `xcodebuild test` 实际运行 0 项仍退出 0 的证据漏洞；不改变产品、API、数据库、依赖或测试筛选范围。

- `scripts/validate-ios.sh`：test 模式在忽略的 `DerivedData` 下创建独立结果目录，以显式 `-resultBundlePath` 保存本次结果；Xcode 非零退出保持原码并停止，不重跑。成功退出后必须检查本次结果包。build 模式不变。
- `scripts/validate-ios-test-result.py`：只读指定结果包，调用当前锁定 Xcode 的 `xcresulttool get test-results summary`；使用顶层注册测试计数，不累计设备或参数化运行次数。要求结构有效、至少一个通过、结果为 Passed、全部注册测试通过且失败/跳过/预期失败均为 0；其他情况明确非零退出，平台跳过表示验收不完整。该检查不证明测试选择覆盖完整产品。
- `scripts/tests/validate_ios_test.py`：用隔离命令替身复现空跑、错误摘要、工具失败、部分验收和成功路径，保留现有失败传播断言；另只读核验既有真实空跑/成功/失败结果包，不将脚本单测称为新产品回归。
- 回滚只涉及以上脚本及文档；无用户数据、migration 或运行时媒体变化。结果包留在忽略目录，不提交。

| 任务 | 状态 | 完成条件 |
| --- | --- | --- |
| GATE-01 | completed | 旧脚本空跑的两组复现失败；修复后 13 项门禁测试及全部 28 项脚本测试通过，真实空跑/成功/失败结果分别退出 1/0/1；见 acceptance 17 的 2026-09-14 检查点。只完成验证基础设施，不关闭产品或发布门禁 |

## 2026-09-12编译阻断复核

用户随后要求修复审核问题。本轮允许补齐真实 iOS 验证入口及其失败传播测试；单模块兼容性已完成隔离调查，独立 transport 模块方案见 [Design 01](../design/01-技术选型.md#2026-09-12生成client编译兼容评估)，仍为 draft，当前不修改 target/生成器/依赖。根规范单模块例外待用户确认，DOC-01 保持 blocked；不能将临时独立编译通过改记成 App 编译通过。

本轮逐文件契约：`scripts/validate-ios.sh` 接收 build/test 模式，调用现有工具链/架构检查和 xcodebuild，失败即停止并保留退出码；test destination 必须显式提供，不修改源码/数据。`scripts/tests/validate_ios_test.py` 在临时隔离目录用替身命令验证失败传播和执行顺序，不伪造真实产品测试结果；不调用真实业务数据或网络。脚本回滚只需移除新增入口及文档引用，不涉及 migration。

角色实现准备审核使用当前固定工具链重新执行 App Debug Simulator build，exit 65，生成 Client 的 MainActor converter/Decodable 错误仍存在。命令与脱敏错误摘要见 [THEN-3D-AUD-20260912-E03](../acceptance/10-OOTD产品系统验收.md#本轮验证证据)。本次仅增加复现证据，不修改本切片契约、代码或任务状态；DOC-01/REL-01 与整体执行继续 blocked，Go/容器历史结果不因此被撤销或补记成新验证。

## 状态

- 契约状态：`approved`。2026-09-08 用户明确要求“先提交 main 并推送，然后开始后端实现”；本切片落实已确认架构的首个可独立验证运行基线，范围如下，不扩大为云业务上线。
- 执行状态：`completed`（历史切片）。后端运行基线随 `0d5c8a7` 推送；当时的 ThenTransport 验证随 App `68e12ac` 交付并通过后续远程 CI。2026-09-14 的 [17-15](17-15-OpenAPI文档门户与Umi生成执行计划.md) 已用 Huma 运行时 OpenAPI 与 GORM AutoMigrate 替代物化契约和 ThenTransport，因此本状态只关闭原切片交付，不恢复失效架构。
- 既有交付：设计基线已提交并推送 main，`6f0513b51134f21950fe69987bab4ea43a559812`。

## 追踪关系

- PRD：[云端生成与任务管理](../prd/17-云端生成与任务管理需求.md)，承接 CLOUD-REQ-001/006 的本地独立与分阶段运行，以及 CLOUD-REQ-002 的契约基础，不关闭完整账号/任务需求。
- design：[技术选型](../design/01-技术选型.md)、[后端架构](../design/02-后端架构.md)、[服务端任务设计](../design/10-OOTD服务端与异步任务设计.md)。
- 产品计划：[产品实施计划](10-OOTD产品实施计划.md)；沿用已登记的 17-01 编号，本次收敛为服务运行与健康契约。认证/幂等云业务 POC 不因本切片通过而算完成。
- 验收：[云端验收](../acceptance/17-云端生成与任务管理验收.md) 的本切片运行证据；所有生产云验收仍 pending/blocked。

## 目标与非目标

交付一个能真实启动、连接 PostgreSQL 18、输出健康状态、接受取消并安全退出的 Gin API，拥有单一 OpenAPI、严格配置、请求 ID、脱敏日志、稳定错误和真实数据库故障验证。

本切片不实现 Apple 登录、用户数据、媒体上传、配额、任务、RabbitMQ/Redis consumer、对象存储、同步或三维业务。没有业务 schema，因此不创建空 migration、atlas.sum、占位 model/service/worker 包，也不声称完成 schema 兼容检查或业务鉴权。原 App 不连接健康接口，不改变离线边界。

## 用户可见与状态契约

运维健康接口与业务接口同源于 backend/openapi.yaml：

| 方法与完整路径 | operationId | 成功 | 失败与边界 |
| --- | --- | --- | --- |
| GET /v1/health/live | getLiveness | 200，status=live、request_id | 不访问数据库；数据库故障不把存活误报为死亡 |
| GET /v1/health/ready | getReadiness | 200，status=ready、request_id | 数据库探测超时/失败或进程退出时 503 NOT_READY，不暴露连接/SQL/版本细节 |

默认只监听 127.0.0.1:8080。两条只读健康路由无需用户身份，但部署必须限制运维访问；没有业务路由，未知路径/错误方法分别返回稳定 404/405 JSON，不重定向到其它路径。X-Request-ID 由服务端生成，不信任请求中的 ID；响应和日志一致。Cache-Control: no-store。

配置错误、数据库首次连接失败、PostgreSQL major 非 18、监听失败均非零退出，不进入假就绪。运行中数据库失联只使 readiness 失败，恢复连接后可再次就绪；SIGTERM/SIGINT 停止就绪和新接纳，在截止时间内清理 HTTP 与连接池。

## 配置与资源边界

| 配置 | 默认 / 校验 |
| --- | --- |
| APP_ROLE | api；worker/all 已保留为目标角色，但当前明确报未实现并退出，禁止空 worker 报成功 |
| HTTP_ADDR | 127.0.0.1:8080；必须是合法 IP:port，测试可显式使用端口 0 |
| DATABASE_URL | 必填 PostgreSQL URL；远端要求 verify-full，仅 loopback 允许 disable；连接选项白名单与重复项校验，不打印值；本地样例仅占位凭据 |
| DB_MAX_OPEN_CONNS / DB_MAX_IDLE_CONNS | 10 / 2；open 为 1–100，idle 为 0–100 且不大于 open |
| DB_CONN_MAX_LIFETIME | 30m，必须正数且有界 |
| STARTUP_TIMEOUT | 5s，必须正数且有界 |
| HEALTH_TIMEOUT | 1s，必须正数且小于 HTTP 写超时 |
| SHUTDOWN_TIMEOUT | 10s，必须正数且有界 |

HTTP 固定 ReadHeaderTimeout=5s、ReadTimeout=5s、WriteTimeout=10s、IdleTimeout=60s、MaxHeaderBytes=16KiB；健康接口不接受 body/query，输入不满足时返回 400。所有 I/O 传递 context，无请求后后台任务。配置解析接收 getter 便于隔离测试，错误只带字段名和原因，不带原始值。

## 领域、数据、API 与系统边界

- 一个 main.go 组装 config、database pool、Gin router 与 HTTP 生命周期；组件显式注入，不建立全局服务容器。
- GORM PostgreSQL driver 管理连接，禁止 AutoMigrate；只执行连接/版本检查和 Ping，不读取用户表。日志关闭 GORM 默认 SQL/错误正文输出。
- transport 只依赖带 context 的数据库探测协议；数据库实现不 import Gin。健康 DTO 是 transport 纯 struct。
- OpenAPI 仍为 3.1.2，servers=/v1，paths=/health/live 与 /health/ready；先校验并构建 Swift Client，再实现 Handler 和响应契约测试。
- Gin/GORM/driver/kin-openapi/Testcontainers 按 Design 01 精确锁定；本切片没有实际 trace exporter 或云指标，OpenTelemetry 的运行接入留给云接纳前的运维切片，不伪报已启用。

## 权限、隐私与安全

仅使用随机生成的隔离测试数据库和合成凭据，不读取生产配置。默认日志记录服务端 request_id、路由模板、状态、耗时；未知路径统一 unmatched，不记录 URL/query、请求体、令牌、数据库连接串或 panic 内容。未知路由不产生账户或任务。数据库集成测试的容器/网络必须由测试生命周期清理。

## 兼容、迁移与回滚

不改业务表、本地 GRDB、历史数据或 iOS 产品页面；2026-09-13 获准将生成器 accessModifier 改为 public，并转移 target membership；新接口为运维增量。Swift 生成产物仍在 DerivedData。回滚代码/停止本地进程即可，数据库无业务 DDL 需要回滚。后续第一份业务 migration 必须独立验证 Atlas/旧 schema，不能把本次 Ping 视为 migration 验收。

## 实现任务与证据

| ID | 任务 | 状态 | 依赖 | 完成证据 |
| --- | --- | --- | --- | --- |
| 17-01-DOC-01 | 固定本切片、OpenAPI 健康契约并编译 Swift Client | completed | 2026-09-13 用户已确认 ThenTransport 技术模块例外；按下述实施契约落地 | THEN-TRANSPORT-20260913-01：真实 Debug/Release、build-for-testing、48 项 Swift Testing 与 2 项 UI 回归通过；插件/显式生成一致，18 项脚本测试通过 |
| 17-01-BE-01 | 实现配置、数据库、Gin 路由和优雅退出 | completed | 已定义的健康契约；平台代码可独立于 Swift 构建继续，最终交付仍要求 DOC-01 关闭 | Go 单元/契约、race、vet 及真实 PostgreSQL 18.4 验证通过 |
| 17-01-TEST-01 | 单元、契约、真实 PostgreSQL 断连恢复、进程退出验证 | completed | BE-01 | 2026-09-08：真实容器 pause/unpause 验证 200→503→200，live 始终 200；实际编译进程 SIGTERM 成功退出 |
| 17-01-BE-02 | 补齐监听失败资源清理及实际进程启动失败验证 | completed | 已有运行与退出契约；不新增 API 或业务 schema | BE-RUNTIME-20260908-02：先复现失败，修复后单元/race/vet/真实 PostgreSQL 集成通过；5 项实际进程启动失败验证通过 |
| 17-01-TEST-02 | 统一 iOS 构建/测试入口与失败传播验证 | completed | 用户 2026-09-12 要求修复；采用 Design 01 验证流程，不改变 App 架构 | THEN-IOS-GATE-20260912-01：6 项替身测试、shell 语法检查通过；真实 App 失败 65 被正确传播，未误报构建通过 |
| 17-01-REL-01 | 同步文档、核对改动与提交交付 | completed | TEST-01、DOC-01 | 后端运行基线 `0d5c8a7`、App 历史实现 `68e12ac` 均已进入各自 main；当前服务端 main `e43cc9a` 的 Actions `34877794928` 三个 job 全部成功。ThenTransport 后续已由 17-15 成组移除 |

## 完成判定

工具链校验、gofmt、go vet、go test/race、OpenAPI 请求/响应测试、Swift 生成 Client 构建均通过；真实 PostgreSQL 18 验证 ready → 503 → ready，liveness 不受数据库故障影响；配置/启动错误非零退出且日志不泄露秘密；进程信号能在截止内退出。记录实际证据后逐项更新状态，不把运行基线称为完整后端或生产云发布。

## 目录调整

2026-09-08 用户要求后端 `backend/`、iOS `app/`，已将原 `ios/` 整体移动到 `app/` 并同步引用。保持 ThenApp target/module 名称和产品代码行为；不额外新建 ThenAPI 模块。当前生成代码编译阻断如实保留，平台部分独立验证。

## 2026-09-08 编码前规范复核

用户进一步要求先规划后端目录职责、规范、SOP 与选型。本轮只完善 Design 01/02 和入口文档，保留既有代码与测试证据；未修改运行代码、OpenAPI、数据库或 app 并发设置。目录及共同流程见 [目录职责](../design/02-后端架构.md#目录)和[开发交付 SOP](../design/02-后端架构.md#开发sop)。

已有运行代码符合当前目录职责：main 组装，transport 承担健康 HTTP，platform 分别拥有配置、连接及退出；无业务用例时不增加空 Service/Repository。后续恢复实现先按 SOP 复核未完成 DOC-01/REL-01：Swift Client 编译阻断尚未解决，本轮文档完善不改变其 blocked 结论，也不补记未经执行的测试通过。首个业务切片再增加真实分层检查、数据迁移及权限验证。

## 逐文件职责复核入口

当前文件逐一对应 [Design 02 现有文件实现契约](../design/02-后端架构.md#目录)。后续修改须在本计划影响面/任务中按[编码前检查卡](../design/02-后端架构.md#plan与checklist)明确功能来源、路径、输入输出、所有权、失败语义与证据。后续目录的功能映射不是本切片的扩容清单，不创建身份、媒体或异步占位文件。当前任务状态不变。

## 本轮实现影响面

继续落实已批准的启动/退出与脱敏契约，不变更业务行为或技术选型：

- `internal/platform/httpserver/server.go`：监听循环异常返回时关闭已接纳连接，使请求 context 取消，不让进程装配函数先关闭 DB 而 HTTP 仍活动。
- `internal/platform/httpserver/server_test.go`：真实连接触发监听异常，验证 drain、请求取消及有界退出；先观察失败再修复。
- `tests/runtime_integration_test.go`：复用固定 PostgreSQL 与实际 binary，验证非法角色、无配置、错误凭据、监听占用等失败非零退出；输出不得泄露随机密码或错误响应正文。
- 资源始终由测试/Serve 所有者关闭；API、schema、app 和依赖版本不变。失败测试通过后记录证据，Swift 构建阻断仍独立保留。


## 2026-09-13ThenTransport实施契约

用户已确认 Design 01 中的具体模块例外；本次在原 17-01 内执行，不新建切片或兼容层。上方 2026-09-12 的 draft/blocked 描述保留为历史检查点；当前状态以本节与任务表为准。

- 目标与时间盒：90 分钟内完成最小 target 调整、相关根因回归及 App Debug/Release 实际编译；失败先定位并记录，不降低 Swift 并发设置或伪造整体通过。
- 文件与职责：project.pbxproj 新增 ThenTransport 静态 framework，唯一 Sources 为原 openapi.yaml/config 两份输入；将 OpenAPIGenerator dependency 和 OpenAPIRuntime 链接移至该 target。ThenApp 链接 ThenTransport，保留业务/SwiftUI/GRDB；ThenAppTests 显式链接 ThenTransport、OpenAPIRuntime 与已锁定的传递依赖 HTTPTypes，以验证静态框架的公开 API；OpenAPIURLSession 保留为客户端装配用既定依赖，不新增框架版本或手写生产代码。
- 唯一契约：该历史切片完成后，iOS 已迁移至 `then-app/ThenApp/openapi.yaml`；符号链接继续读取 `then-server/backend/openapi.yaml`，不复制契约。生成访问级别为 public，生成文件只在 DerivedData；配置归属只有一个 target，实际 build 必须证明 ThenApp 不再重复编译 Client。
- 并发：ThenTransport 使用 Swift 6、Complete Strict Concurrency 和默认 nonisolated；ThenApp/UI 原 MainActor + Approachable Concurrency 保留。不得手改/注释注入生成代码、使用 unchecked Sendable 或关闭 App 检查。
- 验证：增强架构守卫，验证精确 target、插件/YAML 唯一归属、App→Transport 链接及隔离设置；建立正反例防止回退。ThenAppTests 从外部模块导入生成 Client，以合成 transport 验证成功/错误解码、请求路径及非 MainActor 调用，不新增产品网络请求。实际项目运行相关单元测试和编译 UI 测试 target；在本次新建的干净模拟器上执行，禁止接触既有用户 App/数据库。历史测试只算回归，不抵扣 OOTD 旅程验收。
- 影响面：无 OpenAPI/schema/migration、后端业务、照片管线或正式 UI 行为变化；根规范、Design 01、PRD 17、计划与 acceptance 同步记录确认和实际结果。
- 回滚：恢复原 target membership/config 并移除 ThenTransport/link/dependency；不会迁移或删除用户数据，原生成 Client 编译错误会恢复，不能把回滚状态记为绿色基线。

## 2026-09-13实施结果

DOC-01 completed，18 项脚本检查、Debug/Release 实际 App 构建、完整测试 target 构建，以及 48 项 Swift Testing + 2 项现有 UI 回归通过；从启动实施到完成当前验证在 90 分钟检查点内。测试暴露的静态依赖缺失已通过显式测试链接 OpenAPIRuntime/HTTPTypes 修复并加反例门禁。文件哈希、命令、失败重跑、警告与验收边界见 [THEN-TRANSPORT-20260913-01](../acceptance/17-云端生成与任务管理验收.md#2026-09-13生成client正式工程验收)。该实现已提交交付，随后由 17-15 的当前运行时契约方案替代；本地 3D 和云业务发布不因本历史切片完成而完成。
