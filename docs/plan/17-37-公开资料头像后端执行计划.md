# 公开资料头像后端执行计划

## 状态与范围

- 对应 `BL-ACCOUNT-05`；本地合成数据技术合同 `approved`，后端实现 `completed`。只修改 `then-server/backend` 与关联 Design、PRD、Acceptance；Web/App 展示、真实用户上传与生产发布另验。
- 复用 17-19 的私有 JPEG 上传、固定对象版本、后台解码重编码、媒体删除任务和 19-02 的公开资料。头像是独立 `profile_avatar` / `ordinary_image` 用途；人物采集原图 `avatar_source_preparation` 永远不可绑定或公开。

## MVP 合同

1. `POST /media/uploads` 接受 `profile_avatar`，不要求人物照片同意；仍只接 JPEG、最多 12 MiB 和 SHA-256，上传的原始对象保持私有。后台检查通过后只公开重新编码且已清除元数据的固定衍生版本。无自动裁剪、识别、年龄推断或第三方处理。
2. `PUT /users/me/profile/avatar` 以 `{media_id,expected_revision}` 把本人 `ready`、用途正确的媒体绑定到已有资料，成功递增资料 revision；`DELETE /users/me/profile/avatar?expected_revision=` 撤下当前头像并递增 revision。无资料时先按 19-02 创建 handle。错 owner/用途/状态统一拒绝，旧 revision 返回冲突；重复提交须重新读取当前 revision。
3. 公开资料只返回同源 `avatar_url`（有头像时）或 `null`，不返回媒体 ID、对象 key、版本号或原图地址。`GET /profiles/{handle}/avatar` 匿名读取当前 active 资料绑定的净化 JPEG 固定版本；无头像/资料、封禁/删号或媒体不再 ready 返回 404。响应 `no-store`、`nosniff`，读取对象有 12 MiB 上限。
4. 替换和撤下资料绑定时，同一事务先撤销旧头像公开读取，再把旧媒体交给既有异步删除闭环；新媒体绑定与旧媒体撤销具有同一提交边界。单独 `DELETE /media/{id}` 若是当前头像，也必须同事务撤下绑定并递增资料 revision；删号沿 19-06 关闭访问、清理全部媒体。对象存储故障不能把原图降级为公开内容。
5. 实际数据库以 GORM record/`AutoMigrate` 为准，新增可空 `user_profiles.avatar_media_id`，用媒体 ID 外键和事务检查 owner/用途/ready。本人结构化导出含头像关联，媒体档含仍可取的头像原图；公开资料始终不回显媒体 ID。运行时 Huma OpenAPI 是唯一接口 schema；不另建图片服务、CDN 或头像表。开发库若保留旧媒体用途 CHECK，按项目无历史数据约定重建本地库；不把 AutoMigrate 当生产历史迁移。

## Checklist

| ID | 工作 | 状态 | 退出证据 |
| --- | --- | --- | --- |
| 17-37-CONTRACT-01 | 固定头像用途、公开读取与替换/删除边界 | completed | 本 Plan、Design 21/22/23、Backlog 与 Acceptance 对齐 |
| 17-37-MEDIA-01 | 复用上传/净化并为头像增加独立用途 | completed（本地） | 普通图片不借人物同意；JPEG worker 元数据重编码与用途隔离通过 |
| 17-37-DATA-01 | 资料绑定及旧媒体撤销事务 | completed（本地） | 真实 PG 验证 owner、用途、revision、单图删除、替换和删号最终物理删除 |
| 17-37-HTTP-01 | 本人绑定/撤下和匿名固定版本读取 | completed（本地） | Huma API 0.23.0、新增三个 operation；真实 PG/MinIO 路由、对象覆写与旧图撤下通过 |
| 17-37-TEST-01 | Go、真实依赖、实际进程与 OCI 回归 | completed（本地） | 静态检查、全量 Go/服务/进程、OCI 镜像与受限运行测试通过 |
| 17-37-REL-01 | 真实用户与公开发布 | pending | 受控素材、部署/保留、客户端展示和人工可访问性验收；不由合成测试抵扣 |

完成本地后端范围后先回写本 Plan 与 Acceptance，再标记 `BL-ACCOUNT-05` 的后端进度；客户端未实现时全项保持未勾选。每次完成的实施切片验证后单独提交，保持工作区干净。

## 本地实施证据（2026-09-23）

`GIN_MODE=release go test ./... -count=1`、`GIN_MODE=release go test -race ./... -count=1`、`GIN_MODE=release go test -race -tags=services ./tests/... -count=1`、`GIN_MODE=release go test -race -tags=integration ./tests/... -count=1` 均通过；最后一次代码调整的头像专项服务竞态测试也通过。镜像 `then-backend:17-37` 的 ID 为 `sha256:582306342719f26390958b1beb18c5f729fd7f89d9449f278dfdc160ef1c975e`，`THEN_BACKEND_TEST_IMAGE=then-backend:17-37 GIN_MODE=release go test -race -tags=container ./tests/... -count=1` 通过。`go mod verify`、`go vet ./...`、`git diff --check` 和 `gofmt -l .` 无异常。服务用例使用隔离 PostgreSQL、MinIO 与合成 JPEG，覆盖两账号、错误用途、revision、匿名固定版本读取、同 key 覆写、替换、手动删图、删号关闭访问与最终物理删除；元数据净化由 worker 单测验证。没有 Web/App 页面、真实头像素材、远端 CI 或生产迁移证据。

## 依据

- [Design 21](../design/21-后端产品边界与领域设计.md)、[Design 22](../design/22-后端数据库设计.md)、[Design 23](../design/23-后端接口设计.md)、[PRD 17](../prd/17-云端生成与任务管理需求.md)、[Acceptance 17](../acceptance/17-云端生成与任务管理验收.md)、[17-19](17-19-本人照片私有上传与删除闭环执行计划.md)、[19-02](19-02-账号状态与公开资料执行计划.md)。
