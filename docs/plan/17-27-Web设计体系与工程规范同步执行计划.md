# Web 设计体系与工程规范同步

## 状态与范围

- 2026-09-22 用户要求先推送现有代码，再同步前端必要的规范设计与文件；Kafka 基线 `cd41e5977864e36368089af64dbbde9d19158f63` 已推送 main。
- 契约 `approved`；实施 `completed（实现、本地与远端 CI）`。落实 PROJECT 的既定 shadcn/ui + Radix + Tailwind v4 基线。
- 本片维护现有首页与通用路由状态，不新增注册/登录/账户业务，不更改 Go API、生成 DTO 或同源转发合同。

## 实施合同

1. 用 npm shadcn CLI 显式初始化 radix，固定 components.json、RSC、别名与 Lucide；只添加实际首页/路由状态需要的 Button、Badge，不建立空业务目录或第二套主题。
2. globals.css 是唯一语义 token 映射，采用 DESIGN 的系统字体、白色画布、蓝色行动色、无装饰渐变/阴影；补齐必需错误色与 Web 基础状态映射，并将共享 DESIGN 原文同步到 then-app。
3. 移除 Radix Themes 的调用、Provider、CSS 与依赖。QueryClient Provider 保持最小 Client 边界，layout/page 默认 Server Component。
4. 首页只表达产品及准备状态，不展示框架/SDK 等实现信息；通用 404 可回首页，异常边界可重试且不泄露内部错误。交互最小 44px、可见焦点，320px 到桌面及放大文字不横向溢出。
5. 必要配置、前端 README/AGENTS、PROJECT、Design/Plan/Acceptance 同步实际状态；ESLint 约束旧组件体系和 UI/请求层边界，CI 增加 npm 锁文件安装与前端现有质量命令。

## Checklist

- [x] 原工作区 commit/push 与远端 SHA 对账。
- [x] 前端实现与 App 共享设计分别推送 main，两份 DESIGN 内容一致。
- [x] 核验本片远端 CI 并同步最终验收状态。
- [x] 设计与基础组件/token/Provider 同步。
- [x] 必要路由状态、目录与配置同步。
- [x] 文档与 CI 同步；保留账户业务 pending。
- [x] test/lint/typecheck/format/build/生产依赖审计。
- [x] 真实浏览器响应式、键盘导航、404 返回与错误恢复验收。

## 证据与边界

结果归 [Acceptance 17](../acceptance/17-云端生成与任务管理验收.md)。无 API 改动不重新生成客户端。前端实现已推送并通过独立 Frontend quality 与既有后端 CI；App 只同步共享设计文件，不认领 App 新 UI 或设备验证。

官方依据：[shadcn Button](https://ui.shadcn.com/docs/components/radix/button)、[Badge](https://ui.shadcn.com/docs/components/radix/badge)、[主题](https://ui.shadcn.com/docs/theming)、[Radix Composition](https://www.radix-ui.com/primitives/docs/guides/composition)，及已安装 Next.js 16.3.5 的 Project Structure/CSS/Server and Client Components/Error Handling 文档。Vercel Next.js 技能用于框架边界，不改变部署平台。

2026-09-22 本地全部门禁通过，含 13 个视口、200% 文字、键盘导航、404 返回和真实 Next 错误重试。完整证据见 Acceptance 17 的 17-27；最终构建不包含临时故障夹具。前端实现 `06c4f3aa582287e3fabbad14cd64dbb766ed5638` 与 App 设计 `7f10376b83b9ad290e91a7704149cd0fd105440f` 已分别推送 main；[Server CI 35687994743](https://github.com/StephenQiu30/then-server/actions/runs/35687994743) 的 Frontend quality、Go quality、PostgreSQL integration、OCI container 全部通过。App 的既有构建和测试归 [独立 CI 35687995316](https://github.com/StephenQiu30/then-app/actions/runs/35687995316)，不替代新功能或真机验收。
