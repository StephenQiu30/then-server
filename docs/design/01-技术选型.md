# 技术选型

更新：2026-09-22（本次固定 Kafka、根目录入口与 Web 组件体系）。本文是技术栈与启用边界的唯一事实源；工程规则见 [PROJECT.md](../../PROJECT.md)，实现进度见 [产品计划](../plan/10-OOTD产品实施计划.md)。

## 人物技术冻结与变更规则

决策 ID 保留 `AVATAR-BASELINE-01`，现行修订日期为 **2026-09-22**。用户已要求根据本轮调研全面替换设计；本节及 [Design 20](20-OOTD完整产品能力与阶段架构设计.md) 的低成本路线现为 `approved`。这项授权批准设计替换，不表示代码、生产供应商、付费调用或发布已完成。

**现行路线：二维衣橱 → 完整穿搭图 → 用户按需生成整套静态 GLB → Three.js 连续旋转展示 → 按版本缓存复用。** 内置完整 Look 在制作阶段生成并审核，随包提供图片和 GLB，使默认体验零账号、零上传、离线可用。任意新穿搭需要生成新图片；它的模型只在用户主动请求时生成，不能承诺离线任意换衣。

| 边界 | 当前决定 | 实现与验收状态 |
| --- | --- | --- |
| 产品内容 | 三套自有/授权可爱成人预设，共享页面；先验证一套代表 Look，再扩展三人物各两套完整 Look | 六套是首批内容范围，不是已交付资产；取消旧的三人物×八组合强制矩阵 |
| 页面与数据 | SwiftUI + Observation；GRDB/SQLite 保存 Look 版本和衣橱等事实，媒体存受保护文件 | 既有工程沿用，Look 持久化待实现 |
| 展示运行时 | 隔离 WKWebView + 随包 Three.js `0.185.1` + GLTFLoader，标准 GLB；现行 WebGL2 | 工程舞台已有，整套带纹理资产合同与最低真机验收待完成 |
| 资产来源 | 优先验证 Tripo H3.1 图片转 GLB；输入为已确认的完整穿搭图，standard texture、triangle，首轮约 2 万面 | 技术方向确定；真实样本质量、成本、许可与文件预算待测，不宣称供应商已准入 |
| 图片生成 | 优先验证 Tripo 托管 Seedream v5 多参考图片编辑；不合格时才对照专用 VTON | 图片与模型质量分别验收；不同时建设多供应商平台 |
| 首版人物能力 | 固定姿势、整套 Look 切换、连续旋转、有界缩放/复位；图片始终可独立保存 | 不需要骨架、绑定、眼部 morph 或服装独立网格 |
| 后续能力 | 模块化三维换装、体型编辑、眨眼/注视、骨骼动作与独立视频 | 原需求保留追踪并标为 deferred，不是现行首版前置，也不得冒称已经通过 |
| 制作限制 | 不使用 Blender 建模、修复、转换或导出；不自建 GPU 推理/建模流水线 | 继续生效；不合格产物退回来源或更换输入，不手工补模型 |
| 资源保存 | 随包默认 Look；个人生成图/GLB 存私有 MinIO，再经验证下载至受保护缓存 | 私有资产按 owner 和不可变版本隔离；缓存命中不重新生成 |

Three.js 负责加载、相机、灯光和交互；Tripo 负责生成模型。img2threejs 不是 Tripo 的别名，也不是必须经过的转换层。静态 GLB 可实现真正的连续三维观察，但不能证明背面是真实拍摄、服装可独立替换或尺码合身。[技术证据](threejs-avatar-research.md)

### 变更 SOP

1. 本次修订取代旧的独立三视图主线、模块衣物及眼部首版前置要求；旧验收不改成通过，退役/延期情况见 PRD 11、12 和对应 Acceptance。
2. 后续更换运行时、发布格式或扩大生成用途，先记录证据、成本、数据影响与迁移/验收，再同步 Design、PRD、Plan、Acceptance；日常修复和已授权设计整理直接推进。
3. 生产照片入口必须满足已存在的同意、处理地域、供应商许可、删除与设备要求。Tripo 内部内容制作和向终端用户提供生成服务分别核实权利，不因购买额度自动视为两者均获准。
4. 只保留一个现行技术决定；研发完成、资产通过、真机通过与生产启用分别记录。实际版本以本文件后续版本表及 lockfile 为准。

## 客户端最终决策与原生液态玻璃

SwiftUI + Observation 拥有全部产品页面、导航、业务状态与无障碍；最低 iOS 26，采用系统 Liquid Glass 控件。UIKit/WebKit 仅作受控适配。人物舞台固定本地 Three.js + 标准 GLB，由隔离 WKWebView 承载；不并行维护 Flutter、RealityKit/USDZ 或第二个 renderer。

GRDB/SQLite 保存本地业务事实，受保护文件保存媒体，保存、快照、删除与中断恢复使用明确事务。App 当前没有云请求 Client；登录和同步接入前先建立实际业务 spec，不创建空网络模块。

## 2026-09-22 工程基线变更

按用户要求，消息队列由 RabbitMQ 切换为 Kafka，唯一入口放在 `backend/main.go`，保留 bootstrap 组装职责，不再保留 `cmd/then-server` 包装目录。新配置为 `KAFKA_BROKERS` 与 `KAFKA_TOPIC_PREFIX`；实际开发、隔离测试、迁移与回退合同见 [17-26](../plan/17-26-Kafka与工程规范化执行计划.md)。历史已完成计划中的 RabbitMQ 测试只证明当时版本。

## 后端最小技术栈

Go + Gin/Huma + GORM + PostgreSQL，模块化单体、一个 Go module、一个二进制和镜像。已实现 `api|worker|all` 角色，worker/all 仍受本地合成媒体开发开关约束。目录、依赖和 SOP 见 [后端架构](02-后端架构.md)。

Huma operation 与 Go struct tag 是接口唯一声明，运行时提供 `/openapi.json`、`/openapi.yaml`；内嵌 Swagger 门户及已接入的 `@umijs/openapi` 读取同一契约。不存在仓库 OpenAPI 物化文件、额外 Swagger Docker 服务或生成脚本。

Repository GORM record 与集中 AutoMigrate 是开发 schema 的唯一声明，监听 HTTP 前迁移失败即退出。当前不保留旧业务数据，不引入 Atlas、SQL migration 账本、旧 schema 探测或双写兼容层。iOS 本地数据库的 GRDB migration 与后端 AutoMigrate 分属两个数据库。

## 数据库与中间件接入决策

| 组件 | 当前接入 | 后续启用条件 |
| --- | --- | --- |
| PostgreSQL | 账号/会话、衣橱、计划/实际穿着、媒体、私人日记与社区事实及真实集成测试 | 新业务进入批准切片才加表；后端存在不代表 App 同步已交付 |
| MinIO | 已有私有上传、日记/社区图片及异步删除开发闭环，详见 17-19、19-03～19-06 | 正式人物目录和 AI 生成结果按对应切片接入；生产媒体准入仍独立验收 |
| Redis | 账号注册/登录固定窗口与本机 TTL/失效验证；不保存业务事实 | 新用途必须进入批准切片，不能把缓存升级为事实源 |
| Kafka | 替换 RabbitMQ；媒体检查、删除、通知使用 topic + consumer group，开发范围见 17-26 | 生产需 TLS/SASL/ACL、多副本与运维门禁 |
| FFmpeg | 未接入 | 已批准视频处理任务需要时在受限 worker 内运行 |
| OpenTelemetry | 尚未接业务链路；当前 slog/健康端点 | 云业务生产接纳前落实追踪、指标与告警 |

日常使用本机已安装服务。`docker-compose.yml` 与 `docker-compose-env.yml` 用于明确需要的隔离环境，不自动启动重复中间件。模型质量 POC 不要求预建全部队列服务；被正式接纳的用户任务必须具备持久化、重试和删除闭环。

## Web frontend

2026-09-16 用户确认 Web 基础工程使用 Next.js 及其 App Router，不使用 Vite、React Router 或 Vitest。顶层 `frontend/` 以官方 `create-next-app` 配置为基准，保留 TanStack Query、Axios、ESLint、Prettier 与 `@umijs/openapi`；请求文件从运行时 OpenAPI 生成，禁止复制 DTO 和手写第二份接口清单。

2026-09-22 按用户要求确认目标设计实现体系为 **shadcn/ui + Radix Primitives + Tailwind CSS v4**，shadcn 基础类型固定 `radix`。Vercel 插件的 Next.js/React 指南用于框架和性能规范，不等于选择 Vercel 托管。组件、语义 token、目录与验收规则见 [PROJECT.md](../../PROJECT.md)。业务组件归 `src/components/<business>`，请求 hooks 归 `src/hooks/<business>`，纯逻辑归 `src/lib/<business>`，不另建平行 `features` 目录。

17-27 已初始化 shadcn 的 radix-nova 配置，实际接入 Button/Badge、统一 token 与页面状态，移除 Themes Provider/样式/依赖。注册、登录和本人账户页面继续按 [Design 16](16-Web账户管理设计.md) 与 [17-13](../plan/17-13-Web账户管理执行计划.md) 推进。

## 固定版本

下表区分代码中已锁定依赖与未来组件目标；不是全部组件已接入的声明。Web 基础工程已接入，业务页面仍按切片实施；FFmpeg、OpenTelemetry 业务埋点仍未启用，版本在对应切片开工时复核。尚未接入项在首次使用时复核对应发行版/许可与安全状态，不盲目追新。Swift Package 必须提交 `Package.resolved`，Go 依赖必须由 `go.mod` 与 `go.sum` 固定，服务镜像必须固定 tag 与发布 digest；禁止在 CI 或生产使用 `latest`。

| 层 | 技术 | 固定版本或设置 |
| --- | --- | --- |
| iOS IDE | Xcode | `26.6`，Build `17F113` |
| Swift | Xcode 内置 Swift | `6.3.3`，Swift 6 language mode |
| 最低系统 | iOS | `26.0` |
| UI | SwiftUI + Observation | 系统框架；所有产品页面固定使用 |
| 局部 Web 图形宿主 | WebKit / `WKWebView` | 系统框架；最低 iOS 26 通过 `UIViewRepresentable` 封装，只承载批准的渲染表面 |
| 三维舞台 | Three.js | `0.185.1`（r185，MIT）；固定渲染器；已随包使用，正式资产另行验收 |
| 并发 | Swift Concurrency | Complete Strict Concurrency、Main Actor 默认隔离 |
| iOS 本地数据库 | GRDB + 系统 SQLite | GRDB `7.11.1` |
| Go 工具链 | Go | language `1.26.0`，toolchain `1.26.5` |
| HTTP framework | Gin | `v1.12.0` |
| ORM | GORM v2 | `v1.31.2`，使用 Generics API |
| GORM PostgreSQL driver | `gorm.io/driver/postgres` | `v1.6.2` |
| Go UUID | `github.com/google/uuid` | `v1.6.0`；账号与会话业务 ID |
| Go 密码散列 | `golang.org/x/crypto/bcrypt` | module `v0.57.0`；bcrypt cost 12 |
| 服务端数据库 | PostgreSQL | major `18`；本地和 CI 固定 `18.4` |
| 开发 schema | GORM AutoMigrate | 监听 HTTP 前集中执行；当前无历史数据，不维护第二套迁移账本 |
| 消息队列 | Apache Kafka KRaft | `4.3.1`；本地/测试单节点，镜像固定 digest；生产集群另验 |
| Go Kafka client | `twmb/franz-go` / `pkg/kmsg` | `v1.22.0` / `v1.14.0`；acks=all、幂等生产、显式 offset 提交 |
| 缓存与协调 | Redis Open Source | `8.10.0` |
| Go Redis client | `redis/go-redis/v9` | `v9.22.0`；不启用实验性客户端缓存和自动流水线 |
| 媒体处理 | FFmpeg | `8.1.2`，固定构建与 codec 清单 |
| API 规范 | OpenAPI | `3.1.2` |
| Go API 契约生成/请求校验 | Huma / kin-openapi | `v2.39.1` / `v0.149.0`；Huma 生成并执行请求校验，kin-openapi 校验生成文档 |
| 可观测性 | OpenTelemetry Go / otelgin | `v1.46.0` / `v0.71.0` |
| Go 集成测试 | Testcontainers for Go | `v0.44.0` |
| Web UI | React / React DOM | `19.3.0` |
| Web 应用 / 路由 / 构建 | Next.js App Router | `16.3.5`，Turbopack；Node.js `24.19.0`、npm `12.0.2` |
| Web 语言 | TypeScript | `6.0.3`，strict；7.0.2 未通过当前生成器预检 |
| Web 客户端服务端状态 | TanStack Query | `5.102.8`；由 `src/providers/query-provider.tsx` 持有 QueryClient，根 layout 直接使用，不保留 AppProviders 包装层 |
| Web HTTP | Axios | `1.20.0`；唯一实例位于 lib/api/request.ts |
| Web 组件体系 | shadcn/ui + Radix Primitives | CLI `4.21.0`、`radix-ui 1.6.7`、Lucide `1.47.0`；Button/Badge 为本地源码，精确依赖与锁文件同步 |
| Web 样式 | Tailwind CSS / PostCSS | `4.3.3` / `8.5.28`；通过 `@tailwindcss/postcss` `4.3.3` 接入 Next.js |
| Web OpenAPI 生成 | `@umijs/openapi` / `tslib` | `1.14.1` / `2.8.1`；开发时读取 API 暴露的 `/openapi.json`，生成结果进入 `frontend/src/api/` |
| Web 工程质量 | ESLint / eslint-config-next / Prettier | `9.39.5` / `16.3.5` / `3.9.6` |
| Web 测试 | Node.js Test Runner | Node.js `24.19.0`；TypeScript 请求层直接测试，不引入 Vite 测试运行时 |
| CI runner | GitHub-hosted Ubuntu / macOS | 服务端 `ubuntu-24.04`；App `macos-26`，默认 Xcode `26.6` Build `17F113` |
| CI actions | `actions/checkout` / `actions/setup-go` | `d23441a48e516b6c34aea4fa41551a30e30af803`（v6）/ `924ae3a1cded613372ab5595356fb5720e22ba16`（v6），工作流固定完整 commit SHA |

版本依据以各项目官方 release 为准，包括 [Three.js r185](https://github.com/mrdoob/three.js/releases/tag/r185) 与 [npm three 0.185.1](https://registry.npmjs.org/three/0.185.1)、[Gin v1.12.0](https://github.com/gin-gonic/gin/releases/tag/v1.12.0)、[GORM v1.31.2](https://github.com/go-gorm/gorm/releases/tag/v1.31.2)、[GORM PostgreSQL driver v1.6.2](https://github.com/go-gorm/postgres/releases/tag/v1.6.2)、[PostgreSQL 18.4](https://www.postgresql.org/docs/release/18.4/)、[Kafka 官方下载](https://kafka.apache.org/downloads/)、[franz-go v1.22.0](https://github.com/twmb/franz-go/releases/tag/v1.22.0)、[Redis 8.10.0](https://github.com/redis/redis/releases/tag/8.10.0)、[go-redis v9.22.0](https://github.com/redis/go-redis/releases/tag/v9.22.0)、[kin-openapi v0.149.0](https://github.com/getkin/kin-openapi/releases/tag/v0.149.0)、[OpenTelemetry Go v1.46.0](https://github.com/open-telemetry/opentelemetry-go/releases/tag/v1.46.0)、[OpenTelemetry Go Contrib v0.71.0](https://github.com/open-telemetry/opentelemetry-go-contrib/releases/tag/v0.71.0)、[Testcontainers for Go v0.44.0](https://github.com/testcontainers/testcontainers-go/releases/tag/v0.44.0)、[Hey API TypeScript client](https://heyapi.dev/docs/openapi/typescript/get-started) 和 [FFmpeg 8.1.2](https://ffmpeg.org/download.html)。


## 变更策略

先确认用户需求与代码事实，再更新 Design → PRD → 单切片 spec/checklist → 实现 → Acceptance。技术选择不是调研候选列表；新发现不能自动替换已确认栈。资产采购需具体预算确认，未通过来源、画风、换装与设备测试的资产不得标记生产可用。

只新增当前业务需要的依赖、文件和服务。语义化命名，不预建脚本、兼容层、通用 Repository、泛化工作流或多云网关。测试证据绑定版本和环境，模拟器通过不代表真机或整体发布通过。
